# Product spec inventory

Feature list with status. One row per spec in `specs/`. Status: `pilot` (calibrating
the template), `planned`, `draft`, `verified`. The authoritative completeness metric
is the atlas unclaimed-atom count (see `CHARTER.md`), not this table's length —
features are added here as atom clusters get named.

| Spec | Scope | Shared | Status | Atoms claimed |
|------|-------|--------|--------|---------------|
| tasks-discussions | Editorial tasks & discussions on submissions | pkp-lib | verified (pilot) | 57 |
| publication-versioning | Publication versions: create, publish, unpublish, reader display | pkp-lib | verified (pilot) | 18 |
| submission-wizard | Make-a-Submission wizard: start form, steps, submit, acknowledgement, cancel, reconfigure | pkp-lib | verified | 30 |
| submission-wizard-metadata | For-the-Editors metadata questions: per-field collect/ask/require modes, vocab chips, the Metadata settings panel | pkp-lib | verified | 10 |
| reviewer-suggestions | Author suggests reviewers in a wizard step; editors consume them at assignment (prefill + approval); journal toggle + guidance | pkp-lib | verified | 10 |
| submission-drafts | Save-for-Later + resume email, the Incomplete-submissions list, resume-at-step, and draft deletion (single + bulk) | pkp-lib | verified | 5 |
| author-dashboard | My Submissions list (state views, search, filters, per-state rows) + the author's read-mostly tracking view of one submission; legacy authorDashboard redirect + readSubmissionEmail | pkp-lib | verified | 5 |
| editorial-dashboards | Editor Dashboard (role-scoped state views, counts, filters, search, sort, table + row actions) + reviewer My-Assignments dashboard, shared DashboardPage machinery, legacy submissions/dashboard redirects | pkp-lib | verified | 16 |
| workflow-stage-navigation | The workflow-modal shell for editorial roles: header/stage bubble, stage + publication menu (round sub-items), default-pane-per-state, stage-access matrix, workflowMenuKey deep links, legacy workflow/* redirect shims | pkp-lib | verified | 12 |
| editorial-decisions | The decision engine: Record-Decision wizard, notify-author/reviewer/other-authors emails, attach-files, the decision→stage/status/round transition machine, decline & revert, request-revisions vs resubmit, recommend-only flow, Done-stage transitions | pkp-lib | verified | 41 |
| stage-participants | The per-stage Participants panel: add/edit/remove a participant (user + role, recommend-only/metadata flags, optional notify), assignment-grants-access effect, assistant read-only scoping, submission-time auto-assignment from subeditor_submission_group | pkp-lib | verified | 8 |
| send-to-review | The Submission-stage (stage 1) workspace: incoming submission files, Desk Review discussions, participants, the "before any reviewer" state, and the three stage-1 exits (Send for Review / Accept and Skip Review / Decline) with the stage-1 UX + which files carry forward; decisions themselves referenced from editorial-decisions | pkp-lib | verified | 1 |
| assign-and-manage-reviewers | The editor's reviewer-management surface on the review stage: Reviewers grid + live statuses, Add-Reviewer picker/form, and the per-reviewer actions (unassign/cancel, reinstate, resend, thank, edit, details, history) with their 5 emails | pkp-lib | verified | 19 |
| reviewer-response | The reviewer's own side of peer review: reach the assignment (dashboard or one-click email key), accept/decline, the four-step review wizard (request/guidelines/download-&-review/completion), comments-or-review-form + recommendation + attachments, submit; the accept/decline/complete emails, reviewer-comment notification, and per-assignment access gate | pkp-lib | verified | 17 |
| review-forms | The manager's review-form builder (Settings → Workflow → Review): create/edit/copy/delete a form, add/order question elements (six item types + possible-responses list for choice types), required/author-visible flags, activate/deactivate + the in-use lock, and how an active form becomes selectable at assignment | pkp-lib | verified | 9 |
| review-rounds-and-revisions | The review-round lifecycle on External Review: what a round is, the 16-value round-status machine (computed on read), the per-round panel, the author revision-upload → resubmit cycle, the 3.6 editor-requests → author-submits written-response flow, and prior-round history; decisions themselves referenced from editorial-decisions | pkp-lib | verified | 22 |
| recommend-only-editors | The recommend-only editorial role: what the recommend_only flag does — review-pane swaps decision buttons for Recommend Accept/Decline/Revisions, the deciding-editor requirement, where a recommendation surfaces to the decider, retained reviewer/participant management. Recommendation ACT owned by editorial-decisions, flag by stage-participants, reviewer-recommendation-OPTIONS config by workflow-settings | pkp-lib | verified | 0 (role spec — all surfaces owned elsewhere; seam resolved) |
| review-anonymity | The three peer-review methods (double-anonymous/anonymous/open) and the single cross-party visibility matrix — who sees whom (reviewer↔author, reviewer↔reviewer, editor, public reader) under each method, and where the code enforces each redaction; the single home the sibling review specs reference | pkp-lib | verified | 1 |
| copyediting-stage | The Copyediting (stage 4) workspace: Draft Files / Copyedited Files grids + file stages, the assign-copyeditor/awaiting-copyedits editor status prompt, the copyeditor's & author's stage-4 panes, the copyedit-specific notify (Request Copyedit) + email-log corpus, and the two exits (Send To Production / Move to Review) referenced from editorial-decisions | pkp-lib | verified | 16 |
| production-stage | The Production (stage 5) workspace: the Production Ready Files grid (the copyedited/draft files promoted from copyediting) + file stage, the assign-production-user/awaiting-galleys editor status prompt, the layout-editor role + Ready-for-Production notify + layout/proof/index email-log corpus, the galley milestone (galleys/proofing owned by galleys feat. 26), and the two exits (Schedule For Publication nav → publication-publish-flow, Move To Copyediting → editorial-decisions) | pkp-lib | verified | 21 |
| submission-files | The cross-stage file-management machinery every editorial stage shares: the Vue FileManager (list/upload/revise/edit-metadata/delete/download per stage), the multi-step upload wizard, file revisions, dependent files, genres-in-use, non-ASCII filenames, single + download-all downloads, and the file attacher pinning files onto emails/discussions; plus the submission-file model/schema/access-policies and the file event log | pkp-lib | verified | 40 |
| editorial-activity-log | The read surface for a submission's editorial audit trail: the per-submission "Activity Log & Notes" history modal (workflow header, History + Notes tabs, emails merged into History — no separate Email tab) and the per-file Information Center (Notes/History), plus the event-log entity/schema/DB the log is written to and the note-adding UI. Owns the surface + render + note-adding; each event type is co-claimed by the feature that fires it | pkp-lib | verified | 8 |
| document-library | The Document Library: reusable non-scholarly documents (marketing/permissions/reports/other) at two levels — the journal-wide context library (Settings → Workflow → Library) and the per-submission library (workflow Library button), plus upload/edit/delete/download, the Public-Access public URL, and the `_library` email-attach API. Verifier confirmed a ⚠ unauthenticated-disclosure security hole on the legacy download page op (app-changes #88) | pkp-lib | verified | 12 |
| publication-title-abstract-body | The workflow Publication → Title & Abstract tab (prefix/title/subtitle/abstract + optional plain-language summary, multilingual, re-edited post-submission behind the versioning edit gate) plus the Body Text tab: the bundled SciFlow full-text editor + in-browser Pandoc (Word/Markdown → HTML) importer. Verifier retracted all 3 code-read ⚠ candidates against the OJS as-built (the OJS `getPublicationTitleAbstractForm()` override surfaces the abstract's required marker + word-count meter; the Body Text tab shows the published warning banner; author-in-route is API-only/policy-gated) — no ledger rows | pkp-lib | verified | 7 |
| contributors | The Contributors manager (workflow Publication → Contributors tab + reused as the wizard's Contributors step): add/edit/delete/reorder contributors, primary-contact, affiliations, journal-configured contributor roles (Author/Translator) vs NISO CRediT roles+degrees (both in `credit_contributor_roles` under an XOR), the ContributorRoleManager settings. Verifier refuted the "delete/reorder skip the published-lock" ⚠ (all four writes are gated by `PublicationWritePolicy` → `PublicationCanBeEditedPolicy`, same `canEditPublication` check — OQ1 resolved) and downgraded the credit-plugin duplicate-field ⚠ (benign, off by default); confirmed a new ⚠ (refused role-delete → generic network-error modal, ledger §2 row 90) and fixed two atlas mis-tags (author-reviewer + author-submission-details-files grids off `contributors`) | pkp-lib | verified | 28 |
| galleys | The workflow Publication → Galleys tab: create/edit/delete/reorder galleys (a published-format file OR a remote URL, each with a label + locale + optional URL-path), the galley DOI/Identifiers-tab seam, the galley file (a proof file) via Change File, plus the galley entity (`publication_galleys`+settings, `schemas/galley.json`), the reader download/view page (`ArticleHandler`) and the four format-render plugins (pdfJsViewer/htmlArticleGalley/lensGalley/jatsTemplate, all default-enabled). Vue GalleyManager is primary (lists from `publication.galleys`); the legacy `ArticleGalleyGridHandler` backs the full CRUD forms/reorder/delete (both-live). Verifier (2026-07-04) re-ran the retained `galleys.spec.js` (10 tests green) and confirmed live: the published-lock ⚠ is real end-to-end (a label edit on a published galley persists; row action reads "View" over a Save-enabled form) → **confirmed ledger row 22b** (dedup — the wave-6 galleys row, also cited by publication-versioning; no new row); corrected scenario 11 (an unassigned editorial user is refused the Galleys UI outright by WorkflowStageAccessPolicy(PRODUCTION) — the list-only case is the author's, not an unassigned editor's); fixed rule 10 (`addGalley` doesn't recompute — only `updateGalley`/`deleteGalley`), rule 14 (`isApproved` has one OJS reader, the native-XML export filter — not "no caller"), and rule 12 (`downloadSuppFile` is a deprecated 301 compat handler, not a thin redirect). All 13 atoms single-owner. | pkp-lib | verified | 13 |
| publication-metadata-references | The workflow Publication → Metadata tab (keywords/subjects/disciplines/agencies chips + coverage/rights/source/type/funding, multilingual, base `PKPMetadataForm` used directly — no OJS override) and the References tab (the `CitationManager`: add raw refs → parsed rows, raw/structured per-citation edit driven by `citationsMetadataLookup`, delete, reprocess), both behind the `canEditPublication` gate. Verifier re-confirmed the ⚠ single-citation API authorization hole (row 91, the campaign's 2nd) — an unassigned author GETs/PUTs any submission's citation → 200 — and made it precise: only the single-citation routes are ungated; the bulk routes enforce assignment + the published-lock (inline `canEditPublication()` guard) live. Also confirmed the ⚠ initial-import DOI drop (row 92), the no-category-field scope correction (categories are on the Issue tab), and no base-vs-override false alarm | pkp-lib | verified | 17 |
| media-files | The workflow Publication → Media tab: the Vue `MediaFileManager` + `MediaFilesController` REST API + `variant_groups` — batch-upload dependent-genre media (Image/Multimedia/HTML Stylesheet) tagged Web/High-resolution, pair a web image with its high-res original into a variant group (single + batch link, metadata syncs to the sibling), edit/delete with variant-group cleanup, and share a publication-scoped media file across all a publication's galleys (reader download via `ArticleHandler::download` allow-list). Verifier (2026-07-04) confirmed LIVE via the retained `media-files.spec.js` (6 tests) + probes: editor CRUD / author list-only (DELETE → 401 `userCantEdit`) / reviewer excluded; the published-lock is API-only and exempts editorial roles (⚠ **same family as ledger row 22b** — no new row); **SECURITY: media are CLEAN vs the row-88 hole** (anon download of a media file on an *unpublished* pub → real 404, gated by `ArticleHandler::initialize()` + `userCanViewGalley()`, not the ungated libraryFiles page op); the "at-most-2" group cap is enforced by `VariantGroup::link()`'s pairwise semantics (OQ3). Ledger **row 93 FIXED** (grouped-media seeder now uses a `PublicationsProcessor`-local `MAX_GROUP_SIZE = 2`). All 9 atoms single-owner; the download-page + libraryFiles atoms correctly owned by galleys/document-library | pkp-lib | verified | 9 |
| publication-license | The per-publication OVERRIDE of the journal's license/copyright defaults on the workflow Publication → **Permissions & Disclosure** tab: License URL, Copyright Holder (multilingual) and Copyright Year, each **empty-by-default → inherits the journal default** behind an **Override** toggle, with the journal default **snapshotted onto the publication at publish** (`Repository::publish()`). Split sibling of `publication-identifiers`. Verifier (2026-07-04) confirmed LIVE (running server + retained `publication-license.spec.js`): the override/inherit form state via `GET _components/permissionDisclosure` (copyrightHolder+copyrightYear `optIntoEdit=true` behind Override; licenseUrl `optIntoEdit=false`, directly editable because publicknowledge has **no default license**); the publish-time snapshot on a seeded published publication (holder→journal name per locale `en`+`fr_CA`, license→empty, and — **the issue-year nuance** — `copyrightYear = 2026` from the issue's `datePublished` *timestamp*, **not** its 2014 year *label*, since the seed's `IssueProcessor` stamps `datePublished`=today); `licenseUrl` `url` validation; the form **HONORS the published-lock** (`WorkflowPublicationForm` `canSubmit = canEdit` — editor warn-not-lock/edit persists, author hard-locked) — the **CONTRAST** with galleys; editor GET 200 / author (atester) **401** (`roleBasedAccessDenied`, the campaign baseline). No base-vs-override false alarm (base `PKPPublicationLicenseForm` used directly, no OJS subclass/route override; the journal-default `PKPLicenseForm`/`LicenseForm` IS OJS-overridden but is distribution-settings). No ⚠ found. Atlas: **3 atoms** single-owner; journal-default forms (`FORM-pkp-license-form`/`FORM-license-form`) correctly left for distribution-settings (stale `publication-identifiers-license` hint noted). | pkp-lib | verified | 3 |
| publication-issue-assignment | The workflow Publication → Issue ("Publication Settings") tab: assign an article to an issue (current/back, future, or none) + section + categories + cover image/pages/URL path, staging a **pre-publish** status (READY_TO_PUBLISH/READY_TO_SCHEDULE) that the separate Publish action later flips. The OJS `IssueEntryForm` renders section/categories/coverImage/date/pages/urlPath; the required **Issue Assignment** radio + Issue picker are Vue-injected (`useWorkflowPublicationFormIssue`, `isOJS() && issueCount>0`). Verifier (2026-07-04) confirmed LIVE (running server, API + browser): the permission matrix — author **401** on the form+status endpoints (200 only on the static options list), editor 200, manager assigns any issue, **section editor 403 on unpublished-issue list**; the four assignment options + isPublished/status mapping (options body 1/6/null · 2/6/0 · 3/7/0 · 4/6/1); rule-6 direct-Published/Scheduled-via-save **refused 403**; categoryIds persists despite `readOnly:true` (DAO `saveCategories()` special-case, not a bug); the form **HONORS the published-lock** (editor PUT on a published pub → **200**) — the CONTRAST with galleys; `IssueEntryForm` is OJS-only (no pkp base). **⚠ row 94 both facets live-confirmed** (browser screenshot): future-issue options are offered to section editors/assistants but the unpublished-issue picker is manager/admin-only → (a) picking a future option gives an error modal + empty picker; **(b) merely opening the Issue tab on an already-scheduled article pops the same 403 error modal ON LOAD** and blanks the target-issue picker, so a section editor can't even see the placement (OQ3: is future scheduling intended manager-only?). Atlas: **5 atoms** single-owner; SCHEMA-submission-ojs referenced (owned by send-to-review), API-issue-get-many left for issue-management. | no | verified | 5 |
| publication-publish-flow | The publish ACTION — the Publish / Schedule For Publication / Unpublish / Unschedule buttons on the workflow Publication tab, the confirm modal (`PublishHandler`→`PublishForm`), the publish/unpublish API + row-18 recompute, the `validatePublish` preconditions (declined/ORCID/issue/APC), the Queued→Scheduled/Published status machine + `accessStatus`, the reader visibility flip + OAI-tombstone write, and the Move-to-Done/Return-to-Workflow trigger. Version lifecycle, Review-Publishing-Details staging and the Done-decision engine are referenced (owned by versioning / issue-assignment / editorial-decisions). Verifier (2026-07-04) re-confirmed LIVE (running server + `ojs_test` DB): publish → Published/Done(stage 6)+decision 33, unpublish → Queued + OAI tombstone + decision 34, reader 200/404 flip; the confirm-modal role gate (`PublishHandler` admits SUB_EDITOR/MANAGER/SITE_ADMIN/ASSISTANT by role-id, author excluded — refined the recommend-only claim from a modal carve-out to the button+API gate); `setStatusOnPublish` stamps the date only on PUBLISHED; the `PublishSubmissions` task is **dead in OJS** → **ledger row 95** (registered in no scheduler, no `scheduledTasks.xml`, 0/19 SCHEDULED pubs carry a `date_published`). Atlas: **1 atom** (TASK-publishsubmissions) single-owner; the shared publish atoms referenced (owned by versioning, no double-claim); the OAI-tombstone DB tables left for oai-sitemap-feeds (writer/reader seam, OQ3). | pkp-lib | verified | 1 |
| issue-management | The editorial issue lifecycle — the **Issues** page (Editorial → Issues; `ManageIssuesHandler` → Future + Back grid tabs), create/edit/delete an issue (`IssueForm`), the draggable Back-issue custom order (`custom_issue_orders`), the editorial **Table of Contents** grid (`TocGridHandler` — reorder articles/sections, remove, per-article access), issue **cover** + issue-level **galleys** (`IssueGalleyGridHandler`, `issue_galleys`), **publish/unpublish/set-current**, and the `/issues` read API. The whole surface is **legacy grids + AjaxModals — no Vue manager**. Verifier (2026-07-04) confirmed as-built vs code + retained suite: **manager/site-admin ONLY end-to-end** (page + every grid → `[MANAGER, SITE_ADMIN]`; a section editor who *can* assign to a published issue is bounced from the page (`roleBasedAccessDenied`), 200-body-`status:false` on grid ops, **403** on the unpublished-issue API — the manager side of issue-assignment's row-94); publish **flips the issue live + stamps date + makes it current + publishes its SCHEDULED articles in place** (`filterByCurrentPublicationStatus`, `Repo::publication()->publish()` + `MetadataChanged`) **+ fires the ISSUE_PUBLISH_NOTIFY email + NOTIF-published-issue bell** (send-email checkbox `checked=true` by default; suppressed on `publishingMode=NONE`) — live-verified email in Mailpit w/ unsubscribe footer; unpublish → articles back to **Scheduled** (unpublish+republish, issueId kept) + OAI tombstones, vs delete → **Queued**/issueId=null. **Corrected a spec error**: a new issue defaults **all four Show toggles ON** (`initData()` seeds every `show*`=1, not "Show Title off") → an empty Title on a new issue is refused (*"Title is required for the issue."*) unless Show Title is unchecked (code + template + live). **⚠ ledger row 99** (LOW/intent-call): publish makes the issue current **unconditionally**, so publishing a back-dated issue silently moves "current" backwards (reversible via Set-Current; intent adjudication, OQ1). Atlas: **21 atoms** single-owner (issue grids/DB/galleys/JOB/MAIL/AUTHZ reassigned from galleys/archive-toc/notifications owners, all clean); `API-issue-get-assignment-options` correctly left with issue-assignment. | no | verified | 21 |
| publication-amendments | The per-version **Update Type** (12 enum values, default *New Version*) + the public **Summary of Changes** amendment notice, and the **Insert Content** helper that pulls an author's review-revision summaries into the notice. New 3.6 flow (landed May 2026). Captured on two editor-side forms (the *Schedule For Publication* version form in publish mode + the *Publication Settings*/Issue-entry form's "Version and Updates" group); the file-level revision summary is set on review-revision files (submission-files) and drives an *Amendment Notice* badge. **Live-probed 2026-07-04**: editor-side capture is fully wired in OJS (the running page injects all 12 `updateTypeOptions` + `new_version` default via the OJS `DashboardHandler` override; `publications.update_type` exists fresh-install, NOT NULL, 12-value check). **Seam resolved**: does NOT use `DB-review_round_author_responses` (single-owned by review-rounds-and-revisions, a distinct written-response feature). **⚠ Reader-side public display NOT implemented** (no reader template renders the notice/type despite field copy promising it; only Crossref `<update type>` consumes it). Atlas: **2 atoms** (VUE-insert-summary-of-changes-modal, LOC-submission-publication-updateType); the schema `updateType`/`summaryOfChanges` props ride SCHEMA-publication (versioning) with no separate atom. | pkp-lib | verified | 2 |
| publication-identifiers | The article identifier fields as experienced on the workflow Publication tabs — **scattered across three tabs, not gathered on one**: the **Identifiers** tab (empty-shell `PKPPublicationIdentifiersForm` that pub-id plugins inject into) holds URN + custom pub-ids and is gated on a pub-id *plugin* being enabled for publications (DOIs-on is NOT enough → `GET _components/identifier` 403 `noEnabledIdentifiers`); the **Issue** tab (`IssueEntryForm`) holds URL path + pages (editorial/production-only — authors can't reach); the **Metadata** tab holds article-number + publisher-id (author-reachable); the **DOI** has no field/assign on any Publication tab (read-only `doiObject` only; assign lives on the DOIs page → `doi-management`). Verifier (2026-07-04) confirmed LIVE via the retained `publication-identifiers.spec.js` (5 tests) + code: each field's home; the 403 gate; urlPath validation (all-digit/duplicate refused, journal-scoped); URN assign/clear/uniqueness via the plugin `Publication::validate` hook; the identifiers form **HONORS the published-lock** (`WorkflowPublicationForm` `canSubmit = canEdit` — editor warn-not-lock/edit persists, author hard-locked) — the **CONTRAST** with galleys, which ignores it. No base-vs-override false alarm (base form used directly, no OJS subclass/route override). Scattered-fields vs FEATURE-MAP is a scope correction (Known-deviation + OQ1), **not ⚠**; no genuine ⚠ found. Atlas: **claimed the missing `API-submission-get-publication-identifier-form`** (unclaimed shell of the identifier form-fetch endpoint, exact parallel to the metadata/title-abstract siblings) → **4 atoms** single-owner; DOI-seam atoms (`SCHEMA-doi`/`DB-dois`/`DB-doi_settings`) correctly parked for `doi-management` (hint set, Claimed-by empty) | pkp-lib | verified | 4 |
| data-availability-citations | The workflow Publication → **Data** tab: the free-text multilingual **Data Availability Statement** (the same `dataAvailability` field the wizard collects, edited post-submission) + the **Data Citations** manager — an ordered CRUD table of formal dataset citations (title/identifier+type/repository/relationship/year/creators/URL), a **distinct entity** (`data_citations`) from the References tab's `citations`. Off by default; the tab appears once the journal enables the `dataAvailability` and/or `dataCitations` metadata mode. Verifier (2026-07-04) re-confirmed LIVE (retained `data-availability-citations.spec.js`, 4 scenarios green 32 s + code): liveness (WorkflowPageOJS-registered manager reachable), the statement is **never required-marked on this tab** (`getPublicationDataAvailabilityForm()` omits `isRequired`), CRUD (8-field modal, DOI prefix-strip + per-type format check, reorder `seq`, invalid-identifier 400). Data-citation API **properly scoped** — `PublicationWritePolicy`→`PublicationAccessPolicy`→`SubmissionAccessPolicy` on writes / `PublicationAccessPolicy` on reads — so a non-assigned actor is refused on both; **does NOT share the reference-citation single-citation hole (row 91)**. **⚠ ledger row 97** (manager UI controls ungated by the published-lock — hard-locked author sees enabled Add/Order/Edit/Delete behind the red published banner; Add POST → **401**, live-confirmed) + **⚠ ledger row 98** (data citations captured but rendered on NO reader page though copy promises "appear alongside other references" — amendments row-96 shape). No base-vs-override false alarm (base `PKPDataAvailabilityForm`/`DataCitationEditForm` used directly). FORM seam resolved: `FORM-pkp-data-availability-form` stays with submission-wizard-metadata (referenced); this spec claims the publication-tab fetch endpoint. OQs: dataCitations require-mode does not block submit (array `(string)`→"Array" cast, mechanism code-confirmed); editorial-vs-author config asymmetry (author renders the manager unconditionally + API accepts writes regardless of mode — minor ⚠+OQ, no ledger row). Atlas: **13 atoms** single-owner. | pkp-lib | verified | 13 |
| journal-homepage | What an anonymous reader sees at `/{journalPath}`: the current-issue **TOC** inline (or the recent-published list / category header), the **highlights** carousel, the in-content **announcements** section, the homepage **image**/**description**/**custom content**, and the **sidebar blocks** — every surface a projection of journal settings + published state, never the viewer's role (`IndexHandler::index()` runs `validate(null)`, no `authorize()`). Verifier (2026-07-04) re-probed publicknowledge home **LIVE as anon** on the running server (current-issue TOC present — *Vol. 1 No. 2 (2014)* / *View All Issues* / `obj_issue_toc`; sidebar/highlights/announcements/About all absent for documented reasons) and confirmed the **POSITIVE** cases via the retained **GREEN** `journal-homepage.spec.js` (sidebar renders `[languageToggle, information]` in array order; announcements two-part gate `enableAnnouncements`+`numAnnouncementsHomepage`; highlights carousel from an image-less highlight; custom `additionalHomeContent`; `publishingMode=NONE` suppresses the issue section; description hidden by default — theme option off). **Corrected rule 12**: a block's `enabled` flag is **LIVE** (it is the Sidebar-management option set via `loadCategory('blocks', true)` **and** the render gate), only `seq` is a superseded order hint. Calibrated the two could-surprise defaults as **OQ, not ⚠** — the announcements count field *reveals itself* below the enable toggle (`showWhen`), and the sidebar is a *compose-from-empty* selection over the enabled blocks (no affordance promises either renders). Surfaced **⚠ e2e ledger row 25** (recent-published list sorted by `date_submitted`, not publication date) as the sole homepage-owned deviation; row 28 (issue-TOC within-section order) noted as a shared-TOC seam → `issue-archive-toc`. Atlas: **7 atoms** single-owner (`PAGE-index-index`, `DB-journals`, `PLUGIN-blocks-{information,developedBy,languageToggle,makeSubmission,subscription}`); `PLUGIN-blocks-browse` correctly left → `browse-category-section`. **`VUE-highlights-list-panel` transferred out to `highlights-featured-content` (feature 37, written 2026-07-04)** — this spec keeps only the reader carousel render (rule 6, no atom for the Smarty template); remaining interim-ownership seams (`DB-journals`→`journal-setup`/masthead, block atoms→`website-appearance-settings`) kept until those specs land. | pkp-lib | verified | 7 |
| highlights-featured-content | The highlights **management (config) side** — a manager curates an ordered list of featured cards (title + short text + optional image + a labelled link button) at **Settings → Website → Setup → Highlights** (site-admin variant at Administration → Site Settings → Highlights, shown only on a multi-journal install); the reader **carousel render** stays with `journal-homepage`. The `HighlightsListPanel` add/edit/delete/reorder over the `/highlights` REST API; the highlight entity (`highlights` + `highlight_settings`, `contextId` nullable for site-level). **Manager + site-admin only** — all six routes share one role-authorizer middleware group (no per-op split); with no context in scope it narrows to site-admin. Fields: **Title / URL / Button Label required, Description / Image optional**; title/description/urlText multilingual, url single, sequence server-assigned (appends last). Reorder is an explicit mode that rewrites the list sequence (all-or-nothing id check). Author 2026-07-04 live-probed the full CRUD as `dbarnes` (manager): add/edit/delete/reorder 200, `contextId` immutable on edit, section editor `dbuskins` 401; publicknowledge restored to 0 highlights. **⚠ ledger row 100 (new):** `GET /highlights/{id}` throws a 500 TypeError in a journal context (`get()` passes the Context object, not its id — unlike `edit`/`delete`); masked because the panel edits from the pre-loaded list. URL not format-validated (schema `format: uri` never enforced by OJS — plain rule, not ⚠). Atlas: **11 atoms** single-owner — `VUE-highlights-list-panel` **transferred in** from journal-homepage + `FORM-highlight-form`, `SCHEMA-highlight`, `DB-highlights`, `DB-highlight_settings`, `API-highlight-{get-many,get,add,edit,order,delete}`. Verifier (2026-07-04) re-probed the full CRUD **LIVE** on the running server: manager `dbarnes` 200 on getMany/add(seq 1)/delete, section editor `dbuskins` **401** on GET list + GET single; **⚠ ledger row 100 re-confirmed** — `GET /highlights/{id}` for both an existing id (31) and a missing id (99999) returned **HTTP 500** with `Argument #2 ($contextId) must be of type ?int, APP\journal\Journal given … line 117`, and each highlight's `_href` advertises that dead route (the panel masks it by editing from the pre-loaded `items`, never calling GET). URL `format: uri` confirmed unenforced (`PKPSchemaService::getValidationRules()` compiles only `validation[]`, ignores `format`) — plain rule, not ⚠. Atom transfer clean: journal-homepage 8→7 (no longer claims `VUE-highlights-list-panel`, OQ3 resolved), this spec claims all 11 single-owner, no double-claim/orphan. publicknowledge restored to 0 highlights. | pkp-lib | verified | 11 |
| article-landing | The anonymous reader's **article abstract page** (`ArticleHandler::view` at `/article/view/{id}`): the published metadata render (title/authors + ORCID/affiliation-ROR/CRediT, abstract, keywords, section, published date, DOI with a versioning fallback, references, plain-language + data-availability + funding statements), the **galley view/download links** + the **pdf.js** viewer smoke, the **license/copyright** display, the **version view + outdated-version notice** (`noindex` + canonical), the plugin-gated reader blocks (**how-to-cite** server-Blade, **Crossmark** Vue button, **recommend-by** footer blocks), and the default-on **DC/Google-Scholar `<meta>` tags** injected into its head. **Live-probed 2026-07-04** against a throwaway `ojs_main` server (the `:8000` test DB had 0 published articles): confirmed the core render, the pdf.js galley view (`pdf.js/web/viewer.html` + `#pdfCanvasContainer`), the DC.*/citation_* meta tags, published-only **404-to-anonymous** on unpublished subs, and the outdated-version notice + `noindex`/canonical on an older published version. **Seams resolved (single-owner):** claims **5** — `PAGE-article-{view,viewfile}`, `VUE-pkp-crossmark-button`, `PLUGIN-generic-{recommendByAuthor,recommendBySimilarity}`; **leaves** `PAGE-article-{download,downloadsuppfile}` → galleys, `VUE-pkp-cite` → citation-style-language (the live how-to-cite is the CSL plugin's Blade, not the Vue island), `VUE-pkp-orcid-display` → orcid (article page uses raw ORCID markup), `VUE-pkp-open-review` → review-anonymity/open-peer-review-display, `PLUGIN-generic-{dublinCoreMeta,googleScholar}` → indexing-meta-tags (page is ours, meta tags theirs). Verifier (2026-07-04) re-drove **every rule against as-built** (`ArticleHandler` initialize/view/viewFile, `article_details.tpl` raw-ORCID markup + `#data-availability-statement` + license block + the `Templates::Article::{Main,Details,Details::Reference}` hooks, `article.tpl` `PageFooter` hook, the crossref/recommendBy/dublinCore/googleScholar/CSL plugins, `load_frontend.js` — `PkpOrcidDisplay` **not** registered, confirming the ORCID seam — `OjsJournalMustPublishPolicy` DENY-on-`PUBLISHING_MODE_NONE`, `Repo::submission()->canPreview`) and via the retained `article-landing.spec.js` (**10 tests GREEN** on the `:8000` test DB, each seeding its own published article): metadata+ORCID, pdf.js galley view + PDF download, license/copyright/DOI, how-to-cite + BibTeX, version view + outdated notice + `noindex`/canonical, data-availability statement (row 98), DC/Scholar meta tags, published-only 404 + editor-preview 200, subscription gate (link `restricted` → view 302→login), recommend-by/Crossmark absent-by-default. **No spec-vs-code contradiction.** Resolved **OQ3 (feature-39 emptying): fold `article-recommendations` (feature 39) into `article-landing`** — the two recommend-by atoms stay single-owned here; feature 39 dropped as a standalone (FEATURE-MAP stanza annotated RESOLVED; it had no INVENTORY row → no phantom). Resolved **OQ1**: the how-to-cite/CSL is **opt-in, not a stock default** (no `settings.xml enabled`/install row; the test env enables it via the publicknowledge bootstrap). 5 claimed atoms single-owner; left atoms correctly attributed (galleys / indexing-meta-tags / citation-style-language / orcid / review-anonymity). | no | verified | 5 |
| issue-archive-toc | The anonymous reader's **issue pages** (`IssueHandler` at `/issue/*`): the paginated **back-issue archive** (`/issue/archive`), the **current-issue redirect** (`/issue`, `/issue/current` → the current TOC, else a "No Current Issue" placeholder), the **single-issue table of contents** (`/issue/view/{id}` — articles **grouped by section**, plus the issue cover/description/DOI/published-date + Full-Issue galleys), and the reader **full-issue galley download** (`/issue/download/{id}/{galleyId}`, through the subscription/payment gate). Read-only, public — governed by published state + `publishingMode`, not role. Verifier (2026-07-05) re-drove **every rule against as-built** (`IssueHandler` authorize/index/current/view/archive/download/`userCanViewGalley`/`setupIssueTemplate`; `Collector::orderBy` `ORDERBY_SEQUENCE` vs `ORDERBY_PUBLISHED_ISSUES`; `issue/DAO::resequenceCustomIssueOrders()` early-return; `section/DAO::getByIssueId()` `COALESCE(o.seq,s.seq)`; `OjsIssueRequiredPolicy::dataObjectEffect()` published-or-editorial gate; `issue_toc.tpl` preview banner + fixed `issueTocGalleyLabel`) and **independently live-probed anon on the running server** (`/issue`+`/current`+`/index` → 302 → `/issue/view/1`; `/issue/view/99999999` → **302 → `/en/login?source=…`**; `/issue/archive` 200; `/issue/archive/5` 404; `/issue/view/1` 200 `obj_issue_toc`), plus the retained **`issue-archive-toc.spec.js` (6 tests green ×2)** which seeds multi-issue/section/galley scratch journals the sparse `publicknowledge` cannot exercise. **⚠ row 101 re-confirmed** (archive orders **solely** by `custom_issue_orders.seq ASC`, no date/current tiebreaker → a never-reordered journal lists back issues in **undefined DB order**, not newest-first — unlike the editorial `ORDERBY_PUBLISHED_ISSUES` grid + OAI; the test proved it live via seq-collector-order equality + cross-run different-order runs). **⚠ row 28 relocated here** (within-section article order undefined — `publications.seq` all-zero → arbitrary DB order until the editorial custom-TOC ordering; canonical reader-render home). **Login-redirect-not-404 kept as OQ2, NOT a ⚠** (framework-wide `handleAuthorizationFailure()` → `redirectLogin()`; `article-landing` is the 404 outlier; low impact — no ledger row). Seams confirmed: `GRID-grid-toc-toc-grid-handler` (editorial TOC grid) + `SCHEMA-issue` stay with **issue-management** (referenced, not claimed); `PLUGIN-catalog` is a **FEATURE-MAP phantom** (no such atlas atom; the OMP-oriented `PAGE-catalog-*`/`VUE-catalog-list-panel` go to browse-category-section). **5 claimed atoms single-owner** (`PAGE-issue-{index,current,archive,view,download}`); sibling `PAGE-issue-*` mentions in issue-management/journal-homepage are explicit deferrals. | no | verified | 5 |
| site-search | The reader-facing article SEARCH: the `/search` page (query box + advanced date-range filter + site-level journal picker), the fulltext query over published articles (title/abstract/galley-body/authors), the "No Results" behaviour, single-journal vs site-wide (cross-journal) scope, and the async fulltext-index maintenance (publish-triggered `UpdateSubmissionSearchJob` + CLI `rebuildSearchIndex`). 3.6 Laravel-Scout rewrite (default DatabaseEngine, one `submissions_fulltext` table). Verifier 2026-07-05: released the SEO `FORM-pkp-search-indexing-form` to `distribution-settings`; re-confirmed row-102 Postgres 500 live; claimed 4 atoms. | pkp-lib | verified | 4 |
| browse-category-section | The anonymous reader's browse-by-category surface: the **Browse sidebar block** (a category tree, `BrowseBlockPlugin`) + the **category landing page** `/catalog/category/{path}` (`PKPCatalogHandler::category` → `catalogCategory.tpl`) — heading/count/cover/description, subcategory nav, breadcrumb, and a paginated list of the category's published+indexed articles; plus the `fullSize`/`thumbnail` cover-image ops. Read-only, fully public (only `ContextRequiredPolicy` — no must-publish gate, no usage event). Verifier (2026-07-05) re-ran the retained `browse-category-section.spec.js` (**6 tests green ×2**; full suite parallel 281 + serial 16) and re-drove every rule against as-built: **⚠ ledger row 103 re-confirmed** — a category's *Sort by* (`sortOption`) is **computed then discarded** by `PKPCatalogHandler::category()` (never applied to the builder; the *request* `orderBy` is what reaches the template), so readers get submission-id order regardless of the manager's choice; the test now observes it directly with TWO seeded articles (`sortOption=title-ASC` + A[low-id/"Zzz"/early], B[high-id/"Aaa"/late] → listed `[A,B]` submission-id order, ignoring both the title sort and the date-desc default). **Row 102 inherited** (the shared `DatabaseEngine` grouping bug — `?orderBy=datePublished` → Postgres 500, `?orderBy=title` → 200 — referenced, owned by site-search). **Liveness/scope corrections confirmed:** the Browse block is **category-only** (`getContents()` assigns only the category tree; `block.tpl`'s `@uses` docblock's stale `browseNewReleases`/`browseSeriesFactory` are a template comment, not a defect — **downgraded from ⚠**); there is **NO** section/author/title/issue reader browse page (the "section" in the name = section policies on `/about/submissions` + issue-TOC grouping); `PKPCatalogHandler::category/fullSize/thumbnail` LIVE for `type=category` (`type=monograph` → 500); `VUE-catalog-list-panel` is OMP-only dead-code (UNASSIGNED §Dead-code). Rule 4 (no parent roll-up) + rule 5 (published-only, async-index) confirmed live. **Calibrations:** empty-category dead `{if empty($results)}` branch (shows "0 Items" not `noItems`) → cosmetic, **downgraded from ⚠** (no ledger row); must-publish-gate absence → **OQ1** (not ⚠); no usage event → plain read-only fact (rule 9). **PLUGIN-blocks-browse transfer clean** (journal-homepage dropped it → 7 atoms, references-not-claims; claimed here). Atlas: **5 atoms** single-owner (`PAGE-catalog-{category,fullsize,thumbnail}`, `PLUGIN-blocks-browse`, `DB-categories` interim → transfers to `categories` on write); no double-claim. Anchor fix: `breadcrumbs_catalog.tpl` is a **lib/pkp** template. | pkp-lib | verified | 5 |
| public-comments | New 3.6 **reader comments on published articles** — logged-in readers post (via the `<pkp-comments>` island on the article page), moderators (manager/site-admin) approve/hide/delete, readers report abuse; the moderation page (`ManagementHandler::userComments` → `UserCommentsPage`), the comment+report REST API (`UserCommentController`, 11 endpoints in 3 auth tiers), the two moderator TASK notifications, and the enable setting. **Off by default** (`enablePublicComments` schema default false; publicknowledge fixture on). **Two gates, no options**: must be **logged in** to post/report (anon → 403, "Log in to comment"; there is **no** anonymous-posting setting, so the feature-map's "anonymous gating" = the login requirement), and every comment is **born hidden** — approval is **mandatory** (no "require approval" toggle). Comments attach to the **current** published version only; older versions read-only. **Live-probed 2026-07-05** (`:8000`/`ojs_test`, seeded published sub 367): full post→moderate→approve/hide→report→delete flow driven via REST + a browser view of the article page (anon sees "Log in to comment" + the one approved comment). **2 ⚠ found**: un-approve leaves a stale approval stamp (**ledger row 104**, latent — SettingsBuilder can't null a non-multilingual setting), and bulk `DELETE …/reports` 500s on 2+ reports with no UI (**row 105**). NOTIF-user-comment-{posted,reported} resolved as **owned here** (no separate notifications feature). Claims **24 atoms** single-owner. **Verifier 2026-07-05**: every permission/state/API-tier rule re-driven **live** (retained `public-comments.spec.js` — 6 tests green ×2 on `:8000`/`ojs_test`) plus an independent anon-tier curl probe confirming the exact `GET→401 / write-verb→403 / public→200` split (rule 12); the two ⚠ **re-confirmed live and code-traced** — row 104 (hide leaves a stale approver: `setApproval{approved:false}` nulls the settings but `SettingsBuilder::update` drops the null via `has()`/isset AND `EntityUpdate::updateSettings` `continue`s on a non-multilingual null, so the row is never deleted; `UserCommentResource` gates the field on `isModerator` only, and the modal's approver line is `v-if isApproved` so it stays latent) and row 105 (`deleteReports()` double-wraps `[$reportIds]` → nested `whereIn` → 500 on 2+ reports, 200 coerce on 1, no UI caller). NOTIF ownership confirmed (`notifyModerators` → TASK-level DB rows to every manager+site-admin, synchronous, no email). 4 OQs correctly calibrated (none clears the ⚠ bar; older-version visibility + off-by-default kept as plain-rule OQs). One affordance fix: the checkbox reads **"Enable Public Comments"** (was "Enable comments"). | pkp-lib | verified | 24 |
| oai-pmh | The OAI-PMH 2.0 metadata-harvesting endpoint (`/oai`) — the anonymous machine door onto a journal's published record (`OAIHandler`→`JournalOAI`/`OAIDAO`, protocol engine in shared `lib/pkp`): the **6 verbs** (Identify/ListMetadataFormats/ListSets/ListIdentifiers/ListRecords/GetRecord), the **error surface** (badVerb/badArgument/idDoesNotExist/cannotDisseminateFormat/noRecordsMatch/badResumptionToken), **resumption-token paging** (ListRecords **&** ListSets @ `oai_max_records`=100, ListIdentifiers @ hardcoded 500), **sets** (`journalPath`[`:sectionAbbrev`]; site aggregator spans enabled journals), **deleted-record tombstones** (`deletedRecord=persistent`), the 3 default-enabled formats + the two opt-in ones, and **published-only** visibility. Verifier (2026-07-05) re-drove **every verb, error, and paging path LIVE anon via curl** on the running server (`:8000`/`ojs_test`, PostgreSQL): Identify fingerprint (repositoryIdentifier=ojs-test, granularity YYYY-MM-DDThh:mm:ssZ, deletedRecord=persistent), ListMetadataFormats = **exactly** `oai_dc`/`oai_marc`/`marcxml`, GetRecord DC+MARCXML on a real record, ListRecords resumption end-to-end (100+100+36 = completeListSize 236, terminal empty token), every error code, and noRecordsMatch. **Fixed a spec error**: rule 9 claimed ListSets pages at **50** (`maxSets`) — **live-disproved** (a site-scope ListSets returned a first page of **100** sets + a resumptionToken, completeListSize 720). `OAI::ListSets()` pages at `oai_max_records` (100); `OAIConfig::$maxSets` (50) is **dead** — read only as an `== 0` sets-supported gate in ListIdentifiers/ListRecords, never a page size. **⚠ ledger row 106 re-confirmed LIVE** (LOW, no data loss): Identify advertises **second**-granularity but `from`/`until` filter at **DAY** granularity (`DATE(GREATEST(...))`/`whereDate`, a Postgres crash-fix artifact) — a `00:00:01Z`–`00:00:02Z` window still returned a record stamped `01:03:21Z` (the window only ever widens to the whole day). Format/dead-code confirmed: base `getEnabled()`=true → dc/marc/marcxml always on; **oaiJats OFF by default** (prefix `jats`, per-context `enabled` setting — **OQ1 CLOSED** by the retained test: enabling it surfaces `jats` + valid JATS); **driver OFF by default** (generic plugin gated on `getEnabled()`, adds the `driver` set — OQ2 open, test didn't enable it); **rfc1807 DEAD** (removed 3.6 by migration I12948, no dir on disk → UNASSIGNED, `metadataPrefix=rfc1807` → cannotDisseminateFormat). Retained `oai-pmh.spec.js` (**9 tests green ×2**). Atlas: **8 atoms** single-owner (`PAGE-oai-index`, `DB-oai_resumption_tokens`, `PLUGIN-oaiMetadataFormats-{dc,marc,marcxml,oaiJats}`, `PLUGIN-libpkp-oaiMetadataFormats-dc`, `PLUGIN-generic-driver`); tombstone data referenced (content-tombstones), no double-claim. | pkp-lib | verified | 8 |
| about-pages | The anonymous reader's public **informational pages** under `/{journalPath}/about/*` + `/information/*` (`AboutContextHandler`/`AboutSiteHandler` in lib/pkp + OJS `InformationHandler`): About-the-journal, Contact (principal + support blocks, emails **JS-obfuscated** via `{mailto encode='javascript'}`), Submissions guidelines (anon *Login/Register* CTA + author guidelines/checklist/copyright/privacy, **no per-section policies** — sections loaded only to gate the accepting/not-accepting notice), Privacy (context stmt when `sitewide_privacy_statement` off + a journal in context, else site stmt; **empty → 404**, no journal→site fallback), the public **Editorial Masthead** + **Editorial History** rosters (config owned by `editorial-masthead`), About-this-publishing-system (OJS version credit), the For-Readers/Authors/Librarians + competing-interest/sample-copyright Information pages (bare `/information` → 302 home), and manager-authored **custom static pages** (staticPages plugin, **ships DISABLED**). Read-only, fully public (`ContextRequiredPolicy` only; public-cacheable unless `restrictSiteAccess`). **Verifier 2026-07-05**: re-drove every page **live anon** on `publicknowledge` (about-cluster + information all 200; `/about/editorialTeam` **404** dead op; bare `/information` 302; a made-up custom path **404** — plugin disabled) + the retained `about-pages.spec.js` (5 tests green ×2). **Liveness/scope corrections confirmed**: (1) `/about/editorialTeam` is a **phantom op** (router has no such case) → parked in UNASSIGNED, not a ⚠; (2) staticPages **default-disabled** (`<lazy-load>1`, hooks registered only under `getEnabled()`) — the test closes OQ2 by enabling it on a scratch journal → create-via-grid → anon view 200; (3) the Submissions page lists **no** per-section policies (template confirms sections used only for the notice gate). **Empty-masthead calibrated OQ, not ⚠**: the public masthead renders empty on the seed (no non-reviewer users masthead-flagged, no prior-year reviewers) but the flag **is populatable via the normal UI** (`UserForm` per-group masthead selection + `PUT users/{id}/masthead/{userUserGroupId}`) — a seed-data state, not an unpopulatable-affordance defect; downgraded the spec's two stray ⚠ glyphs to a plain rule + OQ. Tightened rule-4 privacy wording (no empty-journal-stmt→site fallback). **Block plugins `PLUGIN-blocks-{information,developedBy,makeSubmission}` referenced, NOT claimed** (owned by journal-homepage; confirmed no double-claim). Claims **14 atoms** single-owner (7 PAGE-about-* + 6 PAGE-information-* + PLUGIN-generic-staticPages); `PAGE-about-subscriptions` correctly left → subscription-access. | pkp-lib | verified | 14 |
| web-feeds-syndication | The anonymous, machine-facing syndication surfaces: the always-on core **XML sitemap** (`/{journal}/sitemap` → `<urlset>` of public URLs, `/index/sitemap` → `<sitemapindex>` over enabled contexts; `Cache-Control: private`), the **webFeed** plugin (RSS 1.0 / RSS 2.0 / Atom of the current issue or recent-N published articles + `<head>` autodiscovery links + `WebFeedBlockPlugin` sidebar block), and the **announcementFeed** plugin (same three formats over active announcements). All at `/{journal}/gateway/plugin/{Web\|AnnouncementFeed}GatewayPlugin/{rss\|rss2\|atom}`. Verifier (2026-07-05) drove every surface **live anon via curl** (`:8000`/`ojs_test`, PostgreSQL) + re-ran the retained `web-feeds-syndication.spec.js` (5 tests): sitemap urlset+index 200; **webFeed ENABLED by default** (`WebFeedPlugin` overrides `getContextSpecificPluginSettingsFile` → `enabled=1` on all 86 seeded contexts, all 3 formats 200, valid RFC3339 Atom `<updated>`); **announcementFeed DISABLED by default** (base returns null → no `enabled` row → 404 on `publicknowledge`, 200 after enabling on a scratch journal with `enableAnnouncements`); no `/feed/{type}` alias and no `/feed/{issueId}` (all 404); sitemap article/galley URLs appear only through the published-issue loop (`filterByStatus([STATUS_PUBLISHED])`, §1-row-4 regression confirmed reverted in the working tree). **⚠ ledger row 107 re-confirmed LIVE** (LOW, feed-validity): the announcementFeed `atom.tpl` (`<updated>`/`<published>`) and `rss.tpl` (`<dc:date>`) format dates with obsolete strftime `%`-codes → garbage `%2026-%07-%05UTC%UTC%185` / `%2026-%07-%05` (invalid Atom); the announcementFeed RSS 2.0 `<pubDate>` and **all** webFeed templates are correct. **Corrected 3 as-built inaccuracies in the draft**: (1) `publicknowledge` runs **issue** mode (`displayItems=issue`, `limit(null)` → 100+ items), not the recent mode the draft claimed; (2) the announcements-off case (plugin on) **redirects to the journal home (302→200)**, not a 404 — only a *disabled plugin* is a true 404 (unregistered gateway → `NotFoundHttpException`), while `fetch()`-returns-false → `GatewayHandler::plugin()` `redirect(null,'index')`; (3) `dateUpdated` is a **conditional** write (only when the newest post date is newer than the marker), not "per fetch". **Calibrations:** webFeed-on/announcementFeed-off asymmetry = **OQ2** (both reachable/configurable, an accident of one plugin overriding `getContextSpecificPluginSettingsFile`); recent-mode issue-agnostic 404-on-click = **OQ3**; unknown web-feed format → **HTTP 500** (throw) vs announcement feed → 302-home (return false) = **new OQ4** (low-severity ledger candidate, malformed-input-only, no data loss). Atlas: **3 atoms** single-owner (`PAGE-sitemap-index`, `PLUGIN-generic-webFeed`, `PLUGIN-generic-announcementFeed`); `PLUGIN-blocks-browse` referenced not claimed (owned by browse-category-section) — webFeed's own `WebFeedBlockPlugin` is part of the webFeed atom. | pkp-lib | verified | 3 |
| registration-login | Public self-registration + login/logout + email-activation: the Register form (identity + credentials + reviewer opt-in + consent), account creation, the Login/Logout forms + redirect, remember-me, and the invitation-framework activation flow. First Area 5 feature. Verifier (2026-07-05) re-drove the front door **live anon** (`:8000`/`ojs_test`, curl + admin users API) + the retained `registration-login.spec.js` (8 parallel) + `serial/registration-login-activation.spec.js` (1): registration **OPEN** + `require_validation` **OFF** + reCAPTCHA/ALTCHA/rate-limit **OFF** by default; a plain registration auto-logs-in with **Reader only** (id 1048576, no mail), a reviewer opt-in gets **Reviewer only** (id 4096) — the two roles are **mutually exclusive** (`execute()`'s default-Reader branch runs only when no reviewer box ticked); Author not offered; admin login → editorial dashboard, reader → journal home; wrong password → generic error + no session. **Fixed** Scenario 2 (was "Reader+Reviewer"), Rule 3, the roles table + Side-effects to the mutually-exclusive reality, and removed a stray author-not-offered ⚠ (it is an OQ). **⚠ ledger row 108** (affiliation UI-required, no server validator) **force-driven live** (no-affiliation POST → 302 + account created). **⚠ ledger row 109 (new)**: login "Keep me logged in" ships **pre-ticked** (`userLogin.tpl` `checked="$remember"`) → persistent-cookie-by-default (UI-affordance contradiction). 14 atoms single-owner (PAGE-user/login-*, MAIL-{user-created,validate-email-context/site}, DB-users/sessions, 3 AUTHZ session middleware); password-flows/login-as/user-management seams referenced, not claimed. | pkp-lib | verified | 14 |
| user-profile | The logged-in user's own account, self-service only: the tabbed Profile page (`ProfileHandler`/`ProfileTabHandler`) — **Identity, Contact, Roles, Public profile, Password, Notifications, API key**. Author (2026-07-05) drove all seven tabs **live** on `:8000`/`ojs_test` with a **throwaway** self-registered user (`profprobe1`, no seeded user touched): identity preferredPublicName saved+persisted; contact non-email fields saved; **email change is DEFERRED** to a confirm-by-email invitation (`ChangeProfileEmailInvite` → "Confirm account contact email change request" mail to the **current** address) — not written on Save; roles interests persisted + Reader/Reviewer/**Author** self-registration join/leave checkboxes render (Author reachable **here** though not at the register form → resolves registration-login's author OQ); public-profile bio+URL persisted, invalid URL rejected, image ≤150×150; **Password** tab (`ChangePasswordForm`) = the auth-gated self-change owned HERE (session-bound, forbids old-password reuse, applies the breach check — stronger than password-flows' anon `/login/changePassword`): wrong-old + new-equals-old rejected, valid change → new password logs in; notifications save OK; **API key** generate → JWT-encoded token (`api_key_secret` set). KEY: there is **no self-service `/users` REST endpoint** — every save is a legacy `ProfileTabHandler` component form (the `/users` API is manager-gated → user-management). 8 atoms single-owner (PAGE-user-{index,profile}, GRID-profile-tab-handler, SCHEMA-user, DB-{user_settings,user_interests,notification_subscription_settings}, API-interest-get-many); **SCHEMA-user re-pointed** user-management→user-profile (self-editable field schema; registration-login owns DB-users creation, user-management owns admin lifecycle); **corrected the notification-prefs atom** — the tab writes `DB-notification_subscription_settings` (blocked/emailed prefs, keyed by user; re-pointed notifications→user-profile), NOT `DB-notification_settings` (per-notification-object metadata, delivery-side, left for notifications). No ⚠; 3 OQs (email-confirm target, author-via-profile, schema ownership). Verifier (2026-07-05) re-confirmed every permission + state rule against code AND re-ran the retained `user-profile.spec.js` (7 tests) **green live** on `:8000`; corrected the Notifications tab's two toggles from "independent" to **coupled** (`enableDisablePairs` disables the email box when on-screen is off → "blocked on-screen yet emailed" unreachable from the UI, backend-permissive off-UI — a plain rule, not a ⚠). Atom audit clean: all 8 single-owner; `DB-notification_settings` left for notifications, no `API-user-*` claimed. | pkp-lib | verified | 8 |
| password-flows | The password lifecycle on the shared `LoginHandler`: forgot/lost-password → reset email → reset-link hash → set new password; the forced first-login change (must-change interception screen); and the standalone `/login/changePassword`. Author (2026-07-05) probed **live** on `:8000`/`ojs_test` (PostgreSQL) with a seeded scratch journal + two **throwaway** users (no seeded password touched): lost-password POST → **Password Reset Confirmation** mail from the **site** contact carrying `…/login/resetPassword/<user>?confirm=<hmac>%3A<expiry>`; unknown email → same confirmation page + **0 mail** (no enumeration); reset hash = `HMAC(username+password-hash+lastlogin+expiry, salt):expiry`, expiry = now + `reset_seconds` (**7200/2h**), **single-use** (embeds the old password hash) — tampered/expired → error page, unknown user → 302; `updateResetPassword` set a new password (new logs in, old rejected, link then dead); `mustChangePassword` login → 302 `…/login/changePassword/<user>` with **no session** granted, flag cleared after `savePassword`; the change form is **anonymous & username-driven**, wrong current password rejected, min length **6**. **⚠ ledger row 110 (new)**: `LoginChangePasswordForm` uses length-only validation and **skips the compromised-password (breach) check** that the reset & profile-change forms apply — the forced-change path bypasses breach protection when enabled. **⚠ ledger row 111 (new)**: `/login/changePassword`+`savePassword` are **anonymous-reachable for any username** (gated only by current password; by design for the forced case, but a general session-less surface) — LOW. Corrected the task's premise that `/login/changePassword` 404s anonymous — it returns **200**. 7 atoms single-owner (PAGE-login-{lost,requestreset,reset,updatereset,change,save}password, MAIL-password-reset-requested); registration-login owns the must-change **trigger**, user-profile owns the auth-gated Profile→Password tab (`ChangePasswordForm`), user-management owns admin reset — all referenced, not claimed. Verifier (2026-07-05) re-confirmed all rules against code + a live anon probe (`:8000`/`ojs_test`): anon `GET …/login/changePassword` → **200** (row 111), tampered reset hash → invalid-hash error page, unknown username → 302 lostPassword, lost-password form emits `{csrf}` but `requestResetPassword` does not enforce it (OQ4, kept as OQ not ⚠ — email-to-owner only); the three-form breach-check asymmetry (row 110) code-confirmed. **Appended ledger rows 110 + 111** (were proposed). Atom audit clean: all 6 PAGE-login-* + MAIL-password-reset-requested single-owner. | pkp-lib | verified | 7 |
| user-management | The manager/site-admin administration of **other** users at Settings → Users & Roles → Users: the searchable users list (3.6 Vue `<user-access-manager>`, owned by roles-permissions, fed by `GET /users`) whose row actions **delegate** — Email/Disable/Remove/Merge open **legacy `UserGridHandler`** modals (this feature), Edit → the **invitation edit-user page** (user-invitations, host of role-end + masthead), Log-in-as → login-as. Author (2026-07-05) drove it **live** on `:8000`/`ojs_test`: manager **dbarnes** read `publicknowledge` (list + `roleIds`/`status` filters + `GET /users/{id},/reviewers`; **`GET /users/report`** streams a valid CSV but has **NO UI control** → ⚠ API-only); all **destructive** ops driven on a **throwaway scratch journal** (`j-umprobe`) w/ throwaway users (no seeded user touched): **create** (`update-user`)→account+role-form+**"Journal Registration"** welcome mail; **disable+reason**→persisted+`status=disabled`+other-sessions ended; **endRole**→role end-dated+**"removed from a role"** mail; **remove-user**→ALL active roles ended, **no** mail (distinct from endRole); **masthead** PUT→Author toggled+**mail**, Reviewer **400**; **email**→delivered from manager's own address, but **HTTP 500 on a DISABLED target** (⚠ `UserEmailForm` fetches recipient w/o `allowDisabled` → null); **merge** (two-step)→reassign all source work + **delete** source (404 after). Notify-Users tab **absent** by default (`enableBulkEmails` off). Permission model = MANAGER (settings access) + SITE_ADMIN, +SUB_EDITOR at the `/users` API; the `getAdministrationLevel` scope guard (can't administer a site admin; manager limited to managed contexts → FULL/PARTIAL/PROHIBITED). 14 atoms single-owner (GRID-user-grid + user-api-handler, API-user-{get-many,get,get-reviewers,get-report,end-role,masthead}, VUE + FORM notify-users, PAGE-management-settings-user, DB-user_user_groups, MAIL-{user-created,user-role-end-notify}). **Seams resolved**: `MAIL-user-created` reassigned registration-login→here (trigger = manager add-user); `MAIL-user-role-end-notify` notifications→here; `VUE/FORM-notify-users` email-delivery→here (email-delivery keeps the `_email` bulk-send job); `DB-users` stays **registration-login** (creation) — referenced here; masthead-roster + masthead mailable stay **editorial-masthead**. Also un-claimed 4 mis-attributed atoms off user-management (GRID-exportable-users + 2 import/export plugins → user-import-export; GRID-user-select → stage-participants; AUTHZ-user-required-policy → roles-permissions). **3 ⚠ ledger rows** (112 disabled-user email 500 — extended: same `allowDisabled` gap also 404s the single `GET /users/{id}`; 113 report CSV has no UI; **114 NEW** — section editors reach the mutating `endRole`/`masthead` PUTs the UI hides + those PUTs run NO `getAdministrationLevel` scope guard, live-confirmed sub-editor 200/404 vs reviewer 401). Verifier (2026-07-05) re-drove the `/users` gate live (session-auth), re-confirmed the report CSV + the disabled-user `allowDisabled` double-gap in code, and audited all 14 atoms single-owner (MAIL-user-created off registration-login's frontmatter; DB-users referenced not claimed; export-grids/user-select/user-required-policy sit with their correct owners). | pkp-lib | verified | 14 |
| user-invitations | The 3.6 invitation framework for **role assignment**: a manager/site-admin (and — as-built — a section editor / assistant) **invites** a new email or an existing user to one or more roles via the multi-step **send wizard** (search user → details+roles → compose email → `invite`), which emails an accept/decline link; the invitee runs the multi-step **accept wizard** — a NEW user fills username/password/consent + name/affiliation/country → **account created + roles assigned** (NOT auto-logged-in — sent to login afterward); an EXISTING user just reviews → **roles added**; plus **decline** (+ confirm-decline POST), the manager's **pending-invitation list** (`<user-invitation-manager>`: create/edit/cancel), and **expiry** (3-day default, `[invitations] expiration_days`; the wired-daily `RemoveExpiredInvitations` task dispatches `RemoveExpiredInvitationsJob`). Author (2026-07-05) drove the FULL flow **live** on `:8000`/`ojs_test` with **throwaway** emails/users (no seeded user's roles touched): manager **dbarnes** created a new-email invite (Author role) → PENDING, expiry now+3d, **"You are invited to new roles"** mail from the inviter's address carrying `/invitation/accept?id&key` + `/decline`; anon **receive**→**refine**(username/pw/name/country)→**finalize** created **enabled** user 187 with Reviewer role (no activation/must-change — immediately active); an **existing-user** invite (user 187 → Author) accepted with **no** account re-creation (187 now Reviewer+Author); **decline** (declineInvitation.tpl → confirmDecline → API decline 200); a **used** invite (ACCEPTED) → friendly "no longer available" landing page + receive **401**; **cancel** dropped it from the list (itemsMax→0); manager list shows only active-PENDING. **Permission**: reviewer `jjanssen` fully **401**; **section editor `dbuskins` CAN invite** (create page 200, `add`/`populate`/`invite` 200) — ⚠. Accept/decline are **public** (anonymous, key-gated); create-side is `[SITE_ADMIN, MANAGER, SUB_EDITOR, ASSISTANT]`. Seams: **accept-creates-and-activates-account** (registration-login owns login/self-register + `DB-users` creation; this owns the invite that triggers create+activate); **email-change invite** (`ChangeProfileEmailInvite`) is user-profile's USE — this owns the framework `DB-invitations` + the role-assignment invite type; `RegistrationAccessInvite` (activation) referenced by registration-login. 24 atoms single-owner (PAGE-invitation-{accept,decline,confirmdecline,create,edit}, VUE-{accept-invitation-page,user-invitation-page,user-invitation-manager}, all 11 API-invitation-*, DB-invitations, MAIL-user-role-assignment-invitation-notify [reassigned notifications→here], JOB-removeexpiredinvitationsjob [? →here], FORM-{accept-user-details,user-details}-form); AUTHZ-anonymous-user-policy reassigned user-invitations→roles-permissions (generic AUTHZ primitive, referenced only). **⚠ ledger row 115 (new)**: the invite path lets a section editor / assistant mint a brand-new account holding **any** role incl. **Journal manager**, with no inviter-authority check — privilege escalation, and broader than the manager/admin-only direct-create. **Verifier (2026-07-05)**: re-ran the retained `user-invitations.spec.js` (7 tests) **green** live + re-confirmed row 115 (section editor mints a manager invite → PENDING; reviewer 401). **SPEC CORRECTION**: the accept link is **NOT** a magic-link — a throwaway live probe drove a full new-invitee accept (enabled account created) and found the browser **unauthenticated** afterward (`/users/{self}` → 401, `/user/profile` → login, `window.pkp.currentUser` null); the existing-user case is the same. The prior "auto-logged-in / logged straight in" claims (Rules 4/5, Purpose, Scenarios, Side-effects) were corrected and OQ2 resolved ("not a magic-link", *safer* than feared). Empty-body (OQ5) + null-expiry sweep (OQ3) re-confirmed as Observations, not ⚠. 24 atoms single-owner (grep-clean). | pkp-lib | verified | 24 |

Pilot outcome (2026-07-02): both specs adversarially verified with live probes; verifier
refuted 3 permission cells + 1 state rule in tasks-discussions pre-fix (all corrected),
confirmed 6 new REAL deviations → e2e ledger §2 rows 53–60; unclaimed atoms: 1,436 of
1,511 (75 claimed). Dead-code candidates so far: 19 (UNASSIGNED.md).

## Atlas sweeps (Phase 0 — completed 2026-07-02)

Total: **1,511 atoms** across 13 modalities. Every sweep file records its own Gaps
section; the liveness audit (Phase 0.5) appends verdicts to `atlas/grids.md` and seeds
`UNASSIGNED.md` §Dead-code candidates.

| Sweep | File | Atoms | Notable gaps/oddities |
|-------|------|-------|-----------------------|
| Page handlers | atlas/pages.md | 164 | pages/manager router fully dead; 6 routed-but-unimplemented ops |
| API endpoints | atlas/api.md | 284 | `_test/` excluded; Route::prefix paths recovered manually |
| Legacy component handlers | atlas/grids.md | 87 | ~18 handlers are base classes; liveness audit appended |
| Vue surfaces | atlas/vue.md | 75 | frontend/components included; OMP-only managers skipped; empty pages/doi dir |
| Forms & schemas | atlas/forms-schemas.md | 121 | Field* widgets excluded; context.json = 131 props from ~15 forms |
| Plugins | atlas/plugins.md | 51 | gateways/ category empty; pflPlugin has no e2e plan |
| Mailables | atlas/mail.md | 69 | two mailables share SUBMISSION_ACK_NOT_USER template |
| Notification types | atlas/notifications.md | 65 | CONFIGURE_PAYMENT_METHOD + 10 BOOK_* never created; 4 editing-status types at wrong level |
| Jobs & scheduled tasks | atlas/jobs-tasks.md | 57 | no OJS scheduledTasks registry; test jobs in production tree |
| Authorization & middleware | atlas/authorization.md | 90 | zero unused policies; ~35 granular sub-policy fragments |
| Event-log types | atlas/event-log.md | 57 | 2 constants with no live writer; 1 duplicate hex value |
| DB entities | atlas/db-entities.md | 149 | highlights table has no e2e plan; 83 tables unhinted |
| Locale prefixes | atlas/locale.md | 242 | coarse net (2-segment prefixes, count≥4) for the completeness critic |
