# Big-Bang Progress — live state

**Live state for the spec+test build loop. Read with `RUNBOOK.md`.** Updated + committed
after every feature. A feature is `done` only when its row says so AND it's committed.

## Totals

- Features: **45 done · 0 parked · 45 pending** (of **91** — feature 39 `article-recommendations` folded into `article-landing`) — Areas 1-4 COMPLETE (Area 4: 10/10); Area 5 (Users, roles & access) next
- Tests used: **322 / 700** (parallel ~306 + serial 16; tier allocation ≈ 639 · headroom for end-of-run top-ups)
- Last full-suite runtime: parallel **~8.0 min** (fresh DB, feature 46), serial **~1.1 min** — Area 4 DB reset dropped parallel to 8.0 min. NOTE: the fresh-DB run exposed a FIXED fragility — the oai-pmh resumption-token test depended on warm-DB accumulation (>100 OAI records); it now seeds its own corpus past the page size (commit b8560bb4be), verified green on fresh DB.
- **ENV NOTE:** the test DB `ojs_test` is **PostgreSQL** (driver postgres9), not MySQL — so Postgres-specific defects (GROUP-BY strictness, etc.) ARE reproducible in the test env (e.g. ledger row 102). Keep this in mind for future DB-portability findings.
- **Serial project now holds 16 tests:** issue-management (10) + site-search (6, the queue-draining/index-flush tests). site-search's rebuild test truncates the shared submissions_fulltext index then restores it via drain in a finally — safe because serial runs single-worker after the parallel project.
- **Suite structure change (feature 38):** `issue-management.spec.js` moved to `playwright/tests/serial/` — its publish-notify test flaked under full-suite parallel load (`job_runner=On` → every web request drains the shared job queue, so a parallel mail-faking seeding request could swallow the batched reader-notify email before the test's CLI drain). Serial isolation (single-worker, runs after the parallel project) makes it deterministic. **Per-iteration gate is now `--project=ojs` then `--project=serial --no-deps`.**
- Watch-item: a transient `Unexpected end of JSON input` (dev-server response truncation under 5-worker load) flaked `galleys.spec.js` once during the parallel run; passed in isolation. Environmental, not a regression. If it recurs, consider `retries: 1` on the parallel project.
- Note: the top-of-file Totals block had drifted (stale at feature 34 — `.replace()` no-ops); corrected here at feature 38.
- Last updated: 2026-07-05 · Last commit: web-feeds-syndication (root only; lib/pkp untouched) — AREA 4 COMPLETE

Status legend — Spec: pending → draft → verified · Tests: pending → written → green(x2) ·
Verified: — / yes (adversarial + live affordance probes done) · Budget: tier·target where
H 10–13 (core workflows/permission matrices/state machines), M 6–8 (standard), L 3–4
(simple CRUD/read-only). Tiers assigned 2026-07-02 per maintainer request to rebalance by
complexity/importance — **provisional; maintainer red-pen welcome on any not-yet-built row**.
The target steers the spec's canonical-scenario count; ±1–2 by author judgment is fine.

## Features

| # | Area | Feature | Spec | Tests | Budget | #tests | Verified | Findings/notes |
|---|------|---------|------|-------|--------|--------|----------|----------------|
| 1 | Area 1 — Author submission & intake | `submission-wizard` | verified | green(x2) | H·12 | 6 | yes | 28→30 atoms; verifier live-probed all 4 unverified affordances + corrected Submit-As and Open/Cancel permission cells; ledger §2 row 61 added; tests green ×3 (2 agent + 1 orchestrator, 24 s) |
| 2 | Area 1 — Author submission & intake | `submission-wizard-metadata` | verified | green(x2) | M·7 | 7 | yes | 10 atoms; verifier live-probed all 4 unverified affordances (all held: rights/source/funding rendering, collect-only on workflow Metadata tab, data-availability require block, /vocabs reviewer exclusion) + assistant/site-admin panel access + both mid-flight flip directions (rule 4 sharpened: require-check re-binds on every Review entry); ledger row 62 re-verified; tests green ×2 (author + verifier, 33 s) |
| 3 | Area 1 — Author submission & intake | `reviewer-suggestions` | verified | green(x2) | M·6 | 6 | yes | 10 atoms; verifier live-probed the full permission+freeze matrix (add/edit/delete pre vs post submit for author/manager, assistant+reviewer refused, declined submission also frozen) and all five flagged affordances: (a) enroll-existing verified via retained test, (b) assistant "dead Add button" REFUTED (assistant can't open the wizard — role gate), (c) Submission-stage panel keeps approved suggestions read-only (verified live post-approval via real UI assignment), (d) toggle-off hides editor surface while DB row persists (verified live), (e) required-locale = context primary confirmed with a fr_CA submission; ledger §2 row 64 appended (author API anonymity leak, re-verified end-to-end on double-anonymous publicknowledge); atlas DB-reviewer_recommendations mis-hint corrected → review-settings |
| 4 | Area 1 — Author submission & intake | `submission-drafts` | verified | green(x2) | M·6 | 6 | yes | 5 atoms (save-for-later API, bulk-delete-incomplete API, single-delete API now claimed, saved-for-later mail, dashboard.submissions bulkDelete locale); DB-submissions referenced (shared, owned by submission-wizard); VUE-submissions-list-panel left for author-dashboard. Verifier re-ran the whole matrix live 2026-07-03: save-for-later by role (author/manager/assigned-SE 200; unassigned-SE/assigned-assistant/reviewer 401), list visibility, delete matrix incl. assigned-SE refused at the route (roles=SITE_ADMIN/MANAGER/AUTHOR), submitted single-DELETE 403 / bulk 404; every step token incl. reviewerSuggestions + bogus-token→step-1 fallback; email link works logged-out→login→wizard; email fires on EVERY save to the saver only. OQ4 RESOLVED — single-DELETE is live (workflow Delete button on declined submissions, dbarnes end-to-end); claimed here. Ledger §2 row 65 re-verified on fresh submission + row 66 ADDED (bulk delete of >30 selected always 404s — verification query caps at limit(30); UI-reachable via cross-page selection persistence). Corrected the "email can be disabled" claim (canDisable flag unwired; send is unconditional) → OQ5. Canonical home for the submissionProgress-whitelist bypass seam |
| 5 | Area 1 — Author submission & intake | `author-dashboard` | verified | green(x2) | M·6 | 6 | yes | 5 atoms; adversarial verifier live-probed the full matrix 2026-07-03: page access by role (admin/editor/reviewer denied, anonymous → login, author 200), row scoping (assigned-not-submitter visible, foreign absent), legacy 302 shim (own + co-author redirect; no-author-role incl. admin → authorRoleMissing; other author → accessibleWorkflowStage), all 8 deferred affordances (site-admin denial; stage-not-accessible message via scratch-journal role edit; copyediting pane; open-review Reviewers panel + anonymous negative; canChangeMetadata save persists; disableSubmissions nav removal; issues/section/categories filter hiding; ID-sort execution). 4 claims REFUTED + corrected: review-stage declines keep "Review update x/y" (not empty → OQ6); Notifications list is oldest-first (not newest) and hidden when empty; scheduled versions get no published-banner (Save-disable only); "redacted reviewer listing" renamed — open review shows reviewer name + Read Review. Upload-revisions persists after revisions submitted (probed via real upload). Dead-grid verdicts re-attacked: direct component-router fetches answer 200 — UI-orphaned, not unroutable (UNASSIGNED evidence sharpened; stale row-27 sibling claim fixed) |
| 6 | Area 2 — Editorial workflow & peer review | `editorial-dashboards` | verified | green(x2) | H·10 | 11 | yes | Spec drafted 2026-07-03, 11 canonical scenarios, 16 atoms claimed (3 dashboard PAGEs + 2 legacy PAGEs, VUE page+table, 2 filter FORMs, 5 `_submissions` APIs, 2 LOC buckets). Author live-probed: view lists per role (manager/admin 15 vs section-editor/assistant 13; reviewer 6), needs-editor semantics, scoping, filter hide-conditions, search/sort, workflow-modal round-trip, both COI guards, reviewer inert controls (ledger row 68), legacy redirects + /submissions/tasks 500 (ledger row 67). Adversarial verifier 2026-07-03 seeded one submission per bucket (edverif-* on publicknowledge + a scratch journal) and confirmed every view membership + nav-count=list-total, all deferred activity cells (Assign Reviewers, revisions-requested/submitted, reviews-completed, minimum-reviews, recommend-only trio via toggled recommendation, declined-during-stage, to-be-published-in-issue), all reviewer texts (await/response-overdue/review-overdue/declined/submitted), nav attention badges (bg-attention), disableSubmissions nav removal, reviewer pager mismatch (73 rows/one page). Three REFUTATIONS/corrections: needs-reviews fallback is **1** not 2 (REVIEWS_REQUIRED_COUNT; setting default 0) — fixed rule 3/Settings/OQ4; reviews-overdue includes due-*today* (`< tomorrow` cutoff); pure-site-admin (no manager enrolment) gets an EMPTY dashboard (systemic caveat, ledger row 56) — added to Actors. Ledger §2 row 69 ADDED: the COI guard is enrolment-gated, so a manager who authored a submission without an Author enrolment gets a normal View (dbarnes/343-344 vs eddpmgr1/353) — contradicts the affordance. createJournal-orphan assessed as scenario-harness-only (no transaction by design), not a product bug. Grooming (OQ6): stats/NOTIF/side-nav atoms deliberately left unclaimed with reasons. NOTE: seeded dbarnes is Manager-level — scoped-role probes used dbuskins/minoue/mfritz |
| 7 | Area 2 — Editorial workflow & peer review | `workflow-stage-navigation` | verified | green(x2) | M·6 | 6 | yes | Spec drafted 2026-07-03, 6 canonical scenarios, 12 atoms claimed (6 legacy workflow PAGEs incl. externalreview — seam note in spec OQ4, 2 VUE workflow pages, 4 AUTHZ stage-access policies). Author live-probed on seeds 538–546 (tag wsnav2); ledger §2 rows 70+71 added; dead-code candidate WorkflowHandler::setupIndex (UNASSIGNED). VERIFIER 2026-07-03: re-drove the full permission matrix (dbarnes/dbuskins/minoue/mfritz/jjanssen/atester/anonymous incl. login-return chain), default-pane matrix (all 8 seeds re-probed), key round-trips (round key, publication sub-tab, bogus fallback), two-hop shim chain + all six ops, rows 70/71 re-verified. All 5 deferred affordances resolved LIVE on scratch `wsnavv2` (journal 89): (a) Done-but-unpublished → Production (SQL-forced; state normally self-heals via ApplyDoneWorkflowStage); (b) revoked-enrolment guard verified + found client/server revocation-granularity divergence → NEW ledger row 72 + spec ⚠; (c) manager-active-reviewer suppression verified client-side-only (legacy door admits, panes deny; control clean); (d) Payments dropdown (needs paymentsEnabled+method+fee — spec wording tightened) + Identifiers via URN enablePublicationURN verified; (e) notification links: editor-assignment-required task clicked end-to-end in-app → legacy URL → modal; submission-submitted/reviewer-comment/new-version are email-only (level NORMAL) — rule 8 corrected. Row 71 STRENGTHENED: author tracking-view door has the same draft bypass (atester on 545). Reviewer stub's Library button probed: opens modal whose body is the denial — no leak. Round-growth live-verified (new round decision → menu grows, default moves). Refuted+fixed: 'authors/reviewers refused the same way' (they bounce at the page door, not the modal). Atlas: 12 atoms single-owner ✓, no unclaimed shell atoms (composables/StageBubble ride in VUE-workflow-page per sweep note). Scratch residue: wsnavv2 journal 89 (676/677/678), empty stray journal `wsnavv` (path collision from a failed run) |
| 8 | Area 2 — Editorial workflow & peer review | `editorial-decisions` | verified | green(x2) | H·13 | 13 | yes | Spec drafted 2026-07-03, 14 canonical scenarios, 41 formal atoms claimed (PAGE-decision-record; SCHEMA-decision; DB-edit_decisions; 3 decision APIs incl. return-to-done; 4 AUTHZ decision policies; 2 select-revision FORMs; 17 mailables incl. recommendation-notify-editors; 3 EVLOG-SUBM-ED-*; 9 NOTIF-editor-decision-*) + the informal Done-stage/constants cluster annotated in db-entities/jobs-tasks Gaps. Live-probed 2026-07-03: 14 seeded decision chains re-read stage/status/round (full variant table verified incl. auto Move-to-Done on publish); drove Accept end-to-end via real UI (composer steps, notify-author email reached Mailpit, submission→copyediting); recommend-only editor (minoue) saw only Recommend* buttons. Seams: MAIL-recommendation-notify-editors + EVLOG-SUBM-ED-REC shared conceptually with recommend-only-editors (that spec owns role config + options); NOTIF-editor-decision-internal-review claimed but OJS-inert (no internal-review stage). OMP-only decisions (14 *_INTERNAL constants) documented as dropped. VERIFIER 2026-07-03: independently re-verified all 14 variant-table transitions (fresh scratch journal, seed→decide→re-read stage/status/round — 14/14 match; added two nuances: cancel-round deletes only decisions carrying the round id, back-from-copyediting-with-round stamps the round Returned-To-Review=16). **REFUTED the suspected returnToDone 500**: live-probed 3 actor classes (assigned SE, assigned manager, manager-enrolled+subeditor-assigned) — none 500. The DecisionAllowedPolicy null-deref is real in isolation but UNREACHABLE: SubmissionAccessPolicy denies every stage-assigned editor 401 `roleBasedAccessDenied` before the decision policies, and unassigned managers take the safe empty-stage-query fallback (manager group has no user_group_stage rows). Rewrote Known-deviation + OQ1 + Done permission-row to the live truth; no ledger row (latent-but-shielded gap, not a user bug). Corrected the permission denial surface: unassigned SE & assigned assistant → 401 roleBasedAccessDenied (not the `noUnassignedDecisions` message, which is effectively unreachable) — new denial-surface note in rule 3. Drove recommend-only end-to-end (RecommendAccept → 200, no state change, editor discussion + RecommendationNotifyEditors email delivered; refused 400 requiredDecidingEditor with no deciding editor). Drove the full Done cycle live (publish→Move-to-Done 33; unpublish→Return-to-Workflow 34; manual returnToDone by unassigned manager→RETURN_TO_DONE 35). Confirmed OQ3 revert asymmetry by code (REVERT_INITIAL_DECLINE → getNotificationTypeByEditorDecision default null). Atlas: 41 atoms single-owner ✓ (marked in mail/api/authorization sweeps). Spec → verified |
| 9 | Area 2 — Editorial workflow & peer review | `stage-participants` | verified | green(x2) | M·8 | 8 | yes | Spec drafted 2026-07-03, 10 canonical scenarios, 8 atoms claimed (VUE-participant-manager, GRID stage-participant + user-select, DB stage_assignments + subeditor_submission_group, EVLOG add/remove-participant, NOTIF-editor-assign). VERIFIER 2026-07-03 re-drove the whole matrix live (fresh seed 554 minoue=recommend-only SE, 416 mfritz assign/remove): **OQ1 RESOLVED YES** — recommend-only SE added a Journal-editor participant end-to-end (new stage_assignment); assistant read-only confirmed (mfritz panel no Assign, row menu `["Notify"]` vs manager `["Edit","Notify","Login As","Remove"]`); **access grant/revoke re-probed as the affected user** (mfritz participants API 401→200 on assign, 200→401 on remove); per-stage role dropdown live-verified — **Copyediting drops Funding coordinator** (not a superset) + label is **"Marketing and sales coordinator"** (corrections #1/#2 applied); checkbox visibility live (manager→rec/‑ccm, section-editor→rec+ccm, copyeditor→‑rec/ccm). Ledger rows 73 (dead EDITOR_ASSIGN, DB=0 of type 16777255; picker value EDITOR_ASSIGN_SUBMISSION) + 74 (build firstOr) re-confirmed; **NEW ledger row 75 ADDED** + spec ⚠ + OQ2 — recommend-only/self flag guards enforced on Edit but SKIPPED on Add (minoue set recommend_only=1 on sberardo she added). Corrected "Log In As = site-admins only" → whoever has full-administration authority (manager dbarnes saw it). Atlas: 8 atoms single-owner (GRID-user-select re-hint from user-management justified; NOTIF-editor-assign ⚠ dead-code w/ evidence). Liveness: BOTH surfaces live — Vue ParticipantManager owns the list (participants API), legacy StageParticipantGridHandler backs add/edit/remove/notify modals. Author live-probed on seeds 416/417/418/419 (tags spart-*): manager-scope + assigned-SE see Assign, assistant (mfritz) read-only (no Assign); per-stage role offering (stage1 vs stage4 differ); recommend-only checkbox shows only for editor-role picks, can-change-metadata only for non-manager; add-with-notify → email to assignee + discussion (NEW_QUERY) + ADD_PARTICIPANT/MESSAGE_SENT event logs (all DB-verified); blank message = silent add; assign copyeditor → grants copyediting access (mfritz opened 416); assigned-SE dbuskins added AND removed a participant end-to-end (REMOVE_PARTICIPANT logged, row deleted). Two DEVIATIONS → ledger §2 rows 73+74: (73) NOTIF-editor-assign is DEAD — switch keys on exact `EDITOR_ASSIGN` but 3.4 migration I5716 renamed it to `EDITOR_ASSIGN_{SUBMISSION,REVIEW,PRODUCTION}` (DB-verified: no EDITOR_ASSIGN notification created); (74) re-assigning an existing participant silently drops modal flags (`build()` firstOr). NOTIF-editor-assign recorded as dead-code candidate (UNASSIGNED). GRID-user-select moved from user-management hint (only caller is addParticipantForm). subeditor_submission_group claimed as the section→sub-editor auto-assign source (write owned by section setup). NOT live-verified: assistant dropdown-only-Notify (popover flaky; Assign-absence confirmed instead), recommend-only-SE add ability (OQ1), post-remove access revocation (code-clear via workflow-stage-navigation). Adversarial verifier pending |
| 10 | Area 2 — Editorial workflow & peer review | `send-to-review` | verified | green(x2) | M·7 | 6 | yes | Spec drafted 2026-07-03, 7 canonical scenarios, 1 atom claimed (SCHEMA-submission-ojs — the intake overlay, sectionId home; other 3 props read-only projections referenced to issue-assignment/reviewer-suggestions/scheduling). Deliberately references (does NOT re-claim) PAGE-workflow-submission (workflow-stage-navigation), GRID-submission-files-*/VUE-file-manager (left for submission-files — seam note), EVLOG-SUBM-SUBMIT (submission-wizard). Author live-probed 2026-07-03: seeded stage-1 sub 555, dbarnes API `availableEditorialDecisions` = ids 3 Send for Review / 17 Accept and Skip Review / 8 Decline Submission; browser-drove the stage-1 pane (panels Submission Files + Desk Review Tasks & Discussions + PARTICIPANTS, header Activity Log/Library, action bar Schedule For Publication + the 3 exits, files grid Upload/Download All showing the Article Text genre PDF). Key finding: Send for Review's wizard carries the incoming SUBMISSION_FILE_SUBMISSION files forward into review as SUBMISSION_FILE_REVIEW_FILE via a Select Files (PromoteFiles) step. Status panel confirmed null/blank at stage 1 (the "before review" state). Delete gated behind decline (manager-only). VERIFIER 2026-07-03: re-read all pane/decision/file/status rules against source (workflowConfigEditorialOJS getPrimary/Secondary/ActionItems, useFileManagerConfig SUBMISSION_FILES, WorkflowSubmissionStatus null-fallthrough, SendExternalReview::withFilePromotionLists=SUBMISSION_FILE_SUBMISSION, SkipExternalReview APC override, InitialDecline getNewStageId=null, maps/Schema global-role fallback) and live-probed the not-yet-covered affordances. **Delete-gate hardened**: guard reads submission.stages[].currentUserAssignedRoles, which maps/Schema fills with an UNASSIGNED manager/admin's global role (fallback skipped only for active reviewers) → verifier-probed both branches: unassigned manager (dbarnes, not a participant) SAW Delete on a declined stage-1 sub, assigned section editor dbuskins did NOT (complements the retained test's assigned-manager case). availableEditorialDecisions re-probed on a fresh sub = ids 3/17/8 @ stage 1. reviewer-suggestions right-rail panel confirmed LIVE (renders once the author suggested ≥1 reviewer; self-hides when empty — v-if list>0). **CORRECTION**: Send to Text Editor (FILE_SEND_TO_EDITOR) is MANAGER/SITE_ADMIN-only (NOT editors/assistants as the prose read) AND pandoc-only — verifier-probed absent on the arrival PDF even for a manager; fixed Actors table + rule 3 (footnote c was already accurate). Send-for-Review promote loop closed: copy machinery is the decision engine's PromoteFiles (driven e2e by editorial-decisions); the stage-1 slice (Select Files SOURCE = arrival Submission Files) is live-tested by the retained spec. APC-on-Accept&Skip code-confirmed, owned by payments (not live-driven; publicknowledge has no payments). Atoms: SCHEMA-submission-ojs single-owned here (defensible sectionId home; reviewer-suggestions defers, submission-wizard doesn't claim); EVLOG-SUBM-SUBMIT→submission-wizard, PAGE-workflow-submission→workflow-stage-navigation, VUE-file-manager/GRID-submission-files-* left for submission-files — all confirmed single-owner. OQ3 (delete-behind-decline) + OQ4 (schedule-from-stage-1) correctly calibrated as plain rules + Open questions, NOT ⚠ (restrictive-but-plausibly-intended per CHARTER). Spec → verified |
| 11 | Area 2 — Editorial workflow & peer review | `assign-and-manage-reviewers` | verified | green(x2) | H·12 | 11 | yes | 19 atoms claimed (VUE reviewer-manager + select-reviewer-list-panel, legacy reviewer-grid BOTH live, DB review_assignments(+settings), SCHEMA review-assignment, 5 MAIL request/subsequent/reinstate/resend/unassign, 2 NOTIF, 5 EVLOG ASSIGN/CLR/REIN/CONF/**UNCON**, **AUTHZ required-policy**); 12 scenarios; author live-probed on sub 695. **VERIFIER 2026-07-03 (sub 849, publicknowledge)**: **OQ6 RESOLVED CLEAN** — assistant `mfritz` (Funding-coordinator review-stage assignment) vs editor `dbarnes`: Add-Reviewer picker HIDES Create-New (`selectCreate`) + Enroll (`enrolExisting`) links from the assistant (`AdvancedSearchReviewerForm::fetch()` gates on MANAGER\|SUB_EDITOR), and submission API returns `canGossip=false`/`canLoginAs=false` for the assistant (`true` for editor) so the row menu omits Editorial-Notes + Login-As — no affordance mismatch, no ledger row. Bonus finding: **assistant grid hides declined/cancelled assignments** (`canSeeAllReviewAssignments` restricts to MANAGER/SUB_EDITOR/SITE_ADMIN — mfritz saw 3 of 4 rows). Permission matrix re-driven live: recommend-only SE `minoue` manages reviewers fully (create/enroll + canGossip); reviewer `jjanssen` refused the grid; author-denied ops cover every management op. State boundaries attacked: reinstate a hard-deleted assignment → `ReviewAssignmentRequiredPolicy` denies; reinstate/resend status gates are UI-only (backend forms open against any status — API-only, shielded, no ⚠). OQ3 email-log-type smell confirmed harmless (`$round===REVIEW_ROUND_STATUS_REVISIONS_REQUESTED(=1)`; mailable chosen cleanly by `getRound()==1`). Atlas: 17 originals single-owner + resolved EVLOG-REV-UNCON (OQ2) + AUTHZ-review-assignment-required-policy (OQ4) → claimed here; access-policy left for reviewer-response. All 5 email template keys + log types code-confirmed |
| 12 | Area 2 — Editorial workflow & peer review | `reviewer-response` | verified | green(x2) | H·10 | 10 | yes | Spec drafted 2026-07-03, 11 canonical scenarios, 17 atoms claimed (5 PAGE-reviewer-{submission,step,savestep,showdeclinereview,savedeclinereview}, VUE-reviewer-submission-page [thin round-history shell over the LIVE legacy 4-step wizard — both live, co-operative], DB-review_form_responses + review_files, MAIL-review-{confirm,decline,acknowledgement,complete-notify-editors}, NOTIF-reviewer-comment, EVLOG-REV-{ACCP,DECL,RDY}, AUTHZ-review-assignment-access-policy [left for me by assign-and-manage]). Live-probed end-to-end on sub 850 (publicknowledge): jjanssen accept→submit (accept + complete emails to editor, recommendation options Accept/Revisions/Resubmit/Resubmit-Elsewhere/Decline/See-Comments), amccrae decline ("Unable to Review" + redirect to journal index), agallego (unassigned) denied. Seams: PKPController /reviews/* author-response + review-export atoms hinted reviewer-response but belong to review-rounds-and-revisions; NO EVLOG-REV-RECOMMENDATION atom exists (recommendation rides on review-ready + review_assignments column). **VERIFIER 2026-07-03 (publicknowledge + scratch journals): VERIFIED.** All 17 atoms confirmed single-owner (EVLOG-REV-ACCP + AUTHZ-access-policy only *mentioned* by assign-and-manage, not claimed); OQ3 resolved (no EVLOG-REV-RECOMMENDATION atom; EVLOG-REV-PROXY-REC is editor-side, unclaimed); PAGE-reviewer-downloadfile confirmed dead (no method) + logged in UNASSIGNED.md. The 3 deferred affordances now LIVE-verified via retained specs: one-click key login (signed-out ctx → logged in on step 1), review-FORM step-3 variant (→ review_form_responses, editor-read), attachment upload (→ SUBMISSION_FILE_REVIEW_ATTACHMENT, editor-read). Permission matrix re-driven: unassigned + cancelled reviewer → `submissionReviewer`; editor + author → earlier `roleBasedAccessDenied` (spec row b refined). Edit-after-submit recommendation `<select>` enabled-but-INERT (direct saveStep POST → HTTP 500, recommendation unchanged) — cosmetic, not a hole. Empty free-text review submittable (recommendation-only → RECEIVED) live-confirmed — Open question, not ⚠. Comment-heading label is "For editor" in OJS (locale override of pkp-lib "For editor only") on both reviewer step-3 + editor Read-Review modal — cosmetic, spec labels corrected. 3 low-severity cosmetic candidates → e2e ledger §2 rows 76–78 |
| 13 | Area 2 — Editorial workflow & peer review | `review-forms` | verified | green(x2) | M·7 | 7 | yes | Spec drafted 2026-07-03, 8 canonical scenarios, 9 atoms claimed (2 GRID handlers review-form-grid + review-form-elements-grid, 1 GRID listbuilder response-item, DB review_forms(+settings,elements,element_settings), 2 LOC manager.reviewForms/reviewFormElements). DB-review_form_responses deliberately NOT claimed (reviewer-response owns the write; structure-vs-write seam noted). Author live-probed on publicknowledge 2026-07-03 (manager dbarnes): built a form, added a Drop-down element with a possible-responses listbuilder, previewed it, activated it → appeared in the Add-Reviewer Review Form dropdown; created an in-use assignment (scenario API) → In Review=1, row lost Edit/Delete (Copy+Preview only). Permission boundary live-verified (section editor dbuskins → "current role does not have access to this operation"). **NEW DEVIATION (⚠) + proposed ledger row**: the activate confirmation promises "once assigned to a review you will no longer be able to deactivate it" but deactivate has NO in-use guard — live-verified deactivating an in-use form succeeded. Also: existing ledger §2 row 17(b) ("confirmActivate/confirmDeactivate ship empty msgstr") is INACCURATE — both are non-empty multi-line PO strings, render full text live. **VERIFIER 2026-07-03: VERIFIED.** Both grid handlers `addRoleAssignment([MANAGER,SITE_ADMIN], all ops)` + `CanAccessSettingsPolicy` re-read; permission matrix + all state rules RE-DRIVEN LIVE via retained review-forms.spec.js (9/9 green — the once-flaky "delete unused form" passed on isolated retry, known legacy-grid click-swallow, ledger §3). Deactivate ⚠ re-confirmed end-to-end (scenario 7 green: In Review=1 form deactivated, assignment kept) → **ledger §2 row 79 ADDED**; row 17(b) **RETRACTED** in ledger (confirmActivate/confirmDeactivate verified non-empty multi-line PO strings, render full live). Element-type labels (6) + validators verified against manager.po — **fixed one spec error**: empty item-type message is "An item type is required for the form item." (spec had guessed "You must select a form item type."). `included=off` withholding code-verified at exact anchors (`ReviewerComments::getReviewFormComments` continue; `reviewDownload.tpl` `authorFriendly && !getIncluded`). In-use trigger tightened (lock trips at assignment creation, not acceptance; DAO `declined<>1` counts). Liveness confirmed: legacy grid is sole live surface (workflow.tpl load_url_in_div), NO Vue ReviewForms manager (sibling reviewerRecommendations IS Vue). Atlas: 9 atoms single-owner ✓; LOC counts 18/23 justified (bare prefix key + dotted); DB-review_form_responses left to reviewer-response (seam). Spec → verified |
| 14 | Area 2 — Editorial workflow & peer review | `review-rounds-and-revisions` | verified | green(x2) | H·12 | 11 | yes | 22 atoms claimed (6 DB review_round(+settings,files)/author_responses(+authors,settings), SCHEMA-review-round, 3 NOTIF incl. pending-internal-revisions [OJS-inert], 3 VUE author-response managers, 4 API author-response, PAGE-reviewresponse-requestauthorresponse, MAIL-request, 3 AUTHZ review-stage policies). **VERIFIER 2026-07-03 (publicknowledge, seeds 695–704):** all 22 atoms confirmed single-owner. Round-status machine live-probed via a CLI `determineStatus()`/`getStatusKey()` probe on 9 seeded submissions — computed IDs 1,2,4,5,6,7,8,9,10,16 observed directly; 11 & 15 driven green by the retained test's author-upload flow; **SENT_TO_EXTERNAL(3) has NO OJS writer → documented OJS-inert** (OMP-only). ⚠(a) status-label author-phrasing **CONFIRMED + ledger §2 row 80** — evidence corrected: divergence is only on REVIEWS_READY/REVIEWS_OVERDUE (browser-probed as atester: sub 696 card = "A review is overdue.", sub 704 = "New reviews have been submitted." — the editor keys; Schema map omits `$isAuthor`, `WorkflowSubmissionStatus.vue` binds `currentReviewRound.status`). ⚠(b) new-round-recomputes-prior-status **DOWNGRADED to plain rule + OQ4** (no data loss — decision history intact; unlike editorial-decisions cancel-round which cascade-deletes; sub 702 round1 stored=6/computes=9). ⚠(c) "inert Submit Response button" **REFUTED** — clicking it as the author opened the "Submit Your Response to Reviewer Feedback" modal (live). Author-response gates live-driven via authenticated API: request=MANAGER/SITE_ADMIN/SUB_EDITOR (author/reviewer→401 roleBasedAccessDenied), submit=AUTHOR only (editor/reviewer→401), 422 min-reviews on PENDING_REVIEWS, 200 e2e author submit (response id 18) then 409, missing associatedAuthorIds→422; one-response guard enforced on BOTH request+submit; 409 keys on an existing *response* not a prior request (re-request re-sends 200). Spec fixes: `locale` REQUIRED on Request Response (spec had "optional"); round-less `workflow_3` URL TypeError re-confirmed not-user-reachable (default nav→workflow_3_356). Atlas: NOTIF-pending-internal-revisions OJS-inert resolved (keyed on absent internal-review stage); both `*RevisionsGridHandler`s atlas-flagged suspected-dead; review-file grids left for submission-files (OQ1); amendments does NOT co-own review_round_author_responses (uses summaryOfChanges + update_type; publication Repository only reads for open-review/DOI). Spec → verified |
| 15 | Area 2 — Editorial workflow & peer review | `recommend-only-editors` | verified | green(x2) | M·6 | 6 | yes | VERIFIER 2026-07-03: zero-atom role spec confirmed correct (owns NO atlas atoms; every surface single-owned elsewhere). Affordances **browser-driven** as each actor (retained recommend-only-editors.spec.js, 6/6 green ~22s): minoue at REVIEW sees Recommend Accept/Revisions/Decline and NONE of the finalizing Accept/Request-Revisions/Decline (currentUserCanRecommendOnly=true); dbarnes sees the inverse; no-deciding-editor → the verbatim message + editorAssigned=false; recommendation surfaces to decider + minoue's own echo; latest-per-editor + multi-editor + recommendationsIn; still runs the review (Reviewer manager + Add Reviewer enabled). **OQ4 resolved**: submission stage → minoue sees ONLY finalizing Send-for-Review (availableEditorialDecisions=[Send for Review]), no Recommend/Skip/Decline. **AFFORDANCE FIX**: change-link label is **"Change decision"** (`editor.submission.workflowDecision.changeDecision`), spec said "Change recommendation" (3×) — corrected + added cosmetic-copy note (unused `changeRecommendation` string). Seam checks: EVLOG-SUBM-ED-REC + MAIL-recommendation-notify-editors Claimed-by=editorial-decisions (not orphans); reviewer-rec OPTIONS cluster uniformly hinted workflow-settings (no lingering recommend-only-editors hint); **EVLOG-REV-PROXY-REC genuine orphan → parked in UNASSIGNED.md** (reviewer-mgmt, natural home assign-and-manage-reviewers). Code re-confirmed: getPropertyStageAssignments top-level editorAssigned excludes recommendOnly; checkDecisionPermissions dual-assignment→decide; areRecommendationsIn naming smell harmless. Spec → verified. Tests/#tests left for orchestrator |
| 16 | Area 2 — Editorial workflow & peer review | `review-anonymity` | verified | green(x2) | H·10 | 10 | yes | Spec drafted 2026-07-03, 11 canonical scenarios, 1 atom claimed (VUE-pkp-open-review — the rule the open-review component applies; SUBMISSION_REVIEW_METHOD constants + per-assignment anonymity flag are columns on DB-review_assignments owned by assign-and-manage-reviewers, not separate atlas atoms). THE single home for the cross-party visibility matrix; ~5 specs reference it. **Matrix fully live-driven** on publicknowledge sub 1 (2026-07-03) as each party — one double-anon (jjanssen), one anonymous (phudson), one open (amccrae) reviewer, all completed: reviewer→author identity HIDDEN only under double-anon (jjanssen authorsString='' vs phudson/amccrae "Author Tester"); reviewer identity → author/co-reviewers HIDDEN unless that review is open (author saw only amccrae named; each reviewer saw only own row + amccrae); author's redacted Reviewers panel lists only OPEN+completed reviews (getOpenAndCompletedReviewAssignmentsForRound — live: Aisla McCrae alone shown despite all three completed); editor view never redacted; redaction is server-side in the schema maps (AnonymizeData + maps/Schema.php), not UI-only. Public open-review rule owned here (PublicationPeerReviewResource: publicly-visible+accepted+confirmed, identity only if open); page owned by open-peer-review-display. Two ⚠ candidates for ledger: (a) reviewer-suggestions author-API leak (existing row 64, cross-ref — defeats double-anon reviewer anonymity to author); (b) manuscript FILE content never scrubbed + `review.ensuringBlindReview` author guidance removed from locale (metadata anonymized, file bytes not). **VERIFIER 2026-07-03 (publicknowledge, seeded subs 214/215): VERIFIED.** All 18 code anchors re-confirmed EXISTS (spec paths accurate; `mapByProperties()` `$anonymize` at Schema.php:471, `getPropertyReviewAssignments` blanking at :701-708, PublicationPeerReviewResource `$isReviewOpen` gate at :217-227, ReviewerComments `reviewerLetter`-vs-fullName, ReviewsMigration `review_method` default=ANONYMOUS(1) confirming OQ1). Core matrix RE-DRIVEN live as all 5 parties on one three-method submission — holds exactly. Public open-review gate re-confirmed (empty for non-publicly-visible reviews). Method-switch statelessness confirmed symmetric (no snapshot column → open→dbl re-hides; added to scenario 11). **NEW ⚠ + LEDGER ROW 81**: a double-anonymous reviewer reads the author's real name + user id via `GET /submissions/{id}` `participants[]` (`getPropertyParticipants()` un-anonymized) despite `authorsString=''` — API-only, no UI surface, reachable from invitation (agallego, invited, saw `{"id":17,"fullName":"Author Tester"}`); internally inconsistent (controller anonymizes metadata + review-assignments for reviewers, and the discussion path `EditorialTask::anonymizeAuthors()` hides the author — but participants is missed). Spec matrix cell + rule 4 qualified; new Known-deviation added. **⚠ DOWNGRADE**: the file no-scrub + removed-guidance entry demoted from ⚠ to a plain note (no live affordance references the removed key; no-scrub is long-standing intent) → left as Open question 2, **no ledger row** for it. Row 64 cross-ref re-confirmed correct (not duplicated). Atoms: VUE-pkp-open-review single-owned here (vue.md claimed=review-anonymity); DB-review_assignments owner=assign-and-manage-reviewers (deferral correct, not orphaned). INVENTORY row added |
| 17 | Area 2 — Editorial workflow & peer review | `copyediting-stage` | verified | green(x2) | M·7 | 6 | yes | Spec drafted 2026-07-03, 7 canonical scenarios, 14 atoms claimed (2 copyedit GRID [copyedit-files + manage-copyedit-files], 3 NOTIF [copyedit-assignment/assign-copyeditor/awaiting-copyedits], 9 EVLOG-EMAIL-COPY-* reassigned from production-stage per FEATURE-MAP). Live-probed 2026-07-03 (curl session, subs 216 skip→copyediting + 217 +copyeditor mfritz): stage 4/Queued; `availableEditorialDecisions` = **id 7 Send To Production + id 30 Move to Review** at stage 4 for both manager-scope (dbarnes) AND assigned SE (dbuskins); participants auto-assigned (dbuskins/sberardo SE + atester Author); 216 files stay `SUBMISSION_FILE_SUBMISSION` (skip does not promote → no Draft Files). Code-verified: stage-4 pane = status prompt + Draft Files (FINAL_DRAFT_FILES/SUBMISSION_FILE_FINAL) + discussions + Copyedited Files (COPYEDITED_FILES/SUBMISSION_FILE_COPYEDIT/CopyeditFilesGridHandler) + participants; author-check = author FILE_LIST-only on Copyedited Files (CopyeditFilesGridHandler withholds edit/manage caps from authors); Send-To-Production promotes copyedited(+draft) → SUBMISSION_FILE_PRODUCTION_READY. **Liveness findings**: (a) `NOTIF-copyedit-assignment` + `EVLOG-EMAIL-COPY-NOTIFY` reachable ONLY when editor picks the retained "Request Copyedit" (COPYEDIT_REQUEST) alternate — default DiscussionCopyediting template → generic NEW_QUERY path (rule 8, ⚠); contrast dead EDITOR_ASSIGN (renamed). (b) 8 of 9 COPYEDIT_NOTIFY_* email-log types never written (OJS-2 remnants) → dead-code candidates in UNASSIGNED. **SEAM**: GRID-final-draft (FinalDraftFilesGridHandler) renders in THIS stage but atom owned by production-stage per FEATURE-MAP (OQ1). NOT live-verified via browser (autofill blocked login; used authenticated curl API instead) — status-card render, Request-Copyedit notify effect, and copyeditor/author panes verified by component/config code + API, not browser screenshot. **VERIFIER 2026-07-03 (Playwright browser-driven, publicknowledge, seeded stage-4 subs 418 skip / 419 accept / 420 accept+copyeditor): VERIFIED.** Whole permission matrix browser-driven as every role: assigned SE dbuskins (full workspace + Send-To-Production/Move-to-Review), manager-scope-unassigned dbarnes (full workspace, **no** status prompt — prompt is per stage-assignment not per access), assigned copyeditor mfritz (full workspace, Participants read-only/no-Assign, **no** action bar, no prompt), unassigned copyeditor svogt ("current role does not have access" error), author atester (Copyedited-Files list-only + Discussions, no Draft/Participants/action-bar), reviewer jjanssen (`roleBasedAccessDenied` page-level). **KNOWN-CONTRADICTION FIXED**: the "Assign a copyeditor" status prompt is NOT a fixed pane part — it is a stored notification seeded only by `getSubmissionNotificationTypes()` for ACCEPT/SEND_TO_PRODUCTION, **not SKIP_EXTERNAL_REVIEW** (DB + browser: accept 419 shows card, skip 418 none; not created on page load) → rule 2/scenario 1/footnote a corrected; this is **existing ledger row 8** (not duplicated). OQ3 confirmed live: copyeditor assigned silently (sub 420, no discussion) → editor still reads "Assign a copyeditor" (prompt keys off stage-4 discussion, not the assignment) → **new ledger row 82**. ⚠ calibration: rule-8 template-conditional task-notification **downgraded** ⚠→plain rule (spec's own Known-deviations frames it as OQ2, not a deviation); dead-email-log ⚠ kept (recorded UNASSIGNED, not app-changes ledger). Atlas audit (subagent): all 14 atoms single-owned, no double-claims/drift; 9 EVLOG-EMAIL-COPY-* reassignment reflected (production-stage owns none); GRID-final-draft→production-stage & PAGE-workflow-editorial→workflow-stage-navigation referenced-not-claimed. Send-To-Production promote source re-confirmed (COPYEDIT pre-selected + FINAL offered → PRODUCTION_READY). Spec → verified. INVENTORY row added. |
| 18 | Area 2 — Editorial workflow & peer review | `production-stage` | verified | green(x2) | M·7 | 7 | yes | Spec drafted 2026-07-03, 7 canonical scenarios, **21 atoms claimed** (2 GRID [production-ready (dead), manage-proof], 5 NOTIF [assign-productionuser, awaiting-representations, layout-assignment, index-assignment (dead), format-needs-approved-submission (OMP-only)], 14 EVLOG-EMAIL-{PROOF*9, LAYOUT*3, INDEX*2}). Browser-driven 2026-07-03 (Playwright, publicknowledge, seeded stage-5 subs 421 skip→copyediting→production+layoutEditor gcox / 422 +galley): stage-5 pane = status prompt (WorkflowNotificationDisplay isOJS branch) + Production Ready Files (PRODUCTION_READY_FILES/SUBMISSION_FILE_PRODUCTION_READY, **no gridComponent** → ProductionReadyFilesGridHandler DEAD) + Production Tasks & Discussions + Participants; action bar = **Schedule For Publication** (nav, always) + **Move To Copyediting** (BackFromProduction=29, warnable). **NO galley grid and NO final-draft grid in the pane** — galleys live on Publication→Galleys tab (galleys feat 26). Status-prompt cycle: ASSIGN_PRODUCTIONUSER ("Assign a user to create galleys…") → AWAITING_REPRESENTATIONS ("Awaiting Galleys.") → clear when a representation exists; seeded ONLY by SEND_TO_PRODUCTION (getSubmissionNotificationTypes) — no skip-path asymmetry (production's sole entry). ⚠ LIVE-CONFIRMED (sub 421): prompt keys off stage-5 discussion existence, NOT the Layout Editor assignment — gcox assigned but editors still read "Assign a user to create galleys" (DB + rendered card; analogue of copyediting ledger row 82). ⚠ prompt only recomputes on a fixed event set (decision/participant/discussion/notify/**galley-grid add-delete** via ArticleGalleyGridHandler) — a galley added off the grid path leaves it stale (sub 422); propose NEW ledger row. Permission boundary browser-driven: layout editor gcox → full pane, Upload production-ready, Participants read-only, **no status prompt, no decision button, but DOES see Schedule For Publication**; author = Production Discussions only (workflowConfigAuthorOJS). Liveness: LAYOUT_REQUEST ("Ready for Production") retained alternate → NOTIF-layout-assignment + LAYOUT_NOTIFY_EDITOR reachable on non-default notify (copyediting pattern); INDEX_REQUEST unregistered → NOTIF-index-assignment + INDEX email-log DEAD; galley isApproved has NO OJS caller (OMP-only, no OJS approval step); FORMAT_NEEDS_APPROVED_SUBMISSION only in isOMP() display branch (unreachable OJS). **SEAM RESOLVED**: GRID-final-draft (+ manage) render ONLY in copyediting (stage-5 pane has only PRODUCTION_READY_FILES) → reassigned production→copyediting-stage in atlas + added to copyediting frontmatter/OQ1 (copyediting count 14→16). **VERIFIER 2026-07-03: VERIFIED.** Code-confirmed every rule at its anchor (production pane = PRODUCTION_READY_FILES only, no final-draft grid → seam consistent; PRODUCTION_READY_FILES has no gridComponent → ProductionReadyFilesGridHandler dead; PRODUCTION branch of the status manager keys the flip off a stage-5 EditorialTask not a Layout-Editor assignment; submission-file events recompute only the COPYEDIT types, never production; isApproved has zero Representation callers in OJS; INDEX_REQUEST unregistered + not in I5716 retained alternates → NOTIF-index + INDEX email-log dead; only LAYOUT_NOTIFY_EDITOR + LAYOUT_NOTIFY_COMPLETE written of the 14 EVLOG-EMAIL types, PROOFREAD_NOTIFY_AUTHOR is a display-filter list not a writer; FORMAT_NEEDS_APPROVED_SUBMISSION requested only in the isOMP() branch + no OJS delegate; PRODUCTION→BackFromProduction(29)→EDITING; Schedule button is an ungated nav shortcut → layout editor seeing it is as-built, plain rule not ⚠). **⚠(a) re-verified live** (DB sub 622: gcox Layout-Editor-assigned, no discussion → 3 editors still hold ASSIGN_PRODUCTIONUSER) → ledger row **9** is the canonical shared entry (row 82 = copyediting sibling). **⚠(b) confirmed live** (DB sub 623: scenario-seeded remote galley exists yet prompt persists — Repo-path add bypasses recompute) → **NEW ledger row 83**. FIXED spec imprecision: "participant add/remove" recomputes → only `deleteParticipant`(remove)+`sendNotification`(notify) do; `saveParticipant`(add) does not (matches copyediting sibling) — corrected in 3 places. Atlas audit (subagent): all 21 atoms single-owner (sweep "Claimed by"=production-stage, no double-claims/orphans); 2 final-draft grids cleanly owned by copyediting-stage in grids.md + frontmatter; copyediting INVENTORY already shows 16. Spec → verified. |
| 19 | Area 2 — Editorial workflow & peer review | `tasks-discussions` | verified | green(x2) | H·10 | 6 | yes | Spec verified in pilot; tests built this iteration (6 = the spec's 6 canonical scenarios). Test-building disproved rule 20/scenario 5 (template prefill does NOT pre-select participants — client setValuesFromTemplate drops them + nulls the assignee); spec corrected + ledger row 84 added |
| 20 | Area 2 — Editorial workflow & peer review | `submission-files` | verified | green(x2) | H·10 | 10 | yes | 40 atoms consolidated (the file-management home); dead legacy grids stay in UNASSIGNED, Vue FileManager is the live surface; test author + verifier browser-drove the FileManager controls (spec author hit the login block); fixed a real error (Review-Revision panel has no Download-All); ledger row 85 (edit() drops the submission-level file-edit event-log entry) |
| 21 | Area 2 — Editorial workflow & peer review | `editorial-activity-log` | verified | green(x2) | L·4 | 4 | yes | 8 surface atoms (grids+schema+event_log DB); EVLOG-* corpus co-claimed by firing features (rendered here); dev-DB-vs-test-DB reconciled — submit event DOES render ('Article submitted'); no separate Email tab (merged into History); ledger rows 86 (header-button vs backend divergence) + 87 (assistant offered a denied file History tab); rows 85+14 confirmed live |
| 22 | Area 2 — Editorial workflow & peer review | `document-library` | verified | green(x2) | L·4 | 4 | yes | 12 atoms; the context + submission Document Library (separate store from submission_files); library-vs-media seam corrected (5 grids + download pages reclaimed from the media hint); SECURITY ledger row 88 (unauthenticated downloadLibraryFile) + row 89 (description unenforced) — AREA 2 COMPLETE |
| 23 | Area 3 — Publication record, issues & scheduling | `publication-title-abstract-body` | verified | green(x2) | M·6 | 6 | yes | 7 atoms; Title & Abstract tab + Body Text (SciFlow) + Pandoc — both LIVE in default OJS (authoring-only, no reader consumer). Live drive RETRACTED all 3 spec ⚠ (false alarms from reading the lib/pkp BASE controller; OJS overrides it — the abstract DOES show required-marker+word-count); FORM-title-abstract reassigned from submission-wizard (base vs Details subclass) |
| 24 | Area 3 — Publication record, issues & scheduling | `contributors` | verified | green(x2) | H·10 | 10 | yes | 28 atoms; the Publication Contributors manager (reused in the wizard). CRediT is CORE (plugin off by default); reorder is arrow-based (drag hidden). Test author drove all mutations live. Verifier REFUTED the new ⚠ (delete/reorder ARE published-lock-gated via PublicationWritePolicy); ledger row 90 (role-delete refusal pops a generic error modal, not the nice message); fixed 2 atlas mis-tags (author-reviewer-grid→review-anonymity, author-files-grid→submission-files dead) |
| 25 | Area 3 — Publication record, issues & scheduling | `publication-metadata-references` | verified | green(x2) | M·7 | 7 | yes | 17 atoms; Publication Metadata tab (base PKPMetadataForm, no override — no false alarm) + References/citations. SECURITY ledger row 91 (SECOND auth hole after row 88): single-citation API routes have NO submission-access/publication-write policy — a non-assigned author edits any submission's citation (200+persisted), re-confirmed by all 3 layers; bulk routes DO enforce the lock inline. FORM-pkp-metadata reassigned from wizard-metadata; FEATURE-MAP categories-scope corrected (categories are on the Issue tab) |
| 26 | Area 3 — Publication record, issues & scheduling | `galleys` | verified | green(x2) | H·10 | 10 | yes | 13 atoms; the Publication Galleys manager (file vs remote URL, label/locale, reorder, DOI seam) + reader download. Test author confirmed the published-lock ⚠ LIVE (galleys editable post-publication behind a misleading 'View' label — ledger row 22b, deduped not re-added); reader PDF.js render driven live; scenario 11 corrected (unassigned editorial user refused the UI entirely; only authors get list-only); all 4 render plugins enabled by default |
| 27 | Area 3 — Publication record, issues & scheduling | `media-files` | verified | green(x2) | M·6 | 6 | yes | 9 atoms; the Publication Media section (batch upload, web/high-res variant_groups, sharing across galleys, author read-only) — confirmed a LIVE default surface. Media do NOT share the row-88 auth hole (unpublished media → anon 404). Published-lock ⚠ deduped to galleys row 22b (media cleaner, no 'View' mislabel). Fixed test-infra seed regression (row 93 → FIXED: seeder referenced removed VariantGroup::MAX_GROUP_SIZE). Variant group capped at 2 (link + version clone) |
| 28 | Area 3 — Publication record, issues & scheduling | `publication-identifiers` | verified | green(x2) | M·6 | 5 | yes | 4 atoms; the identifier fields are SCATTERED (FEATURE-MAP's gathered tab doesn't exist in 3.6): Identifiers tab = URN + custom pub-ids only (gated on a pub-id plugin, NOT DOIs); urlPath+pages on the Issue tab; article-number on Metadata; DOI has no Publication-tab field (read-only doiObject only). The identifiers form HONORS the published-lock (contrast: galleys ignores it, row 22b). base-vs-override checked clean; DOI entity atoms left for doi-management |
| 29 | Area 3 — Publication record, issues & scheduling | `publication-license` | verified | green(x2) | M·6 | 5 | yes | 3 atoms; Publication → Permissions & Disclosure tab (per-publication override of journal license/copyright defaults). Fields: License URL (editable, no Override when journal has no default), Copyright Holder + Year (Override toggles). Form HONORS the published-lock (like identifiers, unlike galleys). Publish snapshots empty fields from journal defaults; the issue-basis year reads datePublished timestamp not the label. base-vs-override clean; journal-default forms left for distribution-settings |
| 30 | Area 3 — Publication record, issues & scheduling | `publication-issue-assignment` | verified | green(x2) | M·6 | 5 | yes | 5 atoms; the Publication Issue tab (IssueEntryForm) — assign to an issue/section, urlPath/pages, categories, schedule into a future issue. Form HONORS the published-lock; base-vs-override clean; categoryIds persists despite readOnly (DAO special-case). SECURITY/UX ledger row 94 (cross-surface role inconsistency, both facets live-confirmed): a section editor is offered 'Assign To Future Issue' but the unpublished-issue list is manager-only 403 — the future option is unfulfillable AND opening the Issue tab on an already-scheduled article pops an on-load error dialog with an empty picker (can't view the placement) |
| 31 | Area 3 — Publication record, issues & scheduling | `publication-versioning` | verified | green(x2) | H·10 | 6 | yes | Spec verified in pilot; tests built this iteration (6 = the 6 canonical scenarios). Test-building found ledger row 18 is now FIXED (pkp/pkp-lib#12799, 2026-06-28, landed after the pilot verification) — a UI-scheduled article now computes SCHEDULED correctly; spec rule 24 + scenario 2 + ledger row 18 updated to FIXED. Also fixed an internal spec-prose inconsistency (scenario 3: a rolled-back non-current version reads 'Unpublished', not 'Unscheduled', per rule 4). Row 23b confirmed as-built |
| 32 | Area 3 — Publication record, issues & scheduling | `publication-publish-flow` | verified | green(x2) | H·11 | 8 | yes | 1 atom (TASK-publishsubmissions) — versioning owns the shared publish form/handler/API atoms (pilot), publish-flow owns the ACTION + the scheduled task. SECURITY-adjacent ledger row 95: the PublishSubmissions date-based auto-publish task is registered in NEITHER scheduler + no scheduledTasks.xml, so it never runs in OJS (doubly inert — scheduled articles carry no future datePublished either); OJS scheduled articles go live when their ISSUE publishes. Tests drove the publish-flow-specific surfaces (Done-stage trigger, OAI tombstone, republish-date, continuous/issue-less publish) avoiding versioning/issue-assignment overlap |
| 33 | Area 3 — Publication record, issues & scheduling | `publication-amendments` | verified | green(x2) | M·7 | 6 | yes | 2 atoms (Insert-Summary-of-Changes modal, updateType locale); the 3.6 amendments flow. LIVENESS SPLIT: editor-side CAPTURE is live (12 update-type options + new_version default, file-level summaryOfChanges, the Insert Content modal — all driven live) but the READER-SIDE display is NOT implemented (ledger row 96: the field copy promises a public amendment notice that no front-end template renders; only the Crossref crossmark deposit consumes update_type). Corrected a spec error: recommend-only editors are NOT view-only — canChangeMetadata gates amendment editing (live-verified 200). review_round_author_responses stays with review-rounds (FEATURE-MAP pointer was wrong) |
| 34 | Area 3 — Publication record, issues & scheduling | `data-availability-citations` | verified | green(x2) | L·4 | 4 | yes | 13 atoms; the Publication Data tab (data-availability statement + data-citation manager) — LIVE but off by default (appears when the journal enables the dataAvailability/dataCitations metadata modes). FORM-pkp-data-availability stays with submission-wizard-metadata (same form, intake+pub-tab); this claims the pub-tab fetch endpoint + the data-citation atoms. Ledger row 97 (data-citation manager controls not gated by the published-lock — but backend refuses 401, so a dead affordance, NOT data-loss like galleys 22b) + row 98 (data citations captured but rendered on no reader page, like amendments 96). POSITIVE: the data-citation API is PROPERLY scoped — non-assigned author refused read+write (the OPPOSITE of the row-91 reference-citation hole) |
| 35 | Area 3 — Publication record, issues & scheduling | `issue-management` | verified | green(x2) | H·11 | 10 | yes | 21 atoms (the issue-entity home — reassigned the issue grids/DB/galleys/notify-job+mail/AUTHZ from galleys/archive-toc/notifications, all single-owner). Legacy grids, manager/site-admin ONLY. HEADLINE side effect driven live: publishing an issue flips its SCHEDULED articles → Published + reader-visible + in the TOC + makes the issue current, and fires the ISSUE_PUBLISH_NOTIFY reader email (send-email checked by default; unchecking sends nothing). Fixed a spec error (new issue defaults all 4 show-toggles ON, so empty Title is refused). Ledger row 99: publish-issue unconditionally makes it current (publishing a back issue moves current backwards — LOW/intent). Area 3 COMPLETE (13/13) |
| 36 | Area 4 — Reader front end | `journal-homepage` | verified | green(x2) | M·7 | 7 | yes | 7 atoms (VUE-highlights-list-panel **transferred out** to highlights-featured-content 2026-07-04); Area 4 begins. The anonymous journal home page (PAGE-index-index) — current-issue TOC display, sidebar blocks, announcements section, highlights carousel. Test author drove all 3 positive cases live on scratch journals (spec author could only verify absent states) + added the sidebar/announcements/highlights scenario passthroughs to lib/pkp. Corrected the block set (6 core block plugins, no currentIssue block) and rule 12 (block enabled flag is LIVE, not legacy). Two could-surprise findings kept as OQ (announcements two-part gate; empty-sidebar default — both compose-from-empty affordances, not ⚠). recent-published list sorts by date_submitted = already-ledgered row 25 |
| 37 | Area 4 — Reader front end | `highlights-featured-content` | verified | green(x2) | L·4 | 4 | yes | 11 atoms; the highlights MANAGEMENT panel (Settings → Website → Highlights, HighlightsListPanel CRUD). Took VUE-highlights-list-panel from journal-homepage (its OQ3 resolved, count 8→7). Test author drove the rendered panel live (add/edit/reorder/delete + required-field guards). Ledger row 100 (NEW ⚠): GET /highlights/{id} always 500s (passes Context where Repository::get expects int contextId — masked because the panel edits from the pre-loaded list, but every highlight _href points at the dead route; one-line fix). URL not format-validated = plain rule (OJS-wide) |
| 38 | Area 4 — Reader front end | `article-landing` | verified | green(x2) | H·10 | 10 | yes | Verified 2026-07-04: all rules confirmed vs as-built (`ArticleHandler`/`article_details.tpl`/plugins/`load_frontend.js`/`OjsJournalMustPublishPolicy`/`canPreview`) + retained `article-landing.spec.js` (10 tests green x2). No spec-vs-code contradiction. The spec author could only verify absent states on the sparse dev DB; the test author SEEDED rich published articles on the test DB and drove ALL blocks live (metadata+ORCID, PDF via pdf.js, download, license/copyright/DOI, how-to-cite+BibTeX, /version/{id}+outdated notice, data-availability statement row 98, DC/Scholar meta tags, published-only 404, subscription gate). Resolved OQ3 (feature 39 folded here) + OQ1 (CSL is opt-in). 5 atoms single-owner. |
| 39 | Area 4 — Reader front end | ~~`article-recommendations`~~ **FOLDED into 38** | merged | n/a | ~~L·3~~ | 0 | n/a | MERGED into article-landing (feature 38) at verification: the recommend-by reader blocks (recommendByAuthor/recommendBySimilarity) are its only atoms and render ON the article page, so article-landing single-owns them. FEATURE-MAP annotated RESOLVED. Total features 92 → 91. |
| 40 | Area 4 — Reader front end | `issue-archive-toc` | verified | green(x2) | M·6 | 6 | yes | 5 atoms (PAGE-issue-{index,current,archive,view,download}); the reader ISSUE pages (IssueHandler) — archive listing, current-issue redirect, single-issue TOC grouped by section, full-issue galley download. GRID-toc-grid stays editorial (issue-management); SCHEMA-issue referenced; PLUGIN-catalog is a phantom (OMP/browse-category-section). Ledger row 101 NEW ⚠: the reader archive orders back issues by custom_issue_orders.seq ASC ONLY (no date/current tiebreaker) — a never-reordered journal has empty custom_issue_orders → UNDEFINED DB order, not newest-first (the editorial grid + OAI DO order correctly). Test proved it by seeding the same years into fresh journals → DIFFERENT orders across runs. Row 28 relocated here (within-section article order undefined). The login-redirect-not-404 for a missing/unpublished issue calibrated as OQ (OJS-wide default-deny; article-landing's 404 is the outlier) |
| 41 | Area 4 — Reader front end | `site-search` | verified | green(x2) | M·7 | 7 | yes | 4 atoms (PAGE-search-{index,search} + JOB-updatesubmissionsearchjob + DB-submissions_fulltext); the anonymous front-end article SEARCH. 3.6 REWROTE search onto Laravel Scout (pkp#8920): default DatabaseEngine, ONE table submissions_fulltext (old submission_search_* tables gone), pluggable OpenSearch engine; the 3 legacy hooks registered unsupported; similarDocuments is a dead 404. Indexing is ASYNC (UpdateSubmissionSearchJob on the database queue); re-index is CLI-only (rebuildSearchIndex.php, no UI form). Ledger row 102 ⚠ (CONFIRMED live on the Postgres test DB): orderBy=datePublished → HTTP 500 (DatabaseEngine::buildQuery orders a non-aggregated cp.date_published under GROUP BY; Postgres 42803, MySQL tolerates) — not reachable from stock UI but breaks any theme/plugin/URL setting it. RELEASED FORM-pkp-search-indexing-form to distribution-settings (it is the Distribution SEO meta-tag form, not search-index maintenance). Not-relevance-ranked (submission-id order) + async-needs-a-worker kept as OQs (job_runner=On drains end-of-request, so a default install DOES index). NOTE: test DB is PostgreSQL, so Postgres-specific bugs ARE reproducible here |
| 42 | Area 4 — Reader front end | `browse-category-section` | verified | green(x2) | M·6 | 6 | yes | 5 atoms (PAGE-catalog-{category,fullsize,thumbnail} + PLUGIN-blocks-browse [taken from journal-homepage] + DB-categories interim). The reader category-browse landing pages. SCOPE CORRECTION: the 3.6 Browse block is CATEGORY-ONLY (no section/author/title reader browse page exists; the stale by-issue/series refs are dead template comments); VUE-catalog-list-panel is OMP-only dead-code (UNASSIGNED). PKPCatalogHandler category/fullSize/thumbnail LIVE for type=category (type=monograph → 500). Ledger row 103 NEW ⚠: a category's Sort-by setting is computed then DISCARDED — reader listings are in submission-id order regardless (proven with A[low-id/Zzz/early] + B[high-id/Aaa/late] → [A,B]). Row 102 inherited (orderBy=datePublished → 500 on Postgres). Test author added a reusable lib/pkp scenario passthrough for publication→category assignment. No parent roll-up |
| 43 | Area 4 — Reader front end | `public-comments` | verified | green(x2) | M·6 | 6 | yes | 24 atoms (11 API-comment-* + VUE-pkp-comments/moderation page/table/reports-table + FORM-content-comments-form + PAGE-management-settings-usercomments + DB-user_comments/_settings/_reports + NOTIF-user-comment-{posted,reported} + 2 LOC); the 3.6 reader-comments feature. LIVENESS: OFF by default (enablePublicComments), LOGGED-IN-only posting (anon → 403, NO anonymous-posting setting — the anonymous gating is just login-required), MANDATORY approval no toggle (born unapproved; public GET = approved + author's own pending), moderators = manager+site-admin, owner-only delete, report by any logged-in reader. NOTIF owned here (synchronous DB task rows, no queue/email). Ledger row 104 ⚠ (un-approve leaves a stale approved-by-X stamp — shared settings writer won't null a non-multilingual setting; moderator-API leak, latent) + row 105 ⚠ (bulk DELETE /reports 500s with 2+ reports via a double-wrapped id array; effectively-dead route). First reader-WRITE flow — driven end-to-end through the real UI compose/report/delete + moderator API |
| 44 | Area 4 — Reader front end | `about-pages` | verified | green(x2) | L·3 | 5 | yes | 14 atoms (7 PAGE-about-* + 6 PAGE-information-* + PLUGIN-generic-staticPages); the public informational reader pages. Live-verified anonymous: About/Contact/Submissions/Privacy/editorialMasthead/editorialHistory/aboutThisPublishingSystem + information/{readers,authors,librarians}. LIVENESS/SCOPE corrections: /about/editorialTeam is a DEAD 404 (only editorialMasthead is live — UNASSIGNED phantom op); staticPages ships DISABLED (custom path 404s until enabled); the Submissions page does NOT list per-section policies (FEATURE-MAP overstated it). The public Editorial Masthead renders EMPTY on the seed (no users carry the per-assignment masthead flag) — calibrated OQ not ⚠ (the masthead-flag affordance exists + roster is UI-populatable, so a seed-data state not a code defect). Test closed OQ2: enable staticPages on a scratch journal → create a custom page (manager grid) → view anonymous (200). Block plugins referenced (journal-homepage owns them), not double-claimed. Bonus: contact emails are document.write-obfuscated |
| 45 | Area 4 — Reader front end | `oai-pmh` | verified | green(x2) | M·8 | 9 | yes | 8 atoms (PAGE-oai-index + DB-oai_resumption_tokens + PLUGIN-oaiMetadataFormats-{dc,marc,marcxml,oaiJats} + libpkp-dc + PLUGIN-generic-driver); the OAI-PMH 2.0 harvesting endpoint /oai (journal + site-level). All 6 verbs + error codes + resumption tokens + the deleted-record tombstone driven live via XML-over-HTTP. Default-enabled formats = exactly oai_dc/oai_marc/marcxml; oaiJats OFF by default (prefix `jats`, per-context — test enabled it on a scratch journal + emitted valid JATS, closing OQ1); rfc1807 REMOVED in 3.6 (I12948) → dead-coded; driver plugin OFF (adds a driver open-access set). Ledger row 106 ⚠ LOW: Identify advertises second granularity but from/until filter at DAY granularity (DATE cast from an earlier Postgres crash-fix) — a precise incremental window silently widens to the whole day (no data loss). Fixed spec rule 9: ListSets pages at 100 (maxRecords), NOT 50 — $maxSets is dead code (live-confirmed) |
| 46 | Area 4 — Reader front end | `web-feeds-syndication` | verified | green(x2) | L·4 | 5 | yes | 3 atoms (PAGE-sitemap-index + PLUGIN-generic-{webFeed,announcementFeed}); the syndication surfaces — LAST Area 4 feature. LIVENESS corrections: sitemap.xml is ALWAYS-ON core (/sitemap → urlset, /index/sitemap → sitemapindex); webFeed is ENABLED BY DEFAULT (overrides getContextSpecificPluginSettingsFile → enabled=1 per journal), served at /gateway/plugin/WebFeedGatewayPlugin/{rss=RSS1,rss2=RSS2,atom} — there is NO /feed/{rss,rss2,atom} alias + NO per-issue /feed/{id} (all 404); announcementFeed is DISABLED by default (+ gated on enableAnnouncements). Ledger row 107 ⚠: the announcementFeed atom.tpl + rss.tpl (RSS 1.0) emit MALFORMED dates (obsolete strftime %-codes → Carbon format → <updated>%2026-%07-%05UTC...</updated>), so the Atom announcement feed is INVALID; RSS 2.0 + all webFeed feeds are correct. PLUGIN-blocks-browse referenced (browse-category-section owns it). OQ4: unknown webFeed format → HTTP 500 (ledger candidate). publicknowledge runs ISSUE mode not recent |
| 47 | Area 5 — Users, roles & access | `registration-login` | pending | pending | H·10 | 0 | — | — |
| 48 | Area 5 — Users, roles & access | `password-flows` | pending | pending | M·6 | 0 | — | — |
| 49 | Area 5 — Users, roles & access | `user-profile` | pending | pending | M·7 | 0 | — | — |
| 50 | Area 5 — Users, roles & access | `user-management` | pending | pending | H·10 | 0 | — | — |
| 51 | Area 5 — Users, roles & access | `user-invitations` | pending | pending | M·8 | 0 | — | — |
| 52 | Area 5 — Users, roles & access | `roles-permissions` | pending | pending | H·11 | 0 | — | — |
| 53 | Area 5 — Users, roles & access | `login-as` | pending | pending | M·6 | 0 | — | — |
| 54 | Area 5 — Users, roles & access | `editorial-masthead` | pending | pending | L·4 | 0 | — | — |
| 55 | Area 5 — Users, roles & access | `site-access-restrictions` | pending | pending | M·6 | 0 | — | — |
| 56 | Area 6 — Journal & site settings | `journal-masthead-settings` | pending | pending | L·4 | 0 | — | — |
| 57 | Area 6 — Journal & site settings | `website-appearance-settings` | pending | pending | M·7 | 0 | — | — |
| 58 | Area 6 — Journal & site settings | `workflow-settings` | pending | pending | M·7 | 0 | — | — |
| 59 | Area 6 — Journal & site settings | `distribution-settings` | pending | pending | M·6 | 0 | — | — |
| 60 | Area 6 — Journal & site settings | `email-templates-management` | pending | pending | M·7 | 0 | — | — |
| 61 | Area 6 — Journal & site settings | `navigation-menus` | pending | pending | M·7 | 0 | — | — |
| 62 | Area 6 — Journal & site settings | `sections` | pending | pending | M·7 | 0 | — | — |
| 63 | Area 6 — Journal & site settings | `categories` | pending | pending | L·4 | 0 | — | — |
| 64 | Area 6 — Journal & site settings | `announcements` | pending | pending | M·6 | 0 | — | — |
| 65 | Area 6 — Journal & site settings | `languages-locales` | pending | pending | M·7 | 0 | — | — |
| 66 | Area 6 — Journal & site settings | `site-settings` | pending | pending | L·4 | 0 | — | — |
| 67 | Area 6 — Journal & site settings | `site-administration` | pending | pending | M·7 | 0 | — | — |
| 68 | Area 7 — Integrations, identifiers & monetization | `plugin-management` | pending | pending | M·6 | 0 | — | — |
| 69 | Area 7 — Integrations, identifiers & monetization | `doi-management` | pending | pending | H·10 | 0 | — | — |
| 70 | Area 7 — Integrations, identifiers & monetization | `doi-deposit` | pending | pending | M·7 | 0 | — | — |
| 71 | Area 7 — Integrations, identifiers & monetization | `open-peer-review-display` | pending | pending | L·4 | 0 | — | — |
| 72 | Area 7 — Integrations, identifiers & monetization | `orcid` | pending | pending | M·7 | 0 | — | — |
| 73 | Area 7 — Integrations, identifiers & monetization | `citation-style-language` | pending | pending | L·4 | 0 | — | — |
| 74 | Area 7 — Integrations, identifiers & monetization | `indexing-meta-tags` | pending | pending | L·4 | 0 | — | — |
| 75 | Area 7 — Integrations, identifiers & monetization | `subscriptions-management` | pending | pending | H·10 | 0 | — | — |
| 76 | Area 7 — Integrations, identifiers & monetization | `subscription-access` | pending | pending | H·10 | 0 | — | — |
| 77 | Area 7 — Integrations, identifiers & monetization | `payments` | pending | pending | M·8 | 0 | — | — |
| 78 | Area 7 — Integrations, identifiers & monetization | `institutions` | pending | pending | L·4 | 0 | — | — |
| 79 | Area 8 — System, communications & administration | `email-delivery` | pending | pending | M·6 | 0 | — | — |
| 80 | Area 8 — System, communications & administration | `notifications` | pending | pending | M·7 | 0 | — | — |
| 81 | Area 8 — System, communications & administration | `jobs-queue` | pending | pending | L·4 | 0 | — | — |
| 82 | Area 8 — System, communications & administration | `scheduled-tasks` | pending | pending | L·4 | 0 | — | — |
| 83 | Area 8 — System, communications & administration | `rest-api` | pending | pending | H·10 | 0 | — | — |
| 84 | Area 8 — System, communications & administration | `native-xml-import-export` | pending | pending | H·10 | 0 | — | — |
| 85 | Area 8 — System, communications & administration | `user-import-export` | pending | pending | M·6 | 0 | — | — |
| 86 | Area 8 — System, communications & administration | `pubmed-export` | pending | pending | L·4 | 0 | — | — |
| 87 | Area 8 — System, communications & administration | `usage-statistics` | pending | pending | M·7 | 0 | — | — |
| 88 | Area 8 — System, communications & administration | `counter-sushi` | pending | pending | M·6 | 0 | — | — |
| 89 | Area 8 — System, communications & administration | `editorial-statistics` | pending | pending | L·4 | 0 | — | — |
| 90 | Area 8 — System, communications & administration | `csv-reports` | pending | pending | L·4 | 0 | — | — |
| 91 | Area 8 — System, communications & administration | `site-maintenance` | pending | pending | L·4 | 0 | — | — |
| 92 | Area 8 — System, communications & administration | `installation-upgrade` | pending | pending | L·4 | 0 | — | — |
## Parked (needs maintainer)

_(none yet)_

## Run log

_(loop appends one line per feature: date · feature · outcome · #tests · runtime delta)_

- 2026-07-02 · submission-wizard · done (spec verified, adversarial + live affordance probes) · 6 tests · file runtime ~24 s
- 2026-07-02 · submission-wizard-metadata · done (spec verified; all 4 deferred affordances held on live probe; ledger row 62: vocab journal-scoping bug) · 7 tests · both wizard files ~39 s
- 2026-07-02 · reviewer-suggestions · done (spec verified; 1 claim refuted+corrected — assistants denied the wizard, no dead button; ledger row 64: author API sees reviewer uptake = anonymity leak; row 63: Steps aria-current gap) · 6 tests · full retained suite ~1.1 min. Also: hardened SubmissionWizardPage.continueStep() against swallowed clicks (4 distinct load-flakes eliminated; suite green ×2 at 21 tests)
- 2026-07-03 · submission-drafts · done (spec verified; email-toggle claim corrected — saveForLater mail is unconditional, toggle unwired; OQ4 settled: single DELETE is the workflow Delete button on declined submissions; ledger row 65 re-proven on fresh data + row 66 added: bulk-delete silently caps at 30 ids, >30 batch 404s wholesale) · 6 tests · full suite 25 tests ~59 s
- 2026-07-03 · author-dashboard · done (spec verified; 4 claims refuted+corrected: review-stage declines keep activity cell, Notifications oldest-first + hidden-when-empty, no published-banner on scheduled, open-review shows reviewer names by design; legacy authorDashboard = live 302 shim, 3 legacy grids UI-orphaned → UNASSIGNED) · 6 tests · full suite 31 tests ~1.2 min — AREA 1 COMPLETE
- 2026-07-03 · editorial-dashboards · done (spec verified; needs-reviews threshold corrected 2→1; pure-site-admin edge documented — empty dashboard without manager enrolment; ledger rows 67 dead legacy tasks page, 68 inert reviewer list controls, 69 COI guard enrolment-gated; every deferred activity-cell/reviewer-text affordance live-verified) · 11 tests · full suite 42 tests ~1.6 min
- 2026-07-03 · workflow-stage-navigation · done (spec verified; all 5 deferred affordances live incl. SQL-forced Done-unpublished + revoked-enrolment surgery; refusal-surface wording refuted+fixed; ledger rows 70 stage-less 500, 71 draft deep-link bypass strengthened, 72 revoked-enrolment group-vs-role mismatch; scenario-API title guard added to lib/pkp testing infra) · 6 tests · full suite 48 tests ~2.1 min
- 2026-07-03 · editorial-decisions · done (spec verified — the decision-engine home; all 14 transition chains live-verified, recommend-only + Done-stage cycle + revert asymmetry confirmed; suspected returnToDone 500 REFUTED as access-gate-shielded — no ledger row; permission surface corrected to real 401 roleBasedAccessDenied). Also hardened submission-drafts flaky search (bare search()→searchAndSettle + selection-mode guard) → full suite green ×2) · 13 tests · full suite 61 tests ~2.4 min
- 2026-07-03 · stage-participants · done (spec verified; OQ1 answered — recommend-only SE CAN add participants; both-live confirmed Vue list + legacy add/remove grid; ledger rows 73 dead editor-assign notification, 74 re-assign drops flags, 75 NEW recommend-only/self flag guards skipped on Add; Copyediting picker not a strict superset — Funding coordinator drops off) · 8 tests · full suite 69 tests ~3.3 min
- 2026-07-03 · send-to-review · done (spec verified — thin stage-1-workspace/seam spec, 1 atom SCHEMA-submission-ojs; tests deliberately assert workspace UX not decision mechanics (owned by editorial-decisions); verifier corrected Send-to-Text-Editor to manager-only, confirmed Delete-gate incl. unassigned-manager fallback, reviewer-suggestions rail self-hides when empty; no ledger rows) · 6 tests · full suite 75 tests ~2.9 min
- 2026-07-03 · assign-and-manage-reviewers · done (spec verified, 19 atoms; PRIORITY affordance probe OQ6 resolved CLEAN via live browser — assistant's Create-New/Enroll links + Editorial-Notes are genuinely hidden, no mismatch; recommend-only-SE-manages-reviewers verified live; both-live grid confirmed; found API-only status-gate gap (resend to accepted nulls dateConfirmed, UI-shielded, no ⚠); claimed EVLOG-REV-UNCON + AUTHZ-review-assignment-required-policy) · 11 tests · full suite 86 tests ~3.3 min
- 2026-07-03 · reviewer-response · done (spec verified, 17 atoms; reviewer's full journey — accept/decline, 4-step wizard, recommendation, comments, attachments, one-click access; edit-after-submit question resolved COSMETIC (saveStep throws 'Review already completed' server-side, enabled <select> is inert); all 3 deferred affordances upgraded to live-verified; ledger rows 76-78 low-severity cosmetic; both-live Vue banner + legacy Smarty wizard) · 10 tests · full suite 96 tests ~4.0 min
- 2026-07-03 · review-forms · done (spec verified, 9 atoms; the manager's review-form builder — legacy grid is the sole live surface, no Vue replacement; ledger row 79 NEW ⚠: activate confirm over-promises — an in-use form CAN be deactivated (no guard); ledger row 17b RETRACTED — confirm strings are not empty; review_form_responses correctly left to reviewer-response, structure-vs-write split) · 7 tests · full suite 103 tests ~3.4 min (fresh DB)
- 2026-07-03 · review-rounds-and-revisions · done (spec verified, 22 atoms; the round lifecycle + author revision/response cycle, decision half referenced to editorial-decisions; author-response manager confirmed LIVE e2e; 10 round-status states verified live; ledger row 80 ⚠ author sees editor status phrasing for REVIEWS_READY/OVERDUE; new-round-status-recompute downgraded to plain rule (no data loss); test-agent 'inert Submit Response button' REFUTED — it opens the modal) · 11 tests · full suite 114 tests ~4.3 min. Also hardened review-forms delete test (retry click+confirm) against a legacy-grid flake that recurred under load
- 2026-07-03 · recommend-only-editors · done (spec verified, ZERO atoms — a role/behavior spec, every surface single-owned elsewhere; re-hinted the reviewer-recommendation OPTIONS cluster to workflow-settings across 6 atlas files; OQ4 resolved live: a recommend-only editor at the Submission stage sees exactly one finalizing 'Send for Review' button (by design); verifier browser-drove the affordances + fixed a spec error 'Change decision' not 'Change recommendation'; parked orphan EVLOG-REV-PROXY-REC in UNASSIGNED) · 6 tests · full suite 120 tests ~5.0 min
- 2026-07-03 · review-anonymity · done (spec verified — the single-home visibility matrix, driven live as all 5 parties × 3 methods via BOTH UI and per-party API blanking; verifier found a NEW anonymity LEAK adversarially — ledger row 81: a double-anon reviewer reads the author's real name+id via the submission API participants[] array (un-anonymized) despite authorsString blanked, reachable from invitation; file-content no-scrub correctly downgraded to plain rule (long-standing intent); 1 atom VUE-pkp-open-review, rest deferred) · 10 tests · full suite 130 tests ~4.4 min (fresh DB)
- 2026-07-03 · copyediting-stage · done (spec verified, 14 atoms; thin stage-4 workspace, decisions referenced to editorial-decisions; test author + verifier browser-drove all 6 roles (the spec author hit a login-autofill block and could only API-verify); CORRECTED a code-verified claim — the 'Assign a copyeditor' prompt is accept/send-to-production-gated, absent on the skip-review path (ref ledger row 8); ledger row 82 NEW: the prompt keys off any stage-4 discussion, not the actual copyeditor assignment; EVLOG-EMAIL-COPY-* reassigned production→copyediting per FEATURE-MAP) · 6 tests · full suite 136 tests ~4.8 min
- 2026-07-03 · production-stage · done (spec verified, 21 atoms; thin stage-5 workspace, decisions referenced; test author + verifier browser-drove all roles incl. the author view the spec author hadn't probed (discussion-only, confirmed); GRID-final-draft seam RESOLVED cross-spec — belongs to copyediting (stage-5 pane exposes only PRODUCTION_READY), reassigned + copyediting bumped 14→16 atoms; ledger row 83 NEW (Repo-path galley leaves 'Awaiting Galleys' stale); dead-code rows recorded in UNASSIGNED (12/14 email-logs, index-notify, OMP galley-approval); layout-editor-sees-Schedule confirmed as-built (nav shortcut, real authority gated elsewhere)) · 7 tests · full suite 143 tests ~5.2 min
- 2026-07-04 · tasks-discussions · done (spec already verified in pilot; tests built this iteration = the spec's 6 canonical scenarios; test-building DISPROVED rule 20/scenario 5 — applying a template does NOT pre-select participants (client setValuesFromTemplate copies title/type/desc/due, drops participants, nulls the assignee) despite the fromTemplate API returning them; spec corrected + ledger row 84; rule-11 Closed-no-reopen re-confirmed as a manager) · 6 tests · full suite 149 tests ~5.2 min (fresh DB)
- 2026-07-04 · submission-files · done (spec verified, 40 atoms — the file-management home consolidating the file-grid seams copyediting/production/send-to-review/review-rounds deferred; Vue FileManager confirmed THE live surface, legacy grids stay dead in UNASSIGNED; test author + verifier browser-drove the FileManager mechanics (upload wizard/revise/dependent/download-all/non-ASCII/delete/attacher) — the spec author hit the login block; verifier fixed a real error (Review-Revision panel offers no Download-All), resolved the seed-revisions quirk (projection always excludes the current revision → fresh file shows revisions:[] whether seeded or UI-uploaded), added 2 undocumented API details; ledger row 85 (edit() drops the submission-level file-edit event-log entry, +isTranslate typo)) · 10 tests · full suite 159 tests ~5.7 min
- 2026-07-04 · editorial-activity-log · done (spec verified, 8 surface atoms; the event-log SURFACE — submission Activity Log modal + file Information Center; EVLOG-* corpus co-claimed by firing features (rendered here, not re-owned); verifier reconciled the spec author's dev-DB evidence against port-8000 — the submit event DOES render ('Article submitted', OJS locale override); no separate Email tab (emails merged into History); ledger rows 86 (unassigned-manager has no button but can open the modal by direct nav) + 87 (assistant offered a file History tab whose grid denies); rows 85+14 confirmed live; the {$contextName} email placeholder is a Mail::fake seeding artifact, NOT ledgered; corrected a wrong inline comment in the retained test) · 4 tests · full suite 163 tests ~5.4 min (fresh DB)
- 2026-07-04 · document-library · done (spec verified, 12 atoms; the context + submission Document Library, a separate store from submission_files; library-vs-media atom seam corrected — 5 grids + 2 download pages reclaimed from the provisional media hint; **SECURITY ledger row 88**: the bare page op libraryFiles/downloadLibraryFile serves NON-PUBLIC journal library files with NO authentication (HTTP 200 + bytes, logged-out AND as a plain author) — the handler declares no role/policy and defaults context-file access to true, while the UI component route is properly gated; independently re-confirmed live by both test author and verifier. Row 89 (description asterisked-but-unenforced). CONTRACT type absent; 403-refusals return 200 under php -S) · 4 tests · full suite 167 tests ~5.9 min — **AREA 2 COMPLETE (17/17)**
- 2026-07-04 · publication-title-abstract-body · done (spec verified, 7 atoms; Area 3 begins — the Publication Title & Abstract tab + the SciFlow Body Text editor + the Pandoc importer, both LIVE in default OJS 3.6 (authoring-only, no reader consumer of SUBMISSION_FILE_BODY_TEXT). Live drive RETRACTED all 3 of the spec author's ⚠ — false alarms from code-reading the lib/pkp BASE getPublicationTitleAbstractForm(); OJS OVERRIDES it (SubmissionController.php) so the abstract DOES show a *Required marker + word-count meter, section-driven, and enforces required; no ledger rows. FORM-title-abstract reassigned from submission-wizard (base class here, Details subclass keeps FORM-details); corrected the retained test's stale header comment) · 6 tests · full suite 173 tests ~5.7 min (fresh DB)
- 2026-07-04 · contributors · done (spec verified, 28 atoms; the Publication Contributors manager reused in the wizard. CRediT collection is CORE (the credit plugin is OFF by default, only adds reader display); reorder is arrow-based (drag handle present-but-CSS-hidden); ORCID field display-only. Test author drove every mutation live (add/edit/delete/reorder/primary/CRediT/org/multilingual/published-lock/role-config on a scratch journal) — the spec author could only render read-only. Verifier REFUTED the spec's new ⚠ (delete/reorder DO honor the published-lock — PublicationWritePolicy embeds PublicationCanBeEditedPolicy); confirmed ledger row 90 (contributor-role-delete refusal pops a generic network-error modal instead of the specific message); fixed 2 atlas mis-tags. Also hardened SubmissionWizardPage.saveForLater() against a click-swallow flake that hit under the 183-test load) · 10 tests · full suite 183 tests ~7.0 min
- 2026-07-04 · publication-metadata-references · done (spec verified, 17 atoms; the Publication Metadata tab (base PKPMetadataForm used directly — no OJS override, so NO abstract-required-style false alarm) + the References/citations tab (raw + structured edit + reprocess). **SECURITY ledger row 91 — the SECOND authorization hole (after the row-88 library-file one)**: the single-citation API routes GET/PUT/DELETE /citations/{id} carry NO SubmissionAccessPolicy and NO PublicationWritePolicy, so a plain author with NO role on the target submission edits any submission's citation via the API (200 + persisted 'TAMPERED') on any publish state — independently confirmed by the spec author, test author, AND verifier. Refined: the BULK routes DO enforce the published-lock inline (403), so only the single-citation routes are the gap (the retained scenario 6 depends on that inline lock). FORM-pkp-metadata reassigned from submission-wizard-metadata (base here, ForTheEditors subclass there); FEATURE-MAP categories-scope corrected — categories are on the Issue tab (publication-issue-assignment), not the Metadata tab. Row 92 (initial-import DOI drop, minor). Reprocess completes synchronously in test mode (job_runner On)) · 7 tests · full suite 190 tests ~6.3 min (fresh DB)
- 2026-07-04 · galleys · done (spec verified, 13 atoms; the Publication Galleys manager — file vs remote-URL galleys, label/locale, arrow reorder, the galley-DOI seam, the reader download/render. All 4 render plugins (htmlArticleGalley/pdfJsViewer/jatsTemplate/lensGalley) enabled by default. Test author closed both spec-author gaps live: (1) seeded a published article with a PDF galley and drove the anonymous reader PDF.js render + %PDF download; (2) CONFIRMED the published-lock ⚠ — on a published article the row action reads 'View' but opens an editable Save-enabled form and the label edit PERSISTS (GalleyManager.vue ignores canEditPublication). Verifier DEDUPED it to the existing ledger row 22b (wave-6, already cited by publication-versioning) + enhanced with live re-confirmation, not a duplicate row; corrected scenario 11 (an unassigned editorial user is refused the Galleys UI entirely — only authors get list-only); fixed 4 spec imprecisions) · 10 tests · full suite 200 tests ~6.8 min
- 2026-07-04 · media-files · done (spec verified, 9 atoms; the Publication Media section — batch upload of SUBMISSION_FILE_MEDIA, web/high-res variant_groups linking, sharing a media file across galleys, author read-only. Confirmed a LIVE default surface (MediaFileManager on the Publication menu after Galleys, modern 2026 code, no legacy grid). SECURITY CHECK: media do NOT share the row-88 library-file hole — an unpublished media file 404s for anon via ArticleHandler (behind userCanViewGalley), unlike the ungated libraryFiles page op. Published-lock ⚠ DEDUPED to galleys row 22b (media is cleaner — no misleading 'View' label). Test author FIXED a test-infra seed regression (ledger row 93 → FIXED: seedMediaFiles referenced the removed VariantGroup::MAX_GROUP_SIZE; replaced with a local const 2 so grouped-media seeding works) + drove the Media manager UI live (spec author could only use the API). Variant group hard-capped at 2 (link() + version() clone)) · 6 tests · full suite 206 tests ~6.4 min (fresh DB)
- 2026-07-04 · publication-identifiers · done (spec verified, 4 atoms; KEY finding — the FEATURE-MAP's gathered 'Identifiers tab: DOI/URN/pages/article-number' does NOT exist in 3.6; the fields are SCATTERED and the spec now documents each home precisely: Identifiers tab = URN + custom pub-ids only (gated on a pub-id plugin enabled for Publication → 403 noEnabledIdentifiers otherwise; DOIs-on does NOT open it); urlPath+pages = Issue tab (IssueEntryForm, editorial/production-only); article-number/publisher-id = Metadata tab (author-reachable); DOI = no Publication-tab field, only read-only doiObject (assign is on the DOIs page, doi-management's surface). The identifiers form HONORS the published-lock (editor warn-not-lock, edit persists behind the banner) — the deliberate CONTRAST with galleys which ignores it (row 22b). base-vs-override checked CLEAN (base form used directly, no false alarm). Test author closed the URN liveness gap via the scenario plugins passthrough (fires the field-injection hook the spec author's SQL-enable missed); verifier claimed a missing form-fetch atom (3→4). DOI entity atoms (SCHEMA-doi/DB-dois) left for doi-management) · 5 tests · full suite 211 tests ~7.1 min
- 2026-07-04 · publication-license · done (spec verified, 3 atoms; the Publication Permissions & Disclosure tab — per-publication override of the journal license/copyright defaults. Fields: License URL (directly editable with no Override when the journal has no default license), Copyright Holder (multilingual, per-locale Override) + Copyright Year (Override), both disabled behind the toggle when a journal default exists. The form HONORS the published-lock (editor warn-not-lock, edit persists) — same as identifiers, the deliberate CONTRAST with galleys. Publish-time snapshot driven live: Repository::publish freezes each EMPTY field from the journal defaults (holder to journal name map, year from issue-basis vs submission-basis, license to journal default). Nuance nailed: the issue-basis year reads the issue datePublished TIMESTAMP not the year label, and IssueProcessor stamps datePublished as TODAY on seed, so an inherited year is the current seed year not 2014 (product rule is correct, the 2014 is only a display label). base-vs-override checked CLEAN. Author refused with 401. Journal-default forms left for distribution-settings) · 5 tests · full suite 216 tests ~6.6 min (fresh DB)
- 2026-07-04 · publication-issue-assignment · done (spec verified, 5 atoms; the Publication Issue tab / IssueEntryForm — the assignment radio (Don't Assign vs current/back/future issue), section, urlPath/pages, categories, scheduling into a future issue. Form HONORS the published-lock (editor warn-not-lock, edit persists) — same as identifiers/license, contrast with galleys. base-vs-override clean (IssueEntryForm is OJS-only); categoryIds persists despite a readOnly schema flag (deliberate DAO special-case, not a bug). Author 401, no Issue tab. SECURITY/UX ledger row 94 (cross-surface role inconsistency, BOTH facets live-confirmed): (a) a section editor is offered 'Assign To Future Issue' by /issues/assignmentOptions (200, no role filter) but /issues?isPublished=0 is manager-only (403) — the future option is offered-but-unfulfillable (empty required picker); (b) opening the Issue tab on an ALREADY-SCHEDULED article auto-fetches unpublished issues on load → 403 → an on-load error dialog + empty picker, so a section editor can't even VIEW the placement of a scheduled article. Fix direction: role-filter the future options + skip the on-load fetch for non-managers, OR widen the unpublished-issue read to assigned section editors) · 5 tests · full suite 221 tests ~7.3 min
- 2026-07-04 · publication-versioning · done (spec already verified in pilot; tests built this iteration = the 6 canonical scenarios: correct-a-published-article/create-new-version, schedule-into-future-issue, roll-back-latest, author-locked-out, blocked-publish, version-numbering-ladder. Test-building surfaced that ledger row 18 (dashboard mis-files a UI-scheduled version as Published) is now FIXED by pkp/pkp-lib#12799 (commit d52aa4c84b, 2026-06-28) which landed AFTER the pilot 2026-07-02 verification — publishPublication now recomputes status via updateStatus+updateCurrentPublication instead of hard-coding STATUS_PUBLISHED; I confirmed the code fix myself and updated spec rule 24 + scenario 2 + ledger row 18 to FIXED. Also fixed an internal spec inconsistency: scenario-3 prose said a rolled-back non-current version reads 'Unscheduled' but rule 4 + the app say 'Unpublished' (Unscheduled is queued-AND-current only). Row 23b (blocked publish persists the pre-publish intent) confirmed as-built) · 6 tests · full suite 227 tests ~7.1 min (fresh DB)
- 2026-07-04 · publication-publish-flow · done (spec verified, 1 atom TASK-publishsubmissions; the publish/unpublish/schedule ACTION + preconditions + status transitions + the reader visibility flip. Atom ownership resolved clean: publication-versioning (pilot) owns the shared publish atoms (FORM-publish-form, the publish handler, publish/unpublish API), publish-flow owns the ACTION + the scheduled task + references the rest — no double-claim. HEADLINE ⚠ ledger row 95: the PublishSubmissions date-based auto-publish task is registered in NEITHER scheduler and there is no scheduledTasks.xml, so it never runs in OJS — and it is DOUBLY inert (0 of 19 SCHEDULED publications carry a datePublished, so even run manually its guard skips everything); OJS scheduled articles instead go live when their ISSUE is published (LOW/intent-call — reads as an OPS continuous-publishing feature). Verifier corrected an overclaim: the recommend-only publish gate is at the button+API, not the modal (the modal only role-excludes the author). Tests (8) drove the publish-flow-SPECIFIC surfaces the siblings do not: the reader visibility flip incl. the issue-TOC listing, unpublish->404+OAI-tombstone (GetRecord status=deleted, which the spec author could not drive), the Done-stage trigger (publish->stage 6 + decision 33, unpublish->stage 5 + decision 34), republish-keeps-date, continuous + issue-less publish, and the issue-publish->scheduled-goes-live path on a scratch journal — skipping schedule/blocked-publish/VoR-ladder as versioning-owned) · 8 tests · full suite 235 tests ~7.7 min
- 2026-07-04 · publication-amendments · done (spec verified, 2 atoms; the 3.6 amendments flow — versioned Update Type + Summary of Changes. LIVENESS SPLIT established: editor-side CAPTURE is fully live (12 update-type options + new_version default on both the publish version-form and the Publication-Settings Version-and-Updates group; the file-level summaryOfChanges on review-revision files; the Insert Content modal that fills the submission-locale public notice — all driven live green) BUT the READER-SIDE display is NOT implemented in 3.6. Ledger row 96: the field copy publication.summaryOfChanges.description promises the summary will appear publicly as the version amendment notice, but NO front-end template renders it (or the update type) — the sole consumer is the Crossref crossmark <update type> deposit; reader-absence live-confirmed. Test author found + verifier live-confirmed a spec error: a recommend-only editor is NOT view-only — recommendOnly gates only the decision pane; publication editing is gated by canChangeMetadata (defaults true for section editors), so a recommend-only editor with metadata-edit permission edits amendment fields fine (200); actor table corrected. Seam resolved: the FEATURE-MAP review_round_author_responses (published-version path) pointer was WRONG — that table stays single-owned by review-rounds; the amendment flow uses summaryOfChanges + publications.update_type) · 6 tests · full suite 241 tests ~7.5 min (fresh DB)
- 2026-07-04 · data-availability-citations · done (spec verified, 13 atoms; the Publication Data tab — the data-availability statement + the data-citation manager. LIVE but off by default (the tab appears once the journal enables the dataAvailability and/or dataCitations metadata mode — both off on publicknowledge). FORM-pkp-data-availability seam resolved: it stays single-owned by submission-wizard-metadata (same form at intake + pub-tab), so this claims the pub-tab fetch endpoint + the data-citation atoms (SCHEMA + 6 API + manager + DB). Ledger row 97 (the data-citation manager Add/Order/Edit/Delete controls are NOT gated by the published-lock — but the backend PublicationWritePolicy refuses 401, so it is a DEAD affordance not a data-loss like galleys row 22b where the write persists — verifier sharpened this distinction) + row 98 (data citations captured but rendered on NO reader page, same shape as amendments row 96 — the manager copy promises they appear alongside references). POSITIVE finding (given 2 auth holes already found): the data-citation API is PROPERLY scoped — a non-assigned author is refused on both read and write, the exact OPPOSITE of the row-91 reference-citation single-citation hole. base-vs-override clean. Test author added the dataCitations scenario passthrough to lib/pkp) · 4 tests · full suite 245 tests ~8.2 min
- 2026-07-04 · issue-management · done (spec verified, 21 atoms — the last Area 3 feature, the issue-entity HOME. Reassigned many atoms from siblings: the issue grids + DB-issues from issue-archive-toc, issue_galleys + the issue AUTHZ policies from galleys, the notify JOB + MAIL from notifications — all confirmed single-owner, no orphan. The Issues management page + all grids (Future/Back/TOC/issue-galley) are LEGACY and MANAGER/SITE-ADMIN ONLY (the manager side of issue-assignment row-94). HEADLINE side effect driven live: publishing a future issue (legacy FutureIssueGridHandler::publishIssue) flips its SCHEDULED articles → Published (status 5→3) + reader-visible + listed in the TOC + makes the issue current, and fires the ISSUE_PUBLISH_NOTIFY reader email (the send-email checkbox is checked by DEFAULT; unchecking sends nothing) + the bell notification. unpublish keeps articles Scheduled-in-issue; delete returns them to Queued. Fixed a spec error: a new issue defaults ALL FOUR show-toggles ON (showTitle=1), so an empty Title is refused unless Show Title is unchecked. Ledger row 99: publishIssue calls updateCurrent unconditionally, so publishing a back-dated issue silently moves current backwards (LOW/intent-call, reversible via Set Current). AREA 3 COMPLETE (13/13)) · 10 tests · full suite 255 tests ~8.4 min (fresh DB)
- 2026-07-04 · journal-homepage · done (spec verified, 8 atoms; AREA 4 (reader front end) BEGINS. The anonymous journal home page (IndexHandler / PAGE-index-index) — the current-issue TOC display, the sidebar block plugins, the announcements section, the highlights carousel. Corrected the block set: 6 core block plugins (browse, developedBy, information, languageToggle, makeSubmission, subscription), NO currentIssue block (the current-issue is an in-content TOC section). The spec author verified the ABSENT states (publicknowledge has no sidebar/highlights/announcements) but could not seed the positive cases; the test author drove ALL THREE live on scratch journals (sidebar renders the array in order + blocks auto-enable per-context; announcements render under the two-part gate enableAnnouncements + numAnnouncementsHomepage>0 + an active announcement; the highlights carousel renders from an image-less highlight) and added the sidebar/announcements/highlights scenario passthroughs + two new bootstrap processors to lib/pkp. Verifier corrected rule 12 (the block enabled flag is LIVE, not legacy — it is both the sidebar-management option set and the render gate) and noted the recent-published homepage list sorts by date_submitted not publication date = already-ledgered row 25. The two could-surprise findings (announcements two-part gate, empty-sidebar default) kept as OQ not ⚠ — both are compose-from-empty affordances, not contradictions. VUE-highlights-list-panel kept interim-owned here (it is the management panel; transfers to highlights-featured-content when feature 37 is written)) · 7 tests · full suite 262 tests ~8.0 min (fresh DB)
- 2026-07-04 · highlights-featured-content · done (spec verified, 11 atoms; the highlights MANAGEMENT config panel — Settings → Website → Highlights, the HighlightsListPanel CRUD. Took VUE-highlights-list-panel FROM journal-homepage (it interim-held it as the management panel; journal-homepage now 7 atoms + keeps only the reader carousel render, its OQ3 resolved). Fields: title/description multilingual, image nullable, url + urlText; url NOT format-validated (OJS-wide, schema format:uri is ignored by PKPSchemaService — a plain rule not a ⚠). Test author drove the rendered panel live: add (with required-field guards on title/url/button-label), edit-from-the-pre-loaded-list, reorder (Save Order), delete, and the manager-only boundary (section editor 401 + redirected off settings). Ledger row 100 NEW ⚠: GET /api/v1/highlights/{id} ALWAYS 500s — HighlightsController::get passes the Context object where Repository::get expects ?int contextId (unlike edit/delete which pass getId), confirmed live for existing + missing ids; masked in normal use because the panel edits from the pre-loaded list and never calls it, but every highlight _href advertises the dead route; one-line fix ->get(id, context?->getId()). The retained tests deliberately don't assert the 500 so they stay green if it is fixed) · 4 tests · full suite 266 tests ~8.7 min
- 2026-07-04 · article-landing · done (spec verified, 5 atoms; the anonymous reader ARTICLE page — ArticleHandler /article/view/{id}. The spec author hit a seeding wall: the freshly-reset TEST DB has 0 published articles and the dev DB is sparse (no DOIs/licenses/citations/plugins), so it could only verify absent states. The test author SEEDED rich published articles on the test DB and drove EVERY block live: metadata + raw-markup ORCID, the PDF galley via the pdf.js viewer + download, license/copyright/DOI, the how-to-cite block + BibTeX (citationStyleLanguage is enabled in the test bootstrap — the verifier resolved it is OPT-IN, not a stock OJS default), the /version/{id} view + outdated-version notice + noindex/canonical, the data-availability STATEMENT (row 98 — the statement shows, the citations do not), the DC + Google-Scholar meta tags, published-only 404 (unpublished/nonexistent → 404 anon, editor-preview 200), and the subscription gate (abstract open, galley link restricted, view→login 302). Conservative atom claims (5): PAGE-article-view/viewfile + VUE-pkp-crossmark-button + the 2 recommend-by plugins; the meta-tag plugins left to indexing-meta-tags (74), how-to-cite to citation-style-language, ORCID to orcid, open-review to review-anonymity, the download atoms to galleys. SCOPE DECISION at verification: feature 39 article-recommendations FOLDED into article-landing (the recommend-by blocks are its only atoms and render on this page) → total features 92→91. ALSO this iteration: relocated issue-management.spec.js to the serial project to kill a job-queue-swallow flake under full-suite parallel load) · 10 tests · parallel suite 267 tests ~9.1 min + serial 10 tests ~1.1 min (fresh DB)
- 2026-07-05 · issue-archive-toc · done (spec verified, 5 atoms; the anonymous reader ISSUE pages — IssueHandler /issue/{index,current,archive,view,download}. The archive back-issue listing, the current-issue 302 redirect, the single-issue TOC grouped by section, the full-issue galley download (test seeded a real Full-Issue PDF galley + drove the anon download streaming %PDF), published-only visibility (manager preview 200 + banner), and publishingMode=NONE suppression. Seams: GRID-toc-grid stays EDITORIAL (issue-management, resolving its OQ4); SCHEMA-issue referenced not claimed; PLUGIN-catalog confirmed a PHANTOM (OMP-oriented, → browse-category-section). Ledger row 101 NEW ⚠ (kept by verifier): IssueHandler::archive() orders by Collector::ORDERBY_SEQUENCE = custom_issue_orders.seq ASC ONLY, with NO date/current tiebreaker — and resequenceCustomIssueOrders early-returns when no custom order exists, so a never-manually-reordered journal has all-NULL seq and lists back issues in UNDEFINED DB order, NOT newest-first; the editorial Back-Issues grid + OAI use ORDERBY_PUBLISHED_ISSUES (seq + currentIssue DESC + date_published DESC) and DO order correctly, so this is an archive-page-specific reader defect. The test proved the non-determinism by seeding the same 3 years into fresh journals and getting DIFFERENT orders across runs, then asserted robustly (anon archive DOM order == manager seq-collector API order). Row 28 (within-section article order undefined, publications.seq all-zero) relocated here as its reader-render home. The login-redirect-not-404 for a missing/unpublished issue calibrated as OQ not ⚠ — it is the OJS-wide default-deny (authorize fail → redirectLogin), and article-landing throwing 404 is the outlier) · 6 tests · parallel 274 + serial 10, both green ~11 min total (fresh DB)
- 2026-07-05 · site-search · done (spec verified, 4 atoms; the anonymous front-end article SEARCH — SearchHandler /search. MAJOR as-built finding: 3.6 rewrote the whole search stack onto Laravel Scout (pkp#8920) — the old submission_search_keyword_list/_object/_object_keyword inverted-index tables are GONE, replaced by ONE submissions_fulltext table (title/abstract/body/authors columns) with pluggable engines (default DatabaseEngine, opt-in OpenSearch); the 3 legacy ArticleSearch/SubmissionSearch hooks are registered addUnsupportedHooks; the similarDocuments op is a dead 404. Indexing is ASYNC: publish/unpublish → UpdateSubmissionInSearchIndex listener sync-deletes the rows + dispatches UpdateSubmissionSearchJob on the database queue (a fresh article is unsearchable until the worker runs). Re-index is CLI-only (tools/rebuildSearchIndex.php truncates + re-queues); there is NO UI re-index form. The stock advanced UI is now just query + date range + (site-wide) journal picker. Ledger row 102 ⚠ (confirmed live on the Postgres test DB): orderBy=datePublished → HTTP 500 — DatabaseEngine::buildQuery orders on a non-aggregated cp.date_published under GROUP BY submission_id (Postgres 42803; MySQL tolerates); not reachable from the stock UI but breaks any theme/plugin/URL that sets it. The test proved the ASYNC round-trip deterministically: seed → absent → jobs.php run → present; and the CLI rebuild: flush → absent → drain → present. DatabaseEngine is NOT relevance-ranked (results in submission-id order — OQ2); async-needs-a-worker refined to OQ3 (job_runner=On drains end-of-request so a default install does index). Released FORM-pkp-search-indexing-form to distribution-settings (it is the Distribution Search Engine Indexing SEO form — searchDescription + customHeaders meta tags — functionally unrelated to the fulltext index). Also CORRECTED my long-held assumption: the test DB is PostgreSQL, not MySQL. Tests: 1 parallel (page + no-result) + 6 serial (queue-draining/index-flush must be single-worker)) · 7 tests · parallel 275 + serial 16, both green ~12 min total
- 2026-07-05 · browse-category-section · done (spec verified, 5 atoms; the anonymous reader category-browse landing pages + the Browse sidebar block. LIVENESS/SCOPE established: the OMP catalog is thin in OJS but the CATEGORY surface is fully live — PKPCatalogHandler::category/fullSize/thumbnail serve real reader pages for type=category (type=monograph → 500). The 3.6 Browse block is CATEGORY-ONLY: its stale by-issue/new-releases/series refs are DEAD template comments in block.tpl @uses; there is NO section/author/title reader browse page (the section in the feature name = section policies on /about/submissions + issue-TOC grouping, not a browse page). Took PLUGIN-blocks-browse FROM journal-homepage (transfer clean, no orphan). Dead-coded VUE-catalog-list-panel (OMP-only, UNASSIGNED). Ledger row 103 NEW ⚠: a category's Sort by setting (CategoryForm sortOption) is computed by PKPCatalogHandler::category() then DISCARDED — never applied to the query, so reader listings fall to submission-id order regardless of the manager choice (proven live with A[low-id/title Zzz/early date] + B[high-id/title Aaa/late date] → listed [A,B], ignoring both the title-sort and the date-default). Row 102 inherited (the category page 500s on orderBy=datePublished on the Postgres test DB; orderBy=title → 200). Calibrated: the empty-category 0-Items dead branch → cosmetic note; the missing must-publish gate → OQ1; no usage event → plain rule. The test author added a REUSABLE lib/pkp scenario passthrough — publications[].categories (assign a publication to categories via the same Repo call the Issue tab uses) + a category sortOption + a categoryPath→id map — the scenario API previously had no article→category assignment. Nested categories: subcategory nav + breadcrumb + NO parent roll-up confirmed) · 6 tests · parallel 281 + serial 16, both green (DB reset dropped parallel to ~8.1 min)
- 2026-07-05 · public-comments · done (spec verified, 24 atoms; the 3.6 reader-comments feature — the campaign's FIRST reader-WRITE flow. A logged-in reader posts a comment on an article → a moderation queue → a manager approves/hides → readers report → moderation of reports. LIVENESS established: comments are OFF by default (enablePublicComments schema default false; publicknowledge turns it on); posting is LOGGED-IN-only (anonymous POST → 403, the article page shows Log in to comment with no compose box — there is NO anonymous-posting setting, so the feature-map anonymous gating is just the login requirement); approval is MANDATORY with no toggle (every comment born isApproved=false; the public GET returns only approved comments plus the author's OWN pending); moderators = manager + site-admin (the isModerator gate); delete is owner-only; report by any logged-in reader. NOTIF-user-comment-{posted,reported} owned here — synchronous NOTIFICATION_LEVEL_TASK DB rows to every manager+site-admin, no queue/email. Two ⚠ (both live-confirmed + root-cause traced): row 104 — un-approving (hiding) a comment leaves a STALE approved-by-X-on-date stamp because setApproval nulls the fields but the shared settings writer (EntityUpdate::updateSettings) continues on a non-multilingual null, so the old values survive and the moderator API leaks approvedByUserName on a hidden comment (latent — the UI only shows the approver line when approved; shared settings-infra root cause); row 105 — the bulk DELETE /comments/{id}/reports endpoint 500s and deletes nothing when a comment has 2+ reports (deleteReports double-wraps the already-flat id array → nested whereIn), an effectively-dead route (the UI deletes reports individually). The test drove the full post→moderate→approve→report→delete matrix end-to-end through the REAL UI (the compose box posts with a live CSRF token, the report dropdown+dialog, the owner-delete dropdown+confirm) plus the moderator REST API; parallel project since the notifications are synchronous DB rows, not queued mail. Verifier wrote back all 24 atlas claims + fixed the Enable Public Comments label + flagged a locale-sweep gap (the reader userComment.* keys have no LOC atom — future grooming)) · 6 tests · parallel 287 + serial 16, both green ~10 min
- 2026-07-05 · about-pages · done (spec verified, 14 atoms; the public informational reader pages — AboutHandler /about/* + InformationHandler /information/*. Live-verified anonymous: /about (index), /about/contact, /about/submissions, /about/privacy, /about/editorialMasthead, /about/editorialHistory, /about/aboutThisPublishingSystem, and /information/{readers,authors,librarians} + the index redirect. LIVENESS/SCOPE corrections: /about/editorialTeam is a DEAD 404 (the router has no such case — only editorialMasthead is live; parked in UNASSIGNED as a phantom op); the staticPages plugin ships DISABLED (a custom page path 404s until the plugin is enabled per journal); the Submissions page does NOT list per-section policies (the FEATURE-MAP overstated it — sections load only to gate the accepting/not-accepting notice). The public Editorial Masthead renders EMPTY on the seed because no users carry the per-assignment masthead flag (only the reviewer group has masthead users + reviewers are excluded) — calibrated as an OQ, not a ⚠: the masthead-flag affordance EXISTS (UserForm mastheadUserGroupIds + a PUT users/{id}/masthead API), so the roster is populatable via the normal UI and the empty seed is a seed-data state, not a code defect. The test CLOSED the spec author OQ2 (the enable→create→view path was code-only): it enabled staticPages on a scratch journal via the existing plugins passthrough, created a custom static page through the manager legacy grid, and viewed it anonymously (200) with a missing path → 404. Block plugins PLUGIN-blocks-{information,developedBy,makeSubmission} referenced (journal-homepage owns them), NOT double-claimed. Bonus finding: contact-page emails are document.write/{mailto encode=javascript}-obfuscated, so the test asserts against raw HTML source not the decoded DOM. No ledger rows — all findings were plain rules/OQs, appropriate for a read-only page feature) · 5 tests · parallel 292 + serial 16, both green ~11 min
- 2026-07-05 · oai-pmh · done (spec verified, 8 atoms; the OAI-PMH 2.0 metadata-harvesting endpoint — OAIHandler /oai, both journal-level (/publicknowledge/oai) and site-level (/index/oai, cross-journal). All 6 verbs driven live via XML-over-HTTP (request.get + XML assertions): Identify (deletedRecord=persistent, repositoryIdentifier=ojs-test, sample oai:ojs-test:article/1), ListMetadataFormats, ListSets (setSpec = journalPath or journalPath:sectionAbbrev), ListIdentifiers (500 cap), ListRecords (100/page + the resumptionToken end-to-end: page → token → terminal empty token), GetRecord (oai_dc DC + marcxml MARC21). Error surface: badVerb/badArgument/idDoesNotExist/cannotDisseminateFormat/noRecordsMatch. The TOMBSTONE: unpublish an article → GetRecord returns a header status=deleted with no metadata (synchronous, no queue — reused the article-landing pattern). Default-enabled formats = EXACTLY oai_dc, oai_marc, marcxml (base getEnabled=true, no override); oaiJats is OFF by default (its prefix is `jats` NOT oai_jats, gated on a per-context setting — the test ENABLED it on a scratch journal via the plugins passthrough and emitted valid JATS, CLOSING OQ1); rfc1807 was REMOVED in 3.6 (migration I12948_RemoveRFC1807Plugin, no dir on disk) → dead-coded in UNASSIGNED; the DRIVER plugin (a DRIVER/OpenAIRE open-access `driver` set) is OFF by default (OQ2 left open). Ledger row 106 ⚠ (LOW/intent-call): Identify advertises granularity YYYY-MM-DDThh:mm:ssZ (second precision) but the from/until filter compares at DAY granularity (a DATE() cast introduced by an earlier Postgres crash-fix), so a harvester requesting a precise 1-second incremental window silently gets the WHOLE DAY — misleading advertised precision, though no data loss (the window only ever widens). Fixed a spec error the test found: rule 9 claimed ListSets pages at 50 (maxSets), but OAI::ListSets uses maxRecords=100 — $maxSets=50 is dead code (live-confirmed a 100-set first page, completeListSize 720). Tests via request.get + XML, parallel project (the tombstone unpublish is per-article, no queue)) · 9 tests · parallel 301 + serial 16, both green ~12 min
- 2026-07-05 · web-feeds-syndication · done (spec verified, 3 atoms; the syndication surfaces — sitemap.xml + the RSS/Atom web feed + the announcement feed. LAST Area 4 feature → AREA 4 COMPLETE (10/10). LIVENESS corrected the FEATURE-MAP/my premise: the sitemap (SitemapHandler /sitemap) is ALWAYS-ON core (journal urlset + site-level /index/sitemap sitemapindex); the webFeed plugin is ENABLED BY DEFAULT (WebFeedPlugin overrides getContextSpecificPluginSettingsFile so settings.xml enabled=true installs per-journal via the Context::add hook — 86/86 seeded journals have enabled=1), and its feeds are served at /gateway/plugin/WebFeedGatewayPlugin/{rss,rss2,atom} — there is NO /feed/{rss,rss2,atom} alias and NO per-issue /feed/{issueId} (all 404, asserted); the announcementFeed plugin is DISABLED by default (it does NOT override the settings file, so no enabled row is installed) and is additionally gated on enableAnnouncements. The three formats: rss = RSS 1.0 (RDF), rss2 = RSS 2.0, atom = Atom 1.0. Ledger row 107 ⚠: the announcementFeed atom.tpl + rss.tpl (RSS 1.0) emit MALFORMED dates — obsolete strftime %-codes (%Y-%m-%dT%T%z) passed to modern Smarty date_format / Carbon format() produce <updated>%2026-%07-%05UTC%UTC%185</updated> and <dc:date>%2026-%07-%05</dc:date>, making the Atom announcement feed INVALID (non-RFC3339); the announcementFeed RSS 2.0 <pubDate> and ALL webFeed feeds emit correct dates (webFeed atom → 2026-07-05T04:43:34+00:00). PLUGIN-blocks-browse stays owned by browse-category-section (referenced for the feed links, not claimed); webFeed's OWN sidebar block WebFeedBlockPlugin is part of the webFeed atom. Verifier calibrations: the webFeed-on/announcementFeed-off asymmetry → OQ2 (both configurable); recent-mode-selects-status-only → OQ3; an unknown webFeed format → HTTP 500 (webFeed throws vs announcementFeed returns false → 302) → OQ4, a low-severity ledger candidate, not opened. Corrected 3 spec inaccuracies: publicknowledge runs ISSUE mode not recent mode (the displayItems bool→string fix landed); the announcements-off case redirects to home (302→200) not not-found; dateUpdated is a conditional write. Tests via request.get + XML, parallel project) · 5 tests · parallel ~306 + serial 16, both green
- 2026-07-04 · media-files · done (spec verified, 9 atoms; the Publication Media manager + MediaFilesController + variant_groups — batch upload, web/high-res variant linking single+batch, metadata-sync to sibling, delete + group cleanup, publication-scoped sharing across galleys. Retained media-files.spec.js (6 tests) drives the Vue manager the spec author only API-verified. Verifier confirmed LIVE: editor CRUD / author list-only (DELETE → 401 userCantEdit) / reviewer excluded; the published-lock ⚠ is API-only + exempts editorial roles → DEDUPED to **ledger row 22b family** (galleys), NO new row (media is cleaner — no misleading 'View' label). **SECURITY: media are CLEAN vs the row-88 hole** — anon download of a media file on an *unpublished* pub → real 404 (ArticleHandler::initialize() 404s unpublished-for-anon before the media allow-list; download() behind userCanViewGalley) vs the ungated libraryFiles page op; probed live on port 8000. OQ3 at-most-2 cap = VariantGroup::link() pairwise semantics (no larger-group path). OQ1 assistant-without-canChangeMetadata: both halves code-confirmed (buttons render / API 401), live affordance render left open. **Ledger row 93 FIXED** (grouped-media seeder now uses a PublicationsProcessor-local MAX_GROUP_SIZE = 2). All 9 atoms single-owner; download-page + libraryFiles atoms correctly owned by galleys/document-library) · 6 tests · full suite 206 tests ~6.4 min
