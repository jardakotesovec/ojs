# The pile — unassigned atoms

Atoms that don't obviously belong to a feature land here instead of being force-fit
(charter invariant). Each entry: atom ID, why it didn't fit, date parked. Grooming
passes cluster this list — maintainer-scheduled, tracked as a `QUEUE.md` item
(none has run yet); an entry leaves the pile by being claimed by a spec or marked
out-of-scope in its sweep file with a reason.

| Atom | Why parked | Parked | Resolved |
|------|-----------|--------|----------|
| EVLOG-REV-PROXY-REC | `SUBMISSION_LOG_REVIEW_RECOMMENDATION_BY_PROXY` — the editor setting a **reviewer's** recommendation by proxy from OJS `ReviewerGridHandler::reviewRead` (which sets `review_assignments.reviewer_recommendation_id`). It is reviewer-management, **not** the recommend-only editor's editor-to-editor recommendation (which is `EVLOG-SUBM-ED-REC`, owned by editorial-decisions), and **not** the reviewer's own completion recommendation. Natural home: `assign-and-manage-reviewers` (fires the sibling `SUBMISSION_LOG_REVIEW_CONFIRMED`/EVLOG-REV-CONF from the same `reviewRead`) — currently verified without it. `recommend-only-editors` verifier confirmed it is correctly disclaimed there (its OQ2) and `reviewer-response`'s verifier independently flagged it as editor-side/unclaimed. Adopt into `assign-and-manage-reviewers` at grooming (verify OJS liveness of the by-proxy write first) or mark out-of-scope. | 2026-07-03 | |
| VUE-submissions-list-panel | FEATURE-MAP guessed an "author variant" under author-dashboard, but the author's My-Submissions rows are rendered by DashboardPage/DashboardTable cells, not `SubmissionsListPanel.vue`; its only live mounts are the export pickers in `plugins/importexport/native/templates/index.tpl` and `plugins/importexport/pubmed/templates/index.tpl` — belongs to a future native-xml-import-export / pubmed-export spec (author-dashboard spec + submission-drafts verifier, both checked live) | 2026-07-03 | |
| NOTIF-approve-submission + NOTIF-visit-catalog | Publish-approval notifications hinted at `editorial-dashboards` by keyword ("dashboard requires-attention/tasks panel" — the legacy tasks grid that spec documents as dead, ledger §2 row 67), but the spec's Side effects section disclaims notification ownership and no FEATURE-MAP atom list names them. They fire from `PKPApproveSubmissionNotificationManager` around publish approval — natural home `publication-publish-flow` (or production-stage / issue-management); atlas re-hinted to publication-publish-flow, adopt or mark out-of-scope when that spec lands (editorial-dashboards atlas verifier, 2026-07-16) | 2026-07-16 | |
| GRID-lib-pkp-grid-users-reviewer-author-reviewer-grid-handler | `AuthorReviewerGridHandler` — the legacy grid backing the author's Read-Review modal for **open** reviews (`readReview`/`reviewRead` ops restricted to open assignments). **LIVE** (not dead): referenced from `lib/ui-library/src/managers/ReviewerManager/useReviewerManagerActions.js` (`reviewerReadReviewByAuthor` → `readReview`), a both-live legacy grid superseded by VUE-reviewer-manager for the primary UI. Its `loadData()`/open-only visibility is already **documented** by `review-anonymity.md` (rule 5). Was mis-hinted `contributors` in `grids.md` (keyword match on "author"); NOT a contributors surface — corrected 2026-07-04. Natural home: a review spec (review-anonymity / reviewer-response / assign-and-manage-reviewers) — adopt at grooming | 2026-07-04 | |

## Dead-code candidates

Atoms judged unreachable in the current UI (charter: "Liveness before documentation").
Each entry needs evidence, not vibes — who would have linked to it and doesn't.
Especially watch legacy grids/handlers superseded by Vue managers. This list is a
maintainer deliverable (cleanup candidates), reviewed at campaign end.

| Atom | Evidence of unreachability | Superseded by | Date |
|------|---------------------------|---------------|------|
| GRID-lib-pkp-grid-admin-context-context-grid-handler — `users` op only (atom itself live, claimed by site-administration) | `ContextGridHandler::users()` (fetches `management/accessUsers.tpl`) has no referrer: `ContextGridRow` adds only Edit/Remove/Settings-wizard actions, and grep over templates/ + lib/pkp/templates/ + js/ + lib/pkp/js finds no `op=users` / `context-grid/users` link (2026-07-06) | Wizard Users tab loads `grid.settings.user.UserGridHandler` directly (`contextSettings.tpl`) | 2026-07-06 |
| PLUGIN-oaiMetadataFormats-rfc1807 (OAIMetadataFormatPlugin_RFC1807) | The RFC1807 OAI metadata format plugin is **removed in OJS 3.6**: `classes/migration/upgrade/v3_6_0/I12948_RemoveRFC1807Plugin.php` deletes its `versions` rows on upgrade, and there is **no** `plugins/oaiMetadataFormats/rfc1807` directory on disk (only the migration references the name). Live-confirmed absent: `ListMetadataFormats` returns only `oai_dc`/`oai_marc`/`marcxml`, and `?verb=GetRecord&metadataPrefix=rfc1807&…` → `cannotDisseminateFormat` (oai-pmh spec, 2026-07-05). Atom is stale in the plugins sweep. | removed (migration I12948) | 2026-07-05 |
| VUE-catalog-list-panel (CatalogListPanel) | OMP-oriented "published catalog entries" list panel. No mount/reference anywhere OJS renders: grep for `CatalogListPanel`/`catalog-list-panel` across `pages/`, `templates/`, `lib/pkp/templates/`, `js/`, `plugins/` returns **zero** hits — the component exists only in `lib/ui-library/src/`. The OJS reader category page (`PKPCatalogHandler::category` → `frontend/pages/catalogCategory.tpl`) renders Smarty `article_summary.tpl` rows, **not** this Vue panel. Dead in OJS (browse-category-section spec, 2026-07-05) | OMP monograph catalog (no OJS surface) | 2026-07-05 |
| GRID-grid-pub-ids-pub-id-export-issues-list-grid-handler | No component-path/extends/`new` reference anywhere in templates/, lib/pkp/templates/, lib/ui-library/src/, pages/, classes/, controllers/, plugins/ (checked via combined grep across those trees) | VUE-doi-list-panel-ojs | 2026-07-02 |
| GRID-grid-pub-ids-pub-id-export-representations-list-grid-handler | Same as above — no reference found in any tree | VUE-doi-list-panel-ojs | 2026-07-02 |
| GRID-grid-pub-ids-pub-id-export-submissions-list-grid-handler | Same as above — no reference found in any tree | VUE-doi-list-panel-ojs | 2026-07-02 |
| GRID-lib-pkp-grid-files-production-ready-production-ready-files-grid-handler | Concrete class extends `FileListGridHandler` but has no component-path reference, no subclass, and no `new` instantiation anywhere | VUE-file-manager | 2026-07-02 |
| GRID-lib-pkp-grid-files-review-workflow-review-revisions-grid-handler | Concrete class extends `FileListGridHandler` but has no component-path reference, no subclass, and no `new` instantiation anywhere | VUE-file-manager | 2026-07-02 |
| GRID-lib-pkp-grid-files-selectable-library-file-grid-handler | A selectable library-file picker extending `LibraryFileGridHandler` (a live-rooted base) but itself never extended, instantiated, or referenced by component path anywhere in this checkout; the live library surfaces (`document-library` context + submission grids) don't use it. OMP-only (monograph catalog) usage can't be ruled out since that source isn't checked out (genuinely uncertain). Claimed by `document-library` as part of the library-grid family (2026-07-04) | claimed by document-library (no live OJS surface; OMP-suspected) | 2026-07-02 |
| GRID-lib-pkp-grid-files-submission-editor-submission-details-files-grid-handler | Concrete class extends `FileListGridHandler` but has no component-path reference, no subclass, and no `new` instantiation anywhere. (The original note that its author-facing sibling was "live" was stale — the sibling is dead too, see the 2026-07-03 row below; corrected by author-dashboard verifier) | VUE-file-manager | 2026-07-02 |
| GRID-lib-pkp-grid-users-author-author-grid-handler | Abstract "base PKP class to handle author grid requests" with zero subclasses anywhere in the repo (unlike its sibling base classes, e.g. `LanguageGridHandler`, `FileListGridHandler`, which do have live subclasses) — no component-path reference either | VUE-contributor-manager | 2026-07-02 |
| PAGE-manager-legacy | `pages/manager/index.php` router is fully dead — every switch case has an empty body; real logic lives in `pages/payments/PaymentsHandler.php` (pages sweep) | PAGE-payments-* | 2026-07-02 |
| PAGE-dois-management | Declared in `addRoleAssignment` but never routed/implemented — would 404 if hit (pages sweep) | PAGE-dois-index → Vue DOI pages | 2026-07-02 |
| PAGE-reviewer-downloadfile | Declared in role list but never routed/implemented (pages sweep) | — | 2026-07-02 |
| PAGE-search-similardocuments | Routed in switch, no implementing method (pages sweep) | — | 2026-07-02 |
| PAGE-authordashboard-reviewroundinfo | Routed, no implementing method (pages sweep) | VUE workflow author views | 2026-07-02 |
| PAGE-manageissues-issuestabs | Routed, no implementing method (pages sweep) | — | 2026-07-02 |
| PAGE-management-statistics | Routed, unimplemented (pages sweep) | PAGE-stats-* | 2026-07-02 |
| NOTIF-configure-payment-method | Defined with URL/style-class switch cases but zero `createNotification` call sites repo-wide (notifications sweep) | — | 2026-07-02 |
| NOTIF-book-* (10 atoms) | All 10 `NOTIFICATION_TYPE_BOOK_*` constants in OJS's Notification.php have zero creation sites in `classes/` or `plugins/` — OMP-inherited dead code (notifications sweep) | — | 2026-07-02 |
| EVLOG-REV-DUE | `SUBMISSION_LOG_REVIEW_SET_DUE_DATE` — no live writer found; appears only in a v3.4.0 field-rename migration (event-log sweep) | — | 2026-07-02 |
| GRID-lib-pkp-grid-files-attachment-author-review-attachments-grid-handler | Only reference is `lib/pkp/templates/authorDashboard/reviewRoundInfo.tpl`, which nothing renders: `PKPAuthorDashboardHandler::submission()` redirects before any display (live-probed 302), the `reviewRoundInfo` op is routed-but-unimplemented (live-probed 404), and `setupTemplate()` has no caller — and would fatal on the undefined `$citationsForm` it references if one existed (author-dashboard spec). Verifier re-attack 2026-07-03: a hand-crafted component-router fetch still answers 200 for an authorized author — UI-orphaned, not unroutable | VUE-file-manager (author workflow view) | 2026-07-03 |
| GRID-lib-pkp-grid-files-review-author-review-revisions-grid-handler | Same dead template (`authorDashboard/reviewRoundInfo.tpl`); no other reference beyond the inert JS handler class in pkp.min.js (author-dashboard spec). Verifier re-attack 2026-07-03: direct component-router fetch answers 200 — UI-orphaned, not unroutable | VUE-file-manager (WORKFLOW_REVIEW_REVISIONS) | 2026-07-03 |
| GRID-lib-pkp-grid-files-submission-author-submission-details-files-grid-handler | Only reference is `lib/pkp/templates/controllers/tab/authorDashboard/submission.tpl`, itself included by nothing (the legacy author-dashboard tab path died with the 3.6 redirect) (author-dashboard spec). Verifier re-attack 2026-07-03: direct component-router fetch answers 200 — UI-orphaned, not unroutable | VUE-file-manager (SUBMISSION_FILES) | 2026-07-03 |
| NOTIF-editor-assign (`NOTIFICATION_TYPE_EDITOR_ASSIGN`) — claimed by stage-participants for ownership, but its **only** creator (`PKPStageParticipantNotifyForm::sendMessage()`, `case 'EDITOR_ASSIGN'`) is unreachable: it matches the exact template key `EDITOR_ASSIGN`, but the 3.4 migration `I5716_EmailTemplateAssignments` split that template into stage variants (`EDITOR_ASSIGN_{SUBMISSION,REVIEW,PRODUCTION}`) and the Add-Participant notify picker only offers those variants. Live-verified 2026-07-03: assigning an editor with the "Assign Editor" template created a `NEW_QUERY` notification but **no** `EDITOR_ASSIGN` (DB type 16777255 absent). The two other references are display/settings handlers for an already-existing notification, not creators. Fix candidate: recognise the stage-variant keys in the switch (stage-participants spec Known deviations) | switch never matches renamed template keys | 2026-07-03 |
| WorkflowHandler::setupIndex() rendering path (part of PAGE-workflow-index; not a separate atom) | `PKPWorkflowHandler::index()` is a bare redirect and the only `setupIndex()` caller anywhere is `PKPDashboardHandler` calling its *own* method — so OJS `WorkflowHandler::setupIndex()` (IssueEntryForm, JATS panel, payments form, section word limits) plus the helpers only it uses (`getTitleAbstractForm` in the workflow handler, `getEditorAssignmentNotificationTypeByStageId` — grep: zero live callers) never execute. It even reads `getState('components')[FORM_TITLE_ABSTRACT]` that nothing on this path ever sets. The workflow modal gets these forms from the dashboard pageInitConfig + `_components/*` API routes instead (live: modal renders publication forms with no workflow-page render) (workflow-stage-navigation spec) | dashboard pageInitConfig + `SubmissionController` `_components/issue` etc. | 2026-07-03 |
| EVLOG-EMAIL-COPY-{AUTH-NOT,FINAL,COMPL,AUTH-COMPL,FINAL-COMPL,ACK,AUTH-ACK,FINAL-ACK} (8 of the 9 `SubmissionEmailLogEventType::COPYEDIT_NOTIFY_*` types — **claimed/owned by copyediting-stage**, listed here as cleanup candidates) | Grep 2026-07-03 across lib/pkp + classes/controllers/pages/api: only `COPYEDIT_NOTIFY_COPYEDITOR` (EVLOG-EMAIL-COPY-NOTIFY) has a live writer (`PKPStageParticipantNotifyForm::sendMessage()` case 'COPYEDIT_REQUEST'). `COPYEDIT_NOTIFY_AUTHOR` (AUTH-NOT) survives only as a *display filter* in `PKPEmailController` (never written). The other 7 (`_FINAL,_COMPLETE,_AUTHOR_COMPLETE,_FINAL_COMPLETE,_ACKNOWLEDGE,_AUTHOR_ACKNOWLEDGE,_FINAL_ACKNOWLEDGE`) have **zero** references — OJS-2 copyediting sub-workflow remnants (copyediting-stage spec Known deviations) | 3.x discussion-driven copyedit exchange (DiscussionCopyediting + copyedited-files grid) | 2026-07-03 |
| NOTIF-index-assignment (`NOTIFICATION_TYPE_INDEX_ASSIGNMENT`) — **claimed/owned by production-stage**, cleanup candidate | Its only creator is `PKPStageParticipantNotifyForm::sendMessage()` `case 'INDEX_REQUEST'`, but the `INDEX_REQUEST` template is not registered in `registry/emailTemplates.xml` and is NOT among the alternates `I5716_EmailTemplateAssignments::mapIncludedAlternateTemplates()` retains (it keeps only COPYEDIT_REQUEST/LAYOUT_REQUEST/LAYOUT_COMPLETE — verified 2026-07-03), so the Add-Participant Notify picker never offers Index Request and the branch is unreachable. The Indexer default stage-5 user group still exists (an indexer can be *assigned*), but the notify is never offered (production-stage spec Known deviations) | OJS-2 indexing sub-workflow retired | 2026-07-03 |
| EVLOG-EMAIL-{LAYOUT-ED-THANK, PROOF-AUTH-NOT, PROOF-AUTH-COMPL, PROOF-AUTH-THANK, PROOF-PROOF-NOT, PROOF-PROOF-COMPL, PROOF-PROOF-THANK, PROOF-LAYOUT-NOT, PROOF-LAYOUT-COMPL, PROOF-LAYOUT-THANK, INDEX-NOT, INDEX-COMPL} (12 of the 14 production `SubmissionEmailLogEventType::{LAYOUT_*,PROOFREAD_*,INDEX_*}` types — **claimed/owned by production-stage**, cleanup candidates) | Grep 2026-07-03 across lib/pkp + classes/controllers/pages/api: only `LAYOUT_NOTIFY_EDITOR` (EVLOG-EMAIL-LAYOUT-ED-NOT) and `LAYOUT_NOTIFY_COMPLETE` (EVLOG-EMAIL-LAYOUT-COMPL) have a live writer (`PKPStageParticipantNotifyForm::sendMessage()` cases LAYOUT_REQUEST/LAYOUT_COMPLETE). `PROOFREAD_NOTIFY_AUTHOR` (PROOF-AUTH-NOT) survives only as a *display filter* in `PKPEmailController` (`$authorEmailLogEventTypes`, never written). `INDEX_NOTIFY_INDEXER`/`INDEX_NOTIFY_COMPLETE` have writers only in the unreachable INDEX_REQUEST/INDEX_COMPLETE branches (see the NOTIF-index-assignment row). The remaining 8 (`LAYOUT_THANK_EDITOR`, `PROOFREAD_NOTIFY_AUTHOR_COMPLETE`, `PROOFREAD_THANK_AUTHOR`, `PROOFREAD_NOTIFY_PROOFREADER`, `PROOFREAD_NOTIFY_PROOFREADER_COMPLETE`, `PROOFREAD_THANK_PROOFREADER`, `PROOFREAD_NOTIFY_LAYOUTEDITOR`, `PROOFREAD_NOTIFY_LAYOUTEDITOR_COMPLETE`, `PROOFREAD_THANK_LAYOUTEDITOR`) have **zero** references — OJS-2 proofreading/layout/indexing sub-workflow remnants (production-stage spec Known deviations) | 3.x galleys + discussion-driven production | 2026-07-03 |
| NOTIF-format-needs-approved-submission (`NOTIFICATION_TYPE_FORMAT_NEEDS_APPROVED_SUBMISSION`) — **claimed/owned by production-stage**, OMP-only / unreachable in OJS | `WorkflowNotificationDisplay.vue` requests it (with VISIT_CATALOG) only in its `isOMP()` branch — OJS's branch requests ASSIGN_PRODUCTIONUSER + AWAITING_REPRESENTATIONS instead — and OJS `NotificationManager::getMgrDelegate()` registers a delegate for APPROVE_SUBMISSION/VISIT_CATALOG but NOT for FORMAT_NEEDS_APPROVED_SUBMISSION, so the type can never be surfaced in OJS. Also covers the OMP `Representation::isApproved` galley-approval concept (getter/setter present, **zero OJS callers** — the galley `is_approved` column stays 0, verified live on seeded sub 623) (production-stage spec Known deviations OQ4) | OMP publication-format approval | 2026-07-03 |
| `/about/editorialTeam` (phantom op — NOT a swept atom, recorded for traceability) | `lib/pkp/pages/about/index.php` routes only `index/editorialMasthead/editorialHistory/submissions/contact` (→ AboutContextHandler) and `privacy/aboutThisPublishingSystem` (→ AboutSiteHandler) — there is **no** `editorialTeam` case. Live-verified 2026-07-05: `GET /publicknowledge/en/about/editorialTeam` → **404**. Stale links/tests expecting the pre-3.x `editorialTeam` path fail by design; the live public roster op is `editorialMasthead` (about-pages spec) | `editorialMasthead` (PAGE-about-editorialmasthead) | 2026-07-05 |
| `NavigationMenuForm` + `navigationMenuForm.tpl` + `<navigation-menu-editor-panel>` (no swept atom — form class under GRID-lib-pkp-grid-navigation-menus-navigation-menus-grid-handler's family, recorded for traceability) | The all-in-one legacy menu form is unreachable: it posts to op `updateNavigationMenu` but `NavigationMenusGridHandler`'s op list is only `fetchGrid,fetchRow,deleteNavigationMenu` (no add/edit/update op exists), no handler instantiates the form, and its template embeds a `<navigation-menu-editor-panel>` Vue tag that is registered nowhere in `lib/pkp/js/load.js`. The live add/edit path is `VueModal('NavigationMenuManagerFormModal')` (registered in load.js) fired from the grid's Add-Menu action / row Edit action, saving via `POST/PUT api/v1/navigationMenus` (navigation-menus spec, live-verified 2026-07-06) | NavigationMenuManagerFormModal + navigation-menus REST API | 2026-07-06 |
