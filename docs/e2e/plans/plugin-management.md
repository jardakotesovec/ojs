# Plugin management

- **Area:** 7. Plugins (key set)
- **Placement:** lib/pkp
- **Budget:** 5 tests
- **Absorbs:** none
- **Scenario needs:** journal scenario (`users`, `issues`, `plugins: {name: {enabled, settings}}` passthroughs — all existing). One-off plugin states are flipped through the grid UI, which is itself the surface under test.
- **Round 2 / out of scope:**
  - Plugin Gallery tab (PluginGalleryGridHandler) — fetches the live PKP plugin-gallery feed; no external calls.
  - Plugin upload / install / upgrade / delete — mutates the shared installation filesystem; not parallel-safe.
  - Functional coverage of long-tail generic plugins (htmlArticleGalley, lensGalley, staticPages, customBlockManager, …) — round-2 backlog per inventory.
  - Role gating (403 for non-managers on the grid) — covered generically by roles-permissions.

## Tests

| # | Title | Actors | Seed | Verifies | Status |
|---|-------|--------|------|----------|--------|
| 1 | Manager toggles a plugin and the toggle gates its front-end surface | dbarnes, anonymous | journal scenario (scratch, published issue via `issues`) | Settings → Website → Plugins → Installed Plugins grid renders (SettingsPluginGridHandler). **Baseline correction:** webFeed installs ENABLED on every new context (Plugin.php:131 Context::add hook + webFeed settings.xml `enabled=true`), so a fresh scratch journal already carries the three feed `link[rel=alternate]` head tags. The test asserts the enabled baseline, disables via the grid checkbox (RemoteActionConfirmationModal → PkpDialog OK), confirms persistence across reload + tags gone, then re-enables and confirms the tags return. Success NOTIFICATION deliberately not asserted — toast assertions are parallel-unsafe (shared per-user notification queue); persistence + front-end effect are the success signals | implemented (lib/pkp/playwright/tests/plugin-management.spec.js) |
| 2 | Manager edits a plugin's settings through the grid's Settings modal | dbarnes | journal scenario (scratch, `plugins: {citationstylelanguageplugin: enabled}`) | Per-row Settings link action (behind `a.show_extras`) opens the AjaxModal with the CSL settings form; changing the primary citation style (ieee) + publisher location and saving closes the modal; reopening shows the saved values. Implementation note: a reopen-click straight after save lands in LinkActionHandler's disabled window (re-enable only fires on the async ModalRequest#finish) — PluginsGridPage#openSettingsModal click-and-verifies under expect().toPass() | implemented (lib/pkp/playwright/tests/plugin-management.spec.js) |
| 3 | Site admin manages plugins at site level — SERIAL project | admin | UI | /admin/settings → Plugins tab renders the AdminPluginGridHandler grid grouped by category; toggling the **googleScholar** plugin's site-level enable checkbox persists across reload, with the original state restored in a `finally` block. **Serial-project note (principle 9):** a site-level plugin toggle affects all journals and parallel workers, so this test lives in the dedicated serial Playwright project, never in a parallel spec. googleScholar was verified as the safe candidate: display-only (`<head>` citation meta tags on article pages), enabled-by-default, and referenced by no other plan (citationStyleLanguage and webFeed are NOT safe — other rows/plans depend on them). **Effect-surface note:** the article-page citation meta tags are deliberately NOT asserted — the site toggle writes only the SITE plugin_settings row (LazyLoadPlugin.php:73-96 resolves SITE_CONTEXT_ID only without a journal context) while article pages read the journal's own row (GoogleScholarPlugin.php:38-40), which every context installs as enabled; the toggle round-trip + persistence + restore is the behavior under test | implemented (lib/pkp/playwright/tests/serial/site-plugin-toggle.spec.js) |
| 4 | Plugin enablement is journal-scoped | dbarnes, anonymous | 2× journal scenario (scratch A default — webFeed installs enabled; scratch B with `plugins: {webfeedplugin: {enabled: false}}` — "without" in the original seed note was wrong, defaults are ON) | Journal A's grid shows webFeed checked + homepage feed tags; journal B's grid unchecked + no tags; then UI toggles in opposite directions (enable on B, disable on A) each move only their own journal's grid state and homepage tags — no cross-context leak either way | implemented (lib/pkp/playwright/tests/plugin-management.spec.js) |
| 5 | Manager filters the installed-plugins grid | dbarnes | journal scenario (scratch) | The grid filter (pluginGridFilter.tpl: name search + category select, collapsed behind the header's `.pkp_linkaction_search` toggle) narrows the listing — name "Web Feed" leaves only the webFeed row; category `themes` leaves only theme rows — and selecting all categories + empty name restores the full grouped listing | implemented (lib/pkp/playwright/tests/plugin-management.spec.js) |
