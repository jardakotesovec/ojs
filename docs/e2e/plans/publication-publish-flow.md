# Publication publish flow

- **Area:** 3. Publishing & issues
- **Placement:** ojs
- **Budget:** 8 tests
- **Absorbs:** lib/pkp/playwright/tests/publish-unpublish.spec.js — MOVED to playwright/tests/publish-unpublish.spec.js (wave 6) to match placement; rows 2–8 live in playwright/tests/publication-publish-flow.spec.js
- **Scenario needs:** submission scenario `publications[]` (versionStage, metadata incl. copyright/license, `issue` ref, `published` flag) — exists; scheduled state seedable via `issue: <future issue>` + `published: true` (PublicationsProcessor calls `Repo::publication()->publish()`, which yields STATUS_SCHEDULED for an unpublished issue); `decisions` (skipExternalReview, decline) — exists. Row 2 note: missing copyright/license metadata does **not** block publish — OJS `validatePublish()` (classes/publication/Repository.php) only errors on declined status, ORCID problems, an invalid issueId, or an enabled-but-unpaid publication fee; row 2 uses the unpaid-publication-fee precondition, so its seed is a scratch journal with the publication (APC) fee enabled via the Distribution → Payments UI. No gaps.
- **Round 2 / out of scope:**
  - DOI auto-assignment on publish → doi-management plan.
  - Event-log entries for publish/unpublish → activity-log plan.
  - Subscription/delayed-open-access visibility gating → subscription-access plan.
  - Scheduling into a future issue and publish-with-issue journeys → issue-assignment-scheduling plan (this plan only covers the Scheduled state's Preview affordance).
  - Versioned republish (publish v2 etc.) → publication-versioning plan.

## Tests

| # | Title | Actors | Seed | Verifies | Status |
|---|-------|--------|------|----------|--------|
| 1 | Editor publishes a draft VoR, unpublishes, republishes; front-end visibility flips each time | dbarnes, anonymous | submission scenario: draft + skipExternalReview + unpublished VoR w/ license metadata + issue Vol 1 No 2 (2014) | Publish via Title & Abstract panel → status STATUS_PUBLISHED + datePublished set; anonymous article URL 200 with title; unpublish → STATUS_QUEUED, reader 404; republish → live again | implemented (playwright/tests/publish-unpublish.spec.js) |
| 2 | Publish is blocked by an unpaid publication fee (`validatePublish` precondition) | dbarnes | journal scenario: scratch journal w/ one published issue; payments (Manual Fee Payment + instructions + currency) enabled via Distribution → Payments UI, APC fee via Payments → Author Fees; submission scenario (`journal` override): production-stage draft VoR, no payment recorded | Publish confirmation modal (PublishForm) lists `editor.article.payment.publicationFeeNotPaid` and renders WITHOUT any committing button; publication stays unpublished (Review Publishing Details' Confirm legitimately persists READY_TO_PUBLISH — asserted as status ∈ {QUEUED, READY_TO_PUBLISH}, datePublished null); anonymous article URL 404 | implemented (playwright/tests/publication-publish-flow.spec.js) |
| 3 | Editor publishes without an issue (continuous publishing) | dbarnes, anonymous | submission scenario: production-stage draft VoR w/ full metadata, no issue | "Don't Assign To An Issue" option in publish flow → confirmation calls out issue-less immediate publication → STATUS_PUBLISHED, issueId null, datePublished set; anonymous landing 200 with title; current-issue TOC does not list it | implemented (playwright/tests/publication-publish-flow.spec.js) |
| 4 | Author and assistant see no publish/unpublish controls | atester (submitter), gcox (participant), dbarnes (sanity) | submission scenario: production-stage draft VoR, submitter atester, gcox as layout-editor participant | Author's My Submissions workflow view and assistant's workflow view show the Title & Abstract panel (Status: Unscheduled chip as the bounding positive) without Schedule For Publication/Publish/Unpublish/Unschedule buttons; editor view shows Schedule For Publication | implemented (playwright/tests/publication-publish-flow.spec.js) |
| 5 | Editor previews a scheduled article before it is public | dbarnes, anonymous | submission scenario: VoR w/ metadata, `issue` = future Vol 2 No 1 (2015), `published: true` → STATUS_SCHEDULED (issue itself never mutated) | Publication panel shows status chip "Scheduled" with Preview + Unschedule buttons (no Schedule For Publication); Preview redirects the editor to the article landing page rendering the title (canPreview); anonymous article URL still 404 | implemented (playwright/tests/publication-publish-flow.spec.js) |
| 6 | Cancelling the publish dialog leaves the publication untouched | dbarnes | submission scenario: production-stage draft VoR w/ full metadata | Review Publishing Details shows issue-assignment options + version fields; Cancel (before Confirm — Confirm is the step that persists issueId/status) → status remains STATUS_QUEUED, datePublished null, Schedule For Publication button still present, reader 404 | implemented (playwright/tests/publication-publish-flow.spec.js) |
| 7 | Publishing moves the submission to the published/archived dashboard view | dbarnes | submission scenario: two production-stage draft VoRs (target + control), target published via UI to Vol 1 No 2 (2014) | After publish, editorial dashboard "Published" view lists the submission with the "Published" stage badge; it leaves the production queue (control submission bounds the tag-scoped absence search) | implemented (playwright/tests/publication-publish-flow.spec.js) |
| 8 | Declined submission cannot be published | dbarnes | submission scenario: submitted + initialDecline decision + VoR metadata | Workflow shows declined state (Revert Decline offered, Send for Review absent, submission status DECLINED). REALITY DEVIATION from the planned "no publish button": workflowConfigEditorialOJS#getPrimaryControlsRight has no declined gate, so Schedule For Publication IS offered; the hard stop is `validatePublish` — the publish confirmation modal lists `publication.required.declined` and renders without a committing button; submission stays DECLINED, publication unpublished | implemented (playwright/tests/publication-publish-flow.spec.js) |
