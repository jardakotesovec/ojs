# OJS code changes & bug findings from the e2e revamp

Review ledger for the END of round 1: every NON-TEST codebase tweak the suite work
required, plus app-bug / flakiness candidates the tests surfaced but did not fix.
Scenario-endpoint work (`api/v1/_test/`, `classes/testing/`) is deliberately NOT
tracked here — it is test-gated and reviewed in `docs/scenario-processor-audit.md`.
Charter rule: any wave that changes non-test code or diagnoses an app-side bug
appends a row here.

## 1. Production code changes (review + likely upstream)

| # | Change | Where | Commit | Why / risk | Upstream? |
|---|--------|-------|--------|------------|-----------|
| 1 | OAI `ListRecords`/`ListIdentifiers` with `from=`/`until=` fataled on Postgres: `whereDate()` received a raw `GREATEST(...)` Expression; Laravel's Postgres grammar probes the column with `str_contains()` → TypeError → 500. Replaced with `whereRaw` + portable `DATE()` casts (MySQL + PG). | `classes/oai/ojs/OAIDAO.php` | root `03516ef9c2` (wave 3) | Harvester-facing bug on every Postgres install that gets date-range harvested. Same semantics after fix. | **YES — cherry-pick to main**; check OMP/OPS OAIDAOs for the same pattern. |
| 2 | `plugins/generic/credit` checkout (stable-3_5_0) references the removed `PKP_STRICT_MODE` constant at class_alias time, fataling ALL plugin loading (web + CLI) on 3.6-dev core. Patched: `if (defined('PKP_STRICT_MODE') && !PKP_STRICT_MODE)`. | `plugins/generic/credit/CreditPlugin.php` — **UNCOMMITTED working-tree patch in the plugin's own repo** | none (wave 1 env unblock) | App cannot boot with this checkout present otherwise. Revert: `git checkout -- CreditPlugin.php` there. | Decide: move the checkout to a 3.6-compatible branch, or upstream the `defined()` guard (harmless on all branches). |
| 3 | `opis/json-schema` added as **require-dev** to lib/pkp (scenario-spec validation was silently dead without any JSON-schema validator in the vendor tree). | `lib/pkp/composer.json` + lock | lib/pkp `a859b7f64f` (wave 1) | Dev-only; production installs unaffected. | No. |
| 4 | **Journal sitemaps emitted ZERO article/galley URLs**: the per-issue article loop used `filterByLatestPublished(true)` — the *continuous-publication* filter (requires issueless or unpublished-issue publications), which contradicts `filterByIssueIds([$publishedIssue])` and always yields the empty set. Restored `filterByStatus([STATUS_PUBLISHED])` (+ import). Regression came in with OJS `da7c68874e` (pkp/pkp-lib#12245, Feb 2026). | `pages/sitemap/SitemapHandler.php:57` | root (wave 8) | Harvester/SEO-facing: every journal sitemap since Feb 2026 lists only issue/static URLs. Verified empirically before/after. | **YES — upstream PR** as a #12245 follow-up; check OMP/OPS sitemap handlers for the same pattern. |
| 5 | **webFeed served the wrong feed scope on fresh installs**: `settings.xml` declared `displayItems` as `type="bool"` (installs boolean `true`) while the gateway compares `=== 'issue'` and the settings form saves a string — so new contexts always took the recent-items branch and neither settings radio showed checked. Changed to `type="string"`. | `plugins/generic/webFeed/settings.xml` — **UNCOMMITTED working-tree patch in the plugin's own repo** (separate submodule, like row 2) | none yet | Correctness-driven (the test is robust to either branch). Revert: `git checkout -- settings.xml` there. | **YES — upstream to pkp/webFeed**. |
| 6 | **Editorial masthead & editorial history rendered ZERO role sections**: since the Eloquent UserGroup refactor (lib/pkp `714d5d5aa4`, pkp/pkp-lib#10506), `AboutContextHandler::editorialMasthead`/`editorialHistory` iterated the role collection using its POSITIONAL keys as user-group IDs (`UserGroup::query()->get()` keys 0-based), so the id-keyed map lookup and the template's `array_key_exists($mastheadRole->id, …)` never matched — `/about/editorialMasthead` was empty on every install regardless of flags/opt-ins. Fixed to index by `$userGroup->id`. | `lib/pkp/pages/about/AboutContextHandler.php:105-126, 178-186` | lib/pkp (wave 9) | Reader-facing regression; DB state verified correct pre-fix (masthead flags + opt-ins + cache map all present, page still empty). editorial-masthead.spec.js row 2 is the regression test. | **YES — upstream PR to pkp-lib** (check OMP/OPS share the handler). |
| 7 | **Roles grid: whichever role the DB returned first had no edit/delete actions** (graduates §2 row 34): `UserGroupGridHandler::loadData` returned 0-indexed results, GridHandler uses array keys as row ids, and `UserGroupGridRow::initialize` gates actions on `!empty($rowId)` — `empty('0')` is true. On Postgres the arbitrary-but-stable row order could pin a just-created role at index 0 indefinitely (failed the wave-11 suite twice through the test workaround's reload loop). Fixed by `->keyBy('user_group_id')` so row ids are real group ids. | `lib/pkp/controllers/grid/settings/roles/UserGroupGridHandler.php:206-210` | lib/pkp (wave 11) | roles-permissions.spec.js rows 2/4 act as regression tests (their POM guard no longer triggers). | **YES — upstream to pkp-lib** (audit other Eloquent-ported grids for the same positional-keys pattern). |

## 2. App-bug / flakiness candidates surfaced by tests (NOT fixed — needs triage)

| # | Finding | Evidence | Suspected mechanism | Suggested action |
|---|---------|----------|---------------------|------------------|
| 1 | **Wizard "Comments for the Editors" intermittently lost on Submit** (`commentsForTheEditors=null` lands in the DB). User-visible data loss, not just test flake. | scenario audit §3; reproduced 4/9 under parallel load, independent of animations and of all Processor changes; legacy test fixme'd; the replacement test passes only because it REST-polls the autosave before submitting | Wizard autosave runner and Review-step `_validateOnly` poll run on independent ~500 ms timers — a queued-but-unsent autosave is invisible to validation, so Submit can race ahead of the field's persistence | Dedicated investigation (was already flagged out-of-scope in the audit); candidate fix: flush pending autosaves before enabling/handling Submit |
| 2 | **`_submissions/reviewerAssignments` ignores `searchPhrase` AND pagination** — always returns the full list. | wave-3 dashboards + reviewer-response agents, verified against the controller | Endpoint never wires the collector's search/pagination params | Upstream API completeness fix; also a perf concern for reviewers with long histories |
| 3 | **`POST /submissions` rejects editor-role user groups (400)** while the Submit-As UI offers only stage-1 groups — managers effectively can't start submissions as themselves. | wave-2 wizard-core agent; matches known pkp/pkp-lib#10929 | UI/API disagreement on permitted `userGroupId` | Track upstream issue; no local change made |
| 4 | **Side-modal wrapper permanently computes `visibility: hidden`** while inner content is visible/interactive. | wave-2/3 agents (assertions must anchor on inner content) | ui-library modal wrapper styling | Minor; worth an a11y/markup look in ui-library |
| 5 | **Submission search tokenizes on spaces and ORs LIKE matches** — multi-word phrases match everything containing any common word; fresh drafts additionally sort last (no `dateSubmitted`), so loosely-scoped searches can't find them. | api-smoke failure analysis (wave 2 verification) | `Collector::searchPhrase` keyword OR-mapping | Behavior call: maybe AND-semantics or phrase matching upstream; tests work around via unique-token search |
| 6 | **`FileAttacherUpload.isUploading` off-by-one** (`.length > 1` instead of `> 0`): with exactly one file mid-upload the computed stays `false`, so the footer "Attach Files" button is enabled while the upload is still in flight — clicking it attaches a progress-only entry with no `temporaryFileId` (Composer maps `file.id`, still undefined), yielding a broken email attachment. | wave-4 review-decisions agent, read while building the Upload-attacher POM helper (`lib/ui-library/src/components/FileAttacher/FileAttacherUpload.vue` ~L150); the test works around it by racing the `temporaryFiles` POST before clicking | Boolean computed compares the in-progress count to `> 1` | One-char ui-library fix (`> 0`); upstream to pkp/ui-library |
| 7 | **Cancel Review Round erases its own decision record**: `edit_decisions.review_round_id` carries an ON DELETE CASCADE FK (`ReviewsMigration.php:46`), and `CancelReviewRound::runAdditionalActions` deletes the cancelled round — so the just-recorded `CANCEL_REVIEW_ROUND` decision row is cascade-deleted in the same request. The submission's decision history retains no trace of the cancellation (only the event log keeps `…cancelReviewRound.log`). | wave-4 review-rounds agent: `decisions.some(d => d.decision === 31)` is `false` immediately after a successful cancel (review-rounds-revisions.spec.js row 8 asserts the surviving round-1 chain instead) | FK cascade fires on the round the decision points at | Audit-trail call for upstream: either null the FK on delete for decision rows, or record the cancel decision without a `reviewRoundId` |
| 8 | **"Accept and Skip Review" never surfaces the "assign a copyeditor" prompt**: `Decision\Repository::getSubmissionNotificationTypes` syncs ASSIGN_COPYEDITOR/AWAITING_COPYEDITS only for `ACCEPT` and `SEND_TO_PRODUCTION` — `SKIP_EXTERNAL_REVIEW` lands the submission at copyediting with no editing-status notification, while a review-stage accept shows it. Inconsistent editor guidance between the two accept paths. | wave-4 copyediting agent: copyediting-stage.spec.js row 2 had to seed `[sendExternalReview, accept]` instead of the planned `[skipExternalReview]` — with skip-review the WorkflowNotificationDisplay stays empty | The decision-type → notification-types map in `lib/pkp/classes/decision/Repository.php` was never extended for the skip-review decisions | Upstream: add SKIP_EXTERNAL_REVIEW (and OMP's internal-skip variants) to the sync list |
| 9 | **Editing/production status notification flips only via the notify MESSAGE, not the assignment itself**: `StageParticipantGridHandler::saveParticipant` never re-syncs ASSIGN_COPYEDITOR/ASSIGN_PRODUCTIONUSER; the flip to "Awaiting Copyedits."/"Awaiting Galleys." happens inside `PKPStageParticipantNotifyForm::sendMessage`, gated on a non-empty message (which creates the stage discussion the manager delegate uses as its "is someone assigned" heuristic — see its literal comment "If a copyeditor is assigned i.e. there is a copyediting discussion"). Assigning a copyeditor/layout editor with the message left blank — legal in the form — leaves the editor staring at the stale "assign a …" prompt. | wave-4 copyediting/production agent: both notification-flip tests (copyediting-stage row 2, production-stage row 1) only pass when the assignment includes a notify message; verified against `PKPEditingProductionStatusNotificationManager::updateNotification` | Notification state keyed on EditorialTask existence rather than assistant stage assignments; the assignment save path skips the sync entirely (legacy FIXME nearby) | Upstream candidate: re-sync the four editing/production types in `saveParticipant` and key the "assigned" check on stage assignments |
| 10 | **Back-from-copyediting button mislabeled when no review round exists**: the action's static label is "Move to Review" (`editor.submission.decision.backFromCopyediting`), but `BackFromCopyediting::getNewStageId` returns the SUBMISSION stage when the submission has no internal/external review round (the skip-review path) — the editor clicks "Move to Review" and the submission lands on the Submission stage. | wave-4 copyediting agent: copyediting-stage.spec.js row 8 (skipExternalReview seed) — REST `stageId` flips to 1 after the "Move to Review" decision | Static locale label vs. round-dependent target-stage logic | Upstream UX call: dynamic label (or a no-review locale variant) matching the actual target stage |
| 11 | **Author dashboard crashes when "Submit Response" is clicked before the publication fetch resolves**: `AuthorResponseManager.vue` enables the button immediately but `openReviewResponseFormModal` passes `publication.value?.authors` from a still-in-flight `useFetch` — an early click mounts `AuthorResponseFormModal` with `authorOptions: undefined`, `useAuthorResponseForm` throws (`authorOptions.map` TypeError) in `setup()`, and the unhandled error unmounts the entire author dashboard (blank page until reload; no user feedback). The component already guards the email-link auto-open path against exactly this race (`isReady` watcher, with a comment describing it) but left the button path unguarded. | wave-4 row-10 fixme investigation: trace shows the click landing ~230 ms before the publications GET resolves, console `TypeError: Cannot read properties of undefined (reading 'map')`, page snapshot reduced to the header; deterministic solo (the publications GET queues behind the dashboard-mount burst) | Unguarded async-prop capture at click time (`lib/ui-library/src/managers/ReviewRoundResponseManager/AuthorResponseManager.vue` + `useAuthorResponseForm.js` ~L112) | Upstream ui-library fix: disable the button until `isReady` (or route the click through the same guard); test covers the flow via the email link instead (review-rounds-revisions row 10) |

| 12 | **`PKPApplication::getHttpClient()` sets no default request timeout**: Guzzle's default is unlimited, so any queued job or scheduled task making an outbound call (ORCID deposit/revoke jobs, Crossref deposits, `UpdateRorRegistryDataset`/`UpdateIPGeoDB` downloads, version checks) can hang until the job runner's `max_execution_time` (30 s) fatal — which aborts the user-facing request the end-of-request runner piggybacks on, and on single-process servers (`php -S`) kills the server outright. | wave-4 full-suite run 2026-06-11: 3 of 5 worker PHP servers died within 13 s of each other on `Fatal error: Maximum execution time of 30+2 seconds exceeded in …CurlHandler.php:44` while hung on outbound calls; ~190 downstream tests failed ECONNREFUSED; `jobs/orcid/*` Guzzle calls in the alphabetically-early orcid-spec window are the prime suspect | `getHttpClient()` passes proxy/UA/redirect options but no `timeout`/`connect_timeout` (`lib/pkp/classes/core/PKPApplication.php:295`) | Upstream: default `timeout` + `connect_timeout` on the shared client (long-download callers override per-request); suite-side the test config now firewalls egress — see §3 |

| 13 | **PubMed export fetches the NLM DTD over the network at request time**: `ArticlePubMedXmlFilter` declares `https://dtd.nlm.nih.gov/ncbi/pubmed/in/PubMed.dtd` as the DOCTYPE and the filter framework DTD-validates every export output, so libxml fetches the DTD (plus its MathML/ISO entity closure) per export. On any install without internet — or when NLM throttles DTD hotlinking, which they actively do — every PubMed export 500s (`Could not load the external subset`, then `displayXMLValidationErrors` fatals with "Could not convert selected objects"). | post-reset full-suite run 2026-06-11: pubmed-metadata.spec.js failed deterministically once the test config firewalled egress (which PKP wires into libxml via `PKPContainer::settingProxyForStreamContext`); 500 body shows the failed-external-subset libxml error | Remote DOCTYPE + unconditional filter output validation (`plugins/importexport/pubmed/filter/ArticlePubMedXmlFilter.php:64`, `lib/pkp/classes/xslt/XMLTypeDescription.php:134`) | Upstream: bundle the DTD with the plugin (resolve via local catalog/entity loader) or degrade validation gracefully when the DTD is unreachable; suite-side fixed with a committed DTD mirror + `XML_CATALOG_FILES` (see §3) |

| 14 | **Activity Log "participant added" entries render a literal `{$userGroupName}`**: both the live grid handler and the scenario processor write event-log param key `userGroupName` (singular), but `eventLog.json` declares only `userGroupNames` (plural, multilingual), so the param is dropped before `EventLogEntry::getTranslatedMessage()` and `submission.event.participantAdded` keeps its raw placeholder. Affects every assignment made through the UI, not just seeded rows. | wave-5 activity-log agent: History grid shows "Daniel Barnes (dbarnes) was assigned to this submission as a {$userGroupName}." | Writer/schema param-key mismatch (`StageParticipantGridHandler.php:397` + ParticipantProcessor vs `lib/pkp/schemas/eventLog.json:307`) | Upstream: align the key (writer → `userGroupNames` or schema += `userGroupName`) |
| 15 | **DiscussionManager stage heading goes stale on in-place stage switch**: `discussionTitleByStage` is evaluated once at setup (`lib/ui-library/src/managers/DiscussionManager/DiscussionManager.vue:115`) instead of `computed()`, so when the workflow side-nav switches stages and Vue reuses the component, the per-stage task list refetches correctly but the `<h3>` keeps the previous stage's title (e.g. "Production Tasks & Discussions" on the Copyediting panel). Cosmetic but misleading. | wave-5 discussions agent: error-context snapshot shows `heading "Workflow: Copyediting"` alongside `heading "Production Tasks & Discussions"` with a correctly-empty list; discussions.spec.js row 2 gates on the page-level stage h2 as workaround | One-line fix: wrap in `computed()` | Upstream ui-library |
| 16 | **Legacy fbv `confirmSubmit` button dies after two validation-rejected confirm cycles** (reviewer step 3 "Submit Review"): the third click no longer opens the confirmation modal (no dialog, no `saveStep` POST). Likely `LinkActionHandler.enableLink` re-running `bindActionRequest()` per completed modal cycle without unbinding, accumulating duplicate click handlers (`lib/pkp/js/controllers/linkAction/LinkActionHandler.js:189-207` with `ButtonConfirmationModalHandler.modalConfirm`). User-facing recovery is a page reload. | wave-5 review-forms agent: reproduced in 2 independent runs pre-workaround; review-forms.spec.js row 4 works around via reload | Handler double-binding across confirm cycles | Upstream pkp-lib JS; affects any fbv form with confirmSubmit |
| 17 | Minor candidates from wave 5: (a) Download-All archive filename carries a double dash (`{id}--submission-files.zip`) — `Str::kebab()` artifact on the id-prefixed locale string (`lib/pkp/controllers/api/file/FileApiHandler.php:168-175`); (b) ~~`manager.reviewForms.confirmActivate`/`confirmDeactivate` ship empty `msgstr` in `lib/pkp/locale/en/manager.po` — the activate/deactivate confirm modal renders with no message text.~~ **RETRACTED (product-spec review-forms verifier, 2026-07-03): inaccurate.** Both keys are non-empty **multi-line PO strings** — an empty leading `msgstr ""` line followed by continuation lines is standard gettext folding, not an empty value. `confirmActivate` = "Are you sure you wish to activate this review form? Once it's assigned to a review you will no longer be able to deactivate it." and `confirmDeactivate` = "Are you sure you wish to deactivate this review form? It will no longer be available for new review assignments." — both render full text live (see §2 row 79). No localization bug here. | wave-5 submission-files + review-forms agents; (b) retracted by review-forms verifier (2026-07-03) | — | (a) Cosmetic fix upstream; (b) no action — not a bug |

| 18 | **[FIXED 2026-06-28, pkp/pkp-lib#12799 commit d52aa4c84b]** **UI-scheduled articles got `submissions.status = PUBLISHED` while their publication stayed SCHEDULED**: `PKPSubmissionController::publishPublication` used to hard-code `Repo::submission()->updateStatus($submission, Submission::STATUS_PUBLISHED)` after every publish PUT, even when `setStatusOnPublish` left the publication `STATUS_SCHEDULED`. Consequence: articles scheduled through the UI landed in the dashboard **Published** view and never in "Scheduled for publication", and the "To be published in issue …" alert never rendered. **The fix** replaced it with `updateStatus($submission)` (recompute via `getStatusByPublications`) + `updateCurrentPublication($submission)`, so a UI-scheduled article now computes 5/5 correctly, matching the seed/CLI path. Re-verified live 2026-07-04 (product-spec publication-versioning test author): a UI-scheduled version now reads `submission.status = SCHEDULED (5)` and files under the dashboard "Scheduled" view. | wave-6 issue-assignment agent (found); product-spec versioning test author (2026-07-04, confirmed FIXED) | `lib/pkp/api/v1/submissions/PKPSubmissionController.php` `publishPublication()` (now `updateStatus($submission)` + `updateCurrentPublication()`) | RESOLVED upstream |
| 19 | **URN plugin: unset `urnCheckNo` silently empties the Identifiers form**: `URNPubIdPlugin::addPublicationFormFields` passes `getSetting(..., 'urnCheckNo')` (null when never saved) into the typed `public bool FieldTextUrn::$applyCheckNumber` → TypeError, which `Hook::run` swallows — `_components/identifier` returns 200 with `fields: []`, so the Identifiers nav item shows but the panel is empty with no Save. The plugin's own settings form always writes the setting, masking this on the pure-UI path. | wave-6 identifiers agent: TypeError in `temp/per-port-logs/8000.log`; panel renders empty until `urnCheckNo` is seeded | `plugins/pubIds/urn/URNPubIdPlugin.php:348` (same hazard at `:361`); swallow at `lib/pkp/classes/plugins/Hook.php:162` | Upstream: `(bool)`-cast the setting read; broader question: should Hook::run swallow TypeErrors silently? |
| 20 | **ContributorManager double-refresh swallows clicks and reverts optimistic reorder**: every contributor mutation triggers both the panel's own refresh AND `triggerDataChange()` workflow refetches; the trailing refetch re-render can swallow the next click (Save Order POST never sent) or revert an optimistic `moveUp`, and `getAndUpdatePublication`'s `complete()` unconditionally resets `isOrdering = false`, kicking the user out of ordering mode on any background refresh. | wave-6 contributors agent: trace shows Save Order click at t=10798 ms, refetch pair landing t=10812 ms, no `saveOrder` POST in server logs; second mechanism verified after partial fix | `lib/ui-library/src/managers/ContributorManager/contributorManagerStore.js:38-46`, `ContributorsListPanel.vue:569-584` | Upstream ui-library: drop the redundant refetch and/or preserve `isOrdering`; POM absorbs the refetch pair meanwhile |
| 21 | **Contributors list never displays affiliations (dead binding)**: `ContributorsListPanel.vue:61` renders `localize(item.affiliation)` but the author schema only carries the `affiliations[]` array — the subtitle is always empty. | wave-6 contributors agent | `lib/ui-library/src/managers/ContributorManager/ContributorsListPanel.vue:61` vs `lib/pkp/schemas/author.json` | Upstream ui-library: derive from `affiliations[].name` |
| 22 | **Galley manager regressions vs the legacy grid** (Vue port): (a) creating a remotely hosted galley always chains into the file-upload wizard the user must cancel — the port lost the legacy `!urlRemote` gate (`useGalleyManagerActions.js:28-41` vs `ArticleGalleyGridHandler.php:452-463`); (b) on a published publication the row menu says "View" but opens a fully editable form that saves straight to the live reader page (`useGalleyManagerConfig.js:130-144` flips the label only; `canEdit()` never blocks stage-assigned users) — either the label or the gate is wrong. | wave-6 galleys agent; both reproduced in spec. **Re-confirmed live 2026-07-04** (product-spec `galleys` verifier): the retained `playwright/tests/galleys.spec.js` drives (b) end-to-end on a **published** article — the row action reads "View", the metadata form is **Save-enabled**, and a label edit ("Corrected PDF") **PERSISTS** via the Vue manager (server-truth API read); (a) still reproduces — adding a remote-URL galley chains the Change-File proof-upload wizard the editor must cancel. This is the **canonical ledger home** for the galleys published-lock ⚠ (cited as **row 22b** by `docs/product/specs/galleys.md` rule 9 / Known deviations **and** `docs/product/specs/publication-versioning.md` rule 13). | lib/ui-library GalleyManager | Upstream ui-library: restore the urlRemote gate; decide view-vs-edit intent for published galleys |
| 23 | **Publish-flow affordance/state niggles**: (a) declined submissions still render the "Schedule For Publication" button — decline is enforced only at the final publish modal via `validatePublish` (`workflowConfigEditorialOJS.js:778-817` never checks `STATUS_DECLINED`); (b) the Review Publishing Details "Confirm" persists `issueId` + `READY_TO_PUBLISH/READY_TO_SCHEDULE` *before* validation runs, so a blocked publish (unpaid APC, declined) silently advances QUEUED → READY_TO_PUBLISH with the issue assignment stored — verify intended; (c) the same form's issue-assignment radio can be overwritten by the late `issueAssignmentStatus` fetch (`isInitialDataLoad` guard) — sub-second clicks schedule as immediate continuous publication instead. | wave-6 publish-flow + issue-assignment agents | lib/ui-library workflow config + `useWorkflowVersionForm.js:104-123`, `useWorkflowPublicationFormIssue` | Upstream UX/intent review |
| 24 | Minor wave-6 candidates: (a) fresh galleys all get `seq = 0` (neither `Repo::galley()->add()` nor the form initializes it) — relative order DB-undefined until manual reorder; (b) `TableCellOrder.vue` reorder buttons have no accessible name (a11y + testability); (c) hand-rolled stage-1 scenario specs without a `publications[].metadata.title` 400 on every panel save — consider a fixture-level default title in SubmissionBuilderProcessor (harness nicety, parity-neutral). | wave-6 galleys/contributors/identifiers agents | — | Cosmetic / a11y / harness follow-ups |

| 25 | **Homepage "Latest Publications" lists articles by `date_submitted`, not `date_published`**: `IndexHandler` never sets `orderBy` on the collector for the RECENT_PUBLISHED content organization, inheriting the `ORDERBY_DATE_SUBMITTED DESC` default — backdated or late-published articles sort wrongly, and same-second submission ties render in arbitrary DB order. | wave-7 homepage agent, observed live: two seeds with identical `date_submitted` rendered the OLDER `datePublished` first | `pages/index/IndexHandler.php:89-92` + `lib/pkp/classes/submission/Collector.php:61` | Upstream: `->orderBy(ORDERBY_DATE_PUBLISHED, DESC)`; test seeds space datePublished across a second boundary meanwhile |
| 26 | **Issue-entry ("Publication Settings") form blocks any save until the required Issue assignment is satisfied** — including a summary-of-changes-only edit ("Issue: This field is required.", Save disabled), and the save PUTs the assignment-derived hidden `status` onto the publication. Editors cannot record an amendment notice without making an issue decision. | wave-7 amendments agent: failure snapshot shows disabled Save + validation error on an issue-less publication | `lib/ui-library/src/pages/workflow/composables/useWorkflowPublicationFormIssue.js:110-154` | Upstream UX: decouple the non-issue fields (or make Issue requirable only when changed) |
| 27 | **Legacy category grids lost their category styling**: `gridRow.tpl` tests `is_a($row, 'GridCategoryRow')` against the pre-namespacing class name, which never matches `PKP\controllers\grid\GridCategoryRow` — category rows render without the `category`/`default_category_style`/`category_controls` classes in every legacy category grid (observed on the issue TOC grid). | wave-7 issue-management agent, observed live | `lib/pkp/templates/controllers/grid/gridRow.tpl:21,57,69,77` | Upstream: use the FQCN |
| 28 | **Issue TOC within-section article order is undefined**: nothing in the publish/assignment flow stamps `publications.seq` (schema default 0), and the TOC orders by `po.seq ASC` — all-zero ties render in arbitrary DB order that can vary between page loads until an editor uses custom TOC ordering. | wave-7 homepage agent (code-verified + observed on Postgres) | `pages/issue/IssueHandler.php:372-376`, `lib/pkp/schemas/publication.json` (seq default) | Upstream candidate: stamp next seq on issue assignment/publish |
| 29 | Minor wave-7 candidates: (a) `updateType` lacks `isRequired` on the Schedule-for-Publication version form (harmless today — no blank option — but inconsistent with intent; `useWorkflowVersionForm.js:333-339`); (b) every issue-galley link's accessible name is overridden to "Full Issue" (`issue_toc.tpl:119` passes a fixed `labelledBy` into `galley_link.tpl`) — per-galley labels invisible to AT; (c) `VariantGroup::MAX_GROUP_SIZE` is backend-only defense — the link modals filter consumed counterparts so the capacity error is unreachable through the UI (fine, worth knowing); (d) high-resolution media variants have no reader-side exposure — `HtmlGalleyHelper` deliberately filters them from embeds and no theme references variantType; they serve un-gated via `ArticleHandler::download` (round-2 theme work). | wave-7 amendments/issue-management/media-files agents | — | UX/a11y/intent review |

| 30 | **Profile email change never verifies the NEW address**: the confirmation invite is addressed to the user's *current (old)* email (`Invitation::getMailableReceiver()` resolves userId → current email, feeding `ChangeProfileEmailInvite`), so the new address's ownership/deliverability is never proven before `finalize()` swaps it in. | wave-8 user-profile agent (row 2 exercises the full pending→confirm arc) | `lib/pkp/classes/invitation/core/Invitation.php:380-385`, `lib/pkp/classes/user/form/BaseProfileForm.php:66-86` | Upstream: confirm intended; typo'd new addresses are silently accepted |
| 31 | **API keys are `sha1(time())`** — timestamp-derived (predictable) and a same-second regenerate yields an identical key. | wave-8 user-profile agent (row 6 had to interleave a reload between creates) | `lib/pkp/classes/user/form/APIProfileForm.php:116` | Upstream: `random_bytes()` |
| 32 | Wave-8 cosmetic/dead-code candidates: (a) `formSection.tpl` `translate=false` branch reads misspelled `$FBV_Label` — pre-translated section labels render empty (e.g. profile Roles tab "Register as …" prompt; one-char fix, `lib/pkp/templates/form/formSection.tpl:15`); (b) category-browse empty state is dead code — `{if empty($results)}` is never true against a `LengthAwarePaginator`, so `catalog.category.noItems` is unreachable and readers get a bare "0 Items" (`templates/frontend/pages/catalogCategory.tpl:68`; fix: `{if !$results->total()}`); (c) atom/rss feeds render keyword arrays as the literal "Array" (`WebFeedGatewayPlugin::getIdentifiers()` → `atom.tpl:81`); (d) the default theme's icon glyph leaks into the search submit button's computed accessible name — exact-name role queries fail; decorative glyph should be `aria-hidden` (`search.tpl:86`). | wave-8 registration-login / search-browse / oai-feeds agents | — | Small upstream fixes |

| 33 | **API-driven role invitations send an empty email**: `UserRoleAssignmentInvite::getMailable()` assigns the body only inside `if (isset($emailComposerValues))` — the `$templateBody` fallback is dead code, so `invite()` without composer values mails just the `<style>` block, with no accept/decline links. The UI never hits it (the composer step always posts), but any API consumer does. | wave-9 invitations agent (specs populate `emailComposer` from the default template as workaround) | `lib/pkp/classes/invitation/invitations/userRoleAssignment/UserRoleAssignmentInvite.php:110-124` | Upstream: `$emailBody = $emailComposerValues['body'] ?? $templateBody;` |
| 34 | **[FIXED wave 11 → §1 row 7]** Roles grid strips edit/delete from the row at positional index 0: `UserGroupGridHandler::loadData` keys rows positionally with no ORDER BY, and `UserGroupGridRow::initialize` gates actions on `!empty($rowId)` — `empty('0')` is true, so whichever role lands first (e.g. a just-created custom role on the in-place refresh) momentarily has no row actions. | wave-9 roles agent; worked around by reload-until-actionable | `lib/pkp/controllers/grid/settings/roles/UserGroupGridHandler.php:170-213`, `UserGroupGridRow.php:48` | Upstream: key by user_group_id (and/or `!== null` check) |
| 35 | **Existing-user invitation accept doesn't persist the browser session**: `UserRoleAssignmentReceiveController` re-runs `Validation::registerUserSession` per API call, but the cookie doesn't reliably reach the invitee's browser — post-finalize navigation bounces through /login; finalize can also intermittently hit authorizationDenied under parallel load. | wave-9 invitations + roles agents independently (specs log in explicitly afterwards / retry) | invitation receive controller session handling | Upstream: persist the session once at wizard entry |
| 36 | **Users & Roles list (Vue) keeps role-less users visible**: with `status=all` the user Collector widens the group-status filter to ALL, so users whose only assignments have ended stay listed with an empty Roles cell; the legacy grid hid them behind the explicit `includeNoRole` filter. Related UI/backend mismatch: the row config renders Remove/Disable on a site-admin's row for a non-admin manager, which the backend rejects (`cannotAdminister`). | wave-9 user-management agent | `lib/pkp/classes/user/Collector.php:500-502`; `useUserAccessManagerConfig.js` | Upstream: confirm intended UX; align row-action gating with `getAdministrationLevel` |
| 37 | Wave-9 minor batch: (a) missing en translations `userAccess.management.options` (row-menu aria-label renders `##…##`) and `##common.help##` in the nav; (b) always-on PHP warnings from invitation resources reading nonexistent payload props (`UserRoleAssignmentInviteResource.php:62`, `…ManagerDataResource.php:60`, `InvitationModel.php:95` undefined `invitation_id`); (c) email-user modal: TinyMCE-hidden textarea is skipped by jquery.validate (`ignore: ':hidden'`), so a missing body surfaces as a native server alert instead of an inline error; (d) OJS `ContextForm` defaults `enabled` to false — an admin who doesn't tick the checkbox creates an invisible journal, and the disabled-journal login bounce still renders journal chrome (made the old reachability assertion vacuous). | wave-9 agents | — | Small upstream fixes / UX review |

| 38 | **Per-journal theme save clears the GLOBAL template/CSS caches**: `PKPContextController::editTheme` wipes the site-wide compiled-template + CSS caches on every single-context theme save, disturbing renders on ALL journals (observed as missing-locale-key artifacts on concurrent backend loads — locale strings ride the shared file cache). | wave-10 appearance agent: failing snapshot showed `##common.help##` coinciding with a sibling worker's theme save | `lib/pkp/api/v1/contexts/PKPContextController.php:518-519` | Upstream: per-context cache invalidation |
| 39 | **Legacy-grid LinkAction clicks are silently lost during post-save grid refreshes**: `fetch-grid` replaces anchors wholesale with no settling signal — a click in that window registers focus but opens nothing. Seen on the review-form elements grid (wave 5, §3 note) and the NavigationMenus "Add Menu" action (wave 10); both specs ship bounded click-retries. | wave-5 + wave-10 agents, a11y snapshots show `[active]` anchor + no modal | legacy grid refresh rebinding | Upstream: delegated handlers or a refresh-complete hook |
| 40 | **NavigationMenuEditor is DnD-only and its DropZone can silently nest items**: keyboard users cannot assign menu items at all (a11y); panel free-area drops get swallowed by the expanding child DropZone ghost so an item lands INSIDE the last root item's collapsed submenu — saved and rendered that way live (user thinks the item was assigned top-level; it's hidden). | wave-10 appearance agent (verified live) | `lib/ui-library/src/components/NavigationMenuEditor/` + `DropZone.vue` | Upstream ui-library: keyboard moves + drop-target hit-area fix |
| 41 | Wave-10 cosmetic batch: (a) `common.help` is referenced (`TopNavActions.vue:11`) but defined in NO locale file — the backend Help link's accessible name renders `##common.help##` on every page; (b) `InstitutionsListPanel.vue:209` delete-confirm interpolates nonexistent `institution.title` (schema field is `name`) — the `{$title}` param is always empty; (c) `dateFormatLong` is rendered by no OJS frontend template (article landing + issue TOC both use `dateFormatShort`) — dead setting on the form. | wave-10 agents | — | Small upstream fixes |

| 42 | **Crossref export 400s for any journal without an abbreviation/acronym**: `IssueCrossrefXmlFilter::createJournalMetadataNode()` appends `<abbrev_title>` unconditionally; an empty element violates the Crossref 5.4.0 XSD (`minLength 1`) although the element itself is optional (`minOccurs=0`) — every export/deposit fails with `api.dois.400.xmlExportFailed`. | wave-11 doi/crossref agent (verified against the mirrored XSD) | `plugins/generic/crossref/filter/IssueCrossrefXmlFilter.php:154-158` | Upstream: omit the node when empty |
| 43 | **`orcidSendMailToAuthorsOnPublication` fires on ACCEPT, not publication**: `SendAuthorOrcidEmail` subscribes to `DecisionAdded` (ACCEPT / SKIP_EXTERNAL_REVIEW); nothing ORCID-mail-related listens on `PublicationPublished` — authors get "Requesting ORCID record access" at accept time even if the article is never published. Also: the listener filters on `orcidAccessToken`, not `orcidIsVerified`, so a flag-verified token-less author is re-mailed. | wave-11 plugins/orcid agent | `lib/pkp/classes/observers/listeners/SendAuthorOrcidEmail.php:34-68` | Upstream: rename the setting or re-hook; confirm token-based filter intent |
| 44 | **Subscription-gated readers dead-end on the homepage when no payment plugin is configured**: the galley gate redirects to `about/subscriptions`, but `AboutHandler::subscriptions` 302s on to the journal index unless payments are enabled+configured — although subscription contact/types are payment-independent information. Verified live: galley → about/subscriptions → index, zero explanation shown. | wave-11 subscriptions agent | `pages/article/ArticleHandler.php:676` → `pages/about/AboutHandler.php:40-45` | Upstream: render the page whenever publishingMode=1 |
| 45 | **Payments settings API silently disables payments on JSON-boolean input**: `PKPBackendPaymentsSettingsController::edit` compares `paymentsEnabled === 'true'`; a boolean `true` persists FALSE and returns 200 echoing it. The Vue form survives only because it posts form-encoded strings. | wave-11 subscriptions agent (verified live) | `lib/pkp/api/v1/_payments/PKPBackendPaymentsSettingsController.php:100-103` | Upstream: accept booleans or 400 |
| 46 | **Stats "All Dates" view is empty on young journals**: with no `dateStart` the publications stats controller substitutes the context's earliest `date_published` while `dateEnd` defaults to YESTERDAY — a journal whose first publication is same-day queries `BETWEEN today AND yesterday` and renders an empty timeline/table despite existing metrics. | wave-11 payments/stats agent (SQL bind log: `$6='2026-06-12', $7='2026-06-11'`) | `PKPStatsPublicationController::_processAllowedParams` + `PKPStatsPublicationService.php:292-293` | Upstream: clamp dateEnd ≥ dateStart (or default dateEnd to today) |
| 47 | **CSL RIS export renders malformed dates** (`PY  - %2026/%06/%12`): the Blade conversion left strftime placeholders feeding `Carbon::format()`, where `%` is a literal. | wave-11 CSL agent | `plugins/generic/citationStyleLanguage/templates/citation-styles/ris.blade:45,48,82` | Upstream: `'Y/m/d'` / `'Y'` |
| 48 | Wave-11 minor batch: (a) record-payment API maps `'paid'` → Waiver and `'waived'` → ManualPayment (labels swapped; no UI renders them today — `BackendSubmissionsController.php:127,155`); (b) `purchaseArticleFeeEnabled`/`purchaseIssueFeeEnabled` are dead settings (declared, no template field, no consumer — gates are `fee > 0`); (c) `PaymentsGridHandler::viewPayment` is an empty FIXME stub (no payment-details surface); (d) OJS stats detail table renders 8 headers but 7-cell body rows (JATS column header without data); (e) legacy LinkAction re-enable lags modal close on the Vue bridge (`disabled` cleared before the handler re-arms — fast reopen clicks silently dropped; same family as §2 row 39); (f) the site-level enable checkbox for context-level generic plugins governs nothing journals see (journals read their own row, which installs enabled). | wave-11 agents | — | Triage/UX review |

| 49 | **Email attachments always deliver as `application/octet-stream`**: `Mailable::attachTemporaryFile()` attaches by the temporary file's on-disk path (random name, no extension) with only `['as' => $name]` — Symfony Mime's extension guess falls back to octet-stream even though the temporary-files row stores the true type. Recipients get type-less downloads; inline preview never works. | wave-12 email-delivery agent: Mailpit `/api/v1/message/{id}` shows `application/octet-stream` for a PNG attached via the discussion composer; the spec pins the current behavior with a comment | `lib/pkp/classes/mail/Mailable.php:615` (the sibling `attachSubmissionFile`/`attachLibraryFile` paths deserve the same audit) | Upstream one-liner: pass `'mime' => $file->getFileType()` in the attach options |
| 50 | **Editorial-reminder in-app notification is created at the wrong level**: the `EditorialReminder` job calls `createNotification()` without a level, inheriting the `NOTIFICATION_LEVEL_NORMAL` default — but the backend bell/tasks grid filters on `NOTIFICATION_LEVEL_TASK`, so the in-app counterpart of the reminder digest can never surface there (NORMAL notifications only flash via the general in-place banner framework). The email is unaffected. | wave-12 serial/scheduled-tasks agent: after a forced `EditorialReminders` run, `notifications` row exists (level 2) but the editor's Tasks grid never lists it | `lib/pkp/jobs/email/EditorialReminder.php:161-166`; default in `PKPNotificationOperationManager::createNotification` | Upstream: pass `NOTIFICATION_LEVEL_TASK` (or drop the in-app row) |
| 51 | **API auth failure codes are inconsistent** (doc pins, both asserted by api-smoke rows 5–6 so upstream changes will surface loudly): (a) `HasRoles` middleware responds **401** `api.403.unauthorized` even for an authenticated-but-underprivileged user — REST convention says 403 (`lib/pkp/classes/middleware/HasRoles.php:70-73`); (b) a tampered-signature API token responds **400** `api.400.invalidApiToken` where 401 is conventional (`lib/pkp/classes/middleware/DecodeApiTokenWithValidation.php:108-112`; the unknown-key branch at `:100-105` correctly 401s). | wave-12 misc agent, verified live both directions | middleware cited | Upstream behavior call; if changed, update api-smoke rows 5–6 in the same PR |
| 52 | **Add Reviewer dialog: reviewer-suggestions list quirks** (found building the G2 cross-check row): (a) every suggestion entry's Select button exposes accessible name **"Select undefined"** — the sr-only label interpolates a missing name param while the visible "Select Reviewer" text is aria-hidden (screen-reader users can't tell entries apart; the sibling Locate-a-Reviewer list interpolates correctly, "Select {name}"); (b) after a successful enroll-and-assign submit from the suggestion path, the dialog pops back to the selection step instead of closing and the reviewer manager underneath never live-refreshes (the legacy `dataChanged` event is dropped on this path) — the editor has no success feedback until a manual close + reload. | wave-12 gap-closure work: a11y snapshot shows `button "Select undefined"` per suggestion; trace shows `enroll-reviewer` 200 `status:true` + `dataChanged` with the manager still "No Items" 20 s later (reviewer-suggestions.spec.js row 5 reloads as workaround) | lib/ui-library ReviewerManager suggestion list + legacy-form event bridge | Upstream ui-library: fix the label interpolation; close the dialog (or at least refresh the manager) on success |
| 53 | **Publish/unpublish authority: API and UI disagree**: the version/publish/unpublish/changeVersion API routes authorize assigned non-recommend-only sub-editors AND (for version ops) assistants at the production stage, while the dashboard renders publish controls only for Manager/Site Admin — roles with no button can publish by direct API call. Live-probed on a scratch journal: sectionEditor `POST …/version` 200, `PUT …/publish` 200 (status→3), `PUT …/unpublish` 200, with `canPublish=false` in the UI config; layoutEditor `POST …/version` 200, `changeVersion` 401. Hidden-but-permitted is security-adjacent: decide the intended authority and align both sides. | product-spec pilot verifier (2026-07-02), live-probed | `lib/pkp/api/v1/submissions/PKPSubmissionController.php:149-162,238-297` vs `lib/ui-library/src/pages/workflow/composables/useWorkflowPermissions.js:79-87` | Upstream: align API authorization with UI intent (or vice versa) |
| 54 | **`POST …/version` input-validation gaps**: (a) an invalid `versionStage` is silently ignored (`tryFrom` → null → inherits the source version's stage) where `PUT …/version` 422s the identical value ("The selected version stage is invalid."); (b) an invalid `versionIsMinor` (e.g. `"banana"`) 500s with a TypeError (`FILTER_NULL_ON_FAILURE` feeds null into a `bool` param). Both live-probed. | product-spec pilot verifier (2026-07-02), live-probed | `lib/pkp/api/v1/submissions/PKPSubmissionController.php:1289-1294` vs `:2337-2351` | Upstream: validate POST like PUT (422 both) |
| 55 | **Editing/production status notifications created at the wrong level (second instance of the row-50 pattern)**: `PKPEditingProductionStatusNotificationManager::_createNotification()` never passes a level, so ASSIGN_COPYEDITOR / AWAITING_COPYEDITS / ASSIGN_PRODUCTIONUSER / AWAITING_REPRESENTATIONS default to NOTIFICATION_LEVEL_NORMAL despite being conceptually stage tasks — same family as §2 rows 9 and 50. | product-spec atlas sweep (2026-07-02) | `lib/pkp/classes/notification/managerDelegate/PKPEditingProductionStatusNotificationManager.php` | Upstream: pass LEVEL_TASK (bundle with row 50's fix) |
| 56 | **Site-admin task/discussion carve-outs check journal scope, so they're dead code** (LOW practical impact — see mitigation): every task/discussion gate calls `hasRole([...], $contextId)`, which never matches site-level groups (`RoleDAO.php:68` — `COALESCE(context_id,0)` vs journal id), so the site-admin exemptions the code clearly intends (`EditTask.php:211/:235`, tasks `Repository.php:258`; cf. the correct `SITE_CONTEXT_ID` pattern at `EditTask.php:92`) never fire. Live-probed with a site-admin-only account: `{"items":[],"itemMax":0}` on a stage with 3 items, `PUT …/close` → 401. **Mitigation (maintainer, verified in code):** journal creation auto-enrols the creating admin as a manager of that journal (`PKPContextService.php:560-578`), so admins normally act through the manager role and never hit this; it only bites an admin account with NO manager enrolment on the journal (a 2nd admin who didn't create it, or an enrolment later removed). | product-spec pilot verifier (2026-07-02), live-probed; mitigation from maintainer | `lib/pkp/classes/security/authorization/internal/` query policies + `RoleDAO.php:68` | Upstream (low priority): make the exemption sites site-context aware (systemic — grep the pattern); harmless today given auto-enrolment |
| 57 | **Vestigial `DELETE …/tasks/{taskId}/notes/{noteId}` endpoint that can never succeed** (LOW — observable behaviour is correct): `NoteAccessPolicy` WRITE permits only headnotes while the controller 403s headnotes, so BOTH reply and headnote deletion 401 (live-probed, even manager with `?stageId`); no UI calls it. **Maintainer intent:** replies are intentionally permanent (a reply is a record), so the user-facing behaviour — no reply deletion — is correct. This is therefore dead code, not a functional bug. | product-spec pilot author + verifier (2026-07-02), live-probed; intent from maintainer | `lib/pkp/classes/security/authorization/NoteAccessPolicy.php:99-101` vs `EditorialTaskController.php:789-793` | Upstream (low priority): remove the dead route, or document what it was intended for |
| 58 | **Overdue tasks are un-editable until re-dated**: `dateDue` is re-validated `after_or_equal:today` on EVERY edit, so renaming or adding a participant to an already-overdue task 422s ("Start date should be greater than or equal to today") until the due date is bumped. Live-probed via DB-forced `date_due`. | product-spec pilot author + verifier (2026-07-02), live-probed | `lib/pkp/classes/submission/task/EditTask.php:75` | Upstream: validate dateDue only when it changes |
| 59 | **Headnote-less tasks/discussions 500 on API edit and render wrongly**: template-auto-created tasks (empty description) and ALL cover-note discussions (`Repository.php:103-108` omits `isHeadnote` on `Note::create`) have no headnote — an edit request without `description` 500s (`EditorialTaskController.php:394-397`, `:1019-1023`, notifyParticipants `:1083` null deref), and the cover-note discussion renders its first message as a reply with an empty description. UI edits are shielded only because the form always sends the required description field. Live-probed both shapes. | product-spec pilot author + verifier (2026-07-02), live-probed | `lib/pkp/classes/query/Repository.php:103-108`, `EditorialTaskController.php:394-397,1019-1023,1083` | Upstream: stamp `isHeadnote` on cover-note creation; null-guard the edit path |
| 60 | Tasks & discussions minor batch (product-spec pilot, live-probed where noted): (a) creator/poster receives their OWN NEW_QUERY notification + email on create/reply (probed; legacy excluded the actor); (b) reviewers keep task/discussion stage access with declined or cancelled assignments (`QueryUserAccessibleWorkflowStageRequiredPolicy.php:45-60` — the active-assignment filter exists only on getParticipants/getReviewers); (c) one-way task completion has holes: `startTask` has no dateClosed guard (closed-but-unstarted task can be started, probed) and the view-modal maps CLOSED→open with no task-type guard (`useDiscussionManagerForm.js:519`); (d) participant validation accepts any user with ANY review assignment in ANY stage (`EditTask.php:249-257`); (e) `QUERY_ACTIVITY` notification type has a Profile→Notifications settings row but no creator anywhere (dead setting, `PKPNotificationSettingsForm.php:92`); (f) manager-reply block enforced by `AddNote.php:58-60` validation while the controller 403 at `:715-717` is unreachable dead code. | product-spec pilot verifier (2026-07-02) | cited per item | Triage/intent review (bundle with rows 56-59) |
| 61 | **Submission-start page and create API disagree on self-registration — API-only authorization bypass**: the start *page* only admits a role-less user when an author group has `permitSelfRegistration` on (`PKPSubmissionHandler::getSubmitUserGroups()` → shows the "Not Allowed" page otherwise), but `POST /submissions` auto-enrols any role-less user into the journal's first author group with **no** self-registration check (`PKPSubmissionController::add()` else-branch, `UserGroup::…withRoleIds(AUTHOR)->first()` then `assignUserToGroup`). Live-probed on a scratch journal with author self-registration turned off + a reviewer-only user: the UI showed "Not Allowed", yet the raw POST enrolled the user as an author (role 65536 appeared) and proceeded to draft creation (only a `sectionId` "field required" validation error came back — not an authorization refusal). Lets a user bypass a journal's "authors must be registered by editorial staff" policy via a hand-crafted API call. | product-spec submission-wizard verifier (2026-07-02), live-probed | `lib/pkp/pages/submission/PKPSubmissionHandler.php` `getSubmitUserGroups()` (`permitSelfRegistration(true)`) vs `lib/pkp/api/v1/submissions/PKPSubmissionController.php` `add()` (auto-enrol else-branch, no self-reg filter) | Upstream: have `add()` respect the author group's `permitSelfRegistration` the way the page does (or 403 role-less users when self-reg is off) |
| 62 | **Keyword/subject autosuggest journal scoping is broken — empty on most journals, cross-journal leak on the "first" one**: `GET /vocabs` (the suggestion source for the Keywords/Subjects/Disciplines/Supporting Agencies chip fields) filters by journal via `ControlledVocab::scopeWithContextId()`, whose inner subquery `(SELECT publications.submission_id FROM publications INNER JOIN controlled_vocabs ON publications.publication_id = controlled_vocabs.assoc_id LIMIT 1)` is **uncorrelated** with the outer row — it always resolves to one arbitrary publication (in practice the lowest-id one, i.e. `publicknowledge`). Result, live-probed 2026-07-02: a keyword stored on a scratch-journal submission is NOT suggested when typing in that same journal's wizard (`/vocabs?...&term=quan` → `[]`), while the SAME term IS suggested inside `publicknowledge`'s wizard (`[{"name":"quantum computing"}]` in the dropdown) even though the entry belongs to another journal. So suggestions are dead on every journal except the arbitrary winner, which leaks all journals' terms. | product-spec submission-wizard-metadata author (2026-07-02), live-probed API + UI dropdown both ways; re-verified by the spec verifier 2026-07-02 (fresh term: `[]` on its own journal, suggested on publicknowledge) | `lib/pkp/classes/controlledVocab/ControlledVocab.php` `scopeWithContextId()` (used by `PKPVocabController::getMany()`) | Upstream: correlate the subquery with the outer `controlled_vocabs` row (join assoc_id → publications → submissions.context_id) |
| 63 | **Steps wizard nav exposes the current step only as a CSS class — no `aria-current`**: the submission wizard's step buttons (`Steps.vue`) mark the active step solely with the `pkpSteps__step__label--current` class; the accessible tree carries no `aria-current="step"` (nor `aria-selected`), so screen-reader users cannot tell which step is active, and e2e locators are forced to the CSS class (skill patterns.md rates CSS "last resort" — this is the component making it the *only* resort). Found while hardening `SubmissionWizardPage.continueStep()` against swallowed clicks (2026-07-02). | product-spec loop orchestrator (2026-07-02), DOM inspection of Steps.vue template | `lib/ui-library/src/components/Steps/Steps.vue` step `<button>` `:class` ternary | Upstream (ui-library): add `aria-current="step"` to the active step button; then POMs can switch to `[aria-current="step"]` |
| 64 | **Author can watch editorial uptake of their reviewer suggestions via the API — reviewer-anonymity leak on double-anonymous journals** (API-only; no UI surface): after submit, the submitting author's reads of `GET /submissions/{id}/reviewers/suggestions` keep returning 200 (AUTHOR is in the controller's route role list and no post-submit read restriction applies), and every item exposes `approvedAt`, `reviewerId`, `existingUserId`, `hasExistingReviewerRole`; adding `?include_reviewer_data=true` additionally returns the linked reviewer's **full editorial summary** — email, biography, affiliation, review-workload stats (`reviewsCompleted`/`averageReviewCompletionDays`/…) and even ORCID token fields. On the double-anonymous default journal (`publicknowledge`, `defaultReviewMode=2`) this tells the author exactly which suggested people became reviewers and when, defeating reviewer anonymity. Live-probed end-to-end 2026-07-02: author `atester` read submission 70 after editor `dbarnes` assigned the matching account (`jjanssen`); the author API returned `approvedAt` + `reviewerId=7` and the full reviewer summary object. | product-spec reviewer-suggestions verifier (2026-07-02), live-probed end-to-end (author account, double-anonymous journal) | `lib/pkp/api/v1/reviewers/suggestions/resources/ReviewerSuggestionResource.php` `toArray()` (no role-based field filtering; `include_reviewer_data` → `summarizeReviewer`); `ReviewerSuggestionController::getMany()`/`get()` (AUTHOR in `getRouteGroupMiddleware()`, no post-submit read gate) | Upstream: strip approval/`reviewerId`/reviewer-summary fields from author-context reads, or restrict post-submit suggestion reads to editorial roles |
| 65 | **Save-for-later has no completeness guard — it can "un-submit" an already-submitted submission via the API** (API-only; no UI surface): `PUT /submissions/{id}/saveForLater` adds only `SubmissionAccessPolicy`, never a completeness/incomplete guard (unlike the reviewer-suggestion controller, which adds `SubmissionIncompletePolicy` on its write verbs), and writes `submissionProgress` directly through `Repo::submission()->edit()` — bypassing the schema `in:,start,details,files,contributors,editors,review` whitelist (it only runs `ctype_alnum` on the step token). Live-probed 2026-07-02: a *completed* submission (submission 88, `submissionProgress=''`, `dateSubmitted` set) answered `saveForLater` with 200 and came back `submissionProgress='details'` — reappearing in the author's Incomplete-submissions list, reopening in the wizard (contradicting submission-wizard rule 9 "there is no un-submit"), and becoming deletable by the author via the incomplete-bulk-delete tool (author DELETE returned 200). `dateSubmitted` is NOT cleared — the resurrected "draft" keeps its submission date, so every incomplete-list/delete gate demonstrably keys on the progress marker alone (`submission_progress <> ''`). The UI never exposes this because a submitted submission routes to the "Submission complete" screen, never rendering Save for Later. | product-spec submission-drafts author (2026-07-02), live-probed end-to-end (author account); re-verified end-to-end on a FRESH submission by the drafts verifier 2026-07-03 (fresh submitted sub 62: saveForLater 200 → `submissionProgress='details'`, `dateSubmitted` retained → listed in author Incomplete view → author bulk-DELETE 200) | `lib/pkp/api/v1/submissions/PKPSubmissionController.php` `authorize()` (no completeness policy for `saveForLater`) + `saveForLater()` (`ctype_alnum` step check, direct `Repo::submission()->edit(['submissionProgress'=>...])`) | Upstream: guard `saveForLater` against completed submissions (add a completeness/incomplete policy), and validate the step token against the schema whitelist |
| 66 | **Bulk "Delete Incomplete Submissions" of more than 30 selected drafts always fails wholesale with a misleading 404**: `bulkDeleteIncompleteSubmissions()` re-verifies the selected ids through `getSubmissionCollector()`, which hard-defaults `->limit(30)`, and the dashboard's delete call (`useDashboardBulkDelete.js apiCall()`) sends only `ids` — never a `count` — so the verification query can return at most 30 rows; `array_diff` then flags every id past the 30th as "missing" and the whole batch is refused 404 ("The requested resource was not found."), deleting nothing. Live-probed 2026-07-03: author `atester` DELETE with 31 valid, own, incomplete draft ids → 404, all 31 drafts still present. UI-reachable: the dashboard pages at 30 rows (`PKPDashboardHandler::$perPage`) and the checkbox selection persists across page changes (live-probed: Delete button stayed enabled on page 2 with a page-1 selection; `bulkDeleteSelectedItems` is only reset on delete/cancel), so a user with 31+ drafts can accumulate >30 and hit a dead Delete that reports "not found" for drafts that plainly exist. | product-spec submission-drafts verifier (2026-07-03), live-probed (API 31-id batch + UI cross-page selection persistence) | `lib/pkp/api/v1/_submissions/PKPBackendSubmissionsController.php` `getSubmissionCollector()` (`->limit(30)` default) + `bulkDeleteIncompleteSubmissions()` (`array_diff` → 404); `lib/ui-library/src/pages/dashboard/composables/useDashboardBulkDelete.js` `apiCall()` (no `count` param) | Upstream: lift the limit for the bulk-delete verification query (`->limit(count($ids))` or no limit), or have the UI send `count=ids.length`; also consider resetting the selection on page change |
| 67 | **Legacy `/{journal}/submissions/tasks` is routed but crashes with HTTP 500 — and four sibling ops are routed to nothing**: the pre-3.6 `submissions` page survives only for the role-priority redirect (`index` op → `PKPPageRouter::redirectHome()`, verified working for every role), but its `tasks` op still renders `dashboard/tasks.tpl`, a template that no longer exists anywhere in the tree → 500 for every logged-in editorial user (live-probed dbarnes + admin 2026-07-03). The handler additionally role-assigns `myQueue`/`unassigned`/`active`/`archives`, which have no implementing methods and 404 (live-probed). Nothing in the current UI links any of these — dead legacy surface. | product-spec editorial-dashboards author (2026-07-03), live-probed | `lib/pkp/pages/dashboard/DashboardHandler.php` (`tasks()` → missing `dashboard/tasks.tpl`; `addRoleAssignment` lists the four ghost ops); `lib/pkp/pages/submissions/index.php` (routes only `index`/`tasks`) | Upstream: delete the `tasks` op + ghost role assignments (keep the `index` redirect), or restore/remove the template |
| 69 | **The dashboard conflict-of-interest guard is enrolment-gated, so it silently fails to protect a manager who authored a submission without an Author enrolment**: the editorial-dashboard cell that replaces the actions with "You cannot access this submission as a Journal Manager since you are the author/reviewer…" (and withholds the **View** button) is driven by `useCurrentUser.hasCurrentUserAtLeastOneAssignedRoleInAnyStage(submission, [ROLE_ID_AUTHOR])`, which reads `submission.stages[].currentUserAssignedRoles`. That array is populated by `maps/Schema.php::getPropertyStages()` via `getAssignmentRoles()`, which resolves a stage assignment's role through the viewer's **live `user_user_groups` enrolment** and returns `null` when the viewer holds the stage assignment but no active enrolment in that role's user group. Result: a manager who is a submission's **author by stage assignment but was never enrolled as an Author** (seeded via SQL, or added as a participant in a role they don't hold) is shown a **normal** View button + ordinary activity cell instead of the guard — the exact mis-access the guard exists to prevent. `DashboardCellSubmissionActions.vue showButton` uses the same predicate, so View is offered too. Live-probed 2026-07-03: dbarnes (AUTHOR stage assignment on `publicknowledge` submissions 343/344, manager-only enrolment) → cell showed "Assign Editor / **View**", no COI notice; contrast eddpmgr1 on `j-eddprobej2` (enrolled Author) whose authored row 353 correctly showed the notice with View withheld, and its manager+reviewer row 354 showed the reviewer twin. Contradicts the UI affordance the guard makes. | product-spec editorial-dashboards verifier (2026-07-03), live-probed both ways | `lib/ui-library/src/composables/useCurrentUser.js` `hasCurrentUserAtLeastOneAssignedRoleInAnyStage()`; `lib/pkp/classes/submission/maps/Schema.php` `getPropertyStages()`/`getAssignmentRoles()` (role resolved via `userUserGroups`, null when unenrolled); `lib/ui-library/src/pages/dashboard/composables/useDashboardConfigEditorialActivity.js` `getEditorialActivityForEditorialDashboard()`; `DashboardCellSubmissionActions.vue` `showButton` | Upstream: key the guard on the stage assignment's user-group role directly (as the assignment already records it) rather than on a separate live enrolment, so an author/reviewer stage assignment always triggers the COI cell |
| 68 | **Reviewer dashboard renders a full set of inert list controls** (extends row 2 with the UI-side inventory): `/dashboard/reviewAssignments` mounts the shared DashboardPage controls — search box, Filters panel (Section/Issues/Categories/Days-since-activity), sortable ID column, pager — but `getReviewAssignments()` ignores `searchPhrase`, all filter params, `orderBy` and `count`/`offset`, always returning the reviewer's complete list. Live-probed 2026-07-03 (jjanssen, 42 active assignments): all 42 rows render on one page while the pager claims "Showing 1 to 30 of 42"; clicking page 2 re-renders the same 42 rows under "Showing 31 to 42"; searching a nonsense phrase, applying a Section filter and toggling ID sort each leave the list byte-identical while still writing their state into the URL (so a shared URL silently misrepresents what the recipient sees). | product-spec editorial-dashboards author (2026-07-03), live-probed UI + API | `lib/pkp/api/v1/_submissions/PKPBackendSubmissionsController.php` `getReviewAssignments()` (no list-param wiring); `lib/ui-library/src/pages/dashboard/` (controls mounted for all dashboard variants) | Upstream: wire search/filters/sort/pagination into the reviewer-assignments endpoint, or hide the inert controls on this dashboard |
| 70 | **Legacy `workflow/index/{submissionId}` without a stage segment crashes with HTTP 500**: `PKPWorkflowHandler::identifyStageId()` has three fallbacks — `stageId` user var, stage-path op name, then a bare `$args[1]` — and the third throws "Undefined array key 1" when the old two-segment form is hit, before the redirect can fire. Live-probed 2026-07-03 as dbarnes on an accessible submission (`/publicknowledge/workflow/index/538` → 500), while every sibling form works and 302s into the dashboard modal: `workflow/access/{id}`, `workflow/access?submissionId={id}`, `workflow/index/{id}/{stageId}`, and the four stage paths. | product-spec workflow-stage-navigation author (2026-07-03), live-probed | `lib/pkp/pages/workflow/PKPWorkflowHandler.php` `identifyStageId()` (unguarded `$args[1]`) | Upstream: treat a missing stage like the `access` op (redirect with no stage) or 404, instead of a fatal |
| 71 | **The workflow modal opens incomplete drafts, and the legacy `workflow/access` shim funnels drafts into it — bypassing the completeness gate the per-stage doors enforce**: deep-linking `dashboard/editorial?workflowSubmissionId={draftId}` as a manager renders the full editorial workflow shell on a never-submitted draft — "Incomplete" stage bubble, Submission-stage pane with the draft's file in the Submission Files manager and the Discussions panel (live-probed 2026-07-03, dbarnes on seeded draft 545) — and `workflow/access/{draftId}` 302s to exactly that URL because `PKPWorkflowHandler::authorize()` adds `SubmissionCompletePolicy` only to the NON-access ops (live-probed: `workflow/access/545` → 302 into the modal; `workflow/index/545/1` → authorizationDenied `user.authorization.submission.incomplete.workflowAccessRestrict`). The dashboard affordance disagrees too: the row withholds **View** for drafts (Complete-submission action instead). The modal path has no completeness check — the page-level `GET submissions/{id}` succeeds for manager scope and the Vue shell renders whatever the state default resolves to. NOT only manager-scoped: the **author's own tracking view has the same hole** — the draft's author deep-linking `dashboard/mySubmissions?workflowSubmissionId={draftId}` gets the full author shell on the never-finished draft (Incomplete bubble, Submission Files with the draft's file, Discussions panel; verifier-probed 2026-07-03, atester on draft 545), while the author's list row deliberately swaps View for Complete-submission. It surfaces workflow panels (and whatever actions those panels gate) on a submission that was never submitted. | product-spec workflow-stage-navigation author (2026-07-03), live-probed all three editorial doors; verifier added the author-door probe (2026-07-03) | `lib/ui-library/src/pages/dashboard/dashboardPageStore.js` `openWorkflowModal()` (no draft guard; called for any `workflowSubmissionId` in the URL, on the editorial AND author dashboards); `lib/pkp/pages/workflow/PKPWorkflowHandler.php` `authorize()` (`SubmissionCompletePolicy` on non-`access` ops only) | Upstream: one consistent completeness rule on every door — either block drafts in the modal + `access` op (match the dashboard affordance) or drop `SubmissionCompletePolicy` from the per-stage shims |
| 72 | **Revoked-role checks disagree between the workflow shell and the legacy/API doors — a partially-revoked user passes the door into a modal that denies everything**: the Vue shell resolves a stage assignment's role through a live enrolment in the assignment's **exact user group** (`Schema::getAssignmentRoles` — `userUserGroups` date-window check), while the server-side accessible-stages computation only requires membership in any group with the same **role id** (`Repository::getAccessibleWorkflowStages` — `in_array($roleId, $userRoleIds)`). Live-probed 2026-07-03 on scratch journal `wsnavv2`: user enrolled as Copyeditor + Layout Editor (both ROLE_ID_ASSISTANT), assigned to a copyediting-stage submission as Copyeditor; with the Copyeditor enrolment revoked via SQL but Layout Editor kept, the legacy `workflow/editorial/{id}` door still 302s them into the modal while every pane (including their assigned stage 4) shows only "You don't currently have access to that stage of the workflow." — revoking the sibling enrolment too closes both doors (`authorizationDenied?message=user.authorization.accessibleWorkflowStage`). The same role-level laxity applies to every consumer of `getAccessibleWorkflowStages` (grids, decision APIs). | product-spec workflow-stage-navigation verifier (2026-07-03), live-probed both halves incl. control | `lib/pkp/classes/submission/maps/Schema.php` `getAssignmentRoles()` (group-level enrolment check) vs `lib/pkp/classes/user/Repository.php` `getAccessibleWorkflowStages()` (role-level check) | Upstream: pick one revocation rule — probably the group-level check — and use it on both sides |
| 73 | **The "editor assigned" task notification (`NOTIFICATION_TYPE_EDITOR_ASSIGN`) is dead — its creator branch keys on a template name that a 3.4 migration renamed away**: `PKPStageParticipantNotifyForm::sendMessage()` only creates the notification in a `switch ($templateKey) { case 'EDITOR_ASSIGN': … }` branch, but the Add-Participant / Notify picker only ever offers the stage's discussion template plus the stage-specific alternates `EDITOR_ASSIGN_{SUBMISSION,REVIEW,PRODUCTION}` that `I5716_EmailTemplateAssignments` created out of the old `EDITOR_ASSIGN` template — so the exact key `EDITOR_ASSIGN` is never submitted and the branch never runs. Live-probed 2026-07-03 (dbarnes assigning an editor on submission 418 with the "Assign Editor" template = `EDITOR_ASSIGN_SUBMISSION`): a `NEW_QUERY` notification was created (query 11) but **no** `EDITOR_ASSIGN` notification (DB type 16777255 absent). The only other references to the constant are display/settings handlers for an already-existing notification, so nothing raises it anywhere. | product-spec stage-participants author (2026-07-03), live-probed UI + DB | `lib/pkp/controllers/grid/users/stageParticipant/form/PKPStageParticipantNotifyForm.php` `sendMessage()` (`case 'EDITOR_ASSIGN'`) vs `lib/pkp/classes/migration/upgrade/v3_4_0/I5716_EmailTemplateAssignments.php` (`EDITOR_ASSIGN_*` split) | Upstream: match the stage-variant keys in the switch (or drop the dead notification type + branch) |
| 74 | **Re-assigning an existing stage participant silently ignores the flags entered in the Assign modal**: `AddParticipantForm::execute()` (add mode) calls `Repo::stageAssignment()->build()`, which is `firstOr` on (submission, user, user-group) — so if the picked user already holds that role on the submission, the existing row is returned untouched and the `recommendOnly` / `canChangeMetadata` checkbox values from the modal are discarded, with no error or warning; the only way to change an existing assignment's flags is the separate **Edit** action. | product-spec stage-participants author (2026-07-03), code-traced (same `firstOr` caveat is documented in the scenario harness notes) | `lib/pkp/classes/stageAssignment/Repository.php` `build()` (`firstOr`); `lib/pkp/controllers/grid/users/stageParticipant/form/AddParticipantForm.php` `execute()` (insert-vs-update branch keyed on `$assignmentId`, not on an existing row) | Upstream: on re-add, update the flags (or warn that the user is already assigned) instead of silently no-op'ing |
| 75 | **The Assign-modal recommend-only / self-flag guards are enforced on Edit but skipped on Add**: `AddParticipantForm::_isChangeRecommendOnlyAllowed()` / `_isChangePermitMetadataAllowed()` (which stop a recommend-only editor from touching recommend-only, and a section editor from changing their own flags) are consulted **only** in the `$assignmentId` (Edit) branch of `fetch()`/`execute()`. In **add** mode the template (`addParticipantForm.tpl`) renders both checkboxes unconditionally, the legacy JS (`StageParticipantNotifyHandler.js` `updateRecommendOnly`) shows+enables them purely by the selected user-group (never checking whether the acting user is itself recommend-only), and `execute()`'s insert branch passes the raw `recommendOnly` post value straight into `Repo::stageAssignment()->build()`. Net effect: a **recommend-only section editor can stamp recommend-only on a brand-new participant they add**, though they cannot change it on an existing assignment via Edit — an internal inconsistency. | product-spec stage-participants verifier (2026-07-03), live-probed end-to-end (minoue, a recommend-only SE, added sberardo as Section editor with the box checked → `stage_assignments.recommend_only=1`; the checkbox read `disabled:false` before any manipulation) | `lib/pkp/controllers/grid/users/stageParticipant/form/AddParticipantForm.php` `execute()` (add branch calls `build()` ungated) + `_isChangeRecommendOnlyAllowed()`/`_isChangePermitMetadataAllowed()` (used only in edit branch); `lib/pkp/templates/controllers/grid/users/stageParticipant/addParticipantForm.tpl` (unconditional checkboxes in add mode); `lib/pkp/js/controllers/grid/users/stageParticipant/form/StageParticipantNotifyHandler.js` `updateRecommendOnly()` | Upstream: apply the same `_isChange*Allowed` guards on the add path (or accept and document that add is intentionally unguarded) |
| 76 | **Reviewer step-3 recommendation `<select>` stays enabled on a completed (read-only) review** — cosmetic only, no data hole. `reviewerRecommendations.tpl` gates the field's `disabled` attribute on an **unset `$readOnly`** template var, while every other step-3 control (comment editors, Submit / Save-for-Later buttons) gates on `$reviewIsClosed` (`ReviewerReviewForm::fetch()` sets only the latter). So on a submitted review the dropdown is the one live-looking control on an otherwise-disabled form. It is inert: Submit/Save are disabled (no UI path), and a hand-crafted `saveStep` POST changing the recommendation is refused server-side — `PKPReviewerHandler::saveStep()` throws "Review already completed!" before touching the form. | product-spec reviewer-response verifier (2026-07-03), live-probed: seeded a completed jjanssen review, `<select>` `disabled` attr = null (enabled); direct `POST reviewer/saveStep` with a different `reviewerRecommendationId` → **HTTP 500**, recommendation reloaded **unchanged** | `templates/reviewer/review/reviewerRecommendations.tpl` (`disabled=$readOnly`, never assigned) vs `lib/pkp/classes/submission/reviewer/form/ReviewerReviewForm.php` `fetch()` (`reviewIsClosed`) | Upstream (cosmetic): drive the recommendation `disabled` off `$reviewIsClosed` too, so the closed-review form is uniformly disabled |
| 77 | **OJS locale shadows pkp-lib's "For editor only" with "For editor"** — the reviewer's private-comment box and the editor's Read-Review modal both label the editor-only stream **"For editor"** in OJS, because `locale/en/locale.po` redefines `submission.comments.cannotShareWithAuthor` = "For editor" (OJS also carries a legacy `submission.comments.forEditor` = "For editor"), overriding pkp-lib's `lib/pkp/locale/en/reviewer.po` value "For editor only". Both surfaces stay consistent with each other; only the two locale files disagree. Cosmetic wording, but a maintenance smell (duplicated key, less-explicit OJS string). | product-spec reviewer-response verifier (2026-07-03), live-probed: reviewer step-3 form and editor *Review Details* modal both render "For editor", never "For editor only" | `locale/en/locale.po` (`submission.comments.cannotShareWithAuthor` = "For editor") vs `lib/pkp/locale/en/reviewer.po` (= "For editor only"); `lib/pkp/templates/controllers/grid/users/reviewer/readReview.tpl` + `lib/pkp/templates/reviewer/review/step3.tpl` both use the key | Upstream: drop the OJS override so the clearer pkp-lib "For editor only" wins (or reconcile the wording deliberately) |
| 78 | **Free-text review is submittable with empty comments + no attachment despite the client hint that promises otherwise** — on the default (no review form) path the only server-required field is the recommendation (`ReviewerReviewStep3Form` adds a required check for `reviewerRecommendationId` only; `PKPReviewerReviewStep3Form` has no comment/file required check). The step-3 UI hint reads "You must enter a review or upload a file before selecting a recommendation", so a reviewer reasonably expects the minimum to be enforced — but it is client-side only. | product-spec reviewer-response verifier (2026-07-03), live-probed: accepted then submitted a review with both comment boxes blank and no file, recommendation set → wizard reached the completion view, assignment went `RECEIVED` | `classes/submission/reviewer/form/ReviewerReviewStep3Form.php` (recommendation-only required check) + `lib/pkp/classes/submission/reviewer/form/PKPReviewerReviewStep3Form.php` (no comment/file check) | Behaviour call for the maintainer: enforce the comment-or-file minimum server-side, or drop the hint's promise (matches reviewer-response Open question 1) |
| 79 | **The review-form *activate* confirmation over-promises: an in-use form CAN still be deactivated** — UI affordance contradicts behaviour (no data loss). The activate confirm reads "Are you sure you wish to activate this review form? Once it's assigned to a review you will no longer be able to deactivate it." (`manager.reviewForms.confirmActivate`), but `ReviewFormGridHandler::deactivateReviewForm()` guards only on `checkCSRF && isset && getActive()` — **no in-use check** — and `ReviewFormGridCellProvider::getCellActions()` renders the deactivate link whenever `getActive()`, so the Active checkbox is never disabled. The in-use lock (`ReviewFormGridRow` `$canEdit`, rule 6 of the spec) withdraws only Edit/Delete, never the activate/deactivate toggle. Deactivating an in-use form succeeds; it drops from the assignment dropdown / section-default set (`getActiveByAssocId`) while the in-flight `review_assignments.review_form_id` is left intact. | product-spec review-forms author + verifier (2026-07-03), live-probed end-to-end: seeded an active form + an accepted reviewer holding it (In Review = 1); as manager `dbarnes` deactivating it succeeded (checkbox flipped unchecked) with the assignment's attached form preserved. Re-verified via `playwright/tests/review-forms.spec.js` scenario 7 (green). | `lib/pkp/controllers/grid/settings/reviewForms/ReviewFormGridHandler.php` `deactivateReviewForm()` (no in-use guard) + `ReviewFormGridCellProvider.php` `getCellActions()` (link by `getActive()` only) vs the promise in `lib/pkp/locale/en/manager.po` `manager.reviewForms.confirmActivate` | Upstream: either block deactivate when In Review + Completed > 0 (match the message + the Edit/Delete lock), or soften the confirm wording to stop promising a restriction the code doesn't enforce |
| 80 | **The review-round status label shows the *editor* phrasing to the author in the workflow Status card** — UI wording inconsistency, no data loss. `ReviewRound::getStatusKey($isAuthor)` defines author-specific wording for exactly two statuses (REVIEWS_READY, REVIEWS_OVERDUE — e.g. the author variant of overdue reassures "…No action is needed from you right now…"), but `maps/Schema.php` `getPropertyReviewRounds()` builds each round's `status` string as `__($reviewRound->getStatusKey())` — **without** the `$isAuthor` flag — and `WorkflowSubmissionStatus.vue` binds that string verbatim (`body: currentReviewRound.status`). So the author's Status card renders the `editor.*` phrasing; the author-specific text only ever surfaces in the `NOTIFICATION_TYPE_REVIEW_ROUND_STATUS` in-app notification, which *does* pass the flag (`PKPNotificationManager::getNotificationMessage`). | product-spec review-rounds-and-revisions verifier (2026-07-03), live-probed as the author (`atester`) on seeded rounds: REVIEWS_OVERDUE (sub 696) Status card read **"A review is overdue."** and REVIEWS_READY (sub 704) read **"New reviews have been submitted."** — both the editor keys; a CLI probe confirmed `getStatusKey(false)` vs `getStatusKey(true)` DIVERGE for exactly these two statuses (every other status resolves to one shared `editor.*` key). | `lib/pkp/classes/submission/maps/Schema.php` `getPropertyReviewRounds()` (`__(getStatusKey())`, no `$isAuthor`) vs `lib/pkp/classes/notification/PKPNotificationManager.php` `getNotificationMessage()` (passes `$isAuthor`); `lib/pkp/classes/submission/reviewRound/ReviewRound.php` `getStatusKey()`; consumed by `lib/ui-library/src/pages/workflow/components/primary/WorkflowSubmissionStatus.vue` (`body: currentReviewRound.status`) | Upstream (cosmetic): pass the author flag when serializing the round `status` on the author's view (or drop the two unused author-variant keys) |
| 81 | **A double-anonymous reviewer can read the author's real name (and user id) through the submission REST API's `participants` array — reviewer→author anonymity leak (API-only, no UI surface)**: `GET /api/v1/submissions/{id}` returns the author metadata blanked (`authorsString=''`, `authors=[]`) and the reviewer-identity fields blanked — but the SAME response's `participants` array lists **every** assigned user, the author included, by real `fullName` + `id`, with no anonymization. `Schema::getPropertyParticipants()` returns all users `assignedTo` the submission unconditionally, whereas the controller deliberately anonymizes the author metadata and review-assignment identity for reviewers (`PKPSubmissionController::get()` comment: "Anonymize sensitive review assignment data if user is a reviewer or author") and the sibling discussion path (`EditorialTask::anonymizeAuthors()`) DOES hide the author from reviewer participants — so omitting `participants` from that anonymization is internally inconsistent. The endpoint is reviewer-authorized by design (`ROLE_ID_REVIEWER` in the `get()` route roles), and the leak is reachable from the moment of invitation (before the reviewer accepts). `participants` is a full-map-only prop (no `apiSummary`), so it leaks via the single-submission GET, not the reviewer dashboard's `reviewerAssignments` list read. On the double-anonymous default journal (`publicknowledge`, `defaultReviewMode=2`) this defeats reviewer→author anonymity (review-anonymity rule 2). Secondary: the publication object additionally exposes `primaryContactId` (the author-record id) to the reviewer. | product-spec review-anonymity verifier (2026-07-03), live-probed end-to-end: as double-anon reviewer `jjanssen` (review completed) and `agallego` (merely *invited*) on seeded submissions, `GET /submissions/{id}` returned `authorsString=''` yet `participants` contained `{"id":17,"fullName":"Author Tester"}`; the reviewer dashboard `_submissions/reviewerAssignments` read did NOT leak it (uses the reviewAssignment schema map, not the submission map) | `lib/pkp/classes/submission/maps/Schema.php` `getPropertyParticipants()` (no anonymize gate) vs `mapByProperties()` / `getPropertyReviewAssignments()` (which DO anonymize) and `lib/pkp/classes/editorialTask/EditorialTask.php` `anonymizeAuthors()`; reviewer access via `lib/pkp/api/v1/submissions/PKPSubmissionController.php` `get()` route roles | Upstream: anonymize / omit the author from `participants` when the current user is a non-open reviewer (mirror the review-assignment + discussion anonymization), or strip `participants` from reviewer-context submission reads |
| 82 | **The copyediting "Assign a copyeditor" status prompt keys off a stage-4 discussion, not the actual Copyeditor assignment — so it goes stale.** `PKPEditingProductionStatusNotificationManager::updateNotification()` (EDITING branch) decides between ASSIGN_COPYEDITOR and AWAITING_COPYEDITS purely on whether *any* stage-4 `EditorialTask` (discussion) exists (code comment: "If a copyeditor is assigned i.e. there is a copyediting discussion"), never checking for a Copyeditor **stage assignment**. So an editor who assigns a copyeditor **silently** (no notify / no discussion opened) keeps seeing the "Assign a copyeditor using the Assign link in the Participants list." card even though a copyeditor is already a participant — the prompt tells them to do something already done. Sibling of row 8 (the skip-review omission): both stem from the editing-status prompt being decision-seeded + discussion-keyed rather than tracking the real Copyeditor-assignment state. UI-affordance inconsistency, no data loss. | product-spec copyediting-stage verifier (2026-07-03), live-probed end-to-end: seeded accept→copyediting sub 420 with `mfritz` assigned as Copyeditor and NO discussion — DB `notifications` still held ASSIGN_COPYEDITOR (type 0x1000023) for both assigned section editors, and the editor's workflow pane rendered the "Assign a copyeditor" card while "Maria Fritz — Copyeditor" sat in the Participants list | `lib/pkp/classes/notification/managerDelegate/PKPEditingProductionStatusNotificationManager.php` `updateNotification()` (EDITING branch keys on `EditorialTask::withStageId(WORKFLOW_STAGE_ID_EDITING)->first()`, not a Copyeditor `StageAssignment`) | Upstream product call: key the prompt off the Copyeditor stage-assignment (or clear it on copyeditor assignment), or accept the discussion proxy and reword the card |
| 84 | **Applying a task/discussion template pre-fills every field except participants — the promoted participants the server returns are silently dropped by the form** (no data loss; a convenience gap). The `fromTemplate` endpoint (`EditorialTaskController::fromTemplate()`) resolves the template's user groups against the submission's stage assignments and returns the matching users as `participants`, but the client prefill `useDiscussionManagerForm.setValuesFromTemplate()` copies only `title`, `type`, `description` and `dateDue` into the form — it never reads `participants`, and it additionally *clears* the responsible assignee (`setValue('taskInfoAssignee', null)`). So after applying a template the editor must re-pick every participant by hand even though the template names their user groups and the API already computed them. Internally inconsistent: the server does the participant-promotion work the client throws away. | product-spec tasks-discussions test author (2026-07-04), confirmed two ways: (a) code — `setValuesFromTemplate()` sets title/taskInfoAdd/dateDue/description + nulls taskInfoAssignee, no participants write; (b) live browser probe — applying a template whose group a section editor holds left that editor unchecked in the participant list | client: `lib/ui-library/src/managers/DiscussionManager/useDiscussionManagerForm.js` `setValuesFromTemplate()` (no `participants` write) vs server `lib/pkp/api/v1/submissions/tasks/EditorialTaskController.php` `fromTemplate()` (returns promoted participants) | Upstream: have `setValuesFromTemplate()` copy the returned `participants` (and keep/prefill the responsible assignee) so the template's user-group promotion actually reaches the form |
| 83 | **The production "create galleys / Awaiting Galleys" status prompt only clears on a galley created through the galley grid — a galley added off the grid path leaves it stale** (LOW reachability, no data loss). `PKPEditingProductionStatusNotificationManager` clears ASSIGN_PRODUCTIONUSER/AWAITING_REPRESENTATIONS the moment a representation exists on the latest publication, but that recompute only runs from a fixed caller set — the SEND_TO_PRODUCTION decision seed, `deleteParticipant`, `sendNotification`, discussion create/close (`EditorialTaskController`), and `ArticleGalleyGridHandler::addGalley/deleteGalley`. A galley created via `Repo::galley()->add()` off the grid (a native-XML import into an already-in-production submission, a plugin, a scenario seed) never triggers the recompute, so the editor keeps seeing "Assign a user to create galleys" / "Awaiting Galleys." even though a galley already exists. The normal UI creates galleys through the grid (which recomputes → clears), so this only bites when a galley reaches an already-prompt-seeded production submission off the grid path. Sibling of rows 9 & 82 (the same decision-seeded, event-recomputed prompt tracking a proxy rather than live state). | product-spec production-stage verifier (2026-07-03), live-probed DB: scenario-seeded production sub 623 has a `publication_galleys` row (`is_approved=0`, remote URL) on its latest publication, yet `notifications` still holds ASSIGN_PRODUCTIONUSER (type 16777254) for all three assigned editors (dbarnes/dbuskins/sberardo); the retained `production-stage.spec.js` scenario 4 shows the *grid* path DOES clear it | recompute trigger set omits non-grid galley creation — `lib/pkp/classes/notification/managerDelegate/PKPEditingProductionStatusNotificationManager.php` `updateNotification()` (PRODUCTION branch clears on `getByPublicationId()` representations) vs the caller set in `controllers/grid/articleGalleys/ArticleGalleyGridHandler.php` only; `Repo::galley()->add()` has no recompute | Upstream (low priority): recompute the production status types after any representation add/delete (e.g. on a Repo/galley event), or accept the grid-only recompute as intended and document it |
| 85 | **Editing a submission file's metadata is logged against the file but not the submission — the submission Activity Log misses file edits.** `SubmissionFile\Repository::edit()` persists the file-assoc event (`SUBMISSION_LOG_FILE_EDIT`, or `…_REVISION_UPLOAD` when the edit replaces the file) via `Repo::eventLog()->add()`, then **builds a second, submission-assoc event object but never calls `->add()` on it** (the `newDataObject(...)` return is discarded), and that dropped object also carries an `isTranslate` typo instead of `isTranslated`. Both the upload path (`add()`) and the delete path (`delete()`) DO persist their submission-assoc entries, so the file's edit is the lone file event missing from the submission-level history — it shows only in that file's Information Center. Internally inconsistent; low severity, no data loss. | product-spec submission-files author (2026-07-04), code-traced: `edit()` calls `Repo::eventLog()->add($logEntry)` once (file assoc), then a bare `Repo::eventLog()->newDataObject([... 'assocType' => ASSOC_TYPE_SUBMISSION ...])` with no `add()`; contrast `add()`/`delete()` which each `add()` both the file- and submission-assoc entries. **Confirmed live by the submission-files verifier (2026-07-04)**: renamed a file's metadata via `PUT …/files/{id}?stageId=1` on sub 478, then queried `event_log` — one file-assoc `FILE_EDIT` row (assoc_type 515), zero submission-assoc edit rows (assoc_type 1048585), while the upload's submission-assoc `fileRevised` row was present | `lib/pkp/classes/submissionFile/Repository.php` `edit()` (the second `newDataObject()` result is never added; `isTranslate` typo) vs `add()`/`delete()` (both persist the submission-assoc entry) | Upstream: `Repo::eventLog()->add()` the submission-assoc edit entry (and fix `isTranslate`→`isTranslated`), or drop the dead object if the submission-level edit event is intentionally omitted |
| 86 | **Activity Log header button and the info-center backend gate disagree for unassigned managers** (LOW, read-only, no data exposure). The workflow header renders the Activity Log button only when `canAccessEditorialHistory` holds — an **active-stage** MANAGER/SITE_ADMIN/SUB_EDITOR *stage assignment* (`currentUserAssignedRoles`) — so an unassigned global manager/site-admin browsing the submission sees **no** button. But `SubmissionInformationCenterHandler::authorize()`'s else-branch admits any global MANAGER/SITE_ADMIN, and the event-log grid's accessible-stages check is role-level, so that same user can open the "Activity Log & Notes" modal (and its History grid) by direct component navigation. Hidden-but-permitted; a manager already has journal-wide oversight so nothing new is exposed, but the affordance and the gate diverge. Same family as row 53 (publish authority UI/API disagreement). | product-spec editorial-activity-log verifier (2026-07-04): live-probed the button matrix (dbarnes present + grid renders; atester/mfritz/jjanssen absent, author + assistant `fetchGrid` refused "The current role does not have access to this operation."); the unassigned-manager admit path code-traced | `lib/ui-library/src/pages/workflow/composables/useWorkflowPermissions.js` `canAccessEditorialHistory` (active-stage `currentUserAssignedRoles` ∩ [MANAGER,SITE_ADMIN,SUB_EDITOR]) vs `lib/pkp/controllers/informationCenter/SubmissionInformationCenterHandler.php` `authorize()` else-branch (global MANAGER/SITE_ADMIN) + `SubmissionEventLogGridHandler` role gate / `Repository::getAccessibleWorkflowStages()` | Upstream (low priority): align the button affordance with the backend gate — show the button to any manager with oversight, or restrict the backend to active-stage assignments |
| 87 | **An assistant opening a file's Information Center is offered a History tab whose grid then denies them** (LOW, broken affordance, no data loss). `FileInformationCenterHandler` adds ROLE_ID_ASSISTANT to `viewInformationCenter`/`viewHistory` and the Vue `fileSeeNotes` path never sets `removeHistoryTab`, so a copyeditor/layout-editor/proofreader is shown the **History** tab in the file Information Center; but its grid `SubmissionFileEventLogGridHandler` inherits `SubmissionEventLogGridHandler`'s `addRoleAssignment([MANAGER,SITE_ADMIN,SUB_EDITOR], ['fetchGrid',…])` with **no** ASSISTANT, so the tab loads *"The current role does not have access to this operation."* Internally inconsistent: `SubmissionFileEventLogGridHandler::getFilterForm()` explicitly intersects ASSISTANT, yet the grid's fetch gate excludes it. The safe direction (history is withheld) so no leak — but a tab that always errors is a bad affordance. | product-spec editorial-activity-log verifier (2026-07-04), live-probed on port 8000: copyeditor `mfritz` GET `grid.eventLog.SubmissionFileEventLogGridHandler::fetchGrid` → `{"status":false,"content":"The current role does not have access to this operation."}`; dbarnes on the same file → grid renders | `lib/pkp/controllers/informationCenter/FileInformationCenterHandler.php` (`__construct` ASSISTANT; `viewInformationCenter` `removeHistoryTab` from request var only) + `lib/ui-library/src/managers/FileManager/useFileManagerActions.js` `fileSeeNotes` (no `removeHistoryTab`) vs `lib/pkp/controllers/grid/eventLog/SubmissionEventLogGridHandler.php` `__construct` role gate (inherited by `SubmissionFileEventLogGridHandler`), whose own `getFilterForm()` intersects ASSISTANT | Upstream: add ASSISTANT to the file event-log grid's `fetchGrid` role assignment (match `getFilterForm`), or set `removeHistoryTab` for assistant openers so the tab isn't offered |
| 88 | **⚠ SECURITY — unauthenticated disclosure of any non-public journal (context) library document via the legacy download *page* op.** The bare page route `GET /{journal}/libraryFiles/downloadLibraryFile?libraryFileId={id}` serves **any context (journal-wide, `submission_id` NULL) library document — including non-public ones — to anyone with no login**, by guessable integer `libraryFileId`. Root cause is a triple gap: (1) `lib/pkp/pages/libraryFiles/index.php` instantiates `new LibraryFileHandler()` (no calling handler) which declares **no `addRoleAssignment` and no authorization policy**; (2) `PKPHandler::authorize()` applies a **blacklist** for page routers (`setDecisionIfNoPolicyApplies(AUTHORIZATION_PERMIT)`) and only adds `UserRolesRequiredPolicy` when a user is already present, so `authorize()` returns `true` for everyone incl. logged-out; (3) in `LibraryFileHandler::downloadLibraryFile()` the context-file branch (`$libraryFile->getSubmissionId()` empty) sets `$allowedAccess = true` **unconditionally** and streams the bytes. The UI-linked/live download path is the *component* route `api.file.FileApiHandler::downloadLibraryFile`, which IS gated (6 roles + `ContextAccessPolicy` requiring context+login) — so this page op is a **latent parallel route nothing in the UI references**, but it is fully reachable by hand-crafted URL. Related: the handler's own refusal branches emit `header('HTTP/1.0 403 Forbidden')` which the PHP dev server does **not** translate — those responses come back **HTTP 200** with a `403 Forbidden<br>` body (so a status-code check would not even catch a refusal). | **Confirmed live on port 8000 (ojs_test), 2026-07-04.** Non-public context file `library_files` id=1 (context 26 `j-dla0jifjg`, `public_access=0`): `GET …/j-dla0jifjg/libraryFiles/downloadLibraryFile?libraryFileId=1` → **HTTP 200, Content-Type application/pdf, Content-Length 14572, Content-Disposition attachment**, full valid PDF streamed — both **(a) with no cookies** and **(b) as `atester` (a plain author)**, while that same `atester` session is correctly **401'd** by `GET …/api/v1/_library`. On `publicknowledge` the op first **302-redirects to `…/en/…`** and then runs the handler (the locale hop precedes, does not gate). `downloadPublic` on a non-public file confirmed the 403→200 quirk: `GET …/libraryFiles/downloadPublic/1` → HTTP 200 body `403 Forbidden<br>`. | `lib/pkp/pages/libraryFiles/LibraryFileHandler.php::downloadLibraryFile()` (context branch `$allowedAccess = true`) + `pages/libraryFiles/index.php` (`new LibraryFileHandler()` — no policy/role) + `lib/pkp/classes/handler/PKPHandler.php::authorize()` (page-router `AUTHORIZATION_PERMIT` default; `UserRolesRequiredPolicy` only when `$user instanceof User`). Contrast the gated `lib/pkp/controllers/api/file/FileApiHandler.php` (`__construct` 6 roles; `authorize()` adds `ContextAccessPolicy` for `libraryFileId`). | **Upstream (security):** add an authorization policy + role gate to the `libraryFiles/downloadLibraryFile` (and `downloadPublic`) page op mirroring the component route (`ContextAccessPolicy` + editorial roles, and for `downloadPublic` a `publicAccess` check that returns a *real* 403 via `Response`/`http_response_code`), or delete the page op entirely since no live UI references it. Also replace the legacy `header('HTTP/1.0 403 …')` refusals with a proper 403 status. Until fixed, every journal's non-public library documents are world-readable by fileId. |
| 89 | **Library-file "Description" is rendered with a required asterisk but never validated** — a library document (context OR submission) saves with an empty description (LOW, cosmetic UI-affordance-vs-behaviour mismatch, no data loss; same family as rows 76/79). All four form templates render `{fbvFormSection title="common.description" required=true}`, yet `LibraryFileForm::__construct()` registers validators only for `libraryFileName` (`FormValidatorLocale`) and `fileType` (`FormValidatorCustom`) — none for `description`, though `readInputData()` does read it. The required marker overstates the constraint. | Code-traced 2026-07-04 (`LibraryFileForm` check list vs the four `newFileForm.tpl`/`editFileForm.tpl` under `settings/library/form` and `submissionDocuments/form`); the spec author additionally live-confirmed a doc saves with an empty description. | `lib/pkp/controllers/grid/files/form/LibraryFileForm.php` `__construct()` (no `description` check) / `readInputData()` (reads `description`); the four `newFileForm.tpl` + `editFileForm.tpl` templates (`required=true` on the description section) | Upstream (low): either drop the required marker from the templates or add a `FormValidatorLocale`/`FormValidator` on `description`. |
| 90 | **A refused contributor-role delete surfaces a generic network-error modal, not the specific reason** (LOW, UI-affordance-vs-behaviour, no data loss; same family as rows 76/79/89). `ContributorRoleController::delete()` refuses with HTTP **406** and a specific, translated body — `manager.contributorRoles.error.delete.inUse` (role still in use by a contributor) or `api.contributorRole.400.errorDeletingAuthorRole` (last AUTHOR role) — but the Vue `roleDelete()` runs `useFetch` **without** `expectValidationError`, so any thrown non-2xx lands in `useFetch`'s default `modalStore.openDialogNetworkError(e)` (and 406 is not in its `[400,422]` validation branch regardless). The user sees a generic "network error" dialog; the friendly, translated refusal the backend prepared is never displayed — after typing the exact identifier to confirm, the outcome the confirm dialog implies is hidden. | Contributors verifier (2026-07-04), code-traced the 406 → `openDialogNetworkError` path end-to-end; the contributors test author had earlier hit the generic modal live when deleting an in-use / last-Author role | `lib/pkp/api/v1/contributorRoles/ContributorRoleController.php` `delete()` (406 + `error` body) vs `lib/ui-library/src/managers/ContributorRoleManager/contributorRoleManagerStore.js` `roleDelete()` (`useFetch`, no `expectValidationError`) + `lib/ui-library/src/composables/useFetch.js` (`openDialogNetworkError` default on any thrown status; `expectValidationError` only handles 400/422) | Upstream ui-library: surface the response `error` body on a role-delete refusal (add `expectValidationError` + a 406 branch, or an `onError` that reads the message), so the in-use / last-Author refusal shows its real reason |
| 91 | **The single-citation API (`/citations/{id}`) enforces neither submission-assignment nor the published-lock — any journal author/editor can tamper with any submission's references** (authorization gap / cross-submission data integrity; API-only, no UI path). `PKPCitationController::authorize()` adds only `UserRolesRequiredPolicy` + `ContextRequiredPolicy` + the role policy (Manager\|SubEditor\|Assistant\|Author) — no `SubmissionAccessPolicy` and no `PublicationWritePolicy`. So get/getMany/edit/delete/reprocess of a citation by id are reachable by any such journal user for **any** submission, regardless of assignment or publish state. This diverges from the metadata form, contributors and title/abstract, which all carry `PublicationWritePolicy` (the `canEditPublication` published-lock). **Only the single-citation routes are the gap:** the publication-level bulk routes (import/delete-all/reprocess-all on `PKPSubmissionController`) carry `SubmissionAccessPolicy` (assignment enforced) **and** enforce the published-lock through an **inline `canEditPublication()` guard** that returns HTTP 403 when locked — they lack only the *declarative* `PublicationWritePolicy`, not the lock itself. This is the **second** authorization hole surfaced by the product-spec campaign (after the library-file unauthenticated disclosure, row 88). | publication-metadata-references spec author, live-confirmed 2026-07-04: `atester` (a plain author with **no** role on the target submission) `GET` + `PUT /citations/{id}` → HTTP 200 and the edit persisted ("HACKED BY ATESTER"); a manager also edited citations on a **published** publication (200). **Verifier re-confirmed 2026-07-04** on the running server: (a) unassigned `atester` single `GET`+`PUT /citations/{id}` → 200 persisted, but bulk `deleteCitationsByPublicationId` on that submission → **401**; (b) an *assigned* author on a **published** submission → bulk `importAdditionalCitations` **403** ("You are not allowed to edit this publication") yet single-citation `PUT` → **200** — isolating the hole to the single-citation routes. Code-traced `authorize()` (no submission/publication policy) vs `PKPSubmissionController` bulk methods (`$requiresSubmissionAccess` + inline `canEditPublication()` 403 guard). | `lib/pkp/api/v1/citations/PKPCitationController.php` `authorize()` / `getRouteGroupMiddleware()`; contrast `lib/pkp/api/v1/submissions/PKPSubmissionController.php` `$requiresSubmissionAccess` + the inline `canEditPublication()` checks in `importAdditionalCitations()`/`deleteCitationsByPublicationId()`/`reprocessCitationsByPublicationId()` | Upstream: add `SubmissionAccessPolicy` (all methods) + `PublicationWritePolicy` (edit/delete/reprocess) to `PKPCitationController` — that closes the real hole. Optionally also add the bulk ops to `PKPSubmissionController::$requiresPublicationWriteAccess` and drop their inline guards, so citation editing honours the same assignment + published-lock *declaratively* as the rest of the publication (the bulk routes already enforce the lock inline, so this is consistency cleanup, not a security fix). |
| 92 | **A DOI extracted from a raw reference on the *initial* import is silently discarded** (LOW, derived-data loss; inconsistency between two import paths). When *References Metadata Lookup* is off, `Repository::importCitations()` extracts a DOI from each raw line via `Doi::extractFromString()` and `setData('doi', $doi)` on the citation — but **after** `$this->dao->insert($citation)` and **without** any follow-up save, so the DOI is never persisted. The sibling `importAdditionalCitations()` (the "Add references" box path) sets the DOI the same way but **does** call `Repo::citation()->edit($citation, [])` afterwards, so it persists. | publication-metadata-references spec author, live-confirmed 2026-07-04: saving a publication's `citationsRaw` with a DOI-bearing line (`https://doi.org/10.1234/abcd`) produced a citation row with `doi = null`. | `lib/pkp/classes/citation/Repository.php` `importCitations()` (the `elseif (!$citationsMetadataLookup)` branch — `setData('doi', …)` with no persist) vs `importAdditionalCitations()` (same branch followed by `Repo::citation()->edit()`) | Upstream (low): persist the extracted DOI in `importCitations()` (mirror the `edit()` call in `importAdditionalCitations()`), or drop the dead extraction. |
| 93 | **[FIXED 2026-07-04 — media-files product-spec verifier] TEST-INFRA (not product): the media-files scenario seeder referenced `VariantGroup::MAX_GROUP_SIZE`, a constant that no longer exists — so any submission scenario that seeded *grouped* media files threw a fatal.** `PublicationsProcessor::seedMediaFiles()` (the wave-7 media seed path, commit `fd3c84a03f`) validated group sizes with `if ($count > \PKP\submissionFile\VariantGroup::MAX_GROUP_SIZE)`, but the current `lib/pkp/classes/submissionFile/VariantGroup.php` (after the #12794 batch/manual-linking refactor, commit `51065dcd4f`) defines **no constants at all** — `MAX_GROUP_SIZE` was undefined and referenced nowhere in product code (grep: only the lines in `PublicationsProcessor.php`). Ungrouped media seeding was unaffected. **Fix applied:** `PublicationsProcessor` now declares its **own** `private const MAX_GROUP_SIZE = 2` (matching the web + high-res pair cap that `VariantGroup::link()` enforces in product code) and the group-size pre-check reads `self::MAX_GROUP_SIZE`; grouped media seeding now works, and the retained `playwright/tests/media-files.spec.js` scenario 7 seeds a linked web/high-res pair directly via the `group` label (verified green). This is the same constant the wave-7 ledger row 59(c) described as a "backend-only defense"; it has since been removed from the model, leaving the test seeder with a dangling reference. **No product impact** — the web↔high-res link semantics cap a group at 2 implicitly (each `VariantGroup::link()` builds a fresh 2-member group and dissolves any prior ones), so the product never needed the constant. | media-files spec author, live 2026-07-04 (port 8000, `ojs_test`): `POST /api/v1/_test/scenarios/submission` with two `mediaFiles[]` sharing a `group` label → HTTP 500 `"Undefined constant PKP\\submissionFile\\VariantGroup::MAX_GROUP_SIZE"` at `PublicationsProcessor.php:358`; the same spec with the `group` labels removed seeds both files successfully (submission 2, media files 3+4). | `lib/pkp/classes/testing/scenario/Processor/PublicationsProcessor.php:340/358/361/362` (references `VariantGroup::MAX_GROUP_SIZE`) vs `lib/pkp/classes/submissionFile/VariantGroup.php` (no `const`); grep confirms no product-code reference | Test-infra fix (DONE): the `seedMediaFiles()` pre-check now reads a `PublicationsProcessor`-local `const MAX_GROUP_SIZE = 2` instead of the removed model constant (matching the web+high-res pair cap); media e2e specs seed groups via the `group` label directly. Alternative considered (not taken): drop the pre-check entirely and rely on `VariantGroup::link()`'s pairwise semantics. |
| 94 | **The Issue-tab "Assign To Future Issue" options are offered to section editors/assistants, but the issue picker that fulfils them (the unpublished-issue list) is manager/site-admin-only → the picker 403s and cannot populate for a non-manager editor — AND the same 403 fires on LOAD for a scheduled article, blanking the tab** (MEDIUM, broken affordance / cross-surface role inconsistency; TWO facets). On the Publication → Issue ("Publication Settings") tab, the assignment radio's options come from `GET /issues/assignmentOptions` (`IssueAssignment::getAvailableAssignmentOption()`), which filters **only by whether an issue exists in each publish-state — not by role**, so it offers *"Assign To Future Issue and Publish Immediately"* and *"…and Schedule Only"* (isPublished=0) to **any** editorial role. But the picker for those options fetches from `GET /issues?isPublished=0`, and `IssueController::getMany()` returns **403 `api.submissions.403.unpublishedIssues`** to anyone who is not a **manager or site admin** (`$isAdmin = hasRole(MANAGER) || hasRole(SITE_ADMIN)` — SUB_EDITOR/ASSISTANT excluded); `useFetch` surfaces that 403 as a blocking **"Error"** modal (`modalStore.openDialogNetworkError`). **Facet (a) — offered-but-unfulfillable:** a **section editor** who *picks* a future option gets the error modal + an **empty, un-fillable required Issue picker** and cannot complete the assignment through the UI (only Current/Back published issues and Don't-Assign work for them). **Facet (b) — on-load blanking of a scheduled article (worse):** because `Repository::getIssueAssignmentStatus()` maps any **SCHEDULED/READY_TO_SCHEDULE** publication (and any article placed in / staged for an unpublished/future issue) to a future option, the composable **auto-fetches** `/issues?isPublished=0` on load to show the current placement — so a section editor merely *opening* the Issue tab on an already-scheduled article hits the 403 **on load**: the "Error" modal pops immediately and the target-issue picker never fills, so they can't even see which future issue the article is scheduled into (the radio itself still renders, with the future option pre-selected — it's the specific issue that's hidden and un-re-selectable). The assignment-options / assignment-status surfaces don't role-restrict the same way, so the radio and the picker disagree. | publication-issue-assignment spec author, live 2026-07-04 (port 8000, publicknowledge): as `dbuskins` (sectionEditor, ROLE_ID_SUB_EDITOR) `GET /issues/assignmentOptions` → **200** returning all four options incl. both isPublished=0 future options; `GET /issues?isPublished=0` → **403** `{"error":"You do not have permission to view unpublished issues."}`; `GET /issues?isPublished=1` → **200**. As a manager (`dbarnes`) all three → 200. **Verifier re-confirmed 2026-07-04 (API + browser)** on the running server: facet (a) as above; **facet (b) reproduced in a real browser** — opening the **Publication Settings** tab on **scheduled submission 583** as dbuskins fired, on load, `GET …/583/publications/583/issueAssignmentStatus` → 200 (`{"assignmentType":3,"status":7}`) then `GET /issues?isPublished=0&count=100` → **403**, popping the **"Error" / "You do not have permission to view unpublished issues."** modal with the required Issue picker rendered **empty** (screenshot); the radio showed *"Assign To Future Issue and Schedule Only"* pre-selected. | radio options `classes/issue/enums/IssueAssignment.php` `getAvailableAssignmentOption()` (no role filter) + `api/v1/issues/IssueController.php` `getAssignmentOptions()`; picker source `IssueController::getMany()` (lines ~206–214: `$isAdmin` = MANAGER/SITE_ADMIN only → 403 on `isPublished` false for non-admins); facet-(b) trigger `classes/publication/Repository.php` `getIssueAssignmentStatus()` (SCHEDULED→`FUTURE_ISSUE_SCHEDULED`; unpublished issue→`FUTURE_*`) + composable `lib/ui-library/.../useWorkflowPublicationFormIssue.js` (`currentAssignmentOption` watcher `await fetchIssues()` when `isPublished !== null`; empty `issueOptions` on 403); 403→modal via `lib/ui-library/src/composables/useFetch.js` → `modalStore.openDialogNetworkError()` | Upstream (triage): either role-filter the assignment options **and skip the on-load unpublished-issue fetch** for non-managers (so neither picking nor viewing hits the 403), or widen the unpublished-issue list to assigned section editors/assistants (they already reach the Issue tab). Note facet (b) means the as-built also blocks a section editor from *inspecting* a scheduled placement, not just creating one. Decide whether touching a future-issue placement is intended to be manager/admin-only; if so the radio/composable should reflect it. |
| 95 | **The `PublishSubmissions` "auto-publish scheduled submissions" task is wired into NO scheduler — date-based auto-publish is dead in OJS** (LOW / intent-call, no data loss; a shipped+named scheduled task that never runs). The task class (`PKP\task\PublishSubmissions`, surfaced by the locale key `admin.scheduledTask.publishSubmissions`) would load every SCHEDULED submission whose current publication's `datePublished` has arrived and publish it — but it is registered in **neither** `APP\scheduler\Scheduler::registerSchedules()` (OJS) **nor** `PKP\scheduledTask\PKPScheduler::registerSchedules()` (pkp-lib), and there is **no** legacy `scheduledTasks.xml` anywhere in the tree, so nothing ever invokes it. It is doubly inert: even if run manually it would publish nothing, because OJS scheduled articles carry **no** future `datePublished` (`setStatusOnPublish()` stamps the date only when the status becomes PUBLISHED, never on SCHEDULED) and the task guards on `if ($datePublished && …)`. In OJS a Scheduled article instead goes live when its **issue** is published (`IssueGridHandler::publishIssue()`, publication-versioning rule 21). Reads as an OPS/continuous-publishing feature OJS deliberately omits; flagged for the maintainer's intent call, **not** asserted as a bug. | publication-publish-flow verifier, 2026-07-04: (a) grep of the whole PHP tree for `PublishSubmissions` returns only the class file's own docblock — zero registration/invocation sites; (b) `find` for `scheduledTasks.xml` → none; (c) live DB (`ojs_test`, running server): **0 of 19** `status=5` (SCHEDULED) publications carry a `date_published`, while all 74 `status=3` (PUBLISHED) do (and 18 `status=1`/QUEUED retain a date — unpublished-and-kept, corroborating publish-flow rule 14). | `lib/pkp/classes/task/PublishSubmissions.php` `executeActions()` (filters `STATUS_SCHEDULED`, guards `if ($datePublished …)`) vs `classes/scheduler/Scheduler.php` + `lib/pkp/classes/scheduledTask/PKPScheduler.php` `registerSchedules()` (task absent from both); no-date-on-schedule at `classes/publication/Repository.php` `setStatusOnPublish()` | Intent call for the maintainer (publish-flow OQ1): either register `PublishSubmissions` in the OJS scheduler **and** add a UI that stamps a future `datePublished` on scheduled articles (nothing does today, so registration alone would still publish nothing), or confirm it is intentionally OPS-only and treat it as an OJS dead-code candidate (issue-publish already covers the use case). |
| 96 | **The publication "Summary of Changes" amendment notice and the per-version "Update Type" are captured + stored editor-side but rendered on NO reader page in 3.6 — although the field copy explicitly promises a public display** (LOW / round-2 gap, UI-copy-contradicts-behaviour, no data loss). The Publication-tab field description `publication.summaryOfChanges.description` reads *"This will appear publicly as the version amendment notice. Ensure it accurately reflects the changes made in this version before publishing."* and the `publication.json` schema documents `summaryOfChanges` as a *"Public-facing amendment notice describing what changed in this version"* — but **no** front-end template (OJS `templates/frontend/` or `lib/pkp/templates/frontend/`) renders either the notice or the update type: `article_details.tpl` renders neither and an exhaustive frontend-template grep for `summaryOfChanges`/`updateType` is empty. The **only** place `update_type` leaves the system is the **Crossref crossmark** deposit (`<update type="…">`, gated on the Crossmark plugin). So an editor recording a correction/retraction notice sees the "will appear publicly" copy, but a reader is shown nothing. | publication-amendments spec verifier, live 2026-07-04 (port 8000, publicknowledge): a manager (dbarnes) set `updateType=correction` + a distinctive `summaryOfChanges` notice on a **published** article (sub 321 → HTTP 200, both read back persisted); the **anonymous** reader article page `GET …/en/article/view/321` (36 KB, article rendered — the title appears 5×) contained **zero** occurrences of the notice text, of "Update Type", of "Amendment Notice", or of "correction". Code-confirmed: `grep -rniE 'summaryOfChanges\|updateType' templates/frontend/ lib/pkp/templates/frontend/` → empty; `ArticleCrossrefXmlFilter.php:736` (`$updateNode->setAttribute('type', $publication->getData('updateType'))`) is the sole consumer. | Editor-side capture (schema + two capture forms) shipped in 3.6 with the promissory field copy, but the reader-side display was not implemented — almost certainly a not-yet-built round-2 surface rather than a logic bug (the e2e plan records the same: "NOT yet implemented in the front end … add reader rows when it lands"). | Round-2: implement the reader-side amendment-notice / update-type display, **or** soften the `publication.summaryOfChanges.description` copy that promises a public display until it exists. The retained `playwright/tests/publication-amendments.spec.js` scenario 7 asserts the **absence** on the live reader page; flip it to a positive assertion when the reader surface lands. |
| 97 | **The Publication → Data tab's "Data Citations" manager shows its Add / Order / per-row Edit + Delete controls unconditionally — a hard-locked author on a published version is offered controls the server then refuses** (LOW, affordance-vs-behaviour, **no data loss**; same shape as the galleys manager's row-22b ungated-control divergence — **but milder**: in galleys 22b the ignored lock lets the write *persist* (a real data change), whereas here the backend correctly refuses the write (**401**), so this is a pure UI-affordance divergence with no data effect). Unlike the sibling **Data Availability Statement** form (which receives `canEdit: permissions.canEditPublication` and goes read-only when locked) and unlike the **References** tab's citation manager (which `v-show`s its controls on `canEditPublication`), the `DataCitationManager` store/config never receive `canEditPublication` — `getTopItems()` (Add + Order) and `getItemActions()` (Edit + Delete) expose every control regardless of publish state. The **backend** is correctly gated (`PKPDataCitationController::authorize()` applies `PublicationWritePolicy` on add/edit/delete/saveOrder), so the write is refused — but the UI presents the affordance anyway. | data-availability-citations spec author, live 2026-07-04 (port 8000, scratch journal `j-dclock`, dataAvailability+dataCitations=request, published submission 323 by pure author `pauthor`): on Publication → **Data** the red "This version has been published and can not be edited" banner showed and the statement rich-text field was read-only, yet "**Add a new Data Citation**" + "**Order**" rendered **enabled** (zoom-confirmed) and the Add side-modal opened and was fully interactive; filling Title + Relationship type and clicking **Save** fired `POST …/publications/333/dataCitations` → **HTTP 401** (write refused). A manager is warn-not-locked (`canEditPublication` true via `_canUserAccessUnassignedSubmissions`), so their controls work. | `lib/ui-library/src/managers/DataCitationManager/dataCitationManagerStore.js` + `useDataCitationManagerConfig.js` (`getTopItems`/`getItemActions`) + `DataCitationManagerCellActions.vue` — none reference `canEditPublication`; contrast `workflowConfigAuthorOJS.js`/`workflowConfigEditorialOJS.js` `dataAvailabilityAndCitation` (passes `canEdit` only to the `dataAvailability` WorkflowPublicationForm) and `lib/pkp/api/v1/dataCitations/PKPDataCitationController.php` `authorize()` (`PublicationWritePolicy` on writes) | Upstream ui-library: pass `canEditPublication` into `DataCitationManager` and gate the top controls + row actions on it, matching the statement form and the References `CitationManager` (`CitationManagerCellActions.vue` `v-show="citationStore.canEditPublication"`). |
| 98 | **Data citations captured on the Publication → Data tab are rendered on NO reader page in 3.6, though the manager's own copy promises they "appear alongside other references in the publication"** (LOW, round-2 gap, UI-copy-contradicts-behaviour, no data loss; same shape as row 96 amendments). The Data Citations manager description (`submission.dataCitations.description`) reads *"This table allows users to add formal data citations, ensuring datasets are properly credited and appear alongside other references in the publication,"* and the `dataCitation.json` schema documents the entity as citations for datasets used — but **no** front-end template renders them: `article_details.tpl` renders the `dataAvailability` **statement** (a `<section id="data-availability-statement">`) but nothing data-citation, and a frontend-template grep for `dataCitation` matches only the submission **wizard's** intake/review templates, never a reader page. So the manager is an editor/author capture surface with no public output. | data-availability-citations spec author, 2026-07-04: `grep -rn 'dataCitation' templates/frontend/ lib/pkp/templates/frontend/ plugins/themes/` → only `templates/submission/wizard.tpl` + `review-details.tpl` (both intake); `templates/frontend/objects/article_details.tpl:408` renders `dataAvailability` only. Editor-side capture (schema + edit form + CRUD API + manager) is fully wired and live-probed, but the reader display is absent. | reader template `templates/frontend/objects/article_details.tpl` (renders `dataAvailability`, no `dataCitations`); copy `submission.dataCitations.description`; entity `lib/pkp/schemas/dataCitation.json` / `lib/pkp/classes/dataCitation/` (captured but never read on a public page) | Round-2: implement the reader-side data-citation display (e.g. alongside References on the article page, as the copy promises), **or** soften `submission.dataCitations.description` until it exists. Confirm whether data citations are instead intended only for metadata export (DOI-deposit/OAI), in which case wire that. |
| 99 | **Publishing *any* issue makes it the journal's *current* issue unconditionally — so publishing an older / back-dated issue after a newer one silently moves "current" backwards** (LOW / intent-call, no data loss, trivially reversible via *Set Current Issue*; a could-surprise-a-manager edge, **not** asserted as a bug). `IssueGridHandler::publishIssue()` calls `Repo::issue()->updateCurrent($contextId, $issue)` for the just-published issue with **no** guard on publish order or date — the call sits **outside** the `if (!$wasPublished)` block, so it runs on every publish. In the normal newest-first workflow this is invisible and desirable (the issue you just published *is* the newest). But a manager who publishes a late-arriving or digitised **back issue** (e.g. an older volume added for the archive) after a newer issue is already current will find the journal's **current-issue** pointer — and the reader home / *Current Issue* page it drives (`journal-homepage` / `issue-archive-toc`) — silently re-pointed to the older issue, until they manually *Set Current Issue* back. There is no confirm-modal warning and no UI copy that says publishing changes "current", so the flip can surprise. Reads as intended convenience for the common case; flagged for the maintainer's **intent call** on the out-of-order edge (whether publish should set current only when the issue is newer than the present current, or offer a "make current" opt-out at publish). | issue-management product-spec verifier, 2026-07-04: code-traced `publishIssue()` — `updateCurrent($contextId, $issue)` is unconditional (outside `if (!$wasPublished)`); corroborated by the spec author's earlier live probe (publishing a future issue on a scratch journal made it current). As-built solid; intent adjudication only (spec Open question 1). | `classes/controllers/grid/issues/IssueGridHandler.php` `publishIssue()` (`Repo::issue()->updateCurrent($contextId, $issue)` — unconditional, no order/date guard); contrast `unpublishIssue()`/`deleteIssue()` which *recompute* current from the remaining published issues; `classes/issue/Repository.php` `updateCurrent()` / `getCurrent()` (reads `journals.current_issue_id`) | Intent call for the maintainer (issue-management OQ1): confirm whether publish-makes-current should be **conditional** (only when the newly-published issue is more recent than the current one, or an explicit "set as current issue" checkbox on the publish modal — mirroring the `sendIssueNotification` opt-out), or is intentionally unconditional. If unconditional is intended, consider a confirm-modal note so a manager publishing a back issue is not surprised. |
| 100 | **`GET /api/v1/highlights/{id}` throws a 500 TypeError whenever a journal is in context — the single-highlight fetch route is broken (and every highlight's `_href` points at it)** (LOW, broken endpoint, no data loss; masked because the management UI never calls it). `HighlightsController::get()` calls `Repo::highlight()->get((int) $illuminateRequest->route('highlightId'), $this->getRequest()->getContext())` — passing the **Context object** where `Repository::get(int $id, ?int $contextId)` expects an **integer context id**. The sibling `edit()` and `delete()` do it correctly (`$context?->getId()`). So the route throws `TypeError: Argument #2 ($contextId) must be of type ?int, APP\journal\Journal given` for **any** id (existing or missing) as long as a journal is in scope. The `HighlightsListPanel` never hits this route (it edits from the pre-loaded `items` list), so the defect is invisible in normal manager use — but the endpoint is dead for context-scoped highlights, and the `_href` the schema map returns for each highlight advertises exactly this URL. | highlights-featured-content product-spec author, live 2026-07-04 (port 8000, publicknowledge, as manager `dbarnes`): `GET /api/v1/highlights/7` (an existing highlight) and `GET /api/v1/highlights/99999` (missing) **both** returned HTTP 500 with the TypeError naming `HighlightsController.php:117`; `POST`/`PUT`/`DELETE`/`GET (many)`/`PUT order` on the same session all worked (200/400 as expected). Code-traced line 117 (`getContext()`) vs lines 201/295 (`getId()`). | `lib/pkp/api/v1/highlights/HighlightsController.php` `get()` (line 117 — `$this->getRequest()->getContext()`) vs `edit()` (201) / `delete()` (295) (`$context?->getId()`); `lib/pkp/classes/highlight/Repository.php` `get(int $id, ?int $contextId)` | Upstream (one line): `get()` should pass `$context?->getId()` (matching `edit`/`delete`) so the route returns the highlight (200) or not-found (404). Trivial fix; add a regression test hitting `GET /highlights/{id}` in a journal context. |

| 101 | **Reader issue *archive* (`/issue/archive`) orders back issues *solely* by `custom_issue_orders.seq ASC` with no date/current tiebreaker — so a journal that has never manually re-ordered its back issues (the common case) lists them in *undefined DB order* rather than newest-first** (LOW/intent-call, no data loss; parallels rows 25 & 28). `IssueHandler::archive()` builds the listing with `->orderBy(Collector::ORDERBY_SEQUENCE)`, which `Collector::orderBy()` maps to a *single* clause `['o.seq' => 'ASC']` (`o` = the LEFT-JOINed `custom_issue_orders`). `DAO::resequenceCustomIssueOrders()` early-returns whenever `customIssueOrderingExists()` is false, and publish/add/edit never seed a row otherwise — so on a journal whose editor never dragged the **Back Issues** list, `custom_issue_orders` is empty, every issue's `o.seq` is **NULL**, and `ORDER BY o.seq ASC` with no secondary key leaves the archive order to the DB (NULLs-first on MySQL, undefined among ties; truly undefined on Postgres). Contrast the editorial Back-Issues grid, which uses `ORDERBY_PUBLISHED_ISSUES` (= `o.seq ASC`, then `currentIssue DESC`, then `date_published DESC`) and the OAI feed — both deterministic newest-first. A reader expecting the newest issue at the top of the archive can instead get oldest-first or arbitrary order. | issue-archive-toc product-spec author, 2026-07-05: **code-derived** (`publicknowledge` has a single published issue, so multi-issue archive order is not live-observable there). Traced `IssueHandler::archive()` → `Collector::orderBy(ORDERBY_SEQUENCE)` (single `o.seq ASC` clause) and `DAO::resequenceCustomIssueOrders()` (early return when no ordering exists). | `pages/issue/IssueHandler.php` `archive()` (`->orderBy(Collector::ORDERBY_SEQUENCE)`); `classes/issue/Collector.php` `orderBy()` (`ORDERBY_SEQUENCE` = one `['orderBy'=>'o.seq','direction'=>'ASC']`; contrast `ORDERBY_PUBLISHED_ISSUES` with the current+date tiebreakers); `classes/issue/DAO.php` `resequenceCustomIssueOrders()` (early return via `customIssueOrderingExists()`) | Upstream candidate: give the archive query a deterministic fallback — reuse `ORDERBY_PUBLISHED_ISSUES` (current-first, then `date_published DESC`) or append a `date_published DESC` secondary key to `ORDERBY_SEQUENCE`, so an un-ordered archive is newest-first rather than DB-order. |

| 102 | **Site search's `orderBy=datePublished` throws HTTP 500 on PostgreSQL — a cross-DB grouping bug in the default DatabaseEngine** (LOW, cross-DB portability; not reachable from the stock reader UI). `DatabaseEngine::buildQuery()` selects matching submission ids under `->groupBy('s.submission_id')`; the `datePublished` order branch adds `->leftJoin('publications AS cp', …)->orderBy('cp.date_published', $dir)` — ordering by a column that is **neither aggregated nor in the GROUP BY**. MySQL tolerates this (functional-dependency relaxation); **PostgreSQL rejects it** with `SQLSTATE[42803]: column "cp.date_published" must appear in the GROUP BY clause or be used in an aggregate function` → 500. The sibling `title` branch avoids the trap by ordering on `MIN(title_current.setting_value)` (an aggregate). The stock `search.tpl` exposes **no** sort control, so a normal reader cannot trigger it — but `orderBy`/`orderDir` are read from request vars and re-emitted into pagination links, so any theme/plugin/URL that sets `orderBy=datePublished` breaks the search results page on every Postgres install. | site-search product-spec author, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, anonymous): `GET …/en/search?query=Reader&orderBy=title&orderDir=asc` → **200** (reordered); `…&orderBy=datePublished&orderDir=desc` → **500**; server log shows the `SQLSTATE[42803]` grouping error naming `cp.date_published` with the full generated SQL (`… group by "s"."submission_id" order by "cp"."date_published" desc`). | `lib/pkp/classes/search/engines/DatabaseEngine.php` `buildQuery()` — order switch `case 'datePublished'` (`->orderBy('cp.date_published', $orderDirection)`) under the trailing `->groupBy('s.submission_id')`; contrast `case 'title'` (`->orderBy($q->raw('MIN(title_current.setting_value)'), …)`) | Upstream candidate: make the `datePublished` order Postgres-safe — order on `MIN(cp.date_published)` (mirroring the `title` branch) or add `cp.date_published` to the `GROUP BY`. Add a regression test that runs a search with `orderBy=datePublished` on Postgres. |
| 103 | **A category's configured "Sort by" order is silently ignored on the reader catalog category page** — UI-affordance contradiction (no data loss). Category management (`CategoryForm`, used in OJS) exposes a **"Sort by"** select (`FieldSelect('sortOption')`, label `catalog.sortBy`, description `catalog.sortBy.categoryDescription`) letting a manager choose how a category's articles are ordered on its public page. But `PKPCatalogHandler::category()` computes the order from the category — `$orderOption = $category->getSortOption() ?: (ORDERBY_DATE_PUBLISHED . '-' . ORDER_DIR_DESC); [$orderBy,$orderDir] = explode('-', $orderOption);` — and then **never applies `$orderBy`/`$orderDir` to the query builder** (the locals are dead) and even assigns the *request* `orderBy`/`orderDir` (not the computed ones) to the template for pagination links. The actual result order therefore comes only from `SubmissionSearchResult::builderFromRequest()`, which orders **only when the request carries `orderBy`**; with no request param the `DatabaseEngine` returns matches in `GROUP BY s.submission_id` (submission-id/insertion) order — not the category's chosen sort, and not even the intended `date_published DESC` default. So the manager's "Sort by" setting has no effect for OJS readers. (OMP's own catalog handler may apply it; the OJS-served `PKPCatalogHandler` does not, and `pages/catalog/index.php` returns it directly with no OJS override.) | browse-category-section product-spec author, 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, anonymous). Code-derived from `PKPCatalogHandler::category()` (computed `$orderBy` provably unused) cross-checked against `builderFromRequest()` (applies order only when `$request->getUserVar('orderBy')` present) and `DatabaseEngine::buildQuery()` (`->groupBy('s.submission_id')`, no default order). Live: `…/browsecatprobe/catalog/category/optics?orderBy=title` → **200** (request-driven order path is the only live sort). **Multi-row order now observed directly by the retained `browse-category-section.spec.js`** (verifier re-run 2026-07-05, green): a category with `sortOption=title-ASC` holding two published articles — A (lower submission id, title "Zzz", earlier date) and B (higher id, title "Aaa", later date) — lists them `[A, B]` in submission-id order, ignoring both the configured title sort and the intended date-desc default. | `lib/pkp/pages/catalog/PKPCatalogHandler.php` `category()` (computes `$orderOption`/`$orderBy`/`$orderDir`, applies none; template gets `$request->getUserVar('orderBy')`); `lib/pkp/classes/search/SubmissionSearchResult.php` `builderFromRequest()` (`if ($orderBy = $request->getUserVar('orderBy'))`); `lib/pkp/classes/components/forms/context/CategoryForm.php` (`FieldSelect('sortOption')`, `catalog.sortBy`) | Upstream candidate: pass the computed category `$orderBy`/`$orderDir` into the builder (e.g. `$builder->orderBy($orderBy, $orderDir)`) when the request does not override, so the category's "Sort by" setting takes effect; add a reader-facing test asserting a category with a set sort renders its articles in that order. Note the `datePublished` value would then hit the row-102 Postgres 500 — fix that first. |

| 104 | **Hiding (un-approving) a public comment leaves a stale "approved by X on <date>" stamp in the DB** (LOW, latent — not currently surfaced in the moderation UI, but the moderator API returns wrong data). `UserCommentController::setApproval()` on `{approved:false}` sets the model's `approvedAt`/`approvedByUserId` to `null` and saves — but those are **non-multilingual settings** (`UserComment::getSettings()`), and the shared settings writer refuses to clear a non-multilingual setting via `null`: in `EntityUpdate::updateSettings()` the `SettingsBuilder` branch only treats an empty/`null` value as a clear signal **for multilingual props** (`if (!$this->isMultilingual($propName)) continue;`), while the upsert loop skips it because `isset($props['approvedAt'])` is false for `null`. Result: the old `approvedAt`/`approvedByUserId` rows survive in `user_comment_settings` and are re-served by `UserCommentResource` (which gates those fields only on `isModerator`, not on `isApproved`). The moderation modal happens to show the approver line only when `isApproved` is true, so a moderator does not see the stale value today — but it is wrong data and the API leaks it. | public-comments product-spec author, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, as manager `dbarnes`): seeded comment approved by admin (`approvedAt=2026-07-05 01:34:54`), then `PUT /comments/1/setApproval {approved:false}` → response `isApproved:false, approvedByUserName:null`; but a fresh `GET /comments/1` returned `isApproved:false` **with** `approvedAt:"2026-07-05 01:34:54"` and `approvedByUserName:"admin admin"`, and `user_comment_settings` still held both rows. | `lib/pkp/api/v1/comments/UserCommentController.php` `setApproval()` (`approvedAt = $isApproved ? now() : null; approvedByUserId = …`); `lib/pkp/classes/core/traits/EntityUpdate.php` `updateSettings()` (SettingsBuilder branch: non-multilingual null `continue`d, never deleted); `lib/pkp/classes/userComment/UserComment.php` `getSettings()`; `lib/pkp/api/v1/comments/resources/UserCommentResource.php` (approvedAt/approvedByUserName gated on `isModerator` only) | Upstream candidate: on un-approve, explicitly delete the `approvedAt`/`approvedByUserId` settings rows (or teach the SettingsBuilder path to clear a non-multilingual setting on explicit `null`); OR gate the resource's approvedAt/approvedByUserName on `isApproved`. Root cause is shared settings-infra, not comment-specific — a fix belongs in `EntityUpdate`/`SettingsBuilder` and should be regression-checked wherever a non-multilingual setting is cleared. |
| 106 | **OAI Identify advertises second-level datestamp granularity but `from`/`until` harvest filtering compares at day granularity** (LOW, intent-call, no data loss). `Identify` reports `granularity YYYY-MM-DDThh:mm:ssZ`, so a harvester may pass second-precision `from`/`until` for incremental harvests — but `OAIDAO::_getRecordsRecordSetQuery()` compares only the *date*: live records via `whereRaw('DATE(GREATEST(a.last_modified,i.last_modified,p.last_modified)) >= / <= ?', [<Y-m-d>])` and tombstones via `whereDate('dot.date_deleted', …)`. A second-precision window is therefore truncated to whole days: the harvest never *misses* records (the window only ever widens to the day boundary) but can re-return a day's worth already held, and the advertised precision is misleading to an incremental harvester. The day-cast was introduced by the Postgres crash fix in §1 row 1 (`whereDate()` on a raw `GREATEST(...)` Expression fataled on PG; replaced with portable `DATE()` casts) — the day-granularity semantics predate and survive that fix. | oai-pmh product-spec author, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, anonymous). `Identify` → `granularity YYYY-MM-DDThh:mm:ssZ`; `?verb=ListRecords&metadataPrefix=oai_dc&from=2099-01-01T00:00:00Z` → `noRecordsMatch`; all 174 records share `last_modified` on 2026-07-05, and `…&from=2026-07-05T02:00:00Z` (a time *after* many records' sub-second stamps) still returns all 174 — the intra-day time is dropped. Code-confirmed in the DAO date clauses. | `classes/oai/ojs/OAIDAO.php` `_getRecordsRecordSetQuery()` (`from`/`until` `whereRaw`/`whereDate` at `Y-m-d`); `lib/pkp/classes/oai/OAI.php` `extractDateParams()` (advertises + validates second granularity; `Identify()` emits `$this->config->granularity`) | Upstream candidate: either honor second granularity in the query (compare the full timestamp, keeping the PG-safe cast) or set Identify's advertised granularity to `YYYY-MM-DD` to match actual behavior. Non-blocking; maintainer to decide which side is correct. |
| 105 | **The bulk "delete all reports for a comment" endpoint (`DELETE /api/v1/comments/{id}/reports`) throws HTTP 500 and deletes nothing when the comment has 2+ reports; it also has no UI affordance** (LOW, effectively-dead API route, no data loss). `UserCommentController::deleteReports()` collects the report ids into an already-flat array (`$reportIds = $comment->reports()->pluck('user_comment_report_id')->all()`) then calls `UserCommentReport::withReportIds([$reportIds])->delete()` — **double-wrapping** the array, so `scopeWithReportIds()` runs `whereIn('user_comment_report_id', [[1,2,…]])` with a nested array. The very next line's notification cleanup uses the correctly-flat `$reportIds`, confirming the wrap is a typo. The moderation UI deletes reports individually (`deleteReport`, `DELETE …/reports/{reportId}`) and never calls the bulk route, so the bug is invisible in normal use. | public-comments product-spec author, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, as manager `dbarnes`): with **one** report on a comment, `DELETE /comments/1/reports` → **200** and the report was removed (single-element nested array coerced); with **two** reports on a comment, `DELETE /comments/2/reports` → **HTTP 500 `{"error":"Nested arrays may not be passed to whereIn method."}`** and both reports remained. | `lib/pkp/api/v1/comments/UserCommentController.php` `deleteReports()` (`UserCommentReport::withReportIds([$reportIds])->delete()` — should be `withReportIds($reportIds)`); `lib/pkp/classes/userComment/relationships/UserCommentReport.php` `scopeWithReportIds()` (`whereIn`) | Upstream candidate: pass the flat `$reportIds` (drop the extra `[ ]`), matching the notification-cleanup line directly below; add a test that reports a comment twice then bulk-deletes. Or remove the endpoint if the per-report delete is the only intended path. |
| 107 | **The announcementFeed Atom & RSS 1.0 feeds emit malformed, non-conformant timestamps** (LOW, feed-validity, no data loss — invisible unless a feed reader/validator rejects the feed). `plugins/generic/announcementFeed/templates/atom.tpl` formats `<updated>`/`<published>` with obsolete `strftime`-style `%` codes — the channel `<updated>` via Smarty `{$dateUpdated|date_format:"%Y-%m-%dT%T%z"}` and each entry via Carbon **directly** (`$announcement->datePosted->format("%Y-%m-%dT%T%z")`) — and `rss.tpl` does the same for `<dc:date>` (`->format("%Y-%m-%d")`). Under PHP 8.1+/modern Smarty these are **not** `strftime` codes: DateTime/Carbon `format()` treats `%` literally, `T`→timezone abbreviation, `%z`→day-of-year, so the output is e.g. `<updated>%2026-%07-%05UTC%UTC%185</updated>` and `<dc:date>%2026-%07-%05</dc:date>`. The Atom `<updated>` is thus invalid RFC3339 → a validating Atom reader rejects the whole feed. The **RSS 2.0** announcement template is correct (`{$smarty.const.DATE_RSS|date:...}` after `strtotime`), and the **webFeed** plugin's templates are all correct (`date_format:"Y-m-d\TH:i:sP"`), which is the intended shape. | web-feeds-syndication product-spec author, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, anonymous): scratch journal seeded with 2 announcements + `announcementfeedplugin` enabled; `…/gateway/plugin/AnnouncementFeedGatewayPlugin/atom` returned `<updated>%2026-%07-%05UTC%UTC%185</updated>` on channel and each entry, `…/rss` returned `<dc:date>%2026-%07-%05</dc:date>`, while `…/rss2` returned a correct `<pubDate>Sun, 05 Jul 2026 04:52:32 +0000</pubDate>`. The webFeed atom feed on publicknowledge rendered a correct `<updated>2026-07-05T04:55:40+00:00</updated>` for contrast. | `plugins/generic/announcementFeed/templates/atom.tpl` (`<updated>`/`<published>` lines) + `rss.tpl` (`<dc:date>`) — legacy strftime `%`-codes passed to Smarty `date_format`/Carbon `format()`; contrast `plugins/generic/webFeed/templates/atom.tpl` `date_format:"Y-m-d\TH:i:sP"` | Upstream candidate (pkp/announcementFeed): template-only fix — format with `date()`-style codes (`Y-m-d\TH:i:sP` for Atom, `Y-m-d` for `dc:date`) as the webFeed templates do, or route through the RSS 2.0 template's `DATE_*`+`strtotime` approach. |
| 108 | **The public registration form marks "Affiliation" required in the UI but the server does not enforce it** (LOW, off-UI validation gap, no data loss). `templates/frontend/components/registrationForm.tpl` renders the affiliation field with a required asterisk and HTML5 `required aria-required="true"`, but `RegistrationForm::__construct()` adds validators only for username, password, givenName, country and email — there is **no** `FormValidator` for `affiliation`. A normal browser user is therefore blocked by the client from submitting without an affiliation, while a scripted / no-JS / API POST that omits it **creates the account anyway**. Internal UI-vs-backend inconsistency: a manager who believes affiliation is mandatory (the form says so) can end up with affiliation-less accounts. Contrast the intentionally-optional last name (no asterisk, no validator on either side). | registration-login product-spec author, 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, anonymous). `RegistrationForm::__construct()` has no affiliation check; `registrationForm.tpl` marks it `required`. **Force-driven live (verifier 2026-07-05):** a scripted register POST that supplied every server-required field + `privacyConsent` but **omitted `affiliation`** returned **302 → "Registration complete"** (auto-logged-in, profile reachable, no mail), and the affiliation-less account was created — reproduced both by curl and by the retained `registration-login.spec.js` off-UI POST sub-assertion. The empty-affiliation case is unreachable from the stock UI only because HTML5 `required` blocks the browser. | `lib/pkp/classes/user/form/RegistrationForm.php` `__construct()` (validator chain — no `affiliation`); `lib/pkp/templates/frontend/components/registrationForm.tpl` (affiliation `required aria-required="true"` + asterisk) | Upstream candidate: pick one side — add `new FormValidator($this, 'affiliation', 'required', 'user.profile.form.affiliationRequired')` to match the UI, or drop the UI-required marker if affiliation is meant to be optional. Non-blocking; maintainer to decide whether affiliation is mandatory. |
| 109 | **The login form's "Keep me logged in" checkbox ships pre-ticked — every user is silently opted into a persistent auto-login cookie they didn't choose** (LOW, privacy/security default-on; UI-affordance contradiction, no data loss). `userLogin.tpl` renders the checkbox as `<input type="checkbox" name="remember" id="remember" value="1" checked="$remember">`, but `$remember` is **never assigned** by `LoginHandler::index()`, and a bare `$remember` is **not interpolated** inside a Smarty attribute value — so the served HTML carries the literal `checked="$remember"`, and HTML treats the mere *presence* of the `checked` boolean attribute as checked (the attribute value is irrelevant). Every fresh `/login` page therefore arrives with "keep me logged in" **already ticked**; a user who signs in without noticing is handed the persistent Laravel `remember_web_*` cookie (a long-lived auto-login) they never opted into — the opposite of what an unticked opt-in checkbox implies, and a real concern on shared/public computers. | registration-login product-spec verifier, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, anonymous): `GET …/publicknowledge/login` returned `<input type="checkbox" name="remember" id="remember" value="1" checked="$remember">`; the retained `registration-login.spec.js` remember-me test asserts the box is **pre-checked** on load, that a login left at the default sets a `remember_web_*` cookie, and that only un-ticking it suppresses the cookie (both directions green ×2). | `lib/pkp/templates/frontend/pages/userLogin.tpl` (`checked="$remember"` on the `remember` checkbox); `lib/pkp/pages/login/LoginHandler.php` `index()` (never assigns `$remember`); `lib/pkp/classes/security/Validation.php` `login(..., $remember)` (issues the persistent remember cookie) | Upstream candidate (template one-liner): render the checkbox **unchecked** by default — drop the `checked="$remember"` attribute, or bind it to a real, default-false template var — so "keep me logged in" is opt-in as its affordance implies. Non-blocking; maintainer to confirm persistent-session-by-default is not intended. |
| 110 | **The forced/standalone change form (`LoginChangePasswordForm`) validates the new password's LENGTH ONLY and skips the compromised-password (breach) check that `ResetPasswordForm` and the profile `ChangePasswordForm` apply** (LOW, only observable when the non-default `passwordUncompromisedEnabled` site setting is ON; no data loss). The reset form and the Profile→Password change form validate the new password through `FormValidatorPassword` (Laravel `Password::min($len)->uncompromised()`), so with breach-protection enabled they reject known-breached passwords; `LoginChangePasswordForm` instead chains a plain `FormValidatorLength` (+ a `required` check + a `password==password2` custom check), enforcing length only. Net effect: exactly the users an admin is *forcing* to pick a fresh password (the forced first-login change) — plus anyone on the standalone `/login/changePassword` — can still choose a known-breached password even with breach-protection on. An internal inconsistency across the three sibling password forms. | password-flows product-spec author + verifier, 2026-07-05 (port 8000, `ojs_test`, PostgreSQL): code-confirmed the validator asymmetry across the three forms — the setting is off by default so a no-op `UncompromisedVerifier` is swapped in (`ValidationServiceProvider::register()`), so the gap is asserted at code level (the retained `password-flows.spec.js` header documents it; observing it in-browser needs a global config flip + a real breached password). The length rule itself IS driven live (spec scenario 6). | `lib/pkp/classes/user/form/LoginChangePasswordForm.php` `__construct()` (`FormValidatorLength` on `password`, no `FormValidatorPassword`); contrast `lib/pkp/classes/user/form/ResetPasswordForm.php` `__construct()` (`FormValidatorPassword` w/ `password2`) and `lib/pkp/classes/user/form/ChangePasswordForm.php` `__construct()` (`FormValidatorPassword` + `passwordSameAsOld`); `lib/pkp/classes/core/ValidationServiceProvider.php` `register()` (no-op verifier when `passwordUncompromisedEnabled` off) | Upstream candidate: route `LoginChangePasswordForm`'s new-password check through `FormValidatorPassword` like its two siblings so the forced/standalone change path honours the compromised-password setting. Non-blocking; maintainer to confirm length-only is not intended for the forced path. |
| 111 | **`/login/changePassword` + `savePassword` are anonymous-reachable and username-driven — a session-less "change any account's password given its current password" surface** (LOW; grants no capability beyond what knowing the current password already allows, since that knowledge already permits login). Neither op carries a role policy (`LoginHandler::authorize()` adds one only for `signInAsUser`) and neither checks `isLoggedIn`: `changePassword` renders the form (HTTP **200**) for any anonymous visitor and `savePassword` acts on whatever `username` is POSTed, gated only by that account's current password (`FormValidatorCustom` → `Validation::checkCredentials`) — no session, and no check that the username is the caller's own. This is *required* for the forced-change case (the interception deliberately `Validation::logout()`s the session first), so the anonymous reachability is plausibly by design, but it exposes a general session-less change surface distinct from the auth-gated Profile→Password tab. Corrects an earlier premise that the endpoint 404s anonymously — it returns **200**. | password-flows product-spec author + verifier, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, anonymous): `GET …/publicknowledge/en/login/changePassword` → **200** with a visible editable `username` text field + `oldPassword`/`password`/`password2`; a throwaway-user anonymous `savePassword` with the correct current password changed that account's password (retained `password-flows.spec.js` scenario 5: wrong current password refused, correct succeeds). | `lib/pkp/pages/login/LoginHandler.php` `authorize()` (policy only for `signInAsUser`), `changePassword()`/`savePassword()` (no `isLoggedIn` guard); `lib/pkp/classes/user/form/LoginChangePasswordForm.php` (`oldPassword` → `Validation::checkCredentials($username, …)`); `lib/pkp/templates/user/loginChangePassword.tpl` (visible `username` text field) | Upstream candidate: maintainer decision — is the standalone anonymous `/login/changePassword` an intended general self-service surface, or should only the forced-change redirect reach it (add a session/self guard to the bare entry, confining ordinary self-service to the auth-gated Profile→Password tab)? Non-blocking. |
| 112 | **Emailing a DISABLED user from the Users & Roles list throws HTTP 500** (LOW–MEDIUM; no data loss, but a visible affordance crashes). The Vue user-access-manager always offers the **Email** row action, including on disabled accounts, but `UserEmailForm` loads the recipient via `Repo::user()->get($userId)` **without** `allowDisabled=true`, which returns **null** for a disabled user — so opening the modal (`fetch()` → `$user->getFullName()` on null) and sending (`execute()` → `$toUser->getEmail()` on null) both throw `Error: Call to a member function …() on null` (uncaught → 500). **The same missing `allowDisabled`** also afflicts the single-user read endpoint — `PKPUserController::get()` loads the target via the same `Repo::user()->get($userId)`, so **`GET /users/{disabledId}` returns 404** for a disabled account (contrast `endRole()`/`masthead()`, which pass `get($userId, true)`); the shared root cause is the missing `allowDisabled` across BOTH read paths (the email form AND the single GET). Enabled users are unaffected. | user-management product-spec author, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL; throwaway scratch journal `j-umprobe`): disabled a throwaway user, then `send-email` to it → **HTTP 500** (`getEmail() on null` at `UserEmailForm.php:97`, server log); the identical op on an enabled throwaway user delivered a direct email from the manager's own address. Verifier 2026-07-05 code-re-confirmed both null-deref sites + the `get()` 404 path (the `Repo::user()->get($id)` vs `get($id, true)` asymmetry within the one controller). | `lib/pkp/controllers/grid/settings/user/form/UserEmailForm.php` `fetch()` + `execute()` (`Repo::user()->get($this->userId)` — no `allowDisabled`); action offered unconditionally in `lib/ui-library/src/managers/UserAccessManager/useUserAccessManagerConfig.js` `getItemActions()`; `lib/pkp/api/v1/users/PKPUserController.php` `get()` (same `Repo::user()->get($userId)` — 404s a disabled user; `endRole()`/`masthead()` correctly pass `true`) | Upstream candidate: fetch the recipient with `allowDisabled=true` (as `UserDisableForm`/`UserDetailsForm` do), or hide the Email action for disabled accounts. Non-blocking. |
| 113 | **The users report (`GET /users/report`) is fully live but has NO UI control in 3.6 — API-only** (LOW). `PKPUserController::getReport()` streams a complete users CSV (`user-report-<date>.csv`, contact columns + a Yes/No column per role group) to a manager/admin/sub-editor, but a repo-wide search finds **no** template, Vue manager, or menu item that links `/users/report` — the download action present on the legacy Users grid in earlier versions was not carried into the Vue user-access-manager. So a capability that plainly exists is reachable only by hand-crafting the API URL. | user-management product-spec author, live 2026-07-05 (port 8000, `ojs_test`): `GET …/api/v1/users/report` returned a valid BOM+header CSV (28 rows on `publicknowledge`); `grep` across `templates/`, `lib/ui-library/src/`, and the stats "Users" page found no reference to `users/report`. | `lib/pkp/api/v1/users/PKPUserController.php` `getReport()` (streamed CSV, live); no consumer in `lib/ui-library/src/managers/UserAccessManager/` or `lib/pkp/templates/management/` | Upstream candidate: maintainer decision — restore a "Download report" action on the users list, or retire the endpoint. Non-blocking. |
| 114 | **Section editors reach the mutating `endRole`/`masthead` `/users` PUTs the UI hides from them, and neither PUT applies the administration-scope guard** (MEDIUM; over-permission + missing target-scope check, no data loss). The `/users` route group middleware is one `roleAuthorizer([SITE_ADMIN, MANAGER, SUB_EDITOR])` applied to the WHOLE group with no per-route override, so `PUT /users/{id}/endRole/{groupId}` (end-dates a role assignment **and emails the user** "removed from a role") and `PUT /users/{id}/masthead/{uugId}` (flips public masthead display **and emails the user**) are reachable by a **section editor** — mutating, manager-level user-administration actions, even though the edit-user page that drives them (`ManagementHandler::editUser` → `UserInvitationUserGroupsTable.vue`) is manager/admin-gated and offers section editors no such control (an API-permits / UI-hides mutating divergence, cf. §1 versioning "section editors publish via API but see no button"). Separately, unlike the legacy grid ops (`editUser`/`updateUser`/`disableUser`/`removeUser`/`mergeUsers`, which ALL call `Validation::getAdministrationLevel()`), `PKPUserController` invokes that scope guard **nowhere** — so these two PUTs run with **no** actor-vs-target scope check at all: a section editor (or even a manager) can end a role / toggle masthead for **any** user holding that assignment in the context, including targets the grid's guard would mark PROHIBITED (e.g. a site admin's context role). | user-management product-spec verifier, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, session-auth): as **sub-editor** `dbuskins`, `GET /users` → **200** (38 users) and `PUT /users/1/endRole/999999` & `PUT /users/1/masthead/999999` → **404** *"resource was not found"* (the handler's OWN target check — reached only AFTER passing authorization; a rejected role returns **401** `roleBasedAccessDenied`, which reviewer `jjanssen` got on `GET /users`). Probes non-mutating (bogus group/uug id 999999). Code: `getGroupRoutes()` defines the PUTs under the group middleware with no extra gate; `grep` finds **zero** `getAdministrationLevel` in `PKPUserController`. | `lib/pkp/api/v1/users/PKPUserController.php` `getRouteGroupMiddleware()` (`[SITE_ADMIN, MANAGER, SUB_EDITOR]`, whole-group), `getGroupRoutes()` (endRole/masthead PUTs — no per-route role), `endRole()`/`masthead()` (no `getAdministrationLevel`); contrast `lib/pkp/controllers/grid/settings/user/UserGridHandler.php` (grid ops call `Validation::getAdministrationLevel()`) | Upstream candidate: gate the mutating `endRole`/`masthead` routes to manager/admin (per-route role), and/or add the `getAdministrationLevel` scope check to these PUTs so the API honours the same target-scope protection as the grid. Non-blocking; maintainer to confirm whether section-editor breadth on these two mutating PUTs is intended. |
| 115 | **The user-invitation flow lets a section editor / assistant mint a brand-new account holding ANY role, including Journal Manager — with no check that the inviter may grant the chosen role** (MEDIUM–HIGH; privilege escalation / broken access control, and broader than the direct-create path). The whole invitation surface — the create/edit pages (`InitializeInvitationUIHandler`) and every mutating create-side API action (`add`/`populate`/`invite`/`cancel`, `InvitationController::getGroupRoutes()`) — is gated to `roleAuthorizer([SITE_ADMIN, MANAGER, SUB_EDITOR, ASSISTANT])` with **no** per-route narrowing, and the role-to-assign is validated only by `AddUserGroupRule` (the target must not already hold the group) + `UserGroupExistsRule` (the group exists in the context) — **neither checks the inviter's own authority**. So a **section editor** or **production assistant** — who **cannot create a user at all** via the direct add-user form (manager/admin-only, `user-management`) — can invite a fresh email to the **Journal manager** group, and on accept a new manager account is created. Contrast: this is strictly wider than direct create (role, and who may create). | user-invitations product-spec author, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, session-auth): as **sub-editor** `dbuskins` (NOT a manager), `POST /invitations/add/userRoleAssignment` (new email) → **200**, `PUT …/populate` with `userGroupsToAdd:[{userGroupId:2}]` (**Journal manager**, roleId 16) → **200**, `PUT …/invite` → **200** `status:PENDING` — the manager list then showed the PENDING invite granting *Journal manager* to `seprobe@example.org` (cancelled after the probe). Reviewer `jjanssen` was correctly refused (`add` → **401** `roleBasedAccessDenied`). | `lib/pkp/api/v1/invitations/InvitationController.php` `getGroupRoutes()` (`roleAuthorizer([SITE_ADMIN, MANAGER, SUB_EDITOR, ASSISTANT])`, whole create-side group, no per-route role); `lib/pkp/pages/invitation/InitializeInvitationUIHandler.php` `__construct()` (same four roles for `create`/`edit`); `lib/pkp/classes/invitation/invitations/userRoleAssignment/rules/AddUserGroupRule.php` + `UserGroupExistsRule.php` (no inviter-authority check); contrast `lib/pkp/controllers/grid/settings/user/UserGridHandler.php` (create is `[MANAGER, SITE_ADMIN]` + `getAdministrationLevel`) | Upstream candidate: restrict which roles an inviter may grant to those at/below their own authority (and/or gate the create-side invitation routes to manager/admin), so the invite path cannot exceed the direct-create path. Non-blocking; maintainer to confirm whether editor/assistant invite breadth — and manager-role granting in particular — is intended. |
| 116 | **The Tools → "Permissions" tool is destructively mislabelled: clicking it bulk-overwrites EVERY article's copyright/licence to the journal defaults, not role permissions** (MEDIUM; destructive + misleading label — but the copyright-reset behaviour itself is intended, only the naming collides with "Users & **Roles**"). The management **Tools** index links an item labelled **"Permissions"** (`settings.libraryFiles.category.permissions`); its page carries a single **Reset Permissions** button (`manager.setup.resetPermissions`, JS `confirmText` prompt) that POSTs to `management/tools/resetPermissions` → `Repo::submission()->resetPermissions($contextId)`, which iterates **every** submission in the journal and rewrites **each publication's** `copyrightYear`/`copyrightHolder`/`licenseUrl` to the context defaults. It touches **no** user group, role, or per-group toggle. The hazard: a manager (or a QA reader of this atlas) who expects a role/permission reset — the feature-map framed it as "reset role permissions to defaults", which is WRONG — can, past one confirm dialog, irreversibly overwrite bespoke per-article copyright/licence across the whole journal. As-built the action is a legitimate intended bulk-licence reset; the deviation is the label + a UI-affordance/behaviour mismatch. | roles-permissions product-spec author + verifier, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL): the reset method's only writes are copyright/holder/licence per publication and no user-group code path exists; proven end-to-end by the retained `roles-permissions.spec.js` scenario 10 (T8) — on a scratch journal a published article whose copyright/licence was overridden to sentinels was reset to the journal defaults by `POST management/tools/resetPermissions`, while a custom user group and the whole role matrix survived untouched. | `lib/pkp/pages/management/PKPToolsHandler.php` `permissions()`/`resetPermissions()` (`Repo::submission()->resetPermissions($context->getId())`, gated `[MANAGER, SITE_ADMIN]` via the `tools` op); `lib/pkp/classes/submission/Repository.php` `resetPermissions()` (copyrightYear/copyrightHolder/licenseUrl → context defaults, per publication); `lib/pkp/templates/management/tools/{index.tpl,permissions.tpl}` ("Permissions" link + confirm-prompted Reset button) | Upstream candidate: rename the tool to "Reset article permissions / licences" (label + `manager.setup.resetPermissions*` keys) to disambiguate from role management; the behaviour needs no change. Non-blocking; maintainer to confirm the rename. |
| 117 | **Impersonation ("Log In As") is ONE-CLICK — it carries no re-authentication gate and leaves no audit trail, although the atlas/feature-map framed the `confirmAccess` reauth as a "login-as confirmation"** (LOW–MEDIUM; security-posture, no data loss — the administration-scope guard still fully gates WHO can be impersonated, so exposure is bounded; a manager/admin can nonetheless silently act as any in-scope user with no password re-entry and no record). `LoginHandler::signInAsUser()` attaches only a `RoleBasedHandlerOperationPolicy` + the `Validation::getAdministrationLevel(...)==FULL` scope check — there is **no** `ReauthenticationRequiredPolicy` and **no** password prompt, so a manager/admin with a live session impersonates any in-scope user with a single `GET login/signInAsUser/{id}`. The reauth gate (`ReauthenticationRequiredPolicy` + `admin/confirmAccess`/`confirmAccessSubmit`) is attached only to `AdminHandler` and guards the **site-administration AREA**, and only when `security.password_timeout > 0` (default 0 / commented out → OFF); the atlas mislabelled the two confirm ops as the "login-as-user confirmation" and the feature-map assumed an admin must "re-confirm their password before impersonating" — neither is as-built (the mismatch is with the campaign's own atlas labels, not necessarily with product intent). Separately, starting/ending an impersonation emits **no** email, notification, or event-log entry — there is no record of who impersonated whom (anything done in the session is attributed to the target). The atlas descriptions of the two confirm ops + the reauth policy were corrected in-spec. | login-as product-spec author + verifier, live-confirmed 2026-07-05 (port 8000, `ojs_test`, fresh browser/curl contexts — impersonation migrates+destroys the session): admin `signInAsUser/7` (jjanssen) switched the acting session with **no** confirm-password page (`window.pkp.currentUser` = `{jjanssen, JJ, isUserLoggedInAs:true, loggedInAsUser:{admin, AA}}`); manager `dbarnes` via `/publicknowledge/login/signInAsUser/7` impersonated in-scope reviewer jjanssen with no prompt (`loggedInAsUser:{dbarnes, DB}`); admin opened `/index/admin` (heading "Administration") → HTTP 200 with **no** `confirmAccess` redirect on the default config. Refuted paths held: `dbarnes → signInAsUser/1` (site admin) → "do not have administrative rights over this user" error, no switch. | `lib/pkp/pages/login/LoginHandler.php` `authorize()` (only `RoleBasedHandlerOperationPolicy` for `signInAsUser`, no reauth policy) + `signInAsUser()` (FULL-scope check, no password); contrast `lib/pkp/pages/admin/AdminHandler.php` `authorize()` (`ReauthenticationRequiredPolicy` on every op except `confirmAccess`/`confirmAccessSubmit`) + `lib/pkp/classes/security/Validation.php` `isReauthenticationRequired()` (`security.password_timeout > 0`); no audit-log write in `signInAs`/`signOutAs` (`lib/pkp/classes/core/PKPSessionGuard.php`); config `config.TEMPLATE.inc.php` (`;password_timeout = 0`) | Upstream candidate: maintainer decision — (a) correct the atlas descriptions of the two confirm ops (they are the Administration-area reauth, NOT a login-as confirmation — done in the spec + atlas); (b) decide whether impersonation, a maximum-trust action, should require re-authentication and/or leave an audit record, or whether the scope guard alone is the intended control. Non-blocking. |
| 118 | **A journal manager can disable — and re-enable — their own journal through the journal-settings API (`enabled`), a flag whose only UI control is the site-admin Hosted Journals area** (MEDIUM; over-permission / API-permits-UI-hides mutating divergence, family of rows 114/115; no data loss — the flag is reversible). `PKPContextController::edit()` (the `PUT contexts/{contextId}` the Users & Roles / settings forms post to) accepts **any** journal-schema property under a whole-group `roleAuthorizer([SITE_ADMIN, MANAGER])` and applies **no field-level authority check**, so a manager's PUT with `enabled:false` is accepted (journal vanishes from the site index; anonymous visitors get bounced to its login) — and, worse, `enabled:true` **re-publishes a journal a site admin deliberately took offline** (a plain escalation over the admin-only control). No manager-facing UI renders the flag (their Settings pages never offer it; the only affordance is the admin contexts grid / settings wizard). **The hole is not confined to `enabled`**: because `edit()` applies no per-field authority check, a manager can PUT **any** admin-only Hosted-Journals-grid property — the verifier confirmed **`seq`** (site-index ordering) and even **`urlPath`** (which rewrites every public URL for the journal) are equally manager-mutable through the same endpoint — so the precise scope is *"any admin-only context property, not just `enabled`."* | site-access-restrictions product-spec author + adversarial verifier, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, session-auth). **Author** (scratch `j-sarprobe` id 2): as **manager** `dbarnes`, `PUT /j-sarprobe/api/v1/contexts/2` `{"enabled":false}` → **200** (echoed `enabled:false`; anon home → 302 login; absent from site index); then **admin** disabled it and `dbarnes` `PUT {"enabled":true}` → **200** — anon home → **200**, journal publicly restored by a non-admin. Reviewer `jjanssen`'s PUT correctly refused (**401** `roleBasedAccessDenied`). **Verifier** (scratch `j-sarv2` id 22): reproduced `enabled` false/true + the admin-disable→manager-reenable escalation, and additionally proved manager `PUT {"seq":99}` → **200** and `PUT {"urlPath":"j-sarv2x"}` → **200** (old path 404s, new path live); also confirmed **section editor** `dbuskins`, **assistant** `mfritz` and reviewer `jjanssen` are each **401** on the PUT (so this endpoint is genuinely manager/admin-only — unlike rows 114/115). All flags/paths restored after each probe. | `lib/pkp/api/v1/contexts/PKPContextController.php` `getGroupRoutes()` (`roleAuthorizer([SITE_ADMIN, MANAGER])` on `PUT contexts/{contextId}`, no per-field gate) + `edit()` (passes the whole request input to `ContextService::validate()/edit()` — special-cases none of `enabled`/`seq`/`urlPath`); contrast the UI: these props are rendered only by the site-admin contexts form (admin Hosted Journals), never by `ManagementHandler`'s settings pages | Upstream candidate: restrict admin-only context properties (`enabled`, `seq`, `urlPath`, …) to `ROLE_ID_SITE_ADMIN` in `PKPContextController::edit()` / `ContextService`, mirroring the UI. Non-blocking; surfaced while probing disabled-journal visibility for `site-access-restrictions` — the control surface itself belongs to `site-administration`. |
| 119 | **Three journal-settings fields marked REQUIRED in the UI are accepted empty by the settings save endpoint — Journal initials (`acronym`), support Name (`supportName`) and support Email (`supportEmail`)** (LOW; validation inconsistency across on-screen-identical fields, no UI path clears them, values restorable). The Masthead form marks `name`+`acronym`(+`country`) `isRequired` and the Contact form marks `contactName`/`contactEmail`/`supportName`/`supportEmail` required, but the context schema's top-level `required` lists only `name`/`contactName`/`contactEmail` (+ urlPath/locale props) — so the client-side form blocks clearing initials/support contact while a direct `PUT contexts/{id}` clears them silently (200). Contrast the siblings on the same forms: empty primary-locale `name` → 400 "You must complete this field in English.", empty `contactName` → 400 "This field is required." Consequence: any API client (or future UI refactor trusting the schema) can leave a journal without initials — which feed the `{$contextAcronym}` email variable (`ContextEmailVariable::CONTEXT_ACRONYM`) — or without a support contact, and QA probing "required" semantics at the API gets opposite results for fields the UI presents identically. (`country` is a separate half-required case: form-required + cannot be cleared once set (`country` validator, not nullable), but absent at wizard creation — recorded in the spec as a note, not part of this row.) | journal-masthead-settings product-spec author, live 2026-07-05 (port 8000, `ojs_test`, PostgreSQL, session-auth, scratch journal `jms2` id 161): manager PUT `{"acronym":{"en":""}}` → **200** (initials cleared), `{"supportName":""}` → **200**; vs `{"name":{"en":""}}` → **400**, `{"contactName":""}` → **400**, bad ISSN/URL/email/country each → 400 with the exact messages. Cleared values restored in the same session. The UI-side required behaviour is exercised by the retained `journal-setup.spec.js` row 4 (clearing name/acronym shows inline errors and blocks save). | `lib/pkp/schemas/context.json` (top-level `required` = name, primaryLocale, supportedLocales, urlPath, contactName, contactEmail — no acronym/supportName/supportEmail); `lib/pkp/classes/components/forms/context/PKPMastheadForm.php` + `PKPContactForm.php` (`isRequired` marks on acronym/supportName/supportEmail); `lib/pkp/classes/services/PKPContextService.php` `validate()` → `ValidatorFactory::required()` (enforces only schema-required props) | Upstream candidate: add `acronym`, `supportName`, `supportEmail` to the context schema's `required` list (or drop the UI marks) so the two layers agree. Non-blocking; maintainer to confirm which direction is intended. |
| 120 | **Schema-required context properties are not enforced at CREATION — `POST contexts` accepts a journal with no name and no principal contact (any required prop may be absent)** (LOW–MEDIUM; the endpoint is site-admin-only so no privilege issue, and the admin wizard UI enforces title/initials client-side — but any API client can mint a nameless, contact-less journal that is `enabled:true` and lists as a blank entry on the public site index, and the add-time half of the schema's `required` contract is dead code for contexts). Mechanism: `PKPContextService::validate()` passes the **action string** (`EntityWriteInterface::VALIDATE_ACTION_ADD` = `'add'`) as the second argument to `ValidatorFactory::required()`, whose `$object` parameter is documented as *the object being edited, null on add* — `is_null('add')` is always false, so the absence branch (required prop missing from the payload) can never run; required props are checked only when the key is present-and-empty (the edit-time semantics). Contrast EDIT on the same schema: present-and-empty `name`/`contactName` → 400 (row 119's controls). | journal-masthead-settings adversarial verifier, live 2026-07-06 (port 8000, `ojs_test`, PostgreSQL, session-auth as site admin, hit while re-verifying the spec's country-absent-at-creation note): `POST /index/api/v1/contexts` `{"name":{"en":"Jmv Admin Made"},"urlPath":"jmvadm1","primaryLocale":"en","supportedLocales":["en"]}` (no contactName/contactEmail) → **200** (id 175, `country:null`, `contactName:null`, `enabled:true`); then `{"urlPath":"jmvadm2","primaryLocale":"en","supportedLocales":["en"]}` with **no `name` at all** → **200** (id 176, nameless). Both scratch journals deleted afterwards (`DELETE contexts/{id}` → 200, site index clean). | `lib/pkp/classes/services/PKPContextService.php` `validate()` (passes `$action` into `ValidatorFactory::required()`'s `$object` slot); `lib/pkp/classes/validation/ValidatorFactory.php` `required()` (`is_null($object)` gates the required-prop-absent check); `lib/pkp/api/v1/contexts/PKPContextController.php` `add()` (`VALIDATE_ACTION_ADD`); `lib/pkp/classes/services/interfaces/EntityWriteInterface.php` (`VALIDATE_ACTION_ADD = 'add'`) | Upstream candidate: pass `null` on add (and the context object on edit) instead of the action string, restoring add-time required enforcement; audit the other `$action`-style `validate()` callers (e.g. site service) for the same slip. Ownership: journal creation is `site-administration` territory; recorded here by the journal-masthead-settings verifier. Non-blocking. |
| 121 | **Custom sidebar blocks cannot be deleted on PostgreSQL — the Custom Block Manager's delete action always throws HTTP 500 and the block survives untouched** (MEDIUM on Postgres installs: a documented manager action is fully broken; no data loss — the failure is total, nothing partial). Mechanism: `PluginSettingsDAO::deleteSetting()` (and the currently-uncalled `deleteSettingsByPlugin()`) build their delete with `->where('plugin_Name', …)` — a column-name case typo. MySQL's case-insensitive identifiers masked it for years; PostgreSQL quotes the identifier and rejects the statement (`Undefined column "plugin_Name" … HINT: perhaps you meant plugin_settings.plugin_name`), so `CustomBlockGridHandler::deleteCustomBlock()`'s first `deleteSetting(...,'enabled')` throws before the block list is rewritten. Blast radius beyond custom blocks: `ThemePlugin::saveOption()` calls `deleteSetting()` when a theme-option value is the empty string — unreachable via the REST API (Laravel's `ConvertEmptyStringsToNull` global middleware converts `""` to null first, live-confirmed 200) but reachable from any PHP caller passing a literal `''`. Secondary (code-read, independent of the typo): `deleteCustomBlock()` deletes only the `enabled`/`context`/`seq`/`blockContent` settings and never `blockTitle`/`showName`, so even a successful delete leaves invisible plugin_settings residue. | website-appearance-settings product-spec author, live 2026-07-06 (port 8000, `ojs_test`, PostgreSQL, session-auth as manager dbarnes on scratch journal `j-was1` id 316): created custom block "Reading List" (`reading-list`) via the grid → offered in the Sidebar picker → rendered publicly; `POST …/custom-block-grid/delete-custom-block` (valid CSRF, `blockName=reading-list`) → **HTTP 500** twice, server log shows the QueryException at `PluginSettingsDAO.php` `deleteSetting()` called from `CustomBlockGridHandler.php` `deleteCustomBlock()`; block still in the grid, the picker and the public sidebar afterwards. | `lib/pkp/classes/plugins/PluginSettingsDAO.php` `deleteSetting()` + `deleteSettingsByPlugin()` (`where('plugin_Name', …)`); `plugins/generic/customBlockManager/controllers/grid/CustomBlockGridHandler.php` `deleteCustomBlock()`; `lib/pkp/classes/plugins/ThemePlugin.php` `saveOption()` (empty-string branch); `lib/pkp/classes/core/PKPRoutingProvider.php` (`ConvertEmptyStringsToNull`) | Upstream fix: `plugin_Name` → `plugin_name` in both DAO methods (one-line each); optionally extend `deleteCustomBlock()` to also delete `blockTitle`/`showName`. Postgres-only reproduction — flag to upstream as a Postgres-compatibility defect. Non-blocking for the campaign. |
| 122 | **The "Make a Submission" sidebar block ships `enabled=true` in its settings.xml but the default never installs — the block starts disabled on every journal and never appears in the Sidebar picker until a manager enables it by hand** (LOW; reversible via Installed Plugins, but the shipped default is dead code and the block silently absent from the sidebar composer on every new journal). Mechanism: per-context plugin defaults install only for plugins that override `Plugin::getContextSpecificPluginSettingsFile()`; the other four stock blocks (information, languageToggle, subscription, developedBy) override it, `MakeSubmissionBlockPlugin` does not — so no `enabled` row is ever written, `PluginRegistry::loadCategory('blocks', true)` never loads it, and both the Sidebar picker options (`PKPAppearanceSetupForm`) and the frontend render (`PKPTemplateManager::displaySidebar()`) skip it. NOTE: contradicts `docs/product/specs/journal-homepage.md` rule 13's "Make a submission — ships enabled" (a code-read of the dead settings.xml); that spec should be corrected at grooming. | website-appearance-settings product-spec author, live 2026-07-06 (port 8000, `ojs_test`, PostgreSQL): `plugin_settings` has **no** `makesubmissionblockplugin` row for context 1 (seed `publicknowledge`, installed at DB creation) **nor** for fresh scratch context 316, while the other four blocks have `enabled`+`seq` rows in both; the Sidebar picker options on `j-was1` were `[WebFeedBlockPlugin, informationblockplugin, subscriptionblockplugin, languagetoggleblockplugin]` (no makeSubmission); the Installed Plugins grid shows the block with an **unchecked** enable checkbox; enabling it via the grid (`settings-plugin-grid/enable`) → 200 and the picker then offers `makesubmissionblockplugin`. | `plugins/blocks/makeSubmission/MakeSubmissionBlockPlugin.php` (no `getContextSpecificPluginSettingsFile()` override — contrast `plugins/blocks/information/InformationBlockPlugin.php`); `plugins/blocks/makeSubmission/settings.xml` (`enabled=true`, never read); `lib/pkp/classes/plugins/PluginRegistry.php` `loadCategory('blocks', true)` | Upstream candidate: add the standard `getContextSpecificPluginSettingsFile()` override (3 lines) so the shipped default takes effect — or delete the dead settings.xml if disabled-by-default is intended. Also correct journal-homepage spec rule 13 at grooming. Non-blocking. |
| 123 | **Clearing the journal style sheet removes the setting and the public `<link>` but never deletes the file — `public/journals/{id}/styleSheet.css` stays served forever at its stable URL** (LOW–MEDIUM: a manager who "removes" the stylesheet reasonably believes it is gone, but anything holding the URL still fetches the full content — mild content-persistence concern; the linked pages themselves are correct, and a later re-upload silently overwrites the orphan). Mechanism: the null-clear branch of `PKPContextService::_saveFileParam()` computes the file to delete as `$fileName = $isImage ? $setting['uploadName'] : $setting;` — for the NON-image `styleSheet` param `$setting` is the stored **array** (`{name, uploadName, dateUploaded}`, the very shape this same method returns on upload), so `PKPPublicFileManager::removeContextFile($contextId, ARRAY)` builds a nonsense path (array-to-string) and the delete no-ops; the setting still nulls, so the `contextStylesheet` link disappears while the file survives. The image fields (favicon/logo/homepageImage/journalThumbnail, `$isImage=true`) use `$setting['uploadName']` and are unaffected (code-read). Contradicted `website-appearance-settings` rule 5 + scenario 3's "clearing deletes the file" (a code-read claim — that spec's liveness note shows clearing was never live-verified; spec corrected 2026-07-06). | website-appearance-settings TEST AUTHOR, live 2026-07-06 (Playwright suite, `ojs_test`, PostgreSQL, per-test scratch journal `j-wat…`): uploaded a `.css` via the Advanced form → saved as `styleSheet.css`, linked cache-busted on every public page, served 200; `PUT contexts/{id}` `{"styleSheet": null}` (manager session, CSRF) → **200**, next anonymous load has NO styleSheet.css link, but `GET public/journals/{id}/styleSheet.css` → **200** with the uploaded content intact (spec expected 404). Pinned by `playwright/tests/website-appearance-settings.spec.js` test 3 (asserts link-gone + file-still-200 as-built). | `lib/pkp/classes/services/PKPContextService.php` `_saveFileParam()` (the `is_null($value)` branch's `$isImage ? $setting['uploadName'] : $setting`; the non-image return shape at the bottom of the same method proves `$setting` is an array); `lib/pkp/classes/file/PKPPublicFileManager.php` `removeContextFile()` | Upstream candidate (one line): `$fileName = is_array($setting) ? $setting['uploadName'] : $setting;` (covers both shapes incl. any legacy string values); optionally purge the orphan on the next upload. Non-blocking. |
| 124 | **Reviewer-recommendation OPTIONS (a Settings → Workflow vocabulary) are mutable by SECTION EDITORS via API, with no UI and no settings gate** (MEDIUM: journal-level configuration writable by a role that is denied every settings page; same over-permission shape as row 114). The `reviewers/recommendations` REST controller's role list is SITE_ADMIN + MANAGER + **SUB_EDITOR**, and its policy chain (`UserRolesRequiredPolicy` + `ContextAccessPolicy` + item-route `RecommendationAccessPolicy` → same-journal + exists) never includes `CanAccessSettingsPolicy` — unlike the page hosting the manager UI and unlike the sibling genre grid (`SetupGridHandler::authorize()`). Consequences: (a) a section editor can exercise the ENTIRE controller — all 6 routes (getMany/add/get/edit/status/delete, i.e. read + create + edit + deactivate + hard-delete) — on the journal-wide recommendation vocabulary by direct call while `management/settings/workflow` bounces them and no UI offers the action; (b) by the same gap, a manager stripped of `permitSettings` retains full API access to this one settings surface. Scope is bounded: strictly SUB_EDITOR (assistant/reviewer/author → 401), and same-journal only (`RecommendationContextPolicy` blocks cross-context, so a section editor cannot reach a journal they have no role in). Adversarial verifier 2026-07-06 re-drove all 6 routes as `dbuskins` → 200 each (incl. hard-delete id 3615) and confirmed the assistant/reviewer/author 401s + the cross-journal 401. Suspected copy of a per-submission controller's role list (e.g. reviewer suggestions) onto a journal-settings resource. | workflow-settings product-spec author, live 2026-07-06 (port 8000, `ojs_test`, PostgreSQL, scratch journal `j-wfs2` id 565, session-auth as seeded section editor `wfssec`): `GET …/management/settings/workflow` → redirect `roleBasedAccessDenied`; `PUT api/v1/contexts/565` → 401; genre-grid `fetch-grid` → JSON role-denial; but `GET api/v1/reviewers/recommendations` → 200 and `POST api/v1/reviewers/recommendations` `{title:{en:"SubEd Injected WFS77"},status:1,type:1}` → **200, created id 3380** (visible in the manager's list; deleted afterwards). | `api/v1/reviewers/recommendations/ReviewerRecommendationController.php` `getRouteGroupMiddleware()` (role list) + `authorize()` (no settings policy); contrast `lib/pkp/controllers/grid/settings/SetupGridHandler.php` `authorize()` (`CanAccessSettingsPolicy`) and `lib/pkp/pages/management/ManagementHandler.php` `authorize()` | Upstream candidate: drop SUB_EDITOR from the role list and add `CanAccessSettingsPolicy` (or an equivalent settings check) to the controller. Spec: `docs/product/specs/workflow-settings.md` Known deviations. Non-blocking. |
| 125 | **Review-reminder thresholds: the 0–14-day bound is client-side only** (LOW; recorded for symmetry with rows 119/123). The four reminder sliders on Settings → Workflow → Review → Setup are hard-bounded 0–14 days (`FieldSlider` min/max from `PKPReviewSetupForm::MIN/MAX_REMINDER_NOTIFICATION_SEND_IN_DAYS`), but the context schema validates only `min:0` with no max — an API client can store any value (e.g. 99), which the slider UI can neither display faithfully nor reproduce, and which silently shifts the scheduled reminder far outside the offered range. | workflow-settings product-spec author, live 2026-07-06 (scratch `j-wfs2` id 565, manager session): slider `aria-valuemin=0 aria-valuemax=14` (live DOM); `PUT api/v1/contexts/565` `{numDaysBeforeReviewSubmitReminderDue: 99}` → **200** (stored; reset to 3 afterwards); `{numWeeksPerResponse: -1}` → 400 "must be at least 0" (the min IS enforced). | `lib/pkp/classes/components/forms/context/PKPReviewSetupForm.php` `addReminderFields()`; `lib/pkp/schemas/context.json` `numDays{Before,After}Review{Response,Submit}ReminderDue` (`min:0` only) | Upstream candidate: add `max:14` to the four schema fields (or drop the UI cap if larger offsets are intended). Non-blocking. |
| 126 | **Delayed-open-access duration: the Disabled/1–60-month bound is client-side only — the API stores any integer, including negative** (LOW; family of rows 119/125, client-only validation). The Distribution → Access "Delayed Open Access" select (visible in subscription mode) offers Disabled (0) + 1–60 months (`SUBSCRIPTION_OPEN_ACCESS_DELAY_MIN/MAX`), but `schemas/context.json` `delayedOpenAccessDuration` is bare `integer/nullable` — no min or max — so a direct `PUT contexts/{id}` stores out-of-range values the UI can neither display nor reproduce. Consequence at issue publish (`IssueGridHandler::publishIssue()` computes openAccessDate = now + N months): 999 → an open-access date ~83 years out; **-5 → a date in the past, i.e. the "subscription" issue is open-access immediately** while its access status still says subscription. | distribution-settings product-spec author, live 2026-07-06 (port 8000, `ojs_test`, PostgreSQL, scratch journal `dst1` id 2, manager dbarnes session + CSRF): UI select enumerated Disabled + exactly 1–60 months; `PUT api/v1/contexts/2` `{delayedOpenAccessDuration: 999}` → **200** (stored), `{delayedOpenAccessDuration: -5}` → **200** (stored); restored to 12 afterwards. Contrast the sibling field on the same form: `{publishingMode: 7}` → **400** "The selected publishing mode is invalid." (schema `in:0,1,2` IS enforced). **Verifier re-drive 2026-07-06** (fresh scratch `j-dsva` id 15): 999 → 200, -5 → 200 (stored), mode 7 → 400 all reconfirmed; **consequence proven end-to-end** — with -5 stored, publishing an issue through the grid stamped `accessStatus=subscription` + `openAccessDate=2025-02-06` (the signed floor/fmod month arithmetic turns -5 into **-17 months**, not -5) and an ANONYMOUS reader was served its PDF galley (200), while a sibling issue published under +12 correctly bounced to login (`reader.subscriptionRequiredLoginText`). Bounded: **not retroactive** — with -5 stored, an already-embargoed issue keeps its stamped future date and stays gated (live); the harm crystallizes only at the next issue publish, and only a manager-credentialed API client can store the value (the UI select cannot). Severity stays LOW; no separate row. | `classes/components/forms/context/AccessForm.php` (`SUBSCRIPTION_OPEN_ACCESS_DELAY_MIN/MAX`, select options); `schemas/context.json` `delayedOpenAccessDuration` (integer, nullable, no bounds); `classes/controllers/grid/issues/IssueGridHandler.php` `publishIssue()` (delay arithmetic) | Upstream candidate: add `min:0` + `max:60` to the schema field (mirroring the form), or widen the UI if longer embargoes are intended. Non-blocking. |
| 127 | **The decision wizard offers section editors a "Find Template" search box whose backing API always denies them — typing in it pops an "Error: You are not authorized to access the requested resource." modal** (LOW–MEDIUM: a visible, interactive control on a core editorial flow that can never work for one of its primary roles; the preloaded default template still works, so the send itself is unharmed). Mechanism: `PKPEmailTemplateController` names SUB_EDITOR + ASSISTANT in its read-route role list, but `authorize()` adds `CanAccessSettingsPolicy` (admin or manager-with-settings only), which vetoes them — the role-list entries are dead letter and `GET /emailTemplates` is manager/admin-only in practice. Meanwhile `DecisionHandler::record()` puts `emailTemplatesApiUrl` in the page state for every authorized decision-taker (section editors included), and `Composer.vue` renders the Email-Templates search box whenever that URL is set, firing `GET ?searchPhrase=` on input → 401 → `ajaxErrorCallback` modal. Same endpoint backs the search box on the request-author-response page. | email-templates-management product-spec author, live 2026-07-06 (port 8000, `ojs_test`, PostgreSQL, scratch journal `etm1` id 177): Playwright-driven browser as seeded section editor `etmsec1` on `decision/record/469?decision=2&reviewRoundId=108` — email step shows "Email Templates / Find Template" searchbox; typing `invitation` fired `GET /etm1/api/v1/emailTemplates?searchPhrase=reviewinvitation` → **401** (network log) and the Error modal appeared (snapshot); results list stayed empty. Same 401 for `dbuskins` (section editor) on publicknowledge via curl; `GET /mailables` also 401 (that role list is manager/admin by design). Managers: same box works (200). | `lib/pkp/api/v1/emailTemplates/PKPEmailTemplateController.php` `getGroupRoutes()` (read group role list) + `authorize()` (`CanAccessSettingsPolicy`); `lib/pkp/pages/decision/DecisionHandler.php` `record()` (`emailTemplatesApiUrl` state); `lib/ui-library/src/components/Composer/Composer.vue` (`v-if="emailTemplatesApiUrl"` search + `search()` ajax) | Upstream candidate — pick a side: exempt the GET routes from the settings policy (letting composer users search accessible templates; the access filter in `getMany()` already scopes results per user), or stop passing `emailTemplatesApiUrl` to non-manager composer surfaces so the box hides. Spec: `docs/product/specs/email-templates-management.md` Known deviations. Non-blocking. |
| 128 | **Saving an email template without the access fields silently wipes its role-access settings — the unrestricted marker and all group assignments are deleted, leaving the template visible to managers/admins only** (LOW today, data-corrupting in shape: stored configuration is destroyed by an unrelated edit; currently masked because the affected templates have no non-manager consumers). Mechanism: `PKPEmailTemplateController::edit()` coerces a missing `assignedUserGroupIds` to `[]` and always calls `setEmailTemplateAccess()`; `Repository::updateTemplateAccessGroups()` runs `whereNotIn('user_group_id', [])->delete()` — an empty NOT-IN matches every row, including the NULL row that encodes "unrestricted" — and `isUnrestricted` arrives null so nothing re-marks it. Reach: API-only for role-assignable mailables (the Edit-Template modal always submits the radio there), but for NON-assignable mailables (system-sent or group "Other": Password Reset, statistics report, …) `ManageEmailsPage.setCurrentTemplateForm()` strips both access fields by design, so **every ordinary UI edit of those templates performs the wipe**. Reset All heals (re-seeds registry defaults); a single-template Reset does not touch access. | email-templates-management product-spec author, live 2026-07-06 (scratch journal `etm1` id 177, manager dbarnes session + CSRF): (a) body-only `PUT /etm1/api/v1/emailTemplates/REVIEW_REQUEST` → 200, `email_template_user_group_access` NULL row for the key GONE (psql before/after), and seeded section editor `etmsec1`'s Add-Reviewer template dropdown shrank to [alternate, REVIEW_REQUEST_SUBSEQUENT] — the default template vanished for them while dbarnes kept all 3; `PUT {"isUnrestricted":true}` restored row + dropdown. (b) UI-shape payload (name/subject/body, exactly what the stripped modal submits) on `PASSWORD_RESET_CONFIRM` → 200, NULL row gone, template reports `isUnrestricted:false` with no groups. (c) `DELETE /emailTemplates/restoreDefaults` re-created the unrestricted marker. **Verifier re-drive 2026-07-06 (real UI, scratch `etv*`, dbarnes):** drove the *actual* Password Reset Confirm side modal — the `isUnrestricted` radio is absent (0 in DOM), and a body-only Save through the real editor deleted the NULL marker (psql 1→0), confirming the every-UI-edit wipe on non-assignable mailables end-to-end (not just via a synthetic payload). Counter-drive on the impactful path: an *assignable* alternate restricted to Author, then a body-only edit through the real modal, kept `isUnrestricted:false` + `assignedUserGroupIds:[Author]` intact (the modal resubmits the radio+checkboxes) — so the consequential "template becomes manager-only / who-may-compose changes" outcome is **not UI-reachable**, only via an API PUT that omits the fields. **Severity stays LOW** (the UI-reachable wipe hits only consumer-less non-assignable templates; the consequential wipe is API-only). | `lib/pkp/api/v1/emailTemplates/PKPEmailTemplateController.php` `edit()` (`$params['assignedUserGroupIds'] = $params['assignedUserGroupIds'] ?: [];`); `lib/pkp/classes/emailTemplate/Repository.php` `updateTemplateAccessGroups()` (empty `whereNotIn` deletes all) + `setEmailTemplateAccess()`; `lib/ui-library/src/components/Container/ManageEmailsPage.vue` `setCurrentTemplateForm()` (strips `assignedUserGroupIds`/`isUnrestricted` when `!canAssignUserGroupToTemplates`) | Upstream candidate: in `edit()`, skip access updates when neither field is present (treat absent ≠ empty), or make `updateTemplateAccessGroups()` never delete the NULL marker row (it encodes unrestrictedness, not a group). Spec: email-templates-management rule 7. Non-blocking. |
| 129 | **Manage Emails cosmetic pair** (LOW, two one-liners): (a) clicking **"Add Template"** on a mailable opens a side modal titled **"Edit Template"** — `ManageEmailsPage.openTemplate()` does `template = template || {}` before the `this.currentTemplate ? editTemplate : addEmail` ternary, so the add-title branch (`manager.emails.addEmail`) is unreachable; (b) the catalogue's per-mailable **Edit button label is hardcoded English** — `manageEmails.tpl` renders `<span aria-hidden="true">Edit</span>` as a literal (only the screen-reader span uses `common.editItem`), so non-English UIs still show "Edit". | email-templates-management product-spec author, live 2026-07-06 (scratch `etm1`, Playwright as dbarnes): Discussion (Submission) → "Add Template" → modal heading "Edit Template" (snapshot); list snapshot shows visible generic "Edit" + SR-only "Edit Discussion (Submission)". | `lib/ui-library/src/components/Container/ManageEmailsPage.vue` `openTemplate()`; `lib/pkp/templates/management/manageEmails.tpl` (aria-hidden literal) | Upstream candidates: title on `template?.key` (or pass an isNew flag); `{translate key="common.edit"}` for the visible span. Non-blocking. |
| 130 | **The navigation-menus REST API omits the settings gate — a manager stripped of *Permit settings* keeps full menu read/create/edit via `api/v1/navigationMenus` while the Settings page and both legacy grids refuse them** (LOW–MEDIUM: journal-level configuration writable by a principal every UI surface denies; same shape as row 124, but narrower — the role list is SITE_ADMIN+MANAGER only, so no sub-editor exposure). Mechanism: `PKPNavigationMenuController::getRouteGroupMiddleware()` is `roleAuthorizer([SITE_ADMIN, MANAGER])` and `authorize()` adds only `UserRolesRequiredPolicy` + role-op policies — no `CanAccessSettingsPolicy` — while `NavigationMenusGridHandler::authorize()`/`NavigationMenuItemsGridHandler::authorize()` and `ManagementHandler::authorize()` all carry it. All 5 routes affected (getAllItems/getAreas/getItems/add/edit — menu delete is grid-only and therefore correctly gated, an inconsistency of its own). | navigation-menus product-spec author, live 2026-07-06 (port 8000, `ojs_test`, PostgreSQL, scratch journal `nav1` id 388, dbarnes sole manager group `permit_settings` flipped 1→0 via psql, restored after): settings page → 302 `roleBasedAccessDenied`; menus-grid `fetch-grid` → "Access denied."; but `GET api/v1/navigationMenus/areas` → **200** and `PUT api/v1/navigationMenus/778` `{"title":"Stripped Probe Renamed"}` → **200** (stored). Control: section editor `navsec1` → 401 on the API (role list holds). **Adversarial verifier re-drive 2026-07-06** (scratch `nmvprobe` id 417, permit flipped 1→0 via psql then restored): page → 302 `roleBasedAccessDenied`, menus-grid → `status:false`/"Access denied.", but `GET items` 200 / `POST` 201 / `PUT` 200 — and **bounded**: section editor `dbuskins` → 401 on all five routes, and dbarnes (manager of `nmvprobe`, not of `nmvother` id 418) → 401 cross-context on `nmvother`'s API, so the hole is SITE_ADMIN+MANAGER + same-journal only. Stays ⚠. | `lib/pkp/api/v1/navigationMenus/PKPNavigationMenuController.php` `getRouteGroupMiddleware()` + `authorize()`; contrast `lib/pkp/controllers/grid/navigationMenus/NavigationMenusGridHandler.php` `authorize()` (`CanAccessSettingsPolicy`) | Upstream candidate: add `CanAccessSettingsPolicy` to the controller's `authorize()` (mirroring the grids). Spec: `docs/product/specs/navigation-menus.md` Known deviations. Non-blocking. |
| 131 | **The four OJS menu-item types (Current Issue, Archives, Subscriptions, My Subscriptions) are invisible to the navigation-menus API/Vue-editor surface — a Subscriptions item shows no conditional-display warning in the menu editor although the front end hides it** (LOW: editor misinformation only; front-end rendering is correct). Mechanism: the OJS types register via `Hook::add('NavigationMenus::itemTypes'/'::displaySettings', …)` in `APP\services\NavigationMenuService::__construct()`, which only runs when the `'navigationMenu'` app-container singleton is resolved (template paths do; e.g. the legacy item form's type dropdown correctly lists all 19 types). The REST controller and `NavigationMenuItemResource` instead resolve `app(PKPNavigationMenuService::class)` — the pkp-lib base class, never the APP singleton — so in API requests the hooks are unregistered: the `itemTypes` payload lacks the 4 types, and per-item `conditionalWarning`/`isVisible` fall back to "no condition" for them. | navigation-menus product-spec author, live 2026-07-06 (scratch `nav1` id 388, dbarnes): `GET api/v1/navigationMenus/777/items` → `itemTypes` has 15 keys, **no** `NMI_TYPE_{CURRENT,ARCHIVES,SUBSCRIPTIONS,MY_SUBSCRIPTIONS}`; a grid-created `NMI_TYPE_SUBSCRIPTIONS` item ("Subscriptions Probe") comes back `isVisible:true, conditionalWarning:null` (the OJS service defines `manager.navigationMenus.subscriptions.conditionalWarning`) while the sibling Announcements item carries its warning text; Playwright: the menu-editor modal shows warning icons on Announcements/Login/Register/etc. but **none** on "Subscriptions Probe" (payments disabled on nav1, so the front end would hide it). | `classes/services/NavigationMenuService.php` `__construct()` (hooks) + `classes/core/AppServiceProvider.php` (`'navigationMenu'` singleton); `lib/pkp/api/v1/navigationMenus/PKPNavigationMenuController.php` + `lib/pkp/classes/navigationMenu/resources/NavigationMenuItemResource.php` (`app(PKPNavigationMenuService::class)`) | Upstream candidate: bind `PKPNavigationMenuService::class` to the app's `'navigationMenu'` singleton (one alias in the service provider), or resolve `app()->get('navigationMenu')` in the controller/resource. Spec: navigation-menus Known deviations. Non-blocking. |
| 132 | **Navigation-menu editor guidance trio** (LOW, cosmetic/edge): (a) the About / About-the-Journal items warn "This link will only be displayed if you have filled out the About the Journal section" but **no such hide-rule exists** — `PKPNavigationMenuService::getDisplayStatus()`'s hide-switch has no `NMI_TYPE_ABOUT` case (live: About renders on a journal with no `about` setting); (b) the Administration item's warning says only "when the visitor is logged in", omitting the real second condition (site admin — live: logged-in manager does **not** see it); (c) an unauthenticated POST to `navigationMenu/preview` dies with a raw **500** (`Exception('The current user is not permitted to preview.')`) instead of a tidy denial — no UI path reaches it (the Preview button lives inside the manager-only item modal). | navigation-menus product-spec author, live 2026-07-06 (scratch `nav1`): editor-modal snapshot shows both warning texts; `journal_settings` for 388 has no `about` row yet the anonymous header shows About + children; dbarnes vs admin user-menu diff (Administration admin-only); anonymous `POST /nav1/navigationMenu/preview` → 500, dbarnes → 200 rendering the unsaved content. **Adversarial verifier re-drive 2026-07-06** (scratch `nmvprobe`): anonymous **GET and POST** to `navigationMenu/preview` both → **500 with a 0-byte body** (no stack trace / message leak), CSRF-gate-free but UI-unreachable — a robustness blemish, **not** an info-disclosure. The API `isVisible` flag is derived *solely* from whether the type owns a `conditionalWarning` string (not the real render state), which is exactly why About reports `isVisible:false` yet always renders (132a). All three sub-findings are LOW; downgraded from ⚠ to plain notes in the spec. | `lib/pkp/classes/services/PKPNavigationMenuService.php` `getMenuItemTypes()` (`manager.navigationMenus.about.conditionalWarning`) vs `getDisplayStatus()` (no ABOUT hide case; ADMINISTRATION case requires site-admin); `lib/pkp/classes/navigationMenu/resources/NavigationMenuItemResource.php` (`isVisible` from `conditionalWarning` presence); `lib/pkp/pages/navigationMenu/NavigationMenuItemHandler.php` `preview()` (bare `throw new Exception`) | Upstream candidates: drop/reword the two warning strings (or reinstate an About hide-rule if that was the intent); make `preview()` throw a proper authorization denial. Non-blocking. |
| 133 | **Menu-tree nesting depth (2) is client-side only — the API stores arbitrarily deep trees whose 3rd-and-deeper levels the reader header silently never renders** (LOW; family of rows 119/125/126, client-only bounds). The drag-drop editor blocks nesting beyond `navigationMenuMaxDepth` (config `interface.navigation_menu_max_depth`, default `PKPNavigationMenuController::DEFAULT_MAX_DEPTH = 2`), but `PUT navigationMenus/{id}` `menuTree` accepts any `parentId` chain — `saveMenuTreeAssignments()` has no depth check — and `frontend/components/navigationMenu.tpl` renders exactly two levels, so a stored grandchild is invisible everywhere except the DB and the editor payload. | navigation-menus product-spec author, live 2026-07-06 (scratch `nav1`, dbarnes + CSRF): `PUT api/v1/navigationMenus/777` with a 3-level chain (6604 → 6605 → 6603) → **200**, all three assignment rows stored (psql); anonymous header rendered the parent and child but **not** the grandchild ("Search" absent); tree restored to 2 levels afterwards. | `lib/pkp/classes/services/PKPNavigationMenuService.php` `saveMenuTreeAssignments()` (no depth validation); `lib/ui-library/src/components/NavigationMenuEditor/useNavigationMenuEditorDragDrop.js` (client guard); `lib/pkp/templates/frontend/components/navigationMenu.tpl` (two-level markup) | Upstream candidate: validate `menuTree` depth against the configured max in the controller (or flatten extra levels on save). Spec: navigation-menus Known deviations. Non-blocking. |
| 134 | **Locale key `common.help` does not exist — every backend page's Help button screen-reader label renders `##common.help##`** (LOW, app-wide a11y cosmetic; found while probing nav-menu modals, not nav-specific). `TopNavActions.vue` renders `{{ t('common.help') }}` as the Help link's `-screenReader` span, but no `en` .po in either repo defines `common.help`, so the missing-key marker is the accessible name everywhere the backend top-nav (incl. side-modal headers) renders. | navigation-menus product-spec author, live 2026-07-06: Playwright accessibility snapshot of the Add-item modal on `nav1` shows `link "##common.help##"`; `grep -rn 'msgid "common.help"' locale/en lib/pkp/locale/en` → zero hits; sole consumer `lib/ui-library/src/components/TopNavActions/TopNavActions.vue`. | `lib/ui-library/src/components/TopNavActions/TopNavActions.vue` (`t('common.help')`) | Upstream candidate: add the `common.help` key (or reuse `help.help`/an existing label). Non-blocking. |
| 135 | **Section/category editor auto-assignment is silently dead on every journal after the first** (HIGH for multi-journal installs). `SubEditorsDAO::assignEditors()` filters candidate assignments with `$userGroupIds = $userGroups->keys()` — the Eloquent collection's 0..n-1 *indexes*, not the group ids — then `$userGroupIds->contains($assignment->userGroupId)`. A journal's ~18 default groups give indexes 0-17, so the filter passes only group ids ≤ 17 — precisely: ids below the journal's group COUNT, so even a group added later to the FIRST journal (id ≥ 18) is dropped there too. On the install's first journal (publicknowledge ids 2-19) the four submission-stage-assignable default groups (Editor 3, Section editor 5, Guest editor 6, Funding coordinator 9) all pass by coincidence; on any later journal (ids 20+, e.g. scratch journal 2 ids 20-37) EVERY auto-assignment is discarded, the configured section editor is never stage-assigned, and managers get the NOTIFICATION_TYPE_EDITOR_ASSIGNMENT_REQUIRED fallback + SubmissionNeedsEditor mail instead. BOUNDS: the section→editor CONFIGURATION is intact (grid Editors column + `subeditor_submission_group` rows correct) — only submit-time execution dies; category auto-assignments pass the same dead filter; `assignEditors()`'s sole caller is the `SubmissionSubmitted` listener, so manual stage-participant assignment is untouched. Severity framing: silent degradation, NOT data loss — every affected submission still lands in the managers' "Needs editor" view via the fallback, but section-based routing (a headline feature) is dead without any error. | sections product-spec author, live 2026-07-06: scratch journal `secprobe` (context 2), dbuskins configured as section editor of "Open Studies" (subeditor_submission_group row present, user in group 23); scenario-submitted submission 1 → stage_assignments holds ONLY the author row; notifications table shows type 16777247 (EDITOR_ASSIGNMENT_REQUIRED) to both managers. Positive control same session: identical submission on publicknowledge ART → dbuskins + sberardo (group 5 ≤ 17) auto-assigned. Verifier independently re-drove 2026-07-06 on fresh scratch `secv1neg` (context 38, group ids 668-685): subeditor row + group membership both present, scenario submit → only the author stage-assigned, type-16777247 fallback to both managers; positive control re-read (publicknowledge submission 4: dbuskins + sberardo group 5 assigned; dbarnes configured-but-not-in-group-5 correctly dropped by the separate `userInGroup` check, confirming the id filter is the only faulty leg). | `lib/pkp/classes/context/SubEditorsDAO.php` `assignEditors()` (`$userGroups->keys()`); `lib/pkp/classes/observers/listeners/AssignEditors.php` | Upstream fix candidate: `$userGroups->pluck('id')` — verifier-confirmed correct: `UserGroup` uses `ModelWithSettings` whose `id` accessor maps to `$primaryKey` = `user_group_id`, and `assignEditors()` itself already relies on `$userGroup->id` (`modelKeys()` equivalent). Spec: sections rule 13 ⚠. |
| 136 | **Section form's two "Will not be …" checkboxes are inert on a default install** (LOW, dead affordance). `metaIndexed` ("Will not be included in the indexing of the journal") has ZERO consumers in 3.6 — grep across both repos + plugins finds only the entity accessors, the form (stored inverted, #2066), the schema, and the native-XML round-trip; unchecking it changes nothing observable anywhere (no OAI/Google-Scholar/search/meta-tag consumer). `metaReviewed` ("Will not be peer-reviewed") has exactly one consumer, the Publication Facts Label plugin (`PflPlugin::displayArticlePfl()` + its stats SQL), which ships disabled — so by default it too has no visible effect. | sections product-spec author 2026-07-06, code sweep (subagent grep of `metaIndexed/meta_indexed/getMetaIndexed` and `metaReviewed` across ojs + lib/pkp + plugins). | `templates/controllers/grid/settings/sections/form/sectionForm.tpl` (labels `manager.sections.submissionIndexing` / `manager.sections.submissionReview`); `classes/section/Section.php`; `plugins/generic/pflPlugin/PflPlugin.php` (sole metaReviewed consumer) | Upstream candidates: drop the metaIndexed checkbox or wire a consumer; note metaReviewed's PFL-only reach in the form help. Spec: sections rule 14 ⚠ + OQ3. |
| 137 | **Deleting a section orphans its subeditor_submission_group rows** (LOW, data hygiene). `subeditor_submission_group.assoc_id` has no FK (only `context_id`/`user_id`/`user_group_id` cascade), and the section delete path (`SectionGridHandler::deleteSection()` → `Repo::section()->delete()` → `EntityDAO::_delete`, plus hooks with no subscriber) never calls `SubEditorsDAO::deleteBySubmissionGroupId()` — that cleanup runs only when a section's editor list is *edited* (`PKPSectionForm::execute()`). Rows pointing at deleted sections accumulate invisibly. CORRECTION (verifier 2026-07-06): category delete has the SAME gap — `category/Repository.php` `delete()` never calls the cleanup either; the `deleteBySubmissionGroupId()` call at `Repository.php:334` sits in `updateEditors()` (the edit path, mirroring `PKPSectionForm::execute()`), so BOTH section and category deletion orphan subeditor rows. | sections product-spec author 2026-07-06, code-verified (migrations + delete path + caller grep); verifier live-drove 2026-07-06 on scratch `secv2orph` (context 39): grid-deleted empty section 106 with a configured editor → `sections` + `section_settings` rows gone (cascade), `subeditor_submission_group` row (assoc_id 106) remains orphaned. Category leg live-confirmed by the categories spec author 2026-07-06 on scratch `cat1` (context 222): manager-UI delete of category 17 (with assigned editor on 17 + 18) removed the whole category subtree but left both `subeditor_submission_group` rows (assoc_type 525, assoc_id 17/18) orphaned. | `lib/pkp/classes/migration/install/SubmissionsMigration.php` (`subeditor_submission_group`, no assoc FK); `controllers/grid/settings/sections/SectionGridHandler.php` `deleteSection()`; `lib/pkp/controllers/grid/settings/sections/form/PKPSectionForm.php` `execute()` | Upstream candidate: `deleteBySubmissionGroupId()` cleanup in BOTH section and category Repository::delete() (mirroring their edit paths). Spec: sections rule 7 note; categories rule 10 note. |
| 138 | **`POST /categories` without a `title` key creates a broken category that then 500-bricks the whole Categories manager** (MEDIUM blast radius, API-only trigger, no data loss). The category API pre-creates the entity object before validating (`CategoryCategoryController::saveCategory()` → `Repo::category()->validate($category, …)`), and `ValidatorFactory::required()` skips required-prop enforcement for props *absent* from the payload when a non-null `$object` is passed — so a POST that wholly **omits** `title` (schema-required) passes validation, inserts a title-less `categories` row (zero `category_settings`), and then 500s serializing its own response (`Category::getLocalizedTitle(): Return value must be of type string, null returned`). From then on **every** `GET /categories` — including the Settings → Journal → Categories tab's own fetch — throws the same 500 and the manager renders an empty table, until the broken row is deleted by id (DELETE by id still works). An **empty-string** title (`title: {en: ''}`) is correctly refused 400 "This field is required", which is the only shape the UI form can send — so the trigger is API-only, but the blast radius is the whole management surface. **BOUND (adversarial verifier 2026-07-06):** `title` is the ONLY omitted-required-prop that persists a bricking row. Omitting `path` (also schema-required) passes the same validation gap but 500s at `setTitle`/`setPath()` (`Argument #1 must be of type string, null given`) *before* the row is inserted — so it is a transient, self-contained 500 with no persisted row and no tab-wide brick (`GET /categories` stays healthy after). Only `title` slips all the way to insert-then-serialize because the pre-set title lands via `setTitle(null, null)` (multilingual setter tolerates null) and only blows up later at `getLocalizedTitle()` read time. | categories product-spec author 2026-07-06, live on scratch `cat1` (context 222, port 8000, Postgres): `POST {path:'notitle'}` (no title key) → 500 + row `category_id=22` inserted with no settings; subsequent `GET /categories` → 500, Categories tab rendered 0 rows; `DELETE /categories/22` → 200 and the list recovered; control `POST {title:{en:''},…}` → 400 required-field error. **Verifier re-drove 2026-07-06** on scratch `catv33157` (context 253): `POST {path:'notitle138'}` → 500 + row 78 inserted (no settings), `GET /categories` → 500 tab-wide, `DELETE /categories/78` → 200 recovered; bound probes: `POST {title:{en:'…'}}` (omit path) → 500 at `setPath()` with NO row inserted + tree GET healthy; `POST {title:{en:''},path:…}` → 400. | `lib/pkp/api/v1/categories/CategoryCategoryController.php` `saveCategory()` (validate-then-set flow); `lib/pkp/classes/category/Repository.php` `validate()`; `lib/pkp/classes/validation/ValidatorFactory.php` `required()` (object-present skip); `lib/pkp/classes/category/maps/Schema.php` `mapByProperties()` `localizedTitle`; `lib/pkp/classes/category/Category.php` `getLocalizedTitle(): string` | Upstream candidates (either closes it): enforce required props on create regardless of the pre-created object (pass `null` to validate on add, as other controllers do), or harden `getLocalizedTitle()`/the schema map against a missing title. Spec: categories rule 12. |
| 139 | **`GET /categories/categoryFormComponent` is a registered API route with no handler — always 500** (LOW, dead route). `CategoryCategoryController::getGroupRoutes()` registers `Route::get('categoryFormComponent', $this->getCategoryFormComponent(...))` but no such method exists anywhere in lib/pkp (Laravel's `Controller::__call` turns the invocation into a 500 "Method … does not exist"). No frontend caller exists either — the Vue CategoryManager receives its form config via `ManagementHandler::context()` `pageInitConfig.categoryForm`, and grep finds no `categoryFormComponent` reference in lib/ui-library or js/. Vestige of an abandoned deliver-form-over-API design. | categories product-spec author 2026-07-06, live on scratch `cat1`: authenticated manager `GET /api/v1/cat1/categories/categoryFormComponent` → 500 `{"error":"Method PKP\\API\\v1\\categories\\CategoryCategoryController::getCategoryFormComponent does not exist."}`; grep: zero frontend references, zero method definitions. | `lib/pkp/api/v1/categories/CategoryCategoryController.php` `getGroupRoutes()`; `lib/pkp/pages/management/ManagementHandler.php` `context()` (the live form-delivery path) | Upstream: delete the route (or implement the method if the API-delivered form is still planned). Atlas atom API-category-get-category-form-component marked dead-route in the categories spec. |
| 140 | **Category tree expand/collapse toggle never updates its accessible label** (LOW, cosmetic/a11y). Two stacked defects cancel into a constant label: `TableCellTreeExpand.vue` computes the label from `props.isExpanded.value` — but `isExpanded` is a plain Boolean prop, so `.value` is always `undefined`/falsy and the collapse branch never fires; AND `CategoryTreeRow.vue` passes the two label props swapped (`:expand-label="t('manager.category.collapseSubcategories')"` / `:collapse-label="t('manager.category.expandSubcategories')"`). Net: the button always reads "Expand sub-categories" (the swapped collapse-label) whether the row is expanded or not; only the chevron icon (Dropup/Dropdown, computed correctly from the same prop *without* `.value`) reflects state. Screen-reader users get a wrong/static action label. | categories product-spec author 2026-07-06, live on scratch `cat1`: Physics row expanded (children visible, icon flipped) still exposes the accessible name "Expand sub-categories"; code inspection of both components. | `lib/ui-library/src/components/Table/TableCellTreeExpand.vue` (`props.isExpanded.value` in the label computed); `lib/ui-library/src/managers/CategoryManager/CategoryTreeRow.vue` (swapped `expand-label`/`collapse-label` bindings) | Upstream: drop `.value` in the computed AND un-swap the two bindings (fixing only one of the two inverts the label). Spec: categories Known deviations. |
| 141 | **Deleting an announcement type silently deletes every announcement carrying that type — behind the GENERIC delete confirm, while the dedicated warning string sits unused in the locale file** (MEDIUM: multi-record data loss from a one-row-looking action; strong intent evidence the warning was meant to show). Mechanism: `AnnouncementTypeDAO::deleteById()` runs `Announcement::withTypeIds([$typeId])->delete()` before dropping the type — an explicit code-level cascade even though the DB schema says `announcements.type_id … onDelete('set null')` (two conflicting intents in the same feature). The grid's confirm modal uses `__('common.confirmDelete')` ("Are you sure you wish to delete this item? This action cannot be undone.") — `manager.announcementTypes.confirmDelete` ("Warning! All announcements with this announcement type will also be deleted.") exists in `manager.po` with ZERO consumers. Side gap: the bulk Eloquent delete bypasses `Announcement::delete()`, so announcement image files are orphaned in public files (settings rows go via FK cascade). | announcements product-spec author, live 2026-07-06 (port 8000, `ojs_test`, PostgreSQL, scratch journal `ann1` id 2, dbarnes): type "Call for Papers" (type_id 1) + announcement 2 ("Ann Two Typed", type_id 1) + announcement 1 (untyped); grid Remove → modal text captured verbatim = generic confirm; OK → `announcement_types` empty AND announcement 2 + its `announcement_settings` gone; untyped announcement 1 untouched. | `lib/pkp/classes/announcement/AnnouncementTypeDAO.php` `deleteById()`; `lib/pkp/controllers/grid/announcements/AnnouncementTypeGridRow.php` (`common.confirmDelete`); `lib/pkp/locale/en/manager.po` `manager.announcementTypes.confirmDelete` (orphaned); `lib/pkp/classes/migration/install/AnnouncementsMigration.php` (`set null` FK) | Upstream candidates: EITHER stop cascading (set type_id null, matching the FK) OR wire the warning string into the confirm modal; if the cascade stays, delete via model instances so images are cleaned. Spec: `docs/product/specs/announcements.md` rule 7 ⚠. Non-blocking. |
| 142 | **Editing an announcement with a failed image upload DELETES the announcement — the user sees only an image-field error while the record is gone** (MEDIUM: silent data loss on the edit path; the code carries a TODO doubting itself). Mechanism: `PKPAnnouncementController::edit()` catches `StoreTemporaryFileException` (thrown by `Announcement::handleImageUpload()` when the temporary file is not a valid image / extension-content mismatch) with `$announcement->delete(); // TODO do we really need to delete an announcement if the image upload fails?` and returns 400 `api.400.errorUploadingImage`. Contrast `add()`, where destroying the just-created id is a legitimate rollback. UI reachability: the form's image field accepts a picked file client-side; a corrupt/mis-extensioned image fails `isValidImage()` server-side → same path. | announcements product-spec author, live 2026-07-06 (scratch `ann1` id 2, dbarnes session): POST `temporaryFiles` with `notimage.txt` → 200 id 1; `PUT api/v1/announcements/1` `{title, image:{temporaryFileId:1}}` → **400** `{"image":["There was an error uploading this image."]}` AND `announcements`/`announcement_settings` rows for id 1 **deleted** (counts 0). | `lib/pkp/api/v1/announcements/PKPAnnouncementController.php` `edit()` catch block; `lib/pkp/classes/announcement/Announcement.php` `handleImageUpload()`/`isValidImage()` | Upstream candidate: on edit-failure keep the record and drop only the pending image attribute (revert to `fresh()` state), mirroring what the TODO suspects. Spec: announcements rule 9 ⚠. Non-blocking. |
| 143 | **Announcement blemish trio** (LOW): (a) the "Send Email" checkbox renders on the announcement **edit** modal but `edit()` never calls `notifyUsers()` — the control is inert everywhere except create (dead affordance, no data harm); (b) `Announcement::fullTitle()` is broken twice over and therefore INERT: the guard `isset($attributes['typeId'])` tests the camelCase key against Eloquent's RAW attribute bag, where the column is snake_case `type_id` — always false, so the compose branch is UNREACHABLE and fullTitle always returns the bare title (no PHP warning ever fires; the `$typeName . ': ' . $title` array-concat that would print "Array: {title}" is dead code behind the dead guard). Correction to the original claim (verifier 2026-07-06): fullTitle DOES have live consumers — the announcementFeed templates (`atom.tpl`/`rss.tpl`/`rss2.tpl` item `<title>` = `getLocalizedData('fullTitle')`) — but they safely receive the plain title (live: typed announcement on scratch `annv30fa6d0` with an en type name, feed titles plain, no "Array:"). Net user effect unchanged: the type label appears NOWHERE reader-side; (c) in-app new-announcement notification rows are never cleaned when their announcement is deleted (their links resolve to the reader-list redirect). | announcements product-spec author 2026-07-06: (a) edit-modal snapshot shows the checkbox, `PKPAnnouncementController::edit()` has no notify path; (b) code inspection + live typed-announcement detail page rendered plain title, no "Array:" visible anywhere, `grep announcementTitle` across frontend templates → zero consumers; (c) psql: notification rows for announcements 1/2 survive both deletions — verifier note: cleanup would be impossible as-built, the rows carry NO reference to the announcement at all (`AnnouncementNotificationManager::notify()` passes the journal id as contextId and null assocType/assocId; only a title snapshot lives in the `contents` setting), so "dangling" means context-scoped rows with a stale title, and it is the EMAIL's link (not the row) that lands on the reader-list redirect. | `lib/pkp/classes/components/forms/announcement/PKPAnnouncementForm.php` (`sendEmail` field, form reused for edit); `lib/pkp/api/v1/announcements/PKPAnnouncementController.php` `add()` vs `edit()`; `lib/pkp/classes/announcement/Announcement.php` `fullTitle()` (dead `typeId` guard); `plugins/generic/announcementFeed/templates/*.tpl` (fullTitle consumers); `lib/pkp/classes/notification/managerDelegate/AnnouncementNotificationManager.php` `notify()` | Upstream candidates: hide `sendEmail` on edit (or honor it); fix the `type_id` guard + `$typeName[$locale]` concat together and decide whether fullTitle should surface; give announcement notifications an assoc so they can be cleaned. Spec: announcements Known deviations row 143. Non-blocking. |
| 144 | **Editing (or creating) an announcement with a dangling image `temporaryFileId` throws an unhandled 500 and, on edit, persists a garbage `image` setting** (LOW, robustness — API-shaped input; the UI normally uploads first, but a temp file purged by the cleanup task before Save reproduces it). Mechanism: `Announcement::handleImageUpload()` calls `TemporaryFileManager::getFile()` which returns null for an unknown/foreign/purged id; the null flows into `getImageFilename()` → `TypeError` (not `StoreTemporaryFileException`), so `edit()`'s catch never fires: 500 response, the announcement row SURVIVES (contrast row 142) but `parent::save()` already persisted the raw image param → `announcement_settings.image = 'Array'` (a PHP array-to-string artifact). Inert downstream: the API maps `image: null` and reader pages render normally. Also the ownership check means a foreign user's temp id cannot be attached (returns null → same 500, no hijack). | announcements adversarial verifier 2026-07-06 (scratch `annv1f62ac5` id 39, dbarnes): `PUT api/v1/announcements/55` `{image:{temporaryFileId:9999999}}` → **500** `Announcement::getImageFilename(): Argument #1 ($temporaryFile) must be of type PKP\file\TemporaryFile, null given`; GET 55 → 200, row intact; psql: `announcement_settings` for 55 shows `image | Array`; reader list/detail for 55 render fine. | `lib/pkp/classes/announcement/Announcement.php` `handleImageUpload()` (no null check after `getFile()`), `getImageFilename()`; `lib/pkp/api/v1/announcements/PKPAnnouncementController.php` `edit()` catch (only `StoreTemporaryFileException`) | Upstream candidate: treat a null temporary file as the same invalid-image failure (throw `StoreTemporaryFileException` or 400 early in `Repository::validate()`), and don't persist the raw image param before the upload is resolved. Spec: announcements Known deviations row 144. Non-blocking. |
| 145 | **Front-end Language block switch dumps the reader on the SITE INDEX when the request host/port doesn't match the app's index URL** (LOW — recalibrated from MEDIUM by adversarial verifier 2026-07-06: the locale DOES switch, only the return navigation is lost — no data loss; and in a *correctly-configured* production deploy the bug is largely self-defeating. On standard ports 80/443 `SERVER_NAME` has no port to drop; and with a real `allowed_hosts = ["myjournal.tld"]` the bad `SERVER_NAME`-only source is *rejected* by `isAllowedHost`, so `_setLocale` falls through to the `HTTP_REFERER` branch which carries the full correct URL. The site-index dump reproduces only on non-standard-port deployments OR under the permissive empty-string `allowed_hosts` of the test config. Real-world exposure is a dev/self-hosted annoyance, not a broad break.). Mechanism: `block.tpl` builds the return address as `$smarty.server.SERVER_NAME|cat:$smarty.server.REQUEST_URI` — `SERVER_NAME` carries no port and can differ from the Host header — and `PKPPageRouter::_setLocale()` only rewrites the path when the target starts with the app's index URL; on mismatch (`$replaceCount == 0`) it falls back to redirecting to `/index/{locale}`. With `allowed_hosts` blank-entry (`config.test.inc.php` uses `"[\"\"]"`, whose empty-string element prefix-matches EVERY host) the bad `source` is *accepted*, so the valid `HTTP_REFERER` fallback is never consulted. The backend switcher (`TopNavActions.vue`) passes full `document.URL` and returns to the exact page — the evident intended behavior. | languages-locales spec author 2026-07-06 (scratch `locspec3`; server `php -S 127.0.0.1:8000`, browsed as `localhost:8000`): sidebar-block "français" click → landed on `/index.php/index/fr_CA` (site index, French chrome); same op via curl with `source=localhost:8000/…` or referer-only → correct `/locspec3/fr_CA/index`; backend user-menu switch → same dashboard URL in French. | `plugins/blocks/languageToggle/templates/block.tpl` (`source=` param); `lib/pkp/classes/core/PKPPageRouter.php` `_setLocale()` fallback + `isAllowedHost()` (empty-string element matches all) | Upstream candidate: build `source` from `HTTP_HOST` (or drop it and rely on the referer), and reject empty-string entries in `allowed_hosts`. Spec: languages-locales Known deviations. Non-blocking. |
| 146 | **"Reload defaults" (overwrite a locale's journal settings with schema defaults) is manager-invokable by direct POST but only site admins get the UI control** (LOW, policy mismatch / API-permits-UI-hides). `LanguageGridRow::initialize()` renders the row expander + "Reload defaults" action only under `Validation::isSiteAdmin()`, yet `ManageLanguageGridHandler` role-assigns `reloadLocale` to `ROLE_ID_MANAGER` too — a crafted POST by a journal manager succeeds and silently overwrites that locale's journal texts (name-derived boilerplate, guidelines, …) with schema defaults. | languages-locales spec author 2026-07-06 (scratch `locspec3`): manager `locmgr1`'s fetch-grid HTML contains no `reload-locale` action; direct POST `…/$$$call$$$/grid/settings/languages/manage-language-grid/reload-locale` `rowId=en`+CSRF → `{"status":true,…,"events":[{"name":"dataChanged"…}]}`; site admin on the same journal (Administration → journal settings wizard → Languages) sees the "Reload defaults" row action. **Verifier re-verification 2026-07-06 (scratch `lcv48220` id 243, throwaway manager):** manager's `fetch-grid` HTML contains no `reload-locale` action AND no `show_extras` row expander at all; a sentinel `authorGuidelines.en` was set via the contexts API, then the manager's direct POST `reload-locale rowId=en` (CSRF) → `{"status":true,…,"events":[{"name":"dataChanged"…}]}` and re-reading the context showed `authorGuidelines.en` **overwritten with schema-default boilerplate while `authorGuidelines.fr_CA` was PRESERVED** — confirming the **blast radius**: only the *target locale's* slot of the 16 localized default-carrying context settings (pkp `authorGuidelines, authorInformation, beginSubmissionHelp, contributorsHelp, detailsHelp, forTheEditorsHelp, librarianInformation, openAccessPolicy, privacyStatement, readerInformation, reviewHelp, submissionChecklist, uploadFilesHelp` + OJS `clockssLicense, lockssLicense, reviewerSuggestionsHelp`) is reset to boilerplate; other locales, other settings, and other journals are untouched. **Severity confirmed LOW** (not MEDIUM): the manager already owns all of these settings via the UI, so this is self-inflicted, single-locale, own-journal-only data loss reachable only by a crafted POST — a UI-hides/API-permits *policy* inconsistency, not a privilege escalation or cross-tenant hole. `restoreLocaleDefaults` also calls `Locale::installLocale($locale)`, but for an already-installed locale that is idempotent (see de-install note under row 148). | `lib/pkp/controllers/grid/languages/LanguageGridRow.php` `initialize()` vs `lib/pkp/controllers/grid/settings/languages/ManageLanguageGridHandler.php` `__construct()`; `PKPContextService::restoreLocaleDefaults()` (per-prop `array_merge` on the target-locale slot) | Upstream: pick one — render the action for managers-with-settings too, or restrict the op to site admin. Spec: languages-locales Known deviations + Open question 1. Non-blocking. |
| 147 | **Broken interpolation placeholder in the fr_CA translation of the "must complete in primary language" validation error** (LOW, i18n/cosmetic). `lib/pkp/locale/fr_CA/common.po` key `form.requirePrimaryLocale` = `"Vous devez remplir ce champ en {$ language}."` — a stray space inside `{$ language}`. PKP's parameter substitution (`LocaleBundle::_format()`) does a literal `str_replace` searching for `"{$language}"` (no space), so the spaced token never matches and French users see the literal `{$ language}` instead of the language name (e.g. "français"). This is the required-in-primary-locale error surfaced on every multilingual settings form when the primary-locale field is left blank. Isolated to fr_CA: a scan of all 50+ bundled locales' `common.po` shows every other locale (incl. France-French `fr`) uses the correct `{$language}`. (Unrelated stray-space placeholders exist in other keys/locales — `{$ count}`/`{$ max}`/`{$ attribut}` in fr_CA:1255/2265, el, gl, mk, zh_Hans — same class of typo, out of this feature's scope, not filed individually.) | languages-locales adversarial verifier 2026-07-06: `lib/pkp/locale/fr_CA/common.po:1293` reads `msgstr "Vous devez remplir ce champ en {$ language}."` vs en `"You must complete this field in {$language}."`; `ValidatorFactory.php:208` calls `__('form.requirePrimaryLocale', ['language' => $primaryLocaleName])`; `LocaleBundle::_format()` maps the param key to the search token `"{\${$search}}"` = `"{$language}"`, which does not match the spaced token. The retained test (`languages-locales.spec.js` scenario 5) asserts the error loosely (`/You must complete this field in French|Vous devez remplir ce champ/`) — the French alternative is a prefix of the broken string AND of the fixed string, so the test survives the fix. | `lib/pkp/locale/fr_CA/common.po` (`form.requirePrimaryLocale`); `lib/pkp/classes/validation/ValidatorFactory.php` (`__('form.requirePrimaryLocale', …)`); `lib/pkp/classes/i18n/translation/LocaleBundle.php` `_format()` | Upstream: fix the fr_CA string to `{$language}` (translation repo). Spec: languages-locales Known deviations. Non-blocking. |
| 148 | **The context settings API silently orphans the primary locale — the "never disable the primary" guard is enforced only in the Languages grid, not in the schema-API `supportedLocales` validation** (LOW, API-permits/UI-guards). The Website Languages grid refuses to uncheck the primary row's UI/Forms box (`LanguageGridHandler::saveLanguageSetting()` guard — spec rule 3), but a manager's direct `PUT /{context}/api/v1/contexts/{id}` with `supportedLocales` that EXCLUDES the current `primaryLocale` is accepted (200) and persists an inconsistent state where `primaryLocale ∉ supportedLocales`. Asymmetric validation: the schema DOES validate membership when `primaryLocale` itself is set (setting `primaryLocale` to a non-supported locale → 400 "The primary locale must be one of the journal's supported locales."), but there is no cross-field re-check when `supportedLocales` is shrunk out from under an unchanged `primaryLocale`. Self-inflicted, own-journal, recoverable, no data loss. Ownership note: the contexts-API validation is `SCHEMA-context-pkp` / journal-masthead-settings territory; this is the grid guard (languages-locales rule 3) being reachable-around, not a languages-grid defect. | languages-locales adversarial verifier 2026-07-06 (scratch `lcv48220` id 243, throwaway manager): reset to `primary=en, supported=[en,fr_CA]`; `PUT …/contexts/243 {"supportedLocales":["fr_CA"]}` → **200**, re-read → `primaryLocale=en, supportedLocales=["fr_CA"]` (primary orphaned). Control: `PUT {"primaryLocale":"de"}` → **400** primary-must-be-supported. | contexts-API schema validation (`PKPContextService`/`SchemaService` validate rules for `supportedLocales`/`primaryLocale`); contrast `LanguageGridHandler::saveLanguageSetting()` guard block | Upstream candidate: add a cross-field validation rule that `primaryLocale ∈ supportedLocales` on any context update (or auto-keep the primary in supportedLocales), mirroring the grid guard. Spec: languages-locales Known deviations + journal-masthead-settings seam. Non-blocking. |

| 149 | **The site API never enforces the site's own required fields — `PKPSiteService::validate()` required-checks against the PUBLICATION schema's prop lists** (LOW-MEDIUM, API-permits/UI-guards + data-integrity). `ValidatorFactory::required()` is called with `getRequiredProps(SCHEMA_PUBLICATION)` / `getMultilingualProps(SCHEMA_PUBLICATION)` instead of `SCHEMA_SITE` (apparent copy-paste), so the site schema's required trio (`title`, `contactName`, `contactEmail`) is never required-validated on edit — a site admin's direct PUT can blank the site name and principal contact site-wide. The UI form still blocks (client-side "This field is required." refused an empty Site Name save, live), so it's API-reachable only; admin-only surface, self-inflicted, recoverable. Bonus observation, same file's caller: `PKPSiteController::edit()` types `REsponse::HTTP_BAD_REQUEST` — harmless (PHP class resolution is case-insensitive), cosmetic only. | site-settings product-spec author 2026-07-06, live on `:8000` as admin: `PUT api/v1/site {"contactName":{"en":""}}` → **200**, `site_settings.contactName[en]` persisted `''` (restored to "Open Journal Systems" immediately, DB-verified); UI control probe: Save with empty required Site Name → in-form refusal, no request. | `lib/pkp/classes/services/PKPSiteService.php` `validate()` (the two `SCHEMA_PUBLICATION` args); `lib/pkp/schemas/site.json` (`required: title/contactName/contactEmail`); `PKPSiteConfigForm`/`PKPSiteInformationForm` (`isRequired` client-side) | Upstream one-liner: pass `SCHEMA_SITE` to both args. Spec: site-settings Known deviations row 149. Non-blocking. |
| 150 | **Single-journal installs hide the only UI for the site's "Journal redirect" — the exact use case the field's own help text names** (LOW, UI-reachability, CODE-ANCHORED ONLY — not live-driven; the shared test site hosts 200+ journals and collapsing it is not probe-safe). `AdminHandler::siteSettingsAvailability()` gates the Site Setup → Settings tab (and Information/Appearance/etc.) on `getCount() !== 1`, so with exactly one journal the `PKPSiteConfigForm` — which carries `redirectContextId`, described as "useful if the site is hosting only a single journal" — is unreachable; the redirect becomes settable only by direct `PUT api/v1/site`. Nothing else auto-redirects a single-journal site index (`IndexHandler::index()` redirects only on the explicit setting). | site-settings product-spec author 2026-07-06: code anchors + live multi-journal state (all tabs render, redirect select offers enabled journals); single-journal state not reproducible in-env. | `lib/pkp/pages/admin/AdminHandler.php` `siteSettingsAvailability()`; `lib/pkp/templates/admin/settings.tpl` (`componentAvailability['siteConfig']`); `PKPSiteConfigForm` (`redirectContextId` + `admin.settings.redirectInstructions`); `pages/index/IndexHandler.php` | Intent call for maintainer: either keep the Settings tab visible on 1-journal sites or drop the redirect's single-journal pitch. Spec: site-settings Known deviations row 150 + Open question 1. Non-blocking. |
| 151 | **Site-settings blemish pair** (LOW): (a) `PKPSiteController::getTheme()` misses `return` before its theme-unavailable `response()->json(..., 404)`, so execution falls through to `$activeTheme->getOptionValues()` on null → 500 fatal instead of the intended 404 (also note the fall-through's `$theme->getDirName()` uses the loop variable, not `$activeTheme`) — only reachable when the site's active theme plugin is missing/disabled, not probe-safe to reproduce (would require breaking the shared site's theme); (b) the Site Settings page renders duplicate `id="setup"` DOM elements (top-level "Site Setup" tabpanel and the Appearance → "Setup" sub-tab both use `id="setup"` in `settings.tpl`), invalid HTML observed live via a Playwright strict-mode violation ("locator('#setup') resolved to 2 elements"); both tab groups also carry `:track-history`, so the shared anchor id is a latent tab-restore ambiguity. | site-settings product-spec author 2026-07-06: (a) code inspection; (b) live strict-mode locator error on `/admin/settings`. | `lib/pkp/api/v1/site/PKPSiteController.php` `getTheme()`; `lib/pkp/templates/admin/settings.tpl` (tab ids) | Upstream: add the `return` (and use `$activeTheme`); rename one of the `setup` tab ids. Spec: site-settings Known deviations. Non-blocking. |
| 152 | **Clearing the site style sheet removes the setting and the public `<link>` but never deletes the file — `public/site/styleSheet.css` stays served forever** (LOW; the site twin of row 123). Mechanism identical to the journal case: the null-clear branch of `PKPSiteService::_saveFileParam()` computes `$fileName = $isImage ? $setting['uploadName'] : $setting;` — for the NON-image `styleSheet` param `$setting` is the stored **array** (`{originalFilename, uploadName, dateUploaded}`), so `PKPPublicFileManager::removeSiteFile(ARRAY)` builds `public/site/Array` (array-to-string) and `deleteByPath()` no-ops; the setting still nulls, so the `siteStylesheet` link disappears while `public/site/styleSheet.css` survives on disk. The site logo (`pageHeaderTitleImage`, `$isImage=true`) uses `$setting['uploadName']` and clears correctly. A site admin who "removes" the site CSS reasonably believes the file is gone, but anything holding its stable URL keeps fetching the full content; a later re-upload silently overwrites the orphan. | site-settings adversarial verifier 2026-07-06 (live on `:8000`, `ojs_test` PostgreSQL): the retained `site-settings.spec.js` test 3 uploads `styleSheet.css` then clears it (`PUT api/v1/site {"styleSheet":null}`); after the suite the `site`/`site_settings` rows carry NO `styleSheet` value (DB-verified) yet `public/site/styleSheet.css` (132 B, the `wat-custom-css-marker` fixture) remains served — an orphan on disk with no DB reference, exactly the row-123 shape. Logo file cleared by the same call is gone. | `lib/pkp/classes/services/PKPSiteService.php` `_saveFileParam()` (`$isImage ? $setting['uploadName'] : $setting`); `lib/pkp/classes/file/PKPPublicFileManager.php` `removeSiteFile()` (`getSiteFilesPath().'/'.$fileName`) | Upstream one-liner (mirror the row-123 fix): on the non-image clear branch use `$setting['uploadName']`. Spec: site-settings Known deviations (rule 9) + cross-ref row 123. Non-blocking. |
| 153 | **The admin "Create Journal" form cannot be submitted without choosing a Country — an optional-looking field (no asterisk, help text says "Select the country…") whose untouched state fails server validation with the cryptic top-of-form error "Country: This is not a valid string."** (LOW–MEDIUM; UI-blocking friction on the primary journal-create path with a misleading message — the field is optional by design, but the never-touched select's empty value is rejected because the schema's `country` validation lacks `nullable`). The select offers only real countries (no blank option), so once a value is picked it can also never be un-set. Mechanism: `PKPContextForm` adds `FieldSelect('country')` with `value: null` for a new context and the ISO roster as the only options; the Vue form always serializes the field into the POST body; `context.json`'s `country` prop validates `["country"]` WITHOUT `nullable` (contrast `contactEmail`: `["nullable","email_or_localhost"]`), so the empty value draws "This is not a valid string." / "This is not a valid country.". A `country`-ABSENT API POST passes — only the UI's always-sent empty value fails, so the bug is invisible to API clients. | site-administration product-spec author, live 2026-07-06 (port 8000, `ojs_test` PostgreSQL, session as site admin `admin`): Hosted Journals → Create Journal filled with every *-marked field (title/initials/contact name+email/path/languages/primary locale), Country untouched → Save → **400**; the form banner reads "Please correct one error." with exactly one entry "Go to Country: This is not a valid string." and Save disables until corrected; replayed `POST /index/api/v1/contexts` with `country:""` → **400** `{"country":["This is not a valid string.","This is not a valid country."]}`; after selecting Canada the same submit → **200** (journal id 3 created); the same payload with `country` absent → **200**. | `lib/pkp/classes/components/forms/context/PKPContextForm.php` (`FieldSelect('country')` — no empty option, not `isRequired`); `lib/pkp/schemas/context.json` (`country`: `["country"]`, no `nullable`); `lib/pkp/classes/services/PKPContextService.php` `validate()` (schema rules via `ValidatorFactory`) | Upstream candidate: add `nullable` to the `country` schema validation and/or prepend a blank "—" option to the select. Found + owned by `site-administration` (spec Known deviations). Non-blocking. |
| 154 | **Deleting a journal orphans its contributor-role rows — `PKPContextService::delete()` never removes the `contributor_roles` (+ `contributor_role_settings`) records that `add()` provisions** (LOW; silent data residue — no FK ties `contributor_roles` to `journals`, so nothing cascades at the DB level either; invisible to users unless a tool joins contributor roles by context). Every journal create adds 2 contributor roles (Author, Translator — `ContributorRole::add()` in `PKPContextService::add()`); the delete path removes announcement types, review assignments, user groups, genres, announcements, highlights, institutions, email-template overrides, plugin settings, review forms, navigation menus, the files tree and the context row — and the OJS `afterDeleteContext()` hook removes sections/issues/reviewer recommendations/subscriptions/submissions/public files — but neither side touches contributor roles. | site-administration product-spec author, live 2026-07-06 (port 8000, `ojs_test` PostgreSQL): five scratch journals created (UI grid + API, ids 3–7) and deleted (grid Remove with confirm + `DELETE contexts/{id}`, all 200); after the deletes the cascade verified clean — `journals`/`journal_settings`/`sections`/`user_groups`/`user_user_groups`/`navigation_menus`/`genres`/`reviewer_recommendations`/`email_templates` all 0 rows for the deleted ids and `files/journals/{id}` removed from disk — EXCEPT `contributor_roles`: 2 rows per deleted context remain (10 rows across contexts 3–7, live-queried) plus their 10 `contributor_role_settings` rows; `pg_constraint` confirms `contributor_roles` carries only a PK and a CHECK — no foreign key. Residue left in place on the scratch DB as evidence. **Adversarial verifier re-confirm 2026-07-06 (blast-radius bound):** re-drove one create→delete via the admin site-level API (scratch `advblast1` id 47), snapshotting EVERY table carrying a `context_id`/`journal_id` column for the id before and after — provisioning at create was `user_groups` 18, `user_group_stage` 31, `email_templates` 6, `email_template_user_group_access` 74, `genres` 12, `navigation_menus` 2 / `navigation_menu_items` 17, `plugin_settings` 22, `reviewer_recommendations` 6, `sections` 1, `journal_settings` 65, `contributor_roles`+settings 2/2; after `DELETE contexts/47` every one of those dropped to **0 EXCEPT `contributor_roles` (2) + `contributor_role_settings` (2)**. So the orphan set is exactly this one table family and nothing broader (`user_group_stage` and `email_template_user_group_access`, though never named in `delete()`, cascade cleanly — confirmed 31→0 and 74→0). All scratch journals + their orphan rows purged afterward; DB back to 1 journal. | `lib/pkp/classes/services/PKPContextService.php` `add()` (two `ContributorRole::add()` calls) vs `delete()` (no contributor-role cleanup); `classes/services/ContextService.php` `afterDeleteContext()` (none either); `lib/pkp/classes/author/contributorRole/ContributorRole.php` | Upstream candidate: delete the context's contributor roles in `PKPContextService::delete()` (mirror the `add()` provisioning), and consider an FK with cascade. Found + owned by `site-administration`. Non-blocking. |
| 155 | **The journal Create/Edit form and `POST/PUT contexts` accept a `urlPath` that collides with a reserved top-level route (`index`, `api`, `admin`) with no reserved-word check — and a journal pathed `index` is then permanently unreachable, shadowed by the site context** (LOW; obscure admin foot-gun, no data loss, fully recoverable by editing the path). `PKPContextService::validate()` enforces only `pathAlphaNumeric` + `pathExists` (uniqueness) + a literal-`0` refusal; there is no blacklist of the framework's reserved path segments. `Application::SITE_CONTEXT_PATH` is the literal `'index'`, so `/{path}` for a journal named `index` always resolves to the site index, never the journal — the journal exists in the grid but can never be visited. Paths `admin` and `api` are accepted too but resolve as ordinary journal contexts (`/admin`, `/api` bounce to the journal login when disabled) and do NOT shadow the site-admin area (`/index/admin/...`) or the API (`/{context}/api/...`), which live under the `index` site context — so only `index` is genuinely broken; `admin`/`api` are merely confusing. | site-administration adversarial verifier, live 2026-07-06 (port 8000, `ojs_test` PostgreSQL, admin session): `POST /index/api/v1/contexts` with `urlPath` `index`/`api`/`admin` each → **200** (ids 50/51/52 created); `urlPath:"_"` → 400 pathAlphaNumeric, `urlPath:"0"` → 400 "A path is required.", duplicate `urlPath` → 400 "already in use by another journal." After the reserved-path creates, `/index/en/admin/contexts` still **200** and the site index still rendered — the admin area is unshadowed; `/index.php/index` resolved to the site index (single-journal auto-redirect), i.e. the journal pathed `index` was unreachable. All scratch journals deleted afterward. | `lib/pkp/classes/services/PKPContextService.php` `validate()` (urlPath after-hooks: `pathAlphaNumeric`, `pathExists`, `'0'` — no reserved-word list); `lib/pkp/classes/core/PKPApplication.php` (`SITE_CONTEXT_PATH = 'index'`); `lib/pkp/classes/core/PKPPageRouter.php` (first path segment → context) | Upstream candidate: reject the framework's reserved path segments (`index`, `api`, plus any first-segment router keywords) in the context `urlPath` validator, or at minimum `index`. Found + owned by `site-administration`. Non-blocking; maintainer to confirm whether reserved-path rejection is wanted. |
| 156 | **The Plugin Gallery tab hard-fails with an empty 500 whenever the gallery feed is unreachable (firewalled/air-gapped/offline installs) instead of rendering an empty grid** (LOW-MEDIUM; breaks a whole settings tab for every offline installation, and the code plainly intends a graceful fallback that never engages). Mechanism: on any fetch failure `PluginGalleryDAO::getExternalDocument()` catches Throwable and returns **null**; `getCachedDocument()` passes it through; `_getDocument()` then calls `DOMDocument::loadXML(null)` → PHP ≥8.1 ValueError/TypeError ("source must not be empty") — an exception class NOT covered by the `catch (\GuzzleHttp\Exception\TransferException)` in `PluginGalleryGridHandler::loadData()` (which exists precisely to return `[]` on network failure), so the request dies as a 500 with an empty body and the tab's load-URL div shows a failed fetch. | plugin-management product-spec author, live 2026-07-06 (port 8000, `ojs_test` PostgreSQL, network egress blocked): `plugin-gallery-grid/fetch-grid` → **500, 0 bytes** as site admin at site level AND as manager at journal level (both scopes); code path traced (loadXML(null) is the only non-TransferException throw site). No live install attempted (network seam). **Adversarial verifier re-confirm 2026-07-06 (scratch `plgv3951d8`):** fetch-grid as manager at journal level → 500 with a byte-count-0 body (no stack trace leaked); the `view-plugin` op (the details modal) 500s the same way — the whole Gallery surface dies, not just the tab list. **Calibration: NOT offline-only.** `getExternalDocument()` swallows Throwable and returns null, so a `TransferException` can never reach `loadData()`'s catch — the graceful branch is provably dead code. On ONLINE installs any transient fetch failure (gallery outage, DNS/TLS hiccup) with a cold or expired cache hits the same 500, and Laravel `Cache::remember` does not store the null, so failures are never cached — the 500 repeats on every request until a fetch succeeds (no stale-cache fallback). LOW-MEDIUM stands. | `lib/pkp/classes/plugins/PluginGalleryDAO.php` `getExternalDocument()` (returns null on Throwable) / `_getDocument()` (`loadXML` on null); `lib/pkp/controllers/grid/plugins/PluginGalleryGridHandler.php` `loadData()` (catch too narrow) | Upstream one-liner candidates: guard `_getDocument()` against null (return empty DOMDocument) or widen the handler catch to Throwable. Spec: plugin-management Known deviations + canonical scenario 6. Non-blocking. |
| 157 | **A site admin who is not enrolled as the journal's manager sees fully active enable/disable checkboxes on the journal's Installed Plugins grid, but every toggle of a journal-level plugin is refused — and the refusal toast is the RAW locale key `##user.authorization.pluginLevel##`, which exists in no locale file** (LOW; affordance-vs-policy divergence + missing translation on a reachable path; normally masked because the admin who *creates* a journal is auto-enrolled as its manager, so it bites admins on journals created by others). Mechanism: `PluginGridCellProvider::getCellActions()` renders the enabled-cell toggle for every viewer with no role check; `SettingsPluginGridHandler::authorize()` puts enable/disable/manage in MANAGE mode, where `PluginAccessPolicy` permits a site admin only via `PluginLevelRequiredPolicy($request, false)` — i.e. only for SITE-level plugins in a journal context — so journal-level toggles deny with the policy's advice key `user.authorization.pluginLevel`, which is defined in no `*.po` file (grep: zero hits across lib/pkp/locale + locale). | plugin-management product-spec author, live 2026-07-06 (scratch `plgspec1` id 267): with the auto-enrolled manager membership left in place, admin's toggle succeeded (manager path — control); after deleting admin's manager membership row, the same `disable?plugin=pflplugin&category=generic` POST → `{"status":false,"content":"##user.authorization.pluginLevel##"}` while the same session's fetch-grid still rendered active checkboxes on pfl/pluginTemplate rows (usageEvent correctly locked); membership reinserted afterward, journal deleted. **Adversarial verifier re-confirm 2026-07-06 (scratch `plgv3951d8` id 316):** after stripping admin's auto-manager membership, `enable?plugin=pflplugin` AND `manage?plugin=webfeedplugin&verb=settings` both → `{"status":false,"content":"##user.authorization.pluginLevel##"}`; the same session's fetch-grid HTML rendered the pfl checkbox WITHOUT the `disabled` attribute (usageEvent + tinymce correctly `disabled="disabled"`); nothing written (0 rows). Key re-confirmed in NO locale file (`grep -rln` across `lib/pkp/locale` + `locale` = zero hits; sibling key `user.authorization.pluginRequired` exists). **Blast radius bounded to exactly this path:** the mirror-image level mismatch — a manager hand-crafting a toggle of a SITE plugin (`enable?plugin=usageeventplugin`) — is refused with the TRANSLATED "The current role does not have access to this operation." (the site-admin sub-policy is evaluated after the manager one and its advice wins), and a bogus plugin name gets the translated "A plugin was not specified and is required." — so the raw key is user-visible ONLY for the pure-site-admin-on-journal-grid case, which is also the only one with a live UI affordance. LOW stands. | `lib/pkp/controllers/grid/plugins/PluginGridCellProvider.php` `getCellActions()` (no role gating); `lib/pkp/classes/security/authorization/PluginAccessPolicy.php` (site-admin MANAGE branch → `PluginLevelRequiredPolicy(false)`); `lib/pkp/classes/security/authorization/internal/PluginLevelRequiredPolicy.php` `__construct()` (advice key `user.authorization.pluginLevel`); missing key: `lib/pkp/locale/*/user.po` | Two-part fix candidate: add the `user.authorization.pluginLevel` locale key (defect either way), and pick a side on the policy — either hide/lock the toggle for non-manager site admins or let site admins manage journal-level plugins (maintainer call, spec Open question 1). Found + owned by `plugin-management` (Known deviations). Non-blocking. |
| 158 | **The two live plugin-settings generations disagree about pure site admins: the modern REST endpoint (`GET/PUT /{journal}/api/v1/plugins/{name}/settings`) lets a site admin who is NOT the journal's manager read AND write a journal-level plugin's settings, while the legacy grid `manage` op refuses the same actor (row 157's raw-key denial) and the Settings row action is hidden from them** (LOW; API-only — no UI affordance reaches the endpoint for this actor, no privilege escalation in spirit since admins are superusers elsewhere — but two authorization sources contradict each other for the same capability, so whichever intent the maintainer picks for row 157, one of the two paths is wrong today). Mechanism: `PluginSettingsController::getRouteGroupMiddleware()` builds its role list as `[SITE_ADMIN]` + (`MANAGER` unless the plugin is site-level) — admins pass for EVERY plugin; `PluginAccessPolicy` MANAGE mode permits admins only for SITE-level plugins; `PluginGridRow::_canEdit()` sides with the policy (journal-level ⇒ manager only), so the modal action never renders for the pure admin. | plugin-management adversarial verifier, live 2026-07-06 (scratch `plgv3951d8` id 316, admin's auto-manager membership stripped): pure admin `GET .../plugintemplateplugin/settings` → **200**; `PUT {"publicationStatement":"admin-put-probe"}` → **200, value persisted**; same session's legacy `manage` op → raw-key refusal and no `settings-button` in the grid HTML. Controls: author + anonymous GET/PUT → 401; enrolled manager GET → 200. Journal deleted afterward. | `lib/pkp/classes/plugins/PluginSettingsController.php` `getRouteGroupMiddleware()` (role list) vs `lib/pkp/classes/security/authorization/PluginAccessPolicy.php` (MANAGE mode) + `lib/pkp/controllers/grid/plugins/PluginGridRow.php` `_canEdit()` | Fold into the row-157 maintainer call: if pure site admins SHOULD manage journal plugins, the grid policy/affordance is wrong; if not, tighten the REST middleware to managers for journal-level plugins. Found + owned by `plugin-management` (Known deviations). Non-blocking. |

## 3. Flakiness sources in the local/CI environment (not app code)

- **Transient parallel-load flakes under 5-worker full-suite runs** (moved from
  PROGRESS at the 2026-07-10 consolidation; the affected round-2 tests were scratched
  but the environmental causes remain for any new suite): (a) dev-server JSON response
  truncation (`Unexpected end of JSON input`), (b) DB sequence-ordering races on
  listing assertions, (c) web/high-res file-linking timing. Each passed in isolation.
  Standing mitigation: `retries: 1` on the parallel `app` project in
  `lib/pkp/playwright/config-factory.js` (committed in lib/pkp) — one retry absorbs
  environmental flakes while a genuine regression still fails both attempts; the
  serial project keeps no retries (single-worker, deterministic).

- **Scenario API accepts a plain-string `metadata.title` and seeds a submission whose
  API map 500s** (`array_keys(): Argument #1 ($array) must be of type array, string
  given`): multilingual metadata passed as a bare string is stored under locale `'0'`
  in `publication_settings`, and from then on `GET submissions/{id}` *and every
  dashboard list containing the row* (`_submissions/assigned` etc.) crash for every
  user assigned to it. Hit twice on the warm test DB (2026-07-03): submissions 529–537
  (workflow-stage-navigation seeding — repaired in place by setting
  `publication_settings.locale='en'` on the affected `title` rows) and pre-existing
  521–528 from an earlier session (same signature, repaired the same way). Guard:
  always pass locale maps (`{"en": "..."}`) in scenario `metadata`; better, the
  scenario processors should validate/wrap multilingual props
  (`lib/pkp/classes/testing/scenario/Processor/PublicationsProcessor.php`). (2026-06-11 mass failure:
  190 ECONNREFUSED after 3 of 5 `php -S` processes died on hung ORCID-job Guzzle calls —
  §2 row 12). Hardened in `seed-test-config.js`: `[proxy]` now points at a dead local
  port (`127.0.0.1:9`) so all server-side egress fails in milliseconds, and
  `[schedule] task_runner = Off` so scheduled tasks run only when a serial spec invokes
  `lib/pkp/tools/scheduler.php` explicitly (charter principle 9 — a reminder task firing
  mid-parallel-run would also pollute Mailpit). `[queues] job_runner` stays On. The
  firewall also covers libxml/PHP-stream fetches (PKP wires `[proxy]` into both via
  `PKPContainer::settingProxyForStreamContext`); remotely-hosted DTDs the app validates
  against at request time (§2 row 13) are mirrored under
  `lib/pkp/playwright/fixtures/dtd/` and resolved offline via `XML_CATALOG_FILES`
  (exported by `start-php-server.js`). NOTE: re-seeding `config.test.inc.php` from the
  template resets `installed = Off` — always `npm run test:e2e:reset` (drops DB +
  config + auth together) rather than deleting the config file alone.
- **Stale gitignored build artifacts** (`styles/build.css`, `js/build.js`): an old build
  silently re-enables UI animations → 3 false failures in wave 1 (reduced-motion,
  task-templates, issues). Open follow-up in the inventory: bootstrap-time guard
  asserting the served CSS contains a `prefers-reduced-motion` block.
- **Rotating parallel-load flake tail**: ~1–2 random specs per local full run time out on
  dialogs/API calls and pass on retry (`retries: isCI ? 1 : 0`); suspected single-threaded
  PHP dev-server saturation. Pre-existing; open follow-up.
- **Test DBs installed before 2026-06-05 carry #12049→#12800 schema drift**
  (`edit_decisions.publication_id`); symptom: 500 on any decision. Fix: reset the DB
  (one-time local ALTERs were applied in wave 1; fresh installs are correct).
- **`playwright/.auth` storage-state files can tear under concurrent cross-agent logins**
  (one corruption observed wave 2); candidate hardening: atomic write in auth.js.
- **Parallel implementation agents must not share the default HTML reporter**: one wave-5
  agent's run died on `ENOENT: scandir test-results-wave5-…` when a sibling's output dir
  vanished mid-scan. Agents run their specs with `--reporter=list` (plus their private
  `--output` dir); only the orchestrator's full-suite runs use the default reporters.
- **Legacy grid header link actions can swallow a click right after a DataChangedEvent
  refresh** (~1 in 3 on the review-form elements grid's second "Create New Item");
  bounded retry pattern in `ReviewFormSettingsPage.openCreateElementForm`.
- **Worker-token search collisions on long-lived DBs (wave-11 diagnosis)**: Postgres
  tokenizes hyphenated tags into separate words (`edd7-w0-x` → `edd7`,`w0`,`x`) and the
  dashboard `searchPhrase` OR-matches tokens (§2 row 5) — so a search for any `-w0-` tag
  matches EVERY worker-0-tagged submission still active. Invisible on fresh-per-run CI
  DBs; deterministic on a local DB that has accumulated incomplete drafts (134 at
  diagnosis time — wizard request-mode tests leave never-submitted drafts active
  forever). Two pre-existing specs failed the wave-11 suite this way and passed
  untouched after a reset. Rules: reset the DB before full-suite verification runs;
  tags that back search assertions should be single hyphenless alphanumeric tokens
  (patterns rule 10 extended) — gradual refit candidate for round 2.
- **Offline XML-schema catalog now covers Crossref**: the long-standing "XSD validation
  timeout" on Crossref exports was firewalled egress fetching crossref5.4.0.xsd + its
  ~19-file import tree (JATS 1.3d2, MathML3, W3C xml.xsd); mirrored under
  `lib/pkp/playwright/fixtures/dtd/{crossref/,w3/}` with rewrite rules in catalog.xml
  (offline compile ≈3 s). NOTE: libxml caches the catalog per PHP process at first use —
  catalog edits may need a server restart if XML validation already ran.
- **Flake candidate (unreproduced)**: doi-crossref "configures Crossref via Settings UI"
  failed once under parallel load (wave 11); its plugins-grid step uses a `networkidle`
  fallback — rework to `waitForJQueryIdle` if it recurs.
- **`pkpMail.inboxFor`/`latestTo` were unscoped until wave 9**: Mailpit's
  `/api/v1/messages?query=…` silently IGNORES the query (only `/api/v1/search` filters,
  verified live on 1.29.7), so `latestTo` returned the globally newest message of ANY
  recipient. Under parallel agents this caused real cross-talk: one agent extracted a
  sibling's invitation mail and followed its decline link, failing the sibling's test.
  Fixed in `mail.js` (inboxFor → /api/v1/search). Rule stands: prefer
  `pkpMail.find({to, contains: tag})` over inbox-wide reads.
- **`clearAll()` is confined to the serial project (resolved wave 12)**: the old
  `mailpit.spec.js` ran `clearAll()` from the parallel project, wiping sibling workers'
  in-flight mail whenever it ran. It now lives at
  `lib/pkp/playwright/tests/serial/mailpit-harness.spec.js` (serial project, no parallel
  neighbors), where the strong global-count Mail::fake leak check
  (`messageCount() === 0`, bounded by a positive control) returned. Rule stands: that
  spec is the ONLY permitted `clearAll()` caller; everything else scopes
  `pkpMail.find({to, contains: tag})`.
- **End-of-request JobRunner can drain OTHER requests' queued jobs inside a Mail::fake'd
  scenario request (wave-12 verified hazard)**: with `[queues] job_runner = On`, every
  web request's shutdown hook pops queued jobs (`PKPQueueProvider::boot`). A scenario
  seeding request runs under `Mail::fake()` — so a mail-sending job queued by anyone
  else (e.g. `scheduler.php` dispatching ReviewReminder from a serial spec) that gets
  popped there commits its side effects (`dateReminded` stamped) while the message is
  swallowed before SMTP. Observed live during wave-12 development (parallel agents
  seeding while the serial scheduled-tasks spec ran). DELIBERATELY NOT "fixed": skipping
  the runner on scenario requests would leave seeding-dispatched jobs to drain in
  neighboring UN-faked requests, leaking seed mail into Mailpit — the inverse and worse
  failure, and the invariant the mailpit-harness spec exists to protect. The canonical
  lifecycle is safe (the serial project depends on the parallel projects finishing;
  scheduler runs only from serial specs). Rule: never run serial scheduled-task specs
  while anything else is seeding on the same DB.
- **Deterministic tags repeat across runs on a long-lived DB**: tag helpers built only
  from workerIndex + test-title slug produce the same tag every run, so count-assertions
  on shared surfaces (issue TOC, archive) match leftovers from prior runs ("expected 1,
  received 3" — wave-7 article-landing, copied from the galleys spec's helper). Rule:
  tags that back COUNT assertions need a per-run random component (the
  `versioning-states.spec.js` shape).
- **Near-midnight TZ-skew on date-boundary seeds**: Node's clock (test runner, local TZ)
  and PHP/Postgres "now" can disagree across midnight — a subscription dated to start
  "today" (Node) read as starting "tomorrow" server-side and failed validity for ~2 h
  (subscription-access subscriber test, 2026-06-12 00:0x). Rule: any date a test writes
  that must already be in effect gets at least a day of slack; exact-boundary semantics
  belong to unit tests.
